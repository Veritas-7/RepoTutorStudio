# RepoTutor Output Format

The CLI prints JSON containing:

- `sessionId`
- `status`
- `path`
- `html`
- `quizQuestions`

User-facing answers should include the HTML path and whether quiz/wrong-note
artifacts were created.
