// biome-ignore-all lint/suspicious/noTemplateCurlyInString: test fixtures assert literal template syntax from source snapshots.
import fs from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { CORE_LEARNING_REPORT_TARGETS } from "@repotutor/shared/report-targets";
import { describe, expect, it } from "vitest";
import { applySourcePrunePlan, findQuizLearningRecord, loadStudyHtmlInput, runStudy, scoreQuizAttempt, verifyEvidenceIndexReport, verifyHtmlExportManifest, verifyStudySessionArtifacts, writeHtmlZipBundle, writeSourcePrunePlan } from "../index.js";

const fixtureRoot = path.resolve("packages/core/tests/fixtures/simple-ts-app");
const sourcePruneSourcePath = path.resolve("packages/core/src/source-prune.ts");

describe("RepoTutor core pipeline - golden-and-session", () => {
  it("generates a complete study session for a TypeScript fixture", async () => {
    const studiesRoot = await fs.mkdtemp(path.join(os.tmpdir(), "repotutor-study-"));
    const result = await runStudy({ source: fixtureRoot, mode: "quick", level: "beginner", studiesRoot });
    expect(result.session.status).toBe("complete");
    expect(result.session.quizSummary.totalQuestions).toBeGreaterThanOrEqual(5);
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "repo-map.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "evidence-index-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "suggested-reads-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "runtime-environment-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "interface-map-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "symbol-map-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "api-reference-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "search-index-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "learning-journal-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "daily-summary-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "MISSION.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "RESOURCES.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "NOTES.md"))).resolves.toBeUndefined();
    const notesMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "NOTES.md"), "utf8");
    expect(notesMarkdown).toContain("reference 문서는 자주 다시 보는 AI 지시 문장, 리뷰 질문, 전이 판단, 소스 정리 근거 역할을 합니다");
    expect(notesMarkdown).toContain("생성된 세션 `source/` 스냅샷 정리 검토 여부는 reference/source-retention-guide.html의 retained artifact, verification checkpoint, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤에만 판단합니다");
    expect(notesMarkdown).not.toContain("생성된 세션 `source/` 스냅샷 정리 검토 여부는 reference/source-retention-guide.html의 retained artifact, verification checkpoint, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤에만 판단합니다");
    expect(notesMarkdown).not.toContain("생성된 세션 `source/` 스냅샷 정리 검토 여부는 reference/source-retention-guide.html의 retained artifact, verification checkpoint, 현재 학습 목표를 먼저 확인한 뒤 판단합니다");
    expect(notesMarkdown).not.toContain("reference 문서는 자주 다시 보는 cheat sheet 역할만 합니다");
    expect(notesMarkdown).not.toContain("source snapshot 삭제 여부는 reference/source-retention-guide.html");
    await expect(fs.access(path.join(result.session.outputPaths.root, "lessons", "0001-source-to-architecture.html"))).resolves.toBeUndefined();
    const firstLessonHtml = await fs.readFile(path.join(result.session.outputPaths.root, "lessons", "0001-source-to-architecture.html"), "utf8");
    expect(firstLessonHtml).toContain("바이브코딩 수업 경계");
    expect(firstLessonHtml).toContain("배우지 않는 것: 언어 문법 암기, 라인별 코딩, 원본 소스 전체 내장");
    expect(firstLessonHtml).toContain("반드시 배우는 것: 제품 목적, 아키텍처 이유, 폴더/파일 책임, 핵심 용어, AI 구현 프롬프트, 수락 기준, 검증 질문");
    expect(firstLessonHtml).toContain("AI에게 맡기는 것: 실제 코드 작성, 반복 구현, 문법 세부사항 적용");
    expect(firstLessonHtml).toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide");
    expect(firstLessonHtml).not.toContain("source-absorption-ledger, session-verification, source-retention-guide");
    expect(firstLessonHtml).toContain("이 lesson 통과만으로 생성된 세션 <code>source/</code> 스냅샷 정리를 결정하지 않고");
    expect(firstLessonHtml).toContain("학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 함께 확인합니다");
    expect(firstLessonHtml).not.toContain("현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 함께 확인합니다");
    expect(firstLessonHtml).not.toContain("현재 학습 목표를 함께 확인합니다");
    expect(firstLessonHtml).not.toContain("이 lesson 통과만으로 생성된 세션 source/ 스냅샷 정리를 결정하지 않고");
    expect(firstLessonHtml).not.toContain("이 lesson 통과만으로 source/를 삭제하지 않고");
    expect(firstLessonHtml).toContain("Prompt Pack</a>에서 첫 구현 프롬프트를 검토하고 본인 말로 줄입니다.");
    expect(firstLessonHtml).not.toContain("Prompt Pack</a>에서 첫 구현 프롬프트를 복사해 본인 말로 줄입니다.");
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "glossary.html"))).resolves.toBeUndefined();
    const glossaryReferenceHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "glossary.html"), "utf8");
    expect(glossaryReferenceHtml).toContain("용어 사용 규칙");
    expect(glossaryReferenceHtml).toContain("용어는 개발자 시험 단어가 아니라 AI와 소통하는 책임 라벨입니다");
    expect(glossaryReferenceHtml).toContain("어떤 책임, 데이터 흐름, 검증 기준에 연결되는지 함께 말합니다");
    expect(glossaryReferenceHtml).toContain("문법 세부사항으로 외우지 말고");
    expect(glossaryReferenceHtml).toContain("source evidence에서 발견한 용어는 구현 프롬프트 문장이나 AI 출력 리뷰 체크리스트로 바꿉니다");
    expect(glossaryReferenceHtml).toContain("data-glossary-use=\"ai-prompt-review-reference\"");
    expect(glossaryReferenceHtml).toContain("AI에게 지시할 문장");
    expect(glossaryReferenceHtml).toContain("AI 출력 리뷰 질문");
    expect(glossaryReferenceHtml).toContain("외우지 말 것");
    expect(glossaryReferenceHtml).toContain("Source evidence:");
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "rebuild-cheatsheet.html"))).resolves.toBeUndefined();
    const rebuildCheatsheetHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "rebuild-cheatsheet.html"), "utf8");
    expect(rebuildCheatsheetHtml).toContain("바이브코딩 재구현 규칙");
    expect(rebuildCheatsheetHtml).toContain("학습자는 제품 목적, 사용자 문제, 아키텍처 책임, 수락 기준, 검증 질문을 먼저 고정합니다");
    expect(rebuildCheatsheetHtml).toContain("AI는 실제 코드 작성, 반복 구현, 문법 세부사항 적용을 맡습니다");
    expect(rebuildCheatsheetHtml).toContain("원본 소스 전체를 복사하거나 앱 지식으로 내장하지 않고");
    expect(rebuildCheatsheetHtml).toContain("source evidence를 구현 지시와 리뷰 기준으로 바꿉니다");
    expect(rebuildCheatsheetHtml).toContain("실행 명령, 테스트, 화면 확인, 사람 판단 항목");
    expect(rebuildCheatsheetHtml).toContain("각 slice는 AI 요청을 보내거나 결과를 수락하기 전에");
    expect(rebuildCheatsheetHtml).toContain("<h3>수락/검증 기준</h3>");
    expect(rebuildCheatsheetHtml).not.toContain("각 slice는 완료 전에");
    expect(rebuildCheatsheetHtml).not.toContain("<h3>Completion Criteria</h3>");
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "architecture-principle-playbook.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "architecture-principle-playbook.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "source-to-build-interview.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "source-to-build-interview.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "similar-app-transfer-map.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "similar-app-transfer-map.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "learner-goal-alignment.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "learner-goal-alignment.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-implementation-loop.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-implementation-loop.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "learner-role-contract.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "learner-role-contract.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-output-review-rubric.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-output-review-rubric.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "vibe-coding-mastery-checklist.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "vibe-coding-mastery-checklist.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "vibe-coding-implementation-brief.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "vibe-coding-implementation-brief.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-prompt-readiness-checklist.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-prompt-readiness-checklist.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-prompt-ab-lab.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "ai-prompt-ab-lab.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "source-retention-guide.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "source-absorption-ledger.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "reference", "source-absorption-ledger.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.root, "learning-records", "README.md"))).resolves.toBeUndefined();
    const learningRecordsReadme = await fs.readFile(path.join(result.session.outputPaths.root, "learning-records", "README.md"), "utf8");
    expect(learningRecordsReadme).toContain("## Evidence purpose");
    expect(learningRecordsReadme).toContain("단순 점수 저장이나 문법 암기표가 아닙니다");
    expect(learningRecordsReadme).toContain("제품 목적, 아키텍처 이유, 파일 책임, 핵심 용어, AI 구현 프롬프트, 수락 기준, 검증 질문");
    expect(learningRecordsReadme).toContain("AI에게 맡길 보강 프롬프트");
    expect(learningRecordsReadme).toContain("통과해도 생성된 세션 `source/` 스냅샷 정리 검토는 source-absorption-ledger, session-verification, verification records, source-retention-guide, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰 확인 뒤 별도로 판단합니다");
    expect(learningRecordsReadme).not.toContain("통과해도 생성된 세션 `source/` 스냅샷 정리 검토는 source-absorption-ledger, session-verification, verification records, source-retention-guide, 현재 학습 목표 확인, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰 확인 뒤 별도로 판단합니다");
    expect(learningRecordsReadme).not.toContain("통과해도 생성된 세션 `source/` 스냅샷 정리 검토는 source-absorption-ledger, session-verification, verification records, source-retention-guide, 현재 학습 목표 확인 뒤 별도로 판단합니다");
    expect(learningRecordsReadme).not.toContain("통과해도 생성된 세션 source/ 스냅샷 정리 검토는 source-absorption-ledger, session-verification, source-retention-guide, 현재 학습 목표 확인 뒤 별도로 판단합니다");
    expect(learningRecordsReadme).toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide");
    expect(learningRecordsReadme).not.toContain("통과해도 원본 source/ 정리 판단은");
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "project-activity-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "code-metrics-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "code-ownership-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "large-asset-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "license-rights-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "sbom-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "security-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "sast-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "dast-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "threat-model-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "scorecard-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "provenance-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "advisory-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "vex-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "policy-gate-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "api-contract-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "consumer-contract-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "observability-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "performance-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "profiling-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "tracing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "debug-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "crash-reporting-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "incident-response-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "slo-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "cost-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "progressive-delivery-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "load-testing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "benchmark-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "e2e-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "flaky-test-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "test-impact-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "test-reporting-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "snapshot-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "property-based-testing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "fuzz-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "test-data-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "integration-test-environment-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "chaos-engineering-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "accessibility-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "storybook-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "design-tokens-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "i18n-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "release-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "secret-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "secret-management-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "container-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "container-scan-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "code-quality-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "documentation-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "database-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "database-migration-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "database-orm-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-transformation-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-quality-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-lineage-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-catalog-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-annotation-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "lakehouse-table-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "feature-store-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "model-registry-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "experiment-tracking-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "model-monitoring-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "model-serving-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "model-training-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "ci-cd-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "unit-test-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "coverage-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "mutation-testing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "typecheck-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "package-manager-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "git-hooks-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "task-runner-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "dependency-update-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "dependency-review-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "lint-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "format-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "commit-conventions-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "changelog-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "bundle-analysis-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "mocking-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-fetching-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "routing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "state-management-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "form-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "auth-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "authorization-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "payment-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "email-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "queue-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "event-stream-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "data-connector-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "semantic-layer-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "schema-registry-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "stream-processing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "pipeline-orchestration-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "service-mesh-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "ingress-controller-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "dns-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "certificate-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "helm-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "admission-policy-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "api-gateway-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "cache-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "logging-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "feature-flag-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "rate-limit-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "error-tracking-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "analytics-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "http-client-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "schema-validation-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "datetime-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "id-generation-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "image-processing-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "file-upload-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "websocket-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "pdf-generation-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "spreadsheet-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "chart-visualization-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "diagram-rendering-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "link-integrity-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "seo-metadata-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "pwa-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "browser-compat-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "browser-extension-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "env-validation-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "security-headers-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "graphql-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "cli-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "llm-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "llm-eval-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "llm-observability-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "vector-db-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "search-service-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "object-storage-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "realtime-collaboration-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "workflow-orchestration-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "openapi-client-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "webhook-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "notification-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "consent-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "privacy-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "server-framework-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "rpc-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "workspace-graph-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "scaffolding-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "scheduler-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "build-tool-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "styling-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "visual-regression-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "infrastructure-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "iac-drift-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "deployment-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "serverless-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "mobile-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "desktop-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "edge-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "compose-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "devcontainer-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "kubernetes-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "gitops-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "backup-readiness-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "context-pack-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "mcp-handoff-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "agent-memory-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "graph-query-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "tutorial-abstraction-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "decision-record-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "dependency-health-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.analysis, "session-verification-report.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "overview.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "component-graph.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "incremental.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "evidence.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "suggested-reads.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "runtime-environment.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "interface-map.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "symbol-map.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "api-reference.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "search-index.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "learning-journal.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "daily-summary.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "project-activity.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "code-metrics-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "code-ownership-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "large-asset-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "license-rights.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "sbom.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "security-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "sast-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "dast-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "threat-model-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "scorecard.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "provenance.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "advisories.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "vex.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "policy-gates.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "api-contracts.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "consumer-contract-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "observability.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "performance.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "profiling-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "tracing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "debug-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "crash-reporting-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "incident-response-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "slo-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "cost-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "progressive-delivery-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "load-testing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "benchmark-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "e2e.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "flaky-test-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "test-impact-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "test-reporting-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "snapshot-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "property-based-testing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "fuzz-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "test-data-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "integration-test-environment-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "chaos-engineering-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "accessibility.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "storybook.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "design-tokens.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "i18n.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "release-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "secret-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "secret-management-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "container-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "container-scan-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "code-quality.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "documentation.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "database-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "database-migration-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "database-orm-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-transformation-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-quality-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-lineage-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-catalog-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-annotation-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "lakehouse-table-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "feature-store-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "model-registry-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "experiment-tracking-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "model-monitoring-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "model-serving-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "model-training-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "ci-cd.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "unit-tests.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "coverage-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "mutation-testing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "typecheck-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "package-manager.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "git-hooks.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "task-runner.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "dependency-updates.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "dependency-review-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "lint-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "format-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "commit-conventions.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "changelog-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "bundle-analysis.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "mocking-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-fetching-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "routing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "state-management-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "form-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "auth-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "authorization-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "payment-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "email-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "queue-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "event-stream-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "data-connector-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "semantic-layer-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "schema-registry-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "stream-processing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "pipeline-orchestration-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "service-mesh-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "ingress-controller-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "dns-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "certificate-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "helm-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "admission-policy-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "api-gateway-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "cache-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "logging-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "feature-flag-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "rate-limit-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "error-tracking-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "analytics-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "http-client-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "schema-validation-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "datetime-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "id-generation-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "image-processing-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "file-upload-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "websocket-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "pdf-generation-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "spreadsheet-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "chart-visualization-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "diagram-rendering-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "link-integrity-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "seo-metadata-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "pwa-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "browser-compat-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "browser-extension-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "env-validation-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "security-headers-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "graphql-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "cli-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "llm-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "llm-eval-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "llm-observability-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "vector-db-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "search-service-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "object-storage-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "realtime-collaboration-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "workflow-orchestration-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "openapi-client-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "webhook-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "notification-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "consent-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "privacy-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "server-framework-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "rpc-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "workspace-graph-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "scaffolding-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "scheduler-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "build-tool-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "styling-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "visual-regression-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "infrastructure-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "iac-drift-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "deployment-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "serverless-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "mobile-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "desktop-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "edge-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "compose-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "devcontainer-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "kubernetes-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "gitops-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "backup-readiness.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "context-pack.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "mcp-handoff.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "agent-memory.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "graph-query.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "tutorial-abstractions.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "decision-records.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "dependency-health.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "vibe-coding-start.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.markdown, "session-verification.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "index.html"))).resolves.toBeUndefined();
    const reportIndexHtml = await fs.readFile(path.join(result.session.outputPaths.html, "index.html"), "utf8");
    expect(reportIndexHtml).toContain("학습 증거 기록은 퀴즈 시도처럼 근거가 생긴 뒤에만 생성됩니다.");
    expect(reportIndexHtml).not.toContain("학습 완료 기록은 퀴즈 시도처럼 증거가 생긴 뒤에만 생성됩니다.");
    await expect(fs.access(path.join(result.session.outputPaths.html, "vibe-coding-start.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "learning-path.html"))).resolves.toBeUndefined();
    const missionText = await fs.readFile(path.join(result.session.outputPaths.root, "MISSION.md"), "utf8");
    expect(missionText).toContain("소스는 AI에게 개발 지식을 새로 가르치는 내장 데이터가 아닙니다");
    expect(missionText).toContain("제품 의도, 책임 지도, 아키텍처 이유, AI 지시 맥락, 검증 기준");
    expect(missionText).toContain("## 학습 경계");
    expect(missionText).toContain("배우지 않는 것: 언어 문법 암기");
    expect(missionText).toContain("라인별 코딩");
    expect(missionText).toContain("반드시 배우는 것: 제품 목적, 아키텍처 이유");
    expect(missionText).toContain("AI에게 맡기는 것: 실제 코딩은 AI가 담당");
    expect(missionText).toContain("## Source cleanup decision");
    expect(missionText).toContain("생성된 세션 `source/` 스냅샷은 영구 학습 콘텐츠가 아니라 이 학습 세션의 프로젝트별 임시 근거입니다");
    expect(missionText).toContain("reference/source-absorption-ledger.html");
    expect(missionText).toContain("html/session-verification.html");
    expect(missionText).toContain("정리 전에는 reference/source-absorption-ledger.html, html/session-verification.html, 검증 기록, reference/source-retention-guide.html, README.study.md가 남아 있어야 합니다");
    expect(missionText).toContain("reference/source-retention-guide.html");
    expect(missionText).toContain("흡수 기록, 세션 검증, 검증 기록, source-retention-guide가 READY_REVIEW여도 이는 정리 검토 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(missionText).toContain("READY_REVIEW여도 이는 정리 검토 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(missionText).toContain("현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다고 명시 확인한 뒤에만");
    expect(missionText).toContain("prune-source --apply --confirm DELETE-SOURCE-SNAPSHOT");
    expect(missionText).toContain("생성된 세션 `source/` 스냅샷만 정리합니다");
    expect(missionText).toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰은 READY_REVIEW가 만든 최종 ACCEPT, 배포, 삭제 권한이 아니라 source 링크가 더 이상 열리지 않아도 된다는 학습자의 마지막 명시 확인입니다");
    expect(missionText).toContain("조사 필요 항목이 남아 있거나 검증 리포트와 검증 기록이 부족하면 생성된 세션 `source/` 스냅샷은 보존합니다");
    expect(missionText).not.toContain("정리 전에는 reference/source-absorption-ledger.html, html/session-verification.html, reference/source-retention-guide.html, README.study.md가 남아 있어야 합니다");
    expect(missionText).not.toContain("흡수 기록, 세션 검증, source-retention-guide가 READY_REVIEW여도 이는 정리 검토 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(missionText).not.toContain("조사 필요 항목이 남아 있거나 검증 리포트가 부족하면 생성된 세션 `source/` 스냅샷은 보존합니다");
    expect(missionText).not.toContain("생성된 세션 source/ 스냅샷은 영구 학습 콘텐츠가 아니라 이 학습 세션의 프로젝트별 임시 근거입니다");
    expect(missionText).not.toContain("생성된 세션 source/ 스냅샷만 정리합니다");
    expect(missionText).not.toContain("조사 필요 항목이 남아 있거나 검증 리포트가 부족하면 생성된 세션 source/ 스냅샷은 보존합니다");
    expect(missionText).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰은 READY_REVIEW가 만든 최종 ACCEPT, 배포, 삭제 권한이 아니라 학습자의 마지막 현재 목표 확인입니다");
    expect(missionText).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰은 READY_REVIEW가 만든 최종 ACCEPT, 배포, 삭제 권한이 아니라 학습자의 마지막 현재 학습 목표 확인과 명시 확인입니다");
    expect(missionText).not.toContain("원본 source/ 스냅샷은 영구 학습 콘텐츠가 아니라 임시 프로젝트 근거입니다");
    expect(missionText).not.toContain("원본 source/ 스냅샷은 보존합니다");
    expect(missionText).not.toContain("흡수 기록과 세션 검증이 READY이고");
    const resourcesText = await fs.readFile(path.join(result.session.outputPaths.root, "RESOURCES.md"), "utf8");
    expect(resourcesText).toContain("생성된 세션 `source/` 스냅샷 정리 검토 판단 기준");
    expect(resourcesText).not.toContain("생성된 세션 source/ 스냅샷 정리 검토 판단 기준");
    expect(resourcesText).not.toContain("원본 소스 보존/정리 판단 기준");
    const studyReadme = await fs.readFile(path.join(result.session.outputPaths.root, "README.study.md"), "utf8");
    const normalizedStudyReadme = studyReadme.replace(/\s+/g, " ");
    expect(studyReadme).toContain("html/vibe-coding-start.html");
    expect(studyReadme).toContain("brief, steer, and review AI");
    expect(studyReadme).toContain("## Vibe-Coding Learning Boundary");
    expect(studyReadme).toContain("Not the goal: language syntax memorization");
    expect(studyReadme).toContain("line-by-line coding practice");
    expect(studyReadme).toContain("Must learn: product purpose, architecture reasons, role boundaries, key terms, AI prompts, acceptance criteria, and verification habits");
    expect(studyReadme).toContain("Delegate to AI: actual code generation");
    expect(studyReadme).toContain("The generated session `source/` snapshot is temporary project evidence, not permanent app knowledge");
    expect(studyReadme).toContain("temporary project evidence");
    expect(studyReadme).toContain("reference/source-absorption-ledger.html");
    expect(studyReadme).toContain("reference/source-retention-guide.html");
    expect(studyReadme).toContain("before pruning that generated session snapshot");
    expect(studyReadme).not.toContain("The copied source is temporary project evidence");
    expect(studyReadme).toContain("dry-run Markdown reports READY_REVIEW");
    expect(studyReadme).toContain("READY_REVIEW is a cleanup review");
    expect(studyReadme).toContain("not final ACCEPT");
    expect(studyReadme).toContain("deployment, or deletion permission");
    expect(normalizedStudyReadme).toContain("preserved learning artifacts and evidence bundle are present");
    expect(normalizedStudyReadme).toContain("learner explicitly confirms source links no longer need to open for the current learning goal");
    expect(normalizedStudyReadme).toContain("The core apply guard rejects cleanup without the explicit `DELETE-SOURCE-SNAPSHOT` confirmation token");
    expect(normalizedStudyReadme).toContain("Treat the token as the learner's final explicit confirmation that source links no longer need to open");
    expect(normalizedStudyReadme).toContain("not permission created by READY_REVIEW");
    expect(normalizedStudyReadme).not.toContain("Treat the token as the learner's final current-goal confirmation, not permission created by READY_REVIEW");
    expect(normalizedStudyReadme).not.toContain("Treat the token as the learner's final current-goal and explicit learner confirmation");
    expect(normalizedStudyReadme).toContain("Cleanup deletes only the generated session `source/` snapshot, not user-provided source");
    expect(studyReadme).toContain("## Source Cleanup Gate");
    expect(studyReadme).toContain("prune-source");
    expect(studyReadme).toContain("--format markdown");
    expect(studyReadme).toContain("--apply --confirm DELETE-SOURCE-SNAPSHOT");
    expect(studyReadme).toContain(result.session.outputPaths.root);
    await expect(fs.access(path.join(result.session.outputPaths.html, "manifest.json"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "EXPORT-README.md"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "component-graph.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "incremental.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "evidence.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "suggested-reads.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "runtime-environment.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "interface-map.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "symbol-map.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "api-reference.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "search-index.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "learning-journal.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "daily-summary.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "project-activity.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "code-metrics-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "code-ownership-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "large-asset-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "license-rights.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "sbom.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "security-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "sast-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "dast-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "threat-model-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "scorecard.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "provenance.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "advisories.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "vex.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "policy-gates.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "api-contracts.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "consumer-contract-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "observability.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "performance.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "profiling-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "tracing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "debug-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "crash-reporting-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "incident-response-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "slo-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "cost-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "progressive-delivery-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "load-testing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "benchmark-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "e2e.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "flaky-test-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "test-impact-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "test-reporting-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "snapshot-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "property-based-testing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "fuzz-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "test-data-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "integration-test-environment-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "chaos-engineering-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "accessibility.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "storybook.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "design-tokens.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "i18n.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "release-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "secret-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "secret-management-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "container-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "container-scan-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "code-quality.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "documentation.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "database-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "database-migration-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "database-orm-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-transformation-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-quality-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-lineage-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-catalog-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-annotation-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "lakehouse-table-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "feature-store-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "model-registry-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "experiment-tracking-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "model-monitoring-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "model-serving-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "model-training-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "ci-cd.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "unit-tests.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "coverage-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "mutation-testing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "typecheck-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "package-manager.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "git-hooks.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "task-runner.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "dependency-updates.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "dependency-review-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "lint-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "format-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "commit-conventions.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "changelog-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "bundle-analysis.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "mocking-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-fetching-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "routing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "state-management-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "form-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "auth-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "authorization-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "payment-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "email-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "queue-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "event-stream-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "data-connector-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "semantic-layer-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "schema-registry-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "stream-processing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "pipeline-orchestration-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "service-mesh-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "ingress-controller-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "dns-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "certificate-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "helm-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "admission-policy-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "api-gateway-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "cache-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "logging-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "feature-flag-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "rate-limit-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "error-tracking-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "datetime-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "id-generation-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "image-processing-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "file-upload-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "websocket-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "pdf-generation-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "spreadsheet-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "chart-visualization-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "diagram-rendering-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "link-integrity-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "seo-metadata-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "pwa-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "browser-compat-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "browser-extension-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "env-validation-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "security-headers-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "graphql-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "cli-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "llm-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "llm-eval-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "llm-observability-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "vector-db-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "search-service-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "object-storage-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "realtime-collaboration-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "workflow-orchestration-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "openapi-client-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "webhook-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "notification-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "consent-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "privacy-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "server-framework-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "rpc-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "workspace-graph-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "scaffolding-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "scheduler-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "build-tool-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "styling-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "visual-regression-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "infrastructure-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "iac-drift-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "deployment-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "serverless-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "mobile-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "desktop-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "edge-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "compose-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "devcontainer-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "kubernetes-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "gitops-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "backup-readiness.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "context-pack.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "mcp-handoff.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "agent-memory.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "graph-query.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "tutorial-abstractions.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "decision-records.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "dependency-health.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "session-verification.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "quiz-print.html"))).resolves.toBeUndefined();
    await expect(fs.access(path.join(result.session.outputPaths.html, "assets", "learning-journal-template.md"))).resolves.toBeUndefined();
    for (const target of CORE_LEARNING_REPORT_TARGETS) {
      await expect(fs.access(path.join(result.session.outputPaths.html, target.fileName))).resolves.toBeUndefined();
    }
    await expect(fs.access(path.join(result.session.outputPaths.codex, "events.jsonl"))).resolves.toBeUndefined();
    const codexPromptsText = await fs.readFile(path.join(result.session.outputPaths.codex, "prompts.jsonl"), "utf8");
    expect(codexPromptsText).toContain("vibe-coding learner");
    expect(codexPromptsText).toContain("architecture roles, essential terms, AI prompt strategy, and verification boundaries");
    expect(codexPromptsText).toContain("does not need language syntax memorization");
    expect(codexPromptsText).not.toContain("beginner learning");
    await expect(fs.access(path.join(result.session.outputPaths.source, ".env"))).rejects.toThrow();
    const snapshotText = await fs.readFile(path.join(result.session.outputPaths.analysis, "source-snapshot-report.json"), "utf8");
    expect(snapshotText).toContain("\"sha256\"");
    const coverageText = await fs.readFile(path.join(result.session.outputPaths.analysis, "coverage-report.json"), "utf8");
    expect(coverageText).toContain("\"evidenceBackedFiles\"");
    expect(coverageText).toContain("\"evidenceCoverageRatio\"");
    expect(coverageText).toContain("\"evidenceKindCounts\"");
    const incrementalText = await fs.readFile(path.join(result.session.outputPaths.analysis, "incremental-report.json"), "utf8");
    expect(incrementalText).toContain("\"baselineSessionId\"");
    expect(incrementalText).toContain("\"coverageDelta\"");
    const graphText = await fs.readFile(path.join(result.session.outputPaths.analysis, "component-graph-report.json"), "utf8");
    expect(graphText).toContain("\"nodes\"");
    expect(graphText).toContain("\"edges\"");
    expect(graphText).toContain("\"nodeTypeCounts\"");
    expect(graphText).toContain("\"topConnectedNodes\"");
    const componentGraphMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "component-graph.md"), "utf8");
    expect(componentGraphMarkdown).toContain("## 큰 그래프 요약");
    const componentGraphHtml = await fs.readFile(path.join(result.session.outputPaths.html, "component-graph.html"), "utf8");
    expect(componentGraphHtml).toContain("큰 그래프 요약");
    expect(componentGraphHtml).toContain("data-graph-filter");
    expect(componentGraphHtml).toContain("data-node-type");
    expect(componentGraphHtml).toContain("component-graph-download-toolbar");
    expect(componentGraphHtml).toContain("data-download-mermaid");
    expect(componentGraphHtml).toContain("component-graph-mermaid");
    expect(componentGraphHtml).toContain("component-node-relations");
    expect(componentGraphHtml).toContain("data-node-relation");
    expect(componentGraphHtml).toContain("연결 관계");
    expect(componentGraphHtml).toContain("component-node-anchor");
    expect(componentGraphHtml).toContain("data-node-id");
    expect(componentGraphHtml).toContain("data-node-link");
    expect(componentGraphHtml).toContain("href=\"#component-node-");
    const componentGraphMermaid = await fs.readFile(path.join(result.session.outputPaths.html, "assets", "component-graph.mmd"), "utf8");
    expect(componentGraphMermaid).toContain("flowchart");
    const learningPathTourText = await fs.readFile(path.join(result.session.outputPaths.html, "assets", "learning-path.tour.json"), "utf8");
    expect(learningPathTourText).toContain("RepoTutor Learning Path");
    const learningPathTour = JSON.parse(learningPathTourText) as {
      $schema: string;
      stepMarker: string;
      when: string;
      steps: Array<{
        title?: string;
        file?: string;
        directory?: string;
        view?: string;
        line?: number;
        pattern?: string;
        commands?: string[];
      }>;
    };
    expect(learningPathTour.$schema).toBe("https://aka.ms/codetour-schema");
    expect(learningPathTour.stepMarker).toBe("repotutor:learning-path");
    expect(learningPathTour.when).toBe("true");
    expect(learningPathTour.steps[0]).toMatchObject({
      title: "투어 시작",
      directory: "html",
      view: "explorer",
      commands: []
    });
    expect(learningPathTour.steps.some((step) => step.file === "html/component-graph.html" && step.directory === "html" && step.view === "explorer" && step.line === 1 && step.pattern === "컴포넌트 그래프 따라가기" && Array.isArray(step.commands) && step.commands.length === 0)).toBe(true);
    expect(learningPathTourText).toContain("\"isPrimary\": true");
    expect(learningPathTourText).toContain("\"file\": \"html/component-graph.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/dependency-health.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/api-reference.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/search-index.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/learning-journal.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/daily-summary.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/vibe-coding-prompt-pack.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/project-activity.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/code-metrics-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/code-ownership-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/large-asset-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/license-rights.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/sbom.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/security-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/sast-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/dast-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/threat-model-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/scorecard.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/provenance.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/advisories.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/vex.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/policy-gates.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/api-contracts.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/consumer-contract-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/observability.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/performance.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/profiling-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/tracing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/debug-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/crash-reporting-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/incident-response-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/load-testing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/benchmark-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/e2e.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/flaky-test-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/test-impact-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/test-reporting-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/snapshot-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/property-based-testing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/fuzz-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/test-data-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/integration-test-environment-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/chaos-engineering-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/accessibility.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/storybook.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/design-tokens.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/i18n.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/release-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/secret-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/secret-management-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/container-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/container-scan-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/code-quality.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/documentation.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/database-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/database-migration-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/database-orm-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-transformation-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-quality-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-lineage-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-catalog-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-annotation-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/lakehouse-table-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/feature-store-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/model-registry-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/experiment-tracking-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/model-monitoring-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/model-serving-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/model-training-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/ci-cd.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/unit-tests.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/coverage-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/mutation-testing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/typecheck-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/package-manager.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/git-hooks.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/task-runner.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/dependency-updates.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/dependency-review-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/lint-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/format-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/commit-conventions.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/changelog-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/bundle-analysis.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/mocking-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-fetching-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/routing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/state-management-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/form-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/auth-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/authorization-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/payment-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/email-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/queue-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/event-stream-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/data-connector-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/semantic-layer-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/schema-registry-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/stream-processing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/pipeline-orchestration-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/service-mesh-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/ingress-controller-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/dns-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/certificate-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/helm-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/admission-policy-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/api-gateway-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/cache-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/logging-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/feature-flag-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/rate-limit-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/error-tracking-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/schema-validation-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/datetime-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/id-generation-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/image-processing-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/file-upload-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/websocket-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/pdf-generation-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/spreadsheet-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/chart-visualization-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/diagram-rendering-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/link-integrity-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/seo-metadata-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/pwa-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/browser-compat-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/browser-extension-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/env-validation-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/security-headers-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/graphql-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/cli-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/llm-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/llm-eval-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/llm-observability-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/vector-db-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/search-service-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/object-storage-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/realtime-collaboration-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/workflow-orchestration-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/openapi-client-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/webhook-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/notification-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/consent-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/privacy-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/server-framework-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/rpc-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/workspace-graph-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/scaffolding-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/scheduler-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/build-tool-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/styling-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/visual-regression-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/infrastructure-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/iac-drift-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/deployment-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/serverless-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/mobile-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/desktop-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/edge-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/compose-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/devcontainer-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/kubernetes-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/gitops-readiness.html\"");
    expect(learningPathTourText).toContain("\"file\": \"html/backup-readiness.html\"");
    const coverageHtml = await fs.readFile(path.join(result.session.outputPaths.html, "coverage.html"), "utf8");
    expect(coverageHtml).toContain("소스 근거 파일");
    expect(coverageHtml).toContain("근거 비율");
    expect(coverageHtml).toContain("소스 근거 종류");
    expect(coverageHtml).toContain("entry:");
    expect(coverageHtml).toContain("소스 근거 부족");
    const coverageMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "coverage.md"), "utf8");
    expect(coverageMarkdown).toContain("## 소스 근거 종류");
    expect(coverageMarkdown).toContain("- entry:");
    const architectureText = await fs.readFile(path.join(result.session.outputPaths.analysis, "architecture-report.json"), "utf8");
    expect(architectureText).toContain("\"vibeCodingLens\"");
    expect(architectureText).toContain("\"architectureRationale\"");
    expect(architectureText).toContain("\"aiPromptBrief\"");
    expect(architectureText).toContain("\"sourceBoundaries\"");
    expect(architectureText).toContain("\"verificationQuestions\"");
    expect(architectureText).toContain("바이브코딩 개발자");
    expect(architectureText).toContain("AI 요청 후보");
    expect(architectureText).toContain("보내기 전 소스 근거, 제외 범위, 수락 기준, 검증 질문을 내 목표에 맞게 다듬습니다");
    expect(architectureText).not.toContain("가장 작은 vertical slice부터 구현해줘\\\"라고 지시하세요");
    const architectureHtml = await fs.readFile(path.join(result.session.outputPaths.html, "architecture.html"), "utf8");
    expect(architectureHtml).toContain("Vibe-Coding Architecture Compass");
    expect(architectureHtml).toContain("AI Prompt Brief");
    expect(architectureHtml).toContain("Verification Questions");
    expect(architectureHtml).toContain("AI 요청 후보");
    expect(architectureHtml).not.toContain("가장 작은 vertical slice부터 구현해줘&quot;라고 지시하세요");
    const architectureMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "architecture.md"), "utf8");
    expect(architectureMarkdown).toContain("## Vibe-Coding Architecture Lens");
    expect(architectureMarkdown).toContain("## Source Boundaries");
    expect(architectureMarkdown).toContain("AI 요청 후보");
    expect(architectureMarkdown).not.toContain("가장 작은 vertical slice부터 구현해줘\"라고 지시하세요");
    const folderLessonsText = await fs.readFile(path.join(result.session.outputPaths.analysis, "folder-lessons.json"), "utf8");
    expect(folderLessonsText).toContain("\"forestViewSummary\"");
    expect(folderLessonsText).toContain("\"architectureRationale\"");
    expect(folderLessonsText).toContain("\"aiImplementationBrief\"");
    expect(folderLessonsText).toContain("폴더 재구현 요청 후보");
    expect(folderLessonsText).toContain("AI에게 보낼 문장으로 다듬으세요");
    expect(folderLessonsText).not.toContain("폴더를 다시 만들라고 요청할 때는");
    expect(folderLessonsText).toContain("\"vibeCodingPrompts\"");
    expect(folderLessonsText).toContain("폴더 이해 요청 후보");
    expect(folderLessonsText).toContain("폴더 경계 요청 후보");
    expect(folderLessonsText).toContain("폴더 vertical slice 요청 후보");
    expect(folderLessonsText).toContain("수락/검증 질문");
    expect(folderLessonsText).not.toContain("완료 검증 질문");
    expect(folderLessonsText).not.toContain("구현 지시 프롬프트를 만들어줘");
    expect(folderLessonsText).toContain("\"verificationQuestions\"");
    expect(folderLessonsText).toContain("나는 전통 개발자가 아니라 바이브코딩 개발자야");
    const folderLessonsHtml = await fs.readFile(path.join(result.session.outputPaths.html, "folders.html"), "utf8");
    expect(folderLessonsHtml).toContain("Vibe-Coding Folder Map");
    expect(folderLessonsHtml).toContain("AI Implementation Brief");
    expect(folderLessonsHtml).toContain("폴더 재구현 요청 후보");
    expect(folderLessonsHtml).not.toContain("폴더를 다시 만들라고 요청할 때는");
    expect(folderLessonsHtml).toContain("폴더 경계 요청 후보");
    expect(folderLessonsHtml).toContain("수락/검증 질문");
    expect(folderLessonsHtml).not.toContain("완료 검증 질문");
    expect(folderLessonsHtml).not.toContain("구현 지시 프롬프트를 만들어줘");
    expect(folderLessonsHtml).toContain("Verification Questions");
    expect(folderLessonsHtml).toContain("data-folder-role=");
    const folderLessonsMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "folders.md"), "utf8");
    expect(folderLessonsMarkdown).toContain("### Forest View");
    expect(folderLessonsMarkdown).toContain("### AI Implementation Brief");
    expect(folderLessonsMarkdown).toContain("폴더 재구현 요청 후보");
    expect(folderLessonsMarkdown).not.toContain("폴더를 다시 만들라고 요청할 때는");
    expect(folderLessonsMarkdown).toContain("### Vibe-Coding Prompts");
    expect(folderLessonsMarkdown).toContain("폴더 vertical slice 요청 후보");
    expect(folderLessonsMarkdown).toContain("수락/검증 질문");
    expect(folderLessonsMarkdown).not.toContain("완료 검증 질문");
    expect(folderLessonsMarkdown).not.toContain("구현 지시 프롬프트를 만들어줘");
    expect(folderLessonsMarkdown).toContain("### Verification Questions");
    const glossaryText = await fs.readFile(path.join(result.session.outputPaths.analysis, "glossary.json"), "utf8");
    expect(glossaryText).toContain("AI build prompt");
    expect(glossaryText).toContain("PRD");
    expect(glossaryText).toContain("SDD");
    expect(glossaryText).toContain("TDD");
    expect(glossaryText).toContain("acceptance criteria");
    expect(glossaryText).toContain("수락/검증 기준");
    expect(glossaryText).toContain("기능을 수락할 수 있는 관찰 가능한 조건입니다");
    expect(glossaryText).not.toContain("완료 기준이 무엇인지");
    expect(glossaryText).not.toContain("기능이 끝났다고 판단할 수 있는 관찰 가능한 조건입니다");
    expect(glossaryText).toContain("context pack");
    expect(glossaryText).toContain("architecture responsibility");
    expect(glossaryText).toContain("vertical slice");
    expect(glossaryText).toContain("verification boundary");
    expect(glossaryText).toContain("source evidence");
    expect(glossaryText).toContain(`"promptUse"`);
    expect(glossaryText).toContain(`"reviewQuestion"`);
    expect(glossaryText).toContain(`"memorizationWarning"`);
    expect(glossaryText).toContain("문법 세부사항으로 외우지 말고");
    const glossaryHtml = await fs.readFile(path.join(result.session.outputPaths.html, "glossary.html"), "utf8");
    expect(glossaryHtml).toContain("Vibe-Coding Terms");
    expect(glossaryHtml).toContain("AI 작업 지시");
    expect(glossaryHtml).toContain("용어는 문법 암기장이 아니라 AI에게 목적, 책임, 검증 기준을 말하기 위한 프롬프트 부품입니다");
    expect(glossaryHtml).toContain("AI에게 지시할 문장");
    expect(glossaryHtml).toContain("AI 출력 리뷰 질문");
    expect(glossaryHtml).toContain("외우지 말 것");
    expect(glossaryHtml).toContain("data-glossary-use=\"ai-prompt-review\"");
    expect(glossaryHtml).toContain("data-glossary-difficulty=\"medium\"");
    const glossaryMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "glossary.md"), "utf8");
    expect(glossaryMarkdown).toContain("## AI 작업 지시 (AI build prompt)");
    expect(glossaryMarkdown).toContain("## 제품 요구사항 문서 (PRD)");
    expect(glossaryMarkdown).toContain("수락/검증 기준");
    expect(glossaryMarkdown).toContain("기능을 수락할 수 있는 관찰 가능한 조건입니다");
    expect(glossaryMarkdown).not.toContain("완료 기준이 무엇인지");
    expect(glossaryMarkdown).not.toContain("기능이 끝났다고 판단할 수 있는 관찰 가능한 조건입니다");
    expect(glossaryMarkdown).toContain("## 명세 주도 개발 (SDD)");
    expect(glossaryMarkdown).toContain("## 테스트 주도 요청 (TDD)");
    expect(glossaryMarkdown).toContain("용어는 문법 암기장이 아니라 AI에게 목적, 책임, 검증 기준을 말하기 위한 프롬프트 부품입니다");
    expect(glossaryMarkdown).toContain("AI에게 지시할 문장:");
    expect(glossaryMarkdown).toContain("AI 출력 리뷰 질문:");
    expect(glossaryMarkdown).toContain("외우지 말 것:");
    expect(glossaryMarkdown).toContain("Related terms:");
    expect(glossaryMarkdown).toContain("Review priority:");
    const rebuildRoadmapText = await fs.readFile(path.join(result.session.outputPaths.analysis, "rebuild-roadmap.json"), "utf8");
    expect(rebuildRoadmapText).toContain("\"vibeCodingMethod\"");
    expect(rebuildRoadmapText).toContain("\"sdd-first\"");
    expect(rebuildRoadmapText).toContain("\"test-driven\"");
    expect(rebuildRoadmapText).toContain("acceptance criteria");
    expect(rebuildRoadmapText).toContain("\"aiPrompt\"");
    expect(rebuildRoadmapText).toContain("\"architectureRationale\"");
    expect(rebuildRoadmapText).toContain("\"sourceRoleFocus\"");
    expect(rebuildRoadmapText).toContain("\"verificationPrompts\"");
    expect(rebuildRoadmapText).toContain("나는 전통 개발자가 아니라 바이브코딩 개발자야");
    expect(rebuildRoadmapText).toContain("source evidence와 내 목표에 맞게 다듬은 AI 구현 지시서");
    expect(rebuildRoadmapText).not.toContain("AI에게 그대로 붙여넣을 수 있는 프롬프트");
    const rebuildHtml = await fs.readFile(path.join(result.session.outputPaths.html, "rebuild.html"), "utf8");
    expect(rebuildHtml).toContain("Vibe-Coding Rebuild Roadmap");
    expect(rebuildHtml).toContain("AI 작업 지시 프롬프트");
    expect(rebuildHtml).toContain("Source Role Focus");
    expect(rebuildHtml).toContain("data-vibe-method=\"context-first\"");
    expect(rebuildHtml).toContain("<h4>수락/검증 기준</h4>");
    expect(rebuildHtml).not.toContain("<h4>완료 기준</h4>");
    const rebuildMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "rebuild.md"), "utf8");
    expect(rebuildMarkdown).toContain("# 맨땅에서 따라 만들기");
    expect(rebuildMarkdown).toContain("Vibe-coding method:");
    expect(rebuildMarkdown).toContain("### AI 작업 지시 프롬프트");
    expect(rebuildMarkdown).toContain("### 검증 프롬프트");
    expect(rebuildMarkdown).toContain("### 수락/검증 기준");
    expect(rebuildMarkdown).not.toContain("### 완료 기준");
    const suggestedReadsText = await fs.readFile(path.join(result.session.outputPaths.analysis, "suggested-reads-report.json"), "utf8");
    expect(suggestedReadsText).toContain("Repo Baby suggested_reads");
    expect(suggestedReadsText).toContain("\"items\"");
    const suggestedReadsHtml = await fs.readFile(path.join(result.session.outputPaths.html, "suggested-reads.html"), "utf8");
    expect(suggestedReadsHtml).toContain("추천 읽기");
    expect(suggestedReadsHtml).toContain("suggested-read-card");
    expect(suggestedReadsHtml).toContain("data-source-pattern=\"Repo Baby\"");
    const suggestedReadsMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "suggested-reads.md"), "utf8");
    expect(suggestedReadsMarkdown).toContain("# 추천 읽기");
    expect(suggestedReadsMarkdown).toContain("Source pattern: Repo Baby");
    const runtimeEnvironmentText = await fs.readFile(path.join(result.session.outputPaths.analysis, "runtime-environment-report.json"), "utf8");
    expect(runtimeEnvironmentText).toContain("docSmith Dockerfile and Docker Compose generation prompts");
    expect(runtimeEnvironmentText).toContain("mise dev tools env vars tasks mise.toml .mise.toml .tool-versions idiomatic version files mise install exec run doctor trust config hierarchy environments task_config includes mise-action");
    expect(runtimeEnvironmentText).toContain("\"setupSignals\"");
    expect(runtimeEnvironmentText).toContain("\"toolVersionSignals\"");
    expect(runtimeEnvironmentText).toContain("\"environmentConfigSignals\"");
    expect(runtimeEnvironmentText).toContain("\"taskRunnerSignals\"");
    const runtimeEnvironmentHtml = await fs.readFile(path.join(result.session.outputPaths.html, "runtime-environment.html"), "utf8");
    expect(runtimeEnvironmentHtml).toContain("실행 환경");
    expect(runtimeEnvironmentHtml).toContain("runtime-env-card");
    expect(runtimeEnvironmentHtml).toContain("data-source-pattern=\"docSmith mise\"");
    expect(runtimeEnvironmentHtml).toContain("Tool Version Signals");
    expect(runtimeEnvironmentHtml).toContain("Environment Config Signals");
    expect(runtimeEnvironmentHtml).toContain("Task Runner Signals");
    const runtimeEnvironmentMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "runtime-environment.md"), "utf8");
    expect(runtimeEnvironmentMarkdown).toContain("# 실행 환경");
    expect(runtimeEnvironmentMarkdown).toContain("Source pattern: docSmith");
    expect(runtimeEnvironmentMarkdown).toContain("## Tool Version Signals");
    expect(runtimeEnvironmentMarkdown).toContain("## Environment Config Signals");
    expect(runtimeEnvironmentMarkdown).toContain("## Task Runner Signals");
    const interfaceMapText = await fs.readFile(path.join(result.session.outputPaths.analysis, "interface-map-report.json"), "utf8");
    expect(interfaceMapText).toContain("repomap page routes GraphQL REST data flow analysis");
    expect(interfaceMapText).toContain("\"routeSignals\"");
    const interfaceMapHtml = await fs.readFile(path.join(result.session.outputPaths.html, "interface-map.html"), "utf8");
    expect(interfaceMapHtml).toContain("인터페이스 맵");
    expect(interfaceMapHtml).toContain("interface-map-card");
    expect(interfaceMapHtml).toContain("data-source-pattern=\"repomap\"");
    const interfaceMapMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "interface-map.md"), "utf8");
    expect(interfaceMapMarkdown).toContain("# 인터페이스 맵");
    expect(interfaceMapMarkdown).toContain("Source pattern: repomap");
    const symbolMapText = await fs.readFile(path.join(result.session.outputPaths.analysis, "symbol-map-report.json"), "utf8");
    expect(symbolMapText).toContain("codebase-map AST-based functions classes constants index");
    expect(symbolMapText).toContain("SCIP Code Intelligence Protocol definitions references implementations occurrences SymbolInformation relationships hover signatures diagnostics snapshot stats language indexers");
    expect(symbolMapText).toContain("Tree-sitter incremental parser concrete syntax tree grammar.js tree-sitter.json node-types.json queries captures predicates highlights locals injections tags parse query test");
    expect(symbolMapText).toContain("\"symbolsByKind\"");
    expect(symbolMapText).toContain("\"codeIntelligenceSignals\"");
    expect(symbolMapText).toContain("\"syntaxParserSignals\"");
    expect(symbolMapText).toContain("\"symbolNavigationPrompts\"");
    expect(symbolMapText).toContain("\"syntaxQueryPrompts\"");
    const symbolMapHtml = await fs.readFile(path.join(result.session.outputPaths.html, "symbol-map.html"), "utf8");
    expect(symbolMapHtml).toContain("심볼 맵");
    expect(symbolMapHtml).toContain("symbol-map-card");
    expect(symbolMapHtml).toContain("symbol-source-link");
    expect(symbolMapHtml).toContain("data-source-pattern=\"codebase-map\"");
    expect(symbolMapHtml).toContain("Code Intelligence Signals");
    expect(symbolMapHtml).toContain("Syntax Parser Signals");
    expect(symbolMapHtml).toContain("Symbol Navigation Prompts");
    expect(symbolMapHtml).toContain("Syntax Query Prompts");
    const symbolMapMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "symbol-map.md"), "utf8");
    expect(symbolMapMarkdown).toContain("# 심볼 맵");
    expect(symbolMapMarkdown).toContain("Source pattern: codebase-map");
    expect(symbolMapMarkdown).toContain("## Code Intelligence Signals");
    expect(symbolMapMarkdown).toContain("## Symbol Navigation Prompts");
    const apiReferenceText = await fs.readFile(path.join(result.session.outputPaths.analysis, "api-reference-report.json"), "utf8");
    expect(apiReferenceText).toContain("TypeDoc entry points reflections ReflectionKind public API documentation export validation typedoc.json typedocOptions outputs html json emit plugin");
    expect(apiReferenceText).toContain("\"entryPoints\"");
    expect(apiReferenceText).toContain("\"publicSymbols\"");
    expect(apiReferenceText).toContain("\"exportWarnings\"");
    expect(apiReferenceText).toContain("\"typedocConfigSignals\"");
    expect(apiReferenceText).toContain("\"outputSignals\"");
    expect(apiReferenceText).toContain("\"validationSignals\"");
    expect(apiReferenceText).toContain("No matching TypeDoc evidence was found in the generated session `source/` snapshot.");
    expect(apiReferenceText).not.toContain("No matching TypeDoc evidence was found in the static source snapshot.");
    const apiReferenceHtml = await fs.readFile(path.join(result.session.outputPaths.html, "api-reference.html"), "utf8");
    expect(apiReferenceHtml).toContain("API Reference");
    expect(apiReferenceHtml).toContain("api-reference-card");
    expect(apiReferenceHtml).toContain("data-source-pattern=\"TypeDoc\"");
    expect(apiReferenceHtml).toContain("ReflectionKind");
    expect(apiReferenceHtml).toContain("TypeDoc Config Signals");
    expect(apiReferenceHtml).toContain("Output Signals");
    expect(apiReferenceHtml).toContain("Validation Signals");
    const apiReferenceMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "api-reference.md"), "utf8");
    expect(apiReferenceMarkdown).toContain("# API Reference");
    expect(apiReferenceMarkdown).toContain("Source pattern: TypeDoc");
    expect(apiReferenceMarkdown).toContain("## Public Symbols");
    expect(apiReferenceMarkdown).toContain("## TypeDoc Config Signals");
    expect(apiReferenceMarkdown).toContain("## Output Signals");
    expect(apiReferenceMarkdown).toContain("## Validation Signals");
    const searchIndexText = await fs.readFile(path.join(result.session.outputPaths.analysis, "search-index-report.json"), "utf8");
    expect(searchIndexText).toContain("Pagefind PageFragmentData MetaIndex filters meta_fields static low-bandwidth search index");
    expect(searchIndexText).toContain("\"documents\"");
    expect(searchIndexText).toContain("\"termIndex\"");
    expect(searchIndexText).toContain("\"filterIndex\"");
    expect(searchIndexText).toContain("\"metadataFields\"");
    expect(searchIndexText).toContain("\"ragChunkingSignals\"");
    expect(searchIndexText).toContain("\"codeSearchQuerySignals\"");
    expect(searchIndexText).toContain("\"conversationStarterPrompts\"");
    expect(searchIndexText).toContain("\"codeSearchDrillPrompts\"");
    expect(searchIndexText).toContain("Repochat GitHub Repository Interactive Chatbot local-first RAG ChromaDB RecursiveCharacterTextSplitter chunk_size chunk_overlap k=3 ConversationBufferMemory standalone question");
    expect(searchIndexText).toContain("Zoekt fast code search substring regexp boolean operators repo file lang branch case type sym trigram index ctags ranking shards JSON API");
    const searchIndexHtml = await fs.readFile(path.join(result.session.outputPaths.html, "search-index.html"), "utf8");
    expect(searchIndexHtml).toContain("Search Index");
    expect(searchIndexHtml).toContain("search-index-card");
    expect(searchIndexHtml).toContain("data-source-pattern=\"Pagefind\"");
    expect(searchIndexHtml).toContain("PageFragmentData");
    expect(searchIndexHtml).toContain("MetaIndex");
    expect(searchIndexHtml).toContain("RAG Chunking Signals");
    expect(searchIndexHtml).toContain("Code Search Query Signals");
    expect(searchIndexHtml).toContain("Conversation Starter Prompts");
    expect(searchIndexHtml).toContain("Code Search Drill Prompts");
    expect(searchIndexHtml).toContain("data-conversation-prompt=\"follow-up\"");
    expect(searchIndexHtml).toContain("data-code-search-drill=\"symbol\"");
    const searchIndexMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "search-index.md"), "utf8");
    expect(searchIndexMarkdown).toContain("# Search Index");
    expect(searchIndexMarkdown).toContain("Source pattern: Pagefind");
    expect(searchIndexMarkdown).toContain("## Filter Index");
    expect(searchIndexMarkdown).toContain("## RAG Chunking Signals");
    expect(searchIndexMarkdown).toContain("## Code Search Query Signals");
    expect(searchIndexMarkdown).toContain("## Conversation Starter Prompts");
    expect(searchIndexMarkdown).toContain("## Code Search Drill Prompts");
    const searchIndexAsset = await fs.readFile(path.join(result.session.outputPaths.html, "assets", "search-index.json"), "utf8");
    expect(searchIndexAsset).toContain("\"documents\"");
    expect(searchIndexAsset).toContain("\"termIndex\"");
    expect(searchIndexAsset).toContain("\"ragChunkingSignals\"");
    expect(searchIndexAsset).toContain("\"codeSearchQuerySignals\"");
    expect(searchIndexAsset).toContain("\"conversationStarterPrompts\"");
    expect(searchIndexAsset).toContain("\"codeSearchDrillPrompts\"");
    const learningJournalText = await fs.readFile(path.join(result.session.outputPaths.analysis, "learning-journal-report.json"), "utf8");
    expect(learningJournalText).toContain("learn-codebase Socratic tutor active recall prediction before revelation persistent learning journal");
    expect(learningJournalText).toContain("\"focusGoals\"");
    expect(learningJournalText).toContain("\"masteryLevels\"");
    expect(learningJournalText).toContain("\"openQuestions\"");
    expect(learningJournalText).toContain("\"spacedReviewQueue\"");
    expect(learningJournalText).toContain("\"socraticPrompts\"");
    expect(learningJournalText).toContain("\"mentorReflectionLoops\"");
    expect(learningJournalText).toContain("\"repoGroundedFeedbackPrompts\"");
    expect(learningJournalText).toContain("\"vibeCodingBuildBriefs\"");
    expect(learningJournalText).toContain("\"aiBuildPromptPacks\"");
    expect(learningJournalText).toContain("\"verificationBoundaries\"");
    expect(learningJournalText).toContain("Which generated report would fail first if a generated session `source/` file disappeared?");
    expect(learningJournalText).not.toContain("Which generated report would fail first if a copied source file disappeared?");
    expect(learningJournalText).toContain("Hephaestus process-aware mentoring self-regulated learning repo-grounded guidance issues commits reviews pull requests standup leaderboard recognition");
    expect(learningJournalText).toContain("AI-native vibe-coding build brief repo map context curation planning agent spec writer task breakdown reviewer debugger verification prompt pack");
    expect(learningJournalText).toContain("전통 문법 암기가 아니라 AI에게 비슷한 앱을 만들도록 지시");
    expect(learningJournalText).toContain("현재 산출물을 학습자 문서 초안으로 바꿔줘");
    expect(learningJournalText).not.toContain("최종 결과를 학습자 문서로 바꿔줘");
    expect(learningJournalText).toContain("\"journalTemplateMarkdown\"");
    const learningJournalHtml = await fs.readFile(path.join(result.session.outputPaths.html, "learning-journal.html"), "utf8");
    expect(learningJournalHtml).toContain("Learning Journal");
    expect(learningJournalHtml).toContain("learning-journal-card");
    expect(learningJournalHtml).toContain("data-source-pattern=\"learn-codebase\"");
    expect(learningJournalHtml).toContain("Active Recall Journal");
    expect(learningJournalHtml).toContain("Spaced Review Queue");
    expect(learningJournalHtml).toContain("Mentor Reflection Loops");
    expect(learningJournalHtml).toContain("Repo-Grounded Feedback Prompts");
    expect(learningJournalHtml).toContain("Vibe-Coding Build Brief");
    expect(learningJournalHtml).toContain("AI Build Prompt Packs");
    expect(learningJournalHtml).toContain("Verification Boundaries");
    expect(learningJournalHtml).toContain("data-mentor-loop=\"goal-strategy-reflection\"");
    expect(learningJournalHtml).toContain("data-feedback-signal=\"pull-requests\"");
    expect(learningJournalHtml).toContain("data-vibe-role=\"context-curator\"");
    expect(learningJournalHtml).toContain("data-prompt-phase=\"specify\"");
    expect(learningJournalHtml).toContain("data-verification-boundary=\"needs-human-review\"");
    expect(learningJournalHtml).toContain("현재 산출물을 학습자 문서 초안으로 바꿔줘");
    expect(learningJournalHtml).not.toContain("최종 결과를 학습자 문서로 바꿔줘");
    const learningJournalMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "learning-journal.md"), "utf8");
    expect(learningJournalMarkdown).toContain("# Learning Journal");
    expect(learningJournalMarkdown).toContain("Source pattern: learn-codebase");
    expect(learningJournalMarkdown).toContain("## Open Questions");
    expect(learningJournalMarkdown).toContain("## Spaced Review Queue");
    expect(learningJournalMarkdown).toContain("## Mentor Reflection Loops");
    expect(learningJournalMarkdown).toContain("## Repo-Grounded Feedback Prompts");
    expect(learningJournalMarkdown).toContain("## Vibe-Coding Build Brief");
    expect(learningJournalMarkdown).toContain("## AI Build Prompt Packs");
    expect(learningJournalMarkdown).toContain("## Verification Boundaries");
    expect(learningJournalMarkdown).toContain("현재 산출물을 학습자 문서 초안으로 바꿔줘");
    expect(learningJournalMarkdown).not.toContain("최종 결과를 학습자 문서로 바꿔줘");
    const learningJournalTemplateAsset = await fs.readFile(path.join(result.session.outputPaths.html, "assets", "learning-journal-template.md"), "utf8");
    expect(learningJournalTemplateAsset).toContain("# Codebase Learning Journal");
    expect(learningJournalTemplateAsset).toContain("바이브코딩 개발자");
    expect(learningJournalTemplateAsset).toContain("## Concept Mastery Map");
    expect(learningJournalTemplateAsset).toContain("## Mentor Reflection Loops");
    expect(learningJournalTemplateAsset).toContain("## Repo-Grounded Feedback Prompts");
    expect(learningJournalTemplateAsset).toContain("## Vibe-Coding Build Brief");
    expect(learningJournalTemplateAsset).toContain("## AI Build Prompt Packs");
    expect(learningJournalTemplateAsset).toContain("## Verification Boundaries");
    const dailySummaryText = await fs.readFile(path.join(result.session.outputPaths.analysis, "daily-summary-report.json"), "utf8");
    expect(dailySummaryText).toContain("\"sourceHandling\"");
    expect(dailySummaryText).toContain("\"architectureLens\"");
    expect(dailySummaryText).toContain("\"termsToKnow\"");
    expect(dailySummaryText).toContain("\"promptsToReuse\"");
    expect(dailySummaryText).toContain("source-as-evidence not source-as-product-knowledge");
    expect(dailySummaryText).toContain("원본 소스 전체를 앱 지식으로 내장하지 않고");
    expect(dailySummaryText).toContain("AI에게 계획과 검증 기준만 요청하세요");
    expect(dailySummaryText).toContain("AI 설명 요청 후보");
    expect(dailySummaryText).toContain("보내기 전 소스 근거와 내 목표에 맞게 다듬으세요");
    expect(dailySummaryText).not.toContain("첫 프롬프트를 복사해 AI에게 계획만 요청하세요");
    expect(dailySummaryText).not.toContain("맥락으로 설명하라고 지시하세요");
    const dailySummaryHtml = await fs.readFile(path.join(result.session.outputPaths.html, "daily-summary.html"), "utf8");
    expect(dailySummaryHtml).toContain("오늘의 학습 요약");
    expect(dailySummaryHtml).toContain("daily-summary-card");
    expect(dailySummaryHtml).toContain("소스 처리 원칙");
    expect(dailySummaryHtml).toContain("다시 쓸 프롬프트");
    expect(dailySummaryHtml).toContain("AI에게 계획과 검증 기준만 요청하세요");
    expect(dailySummaryHtml).toContain("AI 설명 요청 후보");
    expect(dailySummaryHtml).not.toContain("맥락으로 설명하라고 지시하세요");
    const dailySummaryMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "daily-summary.md"), "utf8");
    expect(dailySummaryMarkdown).toContain("# 오늘의 학습 요약");
    expect(dailySummaryMarkdown).toContain("## 소스 처리 원칙");
    expect(dailySummaryMarkdown).toContain("## 다시 쓸 프롬프트");
    expect(dailySummaryMarkdown).toContain("AI 설명 요청 후보");
    expect(dailySummaryMarkdown).not.toContain("맥락으로 설명하라고 지시하세요");
    const vibePromptPackText = await fs.readFile(path.join(result.session.outputPaths.analysis, "vibe-coding-prompt-pack-report.json"), "utf8");
    expect(vibePromptPackText).toContain("\"promptSequence\"");
    expect(vibePromptPackText).toContain("\"copyPastePrompt\"");
    expect(vibePromptPackText).toContain("\"aiGuardrails\"");
    expect(vibePromptPackText).toContain("AI-native vibe-coding prompt pack");
    expect(vibePromptPackText).toContain("나는 전문 개발자가 아니라 바이브코딩 개발자야");
    expect(vibePromptPackText).toContain("PRD");
    expect(vibePromptPackText).toContain("SDD");
    expect(vibePromptPackText).toContain("TDD");
    expect(vibePromptPackText).toContain("acceptance criteria");
    expect(vibePromptPackText).toContain("변경 뒤 확인할 명령");
    expect(vibePromptPackText).not.toContain("완료 후 확인할 명령");
    expect(vibePromptPackText).toContain("구현됐다고 가정하고 리뷰해줘");
    expect(vibePromptPackText).not.toContain("끝났다고 가정하고 리뷰해줘");
    expect(vibePromptPackText).not.toContain("최종 결과를 리뷰해줘");
    const vibePromptPackHtml = await fs.readFile(path.join(result.session.outputPaths.html, "vibe-coding-prompt-pack.html"), "utf8");
    expect(vibePromptPackHtml).toContain("AI Implementation Prompt Pack");
    expect(vibePromptPackHtml).toContain("Source-Grounded Implementation Prompt");
    expect(vibePromptPackHtml).not.toContain("Copy/Paste Prompt");
    expect(vibePromptPackHtml).toContain("AI 도구 지시서 골격");
    expect(vibePromptPackHtml).toContain("Copilot/OpenAI agent instruction context");
    expect(vibePromptPackHtml).toContain("역할과 목표:");
    expect(vibePromptPackHtml).toContain("작업 규칙:");
    expect(vibePromptPackHtml).toContain("검증 루브릭:");
    expect(vibePromptPackHtml).toContain("어떤 명령, 테스트, 화면, 리포트로 변경 결과를 검증할지 먼저 적는다");
    expect(vibePromptPackHtml).not.toContain("어떤 명령, 테스트, 화면, 리포트로 완료를 확인할지 먼저 적는다");
    expect(vibePromptPackHtml).toContain("PASS_REVIEW / REVISE / BLOCK 기준으로 AI 산출물을 검토");
    expect(vibePromptPackHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보일 뿐이다");
    expect(vibePromptPackHtml).not.toContain("PASS / REVISE / BLOCK 기준으로 AI 산출물을 검토");
    expect(vibePromptPackHtml).toContain("변경 뒤 확인할 명령");
    expect(vibePromptPackHtml).not.toContain("완료 후 확인할 명령");
    expect(vibePromptPackHtml).toContain("구현됐다고 가정하고 리뷰해줘");
    expect(vibePromptPackHtml).not.toContain("끝났다고 가정하고 리뷰해줘");
    expect(vibePromptPackHtml).not.toContain("최종 결과를 리뷰해줘");
    expect(vibePromptPackHtml).toContain("data-prompt-phase=\"orient\"");
    expect(vibePromptPackHtml).toContain("vibe-prompt-pack-card");
    const vibePromptPackMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "vibe-coding-prompt-pack.md"), "utf8");
    expect(vibePromptPackMarkdown).toContain("# Vibe-Coding Prompt Pack");
    expect(vibePromptPackMarkdown).toContain("## Source-Grounded Implementation Prompt");
    expect(vibePromptPackMarkdown).not.toContain("## Copy/Paste Prompt");
    expect(vibePromptPackMarkdown).toContain("## AI 도구 지시서 골격");
    expect(vibePromptPackMarkdown).toContain("긴 소스 전체가 아니라");
    expect(vibePromptPackMarkdown).toContain("역할과 목표:");
    expect(vibePromptPackMarkdown).toContain("프로젝트 맥락:");
    expect(vibePromptPackMarkdown).toContain("검증 루브릭:");
    expect(vibePromptPackMarkdown).toContain("어떤 명령, 테스트, 화면, 리포트로 변경 결과를 검증할지 먼저 적는다");
    expect(vibePromptPackMarkdown).not.toContain("어떤 명령, 테스트, 화면, 리포트로 완료를 확인할지 먼저 적는다");
    expect(vibePromptPackMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK 기준으로 AI 산출물을 검토");
    expect(vibePromptPackMarkdown).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보일 뿐이다");
    expect(vibePromptPackMarkdown).not.toContain("PASS / REVISE / BLOCK 기준으로 AI 산출물을 검토");
    expect(vibePromptPackMarkdown).toContain("변경 뒤 확인할 명령");
    expect(vibePromptPackMarkdown).not.toContain("완료 후 확인할 명령");
    expect(vibePromptPackMarkdown).toContain("구현됐다고 가정하고 리뷰해줘");
    expect(vibePromptPackMarkdown).not.toContain("끝났다고 가정하고 리뷰해줘");
    expect(vibePromptPackMarkdown).not.toContain("최종 결과를 리뷰해줘");
    expect(vibePromptPackMarkdown).toContain("필요한 프로세스 용어: PRD, SDD, TDD, acceptance criteria");
    expect(vibePromptPackMarkdown).toContain("## Learner Checklist");
    const projectActivityText = await fs.readFile(path.join(result.session.outputPaths.analysis, "project-activity-report.json"), "utf8");
    expect(projectActivityText).toContain("Repowise git analytics code health hotspots ownership co-change dead code architectural decisions MCP risk");
    expect(projectActivityText).toContain("\"historyAvailability\"");
    expect(projectActivityText).toContain("\"activitySignals\"");
    expect(projectActivityText).toContain("\"hotspotCandidates\"");
    expect(projectActivityText).toContain("\"deadCodeCandidates\"");
    expect(projectActivityText).toContain("\"reviewQueues\"");
    expect(projectActivityText).toContain("\"architectureDecisionPrompts\"");
    const projectActivityHtml = await fs.readFile(path.join(result.session.outputPaths.html, "project-activity.html"), "utf8");
    expect(projectActivityHtml).toContain("Project Activity");
    expect(projectActivityHtml).toContain("project-activity-card");
    expect(projectActivityHtml).toContain("data-source-pattern=\"Repowise\"");
    expect(projectActivityHtml).toContain("Project Activity Snapshot");
    expect(projectActivityHtml).toContain("history mode");
    const projectActivityMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "project-activity.md"), "utf8");
    expect(projectActivityMarkdown).toContain("# Project Activity");
    expect(projectActivityMarkdown).toContain("Source pattern: Repowise");
    expect(projectActivityMarkdown).toContain("## History Availability");
    expect(projectActivityMarkdown).toContain("## Review Queues");
    const codeMetricsText = await fs.readFile(path.join(result.session.outputPaths.analysis, "code-metrics-readiness-report.json"), "utf8");
    expect(codeMetricsText).toContain("scc lizard tokei cloc radon cyclomatic complexity code lines comments blanks hotspots COCOMO LOCOMO JSON CSV HTML OpenMetrics thresholds");
    expect(codeMetricsText).toContain("CodeCharta local code maps cc.json Web Studio area height color delta parser importer filter ValidationTool InspectionTool");
    expect(codeMetricsText).toContain("\"languageMetrics\"");
    expect(codeMetricsText).toContain("\"hotspots\"");
    expect(codeMetricsText).toContain("\"toolSignals\"");
    expect(codeMetricsText).toContain("\"metricSignals\"");
    expect(codeMetricsText).toContain("\"workflowSignals\"");
    expect(codeMetricsText).toContain("\"codeMapMetricBindings\"");
    expect(codeMetricsText).toContain("\"codeMapSignals\"");
    expect(codeMetricsText).toContain("\"complexityDensity\"");
    expect(codeMetricsText).toContain("scc --by-file --wide --format json .");
    expect(codeMetricsText).toContain("ccsh rawtextparser .");
    const codeMetricsHtml = await fs.readFile(path.join(result.session.outputPaths.html, "code-metrics-readiness.html"), "utf8");
    expect(codeMetricsHtml).toContain("Code Metrics Readiness");
    expect(codeMetricsHtml).toContain("code-metrics-readiness-card");
    expect(codeMetricsHtml).toContain("data-source-pattern=\"scc\"");
    expect(codeMetricsHtml).toContain("Code Metrics Snapshot");
    expect(codeMetricsHtml).toContain("Code Map Metric Bindings");
    expect(codeMetricsHtml).toContain("Code Map Signals");
    expect(codeMetricsHtml).toContain("branch tokens");
    const codeMetricsMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "code-metrics-readiness.md"), "utf8");
    expect(codeMetricsMarkdown).toContain("# Code Metrics Readiness");
    expect(codeMetricsMarkdown).toContain("Source pattern: scc");
    expect(codeMetricsMarkdown).toContain("## Language Metrics");
    expect(codeMetricsMarkdown).toContain("## Workflow Signals");
    expect(codeMetricsMarkdown).toContain("## Code Map Metric Bindings");
    expect(codeMetricsMarkdown).toContain("## Code Map Signals");
    const codeOwnershipText = await fs.readFile(path.join(result.session.outputPaths.analysis, "code-ownership-readiness-report.json"), "utf8");
    expect(codeOwnershipText).toContain("CODEOWNERS standard locations root .github docs gitignore-style patterns owners teams users email last matching rule branch protection required code owner reviews rulesets syntax owner file duplicate not-owned validation");
    expect(codeOwnershipText).toContain("\"codeownerFiles\"");
    expect(codeOwnershipText).toContain("\"ownershipSignals\"");
    expect(codeOwnershipText).toContain("\"validationSignals\"");
    expect(codeOwnershipText).toContain("\"reviewSignals\"");
    expect(codeOwnershipText).toContain("\"coverageSignals\"");
    expect(codeOwnershipText).toContain("\"packageSignals\"");
    expect(codeOwnershipText).toContain("codeowners-validator --checks files,owners,duppatterns,syntax");
    const codeOwnershipHtml = await fs.readFile(path.join(result.session.outputPaths.html, "code-ownership-readiness.html"), "utf8");
    expect(codeOwnershipHtml).toContain("Code Ownership Readiness");
    expect(codeOwnershipHtml).toContain("code-ownership-readiness-card");
    expect(codeOwnershipHtml).toContain("data-source-pattern=\"CODEOWNERS\"");
    expect(codeOwnershipHtml).toContain("CODEOWNERS Files");
    expect(codeOwnershipHtml).toContain("Review Signals");
    const codeOwnershipMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "code-ownership-readiness.md"), "utf8");
    expect(codeOwnershipMarkdown).toContain("# Code Ownership Readiness");
    expect(codeOwnershipMarkdown).toContain("Source pattern: CODEOWNERS");
    expect(codeOwnershipMarkdown).toContain("## CODEOWNERS Files");
    expect(codeOwnershipMarkdown).toContain("## Review Signals");
    const largeAssetText = await fs.readFile(path.join(result.session.outputPaths.analysis, "large-asset-readiness-report.json"), "utf8");
    expect(largeAssetText).toContain("Git LFS DVC large file data versioning .gitattributes filter=lfs pointer oid sha256 lockable migrate track status fsck prune dvc.yaml dvc.lock outs deps metrics params remote cache push pull status repro");
    expect(largeAssetText).toContain("\"assetSetups\"");
    expect(largeAssetText).toContain("\"lfsSignals\"");
    expect(largeAssetText).toContain("\"dvcSignals\"");
    expect(largeAssetText).toContain("\"submoduleSignals\"");
    expect(largeAssetText).toContain("\"workflowSignals\"");
    expect(largeAssetText).toContain("\"packageSignals\"");
    expect(largeAssetText).toContain("git lfs fsck --pointers BASE..HEAD");
    const largeAssetHtml = await fs.readFile(path.join(result.session.outputPaths.html, "large-asset-readiness.html"), "utf8");
    expect(largeAssetHtml).toContain("Large Asset Readiness");
    expect(largeAssetHtml).toContain("large-asset-readiness-card");
    expect(largeAssetHtml).toContain("data-source-pattern=\"Git LFS DVC\"");
    expect(largeAssetHtml).toContain("Asset Setups");
    expect(largeAssetHtml).toContain("DVC Signals");
    const largeAssetMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "large-asset-readiness.md"), "utf8");
    expect(largeAssetMarkdown).toContain("# Large Asset Readiness");
    expect(largeAssetMarkdown).toContain("Source pattern: Git LFS DVC");
    expect(largeAssetMarkdown).toContain("## Asset Setups");
    expect(largeAssetMarkdown).toContain("## DVC Signals");
    const licenseRightsText = await fs.readFile(path.join(result.session.outputPaths.analysis, "license-rights-report.json"), "utf8");
    expect(licenseRightsText).toContain("Licensee license file detection filename score SPDX confidence matched_files package metadata README references human compliance review");
    expect(licenseRightsText).toContain("\"detectedProjectLicense\"");
    expect(licenseRightsText).toContain("\"licenseFiles\"");
    expect(licenseRightsText).toContain("\"packageLicenseSignals\"");
    expect(licenseRightsText).toContain("\"readmeLicenseReferences\"");
    expect(licenseRightsText).toContain("\"reviewWarnings\"");
    expect(licenseRightsText).toContain("\"rightsChecklist\"");
    expect(licenseRightsText).toContain("No project license could be detected");
    const licenseRightsHtml = await fs.readFile(path.join(result.session.outputPaths.html, "license-rights.html"), "utf8");
    expect(licenseRightsHtml).toContain("License Rights");
    expect(licenseRightsHtml).toContain("license-rights-card");
    expect(licenseRightsHtml).toContain("data-source-pattern=\"Licensee\"");
    expect(licenseRightsHtml).toContain("License Rights Snapshot");
    const licenseRightsMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "license-rights.md"), "utf8");
    expect(licenseRightsMarkdown).toContain("# License Rights");
    expect(licenseRightsMarkdown).toContain("Source pattern: Licensee");
    expect(licenseRightsMarkdown).toContain("## Rights Checklist");
    expect(licenseRightsMarkdown).toContain("## Review Warnings");
    const sbomText = await fs.readFile(path.join(result.session.outputPaths.analysis, "sbom-report.json"), "utf8");
    expect(sbomText).toContain("Syft SBOM source descriptor artifacts packages file metadata relationships CycloneDX SPDX output formats");
    expect(sbomText).toContain("\"sourceDescriptor\"");
    expect(sbomText).toContain("\"packageManifests\"");
    expect(sbomText).toContain("\"packageArtifacts\"");
    expect(sbomText).toContain("\"fileArtifacts\"");
    expect(sbomText).toContain("\"relationships\"");
    expect(sbomText).toContain("\"outputFormats\"");
    expect(sbomText).toContain("\"purl\"");
    expect(sbomText).toContain("zod");
    const sbomHtml = await fs.readFile(path.join(result.session.outputPaths.html, "sbom.html"), "utf8");
    expect(sbomHtml).toContain("SBOM");
    expect(sbomHtml).toContain("sbom-card");
    expect(sbomHtml).toContain("data-source-pattern=\"Syft\"");
    expect(sbomHtml).toContain("SBOM Snapshot");
    const sbomMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "sbom.md"), "utf8");
    expect(sbomMarkdown).toContain("# SBOM");
    expect(sbomMarkdown).toContain("Source pattern: Syft");
    expect(sbomMarkdown).toContain("## Package Artifacts");
    expect(sbomMarkdown).toContain("## Output Formats");
    const securityReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "security-readiness-report.json"), "utf8");
    expect(securityReadinessText).toContain("Trivy targets scanners vulnerability secret misconfiguration license SBOM severity security readiness");
    expect(securityReadinessText).toContain("\"scannerTargets\"");
    expect(securityReadinessText).toContain("\"scannerCoverage\"");
    expect(securityReadinessText).toContain("\"securitySignals\"");
    expect(securityReadinessText).toContain("\"actionQueue\"");
    expect(securityReadinessText).toContain("\"recommendedCommands\"");
    expect(securityReadinessText).toContain("trivy fs --scanners vuln,secret,misconfig,license");
    const securityReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "security-readiness.html"), "utf8");
    expect(securityReadinessHtml).toContain("Security Readiness");
    expect(securityReadinessHtml).toContain("security-readiness-card");
    expect(securityReadinessHtml).toContain("data-source-pattern=\"Trivy\"");
    expect(securityReadinessHtml).toContain("Security Readiness Snapshot");
    const securityReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "security-readiness.md"), "utf8");
    expect(securityReadinessMarkdown).toContain("# Security Readiness");
    expect(securityReadinessMarkdown).toContain("Source pattern: Trivy");
    expect(securityReadinessMarkdown).toContain("## Scanner Targets");
    expect(securityReadinessMarkdown).toContain("## Action Queue");
    const sastReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "sast-readiness-report.json"), "utf8");
    expect(sastReadinessText).toContain("SAST readiness Semgrep rules pattern pattern-either metavariable CodeQL init analyze queries security-extended security-and-quality qlpack SonarQube sonar-project.properties sonar.sources sonar.exclusions quality gate Snyk Code SARIF upload-sarif code scanning");
    expect(sastReadinessText).toContain("\"sastSetups\"");
    expect(sastReadinessText).toContain("\"toolSignals\"");
    expect(sastReadinessText).toContain("\"ruleSignals\"");
    expect(sastReadinessText).toContain("\"querySignals\"");
    expect(sastReadinessText).toContain("\"outputSignals\"");
    expect(sastReadinessText).toContain("RepoTutor records SAST readiness only");
    const sastReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "sast-readiness.html"), "utf8");
    expect(sastReadinessHtml).toContain("SAST Readiness");
    expect(sastReadinessHtml).toContain("sast-readiness-card");
    expect(sastReadinessHtml).toContain("data-source-pattern=\"SAST\"");
    expect(sastReadinessHtml).toContain("SAST Snapshot");
    const sastReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "sast-readiness.md"), "utf8");
    expect(sastReadinessMarkdown).toContain("# SAST Readiness");
    expect(sastReadinessMarkdown).toContain("Source pattern: SAST");
    expect(sastReadinessMarkdown).toContain("## Rule Signals");
    expect(sastReadinessMarkdown).toContain("## Output Signals");
    const dastReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "dast-readiness-report.json"), "utf8");
    expect(dastReadinessText).toContain("DAST readiness OWASP ZAP zap-baseline-scan zap-full-scan zap-api-scan spider ajaxSpider active scan context auth policy report nuclei -dast templates workflows severity rate-limit headless interactsh secureCodeBox ScanType parser hooks findings SARIF JUnit HTML");
    expect(dastReadinessText).toContain("\"dastSetups\"");
    expect(dastReadinessText).toContain("\"activeScanSignals\"");
    expect(dastReadinessText).toContain("\"safetySignals\"");
    expect(dastReadinessText).toContain("Run OWASP ZAP, nuclei, secureCodeBox, browser, HTTP, spider, active scan, fuzzing, auth, and webhook commands only against authorized test targets");
    const dastReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "dast-readiness.html"), "utf8");
    expect(dastReadinessHtml).toContain("DAST Readiness");
    expect(dastReadinessHtml).toContain("dast-readiness-card");
    expect(dastReadinessHtml).toContain("data-source-pattern=\"DAST\"");
    expect(dastReadinessHtml).toContain("Active Scan Signals");
    expect(dastReadinessHtml).toContain("does not launch browsers");
    const dastReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "dast-readiness.md"), "utf8");
    expect(dastReadinessMarkdown).toContain("# DAST Readiness");
    expect(dastReadinessMarkdown).toContain("Source pattern: DAST readiness");
    expect(dastReadinessMarkdown).toContain("## Active Scan Signals");
    const threatModelReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "threat-model-readiness-report.json"), "utf8");
    expect(threatModelReadinessText).toContain("Threat model readiness pytm Threat Dragon Threagile open threat model STRIDE DFD data flow diagram trust boundary technical_assets data_assets communication_links risk_tracking abuse_cases mitigation report JSON Markdown PDF Graphviz");
    expect(threatModelReadinessText).toContain("\"threatModelSetups\"");
    expect(threatModelReadinessText).toContain("\"modelSignals\"");
    expect(threatModelReadinessText).toContain("\"threatSignals\"");
    expect(threatModelReadinessText).toContain("Run pytm, Threat Dragon, Threagile, Graphviz, Docker, and report generation only in an authorized local or CI environment");
    const threatModelReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "threat-model-readiness.html"), "utf8");
    expect(threatModelReadinessHtml).toContain("Threat Model Readiness");
    expect(threatModelReadinessHtml).toContain("threat-model-readiness-card");
    expect(threatModelReadinessHtml).toContain("data-source-pattern=\"Threat Model\"");
    expect(threatModelReadinessHtml).toContain("does not execute pytm");
    const threatModelReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "threat-model-readiness.md"), "utf8");
    expect(threatModelReadinessMarkdown).toContain("# Threat Model Readiness");
    expect(threatModelReadinessMarkdown).toContain("Source pattern: Threat model readiness");
    expect(threatModelReadinessMarkdown).toContain("## Threat Signals");
    const scorecardText = await fs.readFile(path.join(result.session.outputPaths.analysis, "scorecard-report.json"), "utf8");
    expect(scorecardText).toContain("OpenSSF Scorecard checks score 0-10 risk remediation structured results policy measurement");
    expect(scorecardText).toContain("\"aggregateScore\"");
    expect(scorecardText).toContain("\"checks\"");
    expect(scorecardText).toContain("\"categoryScores\"");
    expect(scorecardText).toContain("\"policyFindings\"");
    expect(scorecardText).toContain("\"riskQueue\"");
    expect(scorecardText).toContain("\"structuredResults\"");
    expect(scorecardText).toContain("Pinned-Dependencies");
    expect(scorecardText).toContain("Branch-Protection");
    expect(scorecardText).toContain("Branch protection requires live source-host provider settings and cannot be proven from the generated session `source/` snapshot.");
    expect(scorecardText).not.toContain("Branch protection requires live source-host provider settings and cannot be proven from the safe local source snapshot.");
    const scorecardHtml = await fs.readFile(path.join(result.session.outputPaths.html, "scorecard.html"), "utf8");
    expect(scorecardHtml).toContain("Project Scorecard");
    expect(scorecardHtml).toContain("scorecard-card");
    expect(scorecardHtml).toContain("data-source-pattern=\"OpenSSF Scorecard\"");
    expect(scorecardHtml).toContain("Scorecard Snapshot");
    expect(scorecardHtml).toContain("Risk Queue");
    const scorecardMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "scorecard.md"), "utf8");
    expect(scorecardMarkdown).toContain("# Project Scorecard");
    expect(scorecardMarkdown).toContain("Source pattern: OpenSSF Scorecard");
    expect(scorecardMarkdown).toContain("## Category Scores");
    expect(scorecardMarkdown).toContain("## Structured Results");
    const provenanceText = await fs.readFile(path.join(result.session.outputPaths.analysis, "provenance-report.json"), "utf8");
    expect(provenanceText).toContain("Cosign signature bundle attestation transparency log trusted root certificate identity verification");
    expect(provenanceText).toContain("\"artifactSignals\"");
    expect(provenanceText).toContain("\"signatureSignals\"");
    expect(provenanceText).toContain("\"attestationSignals\"");
    expect(provenanceText).toContain("\"identityRequirements\"");
    expect(provenanceText).toContain("\"verificationCommands\"");
    expect(provenanceText).toContain("\"riskQueue\"");
    expect(provenanceText).toContain("cosign verify-blob");
    expect(provenanceText).toContain("expected certificate identity");
    expect(provenanceText).toContain("generated session `source/` snapshot");
    expect(provenanceText).toContain("generated session `source/` snapshot text file digest");
    expect(provenanceText).toContain("Trusted root and Rekor proof must be fetched or provided outside the generated session `source/` snapshot.");
    expect(provenanceText).not.toContain("Trusted root and Rekor proof must be fetched or provided outside the static source snapshot.");
    expect(provenanceText).not.toContain("\"artifact\": \"source snapshot\"");
    expect(provenanceText).not.toContain("No source snapshot digest evidence is available.");
    const provenanceHtml = await fs.readFile(path.join(result.session.outputPaths.html, "provenance.html"), "utf8");
    expect(provenanceHtml).toContain("Provenance Readiness");
    expect(provenanceHtml).toContain("provenance-card");
    expect(provenanceHtml).toContain("data-source-pattern=\"Cosign\"");
    expect(provenanceHtml).toContain("Provenance Snapshot");
    expect(provenanceHtml).toContain("Verification Commands");
    const provenanceMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "provenance.md"), "utf8");
    expect(provenanceMarkdown).toContain("# Provenance Readiness");
    expect(provenanceMarkdown).toContain("Source pattern: Cosign");
    expect(provenanceMarkdown).toContain("## Signature Material");
    expect(provenanceMarkdown).toContain("## Identity Requirements");
    const advisoryText = await fs.readFile(path.join(result.session.outputPaths.analysis, "advisory-report.json"), "utf8");
    expect(advisoryText).toContain("OSV-Scanner package extraction vulnerability matching OSV.dev lockfile SBOM offline remediation ignore policy");
    expect(advisoryText).toContain("\"packageQueryTargets\"");
    expect(advisoryText).toContain("\"lockfileSignals\"");
    expect(advisoryText).toContain("\"advisorySources\"");
    expect(advisoryText).toContain("\"policyControls\"");
    expect(advisoryText).toContain("\"resultModel\"");
    expect(advisoryText).toContain("\"remediationQueue\"");
    expect(advisoryText).toContain("osv-scanner scan source");
    expect(advisoryText).toContain("IgnoredVulns");
    expect(advisoryText).toContain("Offline mode needs a downloaded OSV database; none was found in the generated session `source/` snapshot.");
    expect(advisoryText).not.toContain("Offline mode needs a downloaded OSV database; none was found in the source snapshot.");
    const advisoryHtml = await fs.readFile(path.join(result.session.outputPaths.html, "advisories.html"), "utf8");
    expect(advisoryHtml).toContain("Advisory Query Readiness");
    expect(advisoryHtml).toContain("advisory-card");
    expect(advisoryHtml).toContain("data-source-pattern=\"OSV-Scanner\"");
    expect(advisoryHtml).toContain("Advisory Query Snapshot");
    expect(advisoryHtml).toContain("Recommended Commands");
    const advisoryMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "advisories.md"), "utf8");
    expect(advisoryMarkdown).toContain("# Advisory Query Readiness");
    expect(advisoryMarkdown).toContain("Source pattern: OSV-Scanner");
    expect(advisoryMarkdown).toContain("## Package Query Targets");
    expect(advisoryMarkdown).toContain("## Policy Controls");
    const vexText = await fs.readFile(path.join(result.session.outputPaths.analysis, "vex-report.json"), "utf8");
    expect(vexText).toContain("OpenVEX affected not_affected fixed under_investigation justification product subcomponent vulnerability statement attestation SARIF filter");
    expect(vexText).toContain("\"productTargets\"");
    expect(vexText).toContain("\"vulnerabilityInputs\"");
    expect(vexText).toContain("\"statusMatrix\"");
    expect(vexText).toContain("\"justificationCatalog\"");
    expect(vexText).toContain("\"statementDrafts\"");
    expect(vexText).toContain("\"documentWorkflow\"");
    expect(vexText).toContain("\"attestationReadiness\"");
    expect(vexText).toContain("pending-advisory-id");
    expect(vexText).toContain("vexctl filter");
    expect(vexText).toContain("statementDrafts는 실제 취약점 확인 결과가 아니라 pending triage 템플릿입니다.");
    expect(vexText).not.toContain("statementDrafts는 실제 취약점 판정이 아니라 pending triage 템플릿입니다.");
    const vexHtml = await fs.readFile(path.join(result.session.outputPaths.html, "vex.html"), "utf8");
    expect(vexHtml).toContain("OpenVEX Impact Readiness");
    expect(vexHtml).toContain("vex-card");
    expect(vexHtml).toContain("data-source-pattern=\"OpenVEX\"");
    expect(vexHtml).toContain("OpenVEX Snapshot");
    expect(vexHtml).toContain("Document Workflow");
    expect(vexHtml).toContain("statementDrafts는 실제 취약점 확인 결과가 아니라 pending triage 템플릿입니다.");
    expect(vexHtml).not.toContain("statementDrafts는 실제 취약점 판정이 아니라 pending triage 템플릿입니다.");
    const vexMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "vex.md"), "utf8");
    expect(vexMarkdown).toContain("# OpenVEX Impact Readiness");
    expect(vexMarkdown).toContain("Source pattern: OpenVEX");
    expect(vexMarkdown).toContain("## Product Targets");
    expect(vexMarkdown).toContain("## Attestation Readiness");
    expect(vexMarkdown).toContain("statementDrafts는 실제 취약점 확인 결과가 아니라 pending triage 템플릿입니다.");
    expect(vexMarkdown).not.toContain("statementDrafts는 실제 취약점 판정이 아니라 pending triage 템플릿입니다.");
    const policyGateText = await fs.readFile(path.join(result.session.outputPaths.analysis, "policy-gate-report.json"), "utf8");
    expect(policyGateText).toContain("OPA Rego policy input data decision eval test bundle schema strict fail gate");
    expect(policyGateText).toContain("\"policyDocuments\"");
    expect(policyGateText).toContain("\"inputDocuments\"");
    expect(policyGateText).toContain("\"gateQueries\"");
    expect(policyGateText).toContain("\"testCoverage\"");
    expect(policyGateText).toContain("\"bundleReadiness\"");
    expect(policyGateText).toContain("\"decisionOutputs\"");
    expect(policyGateText).toContain("opa check --strict");
    expect(policyGateText).toContain("opa test <policy-dir> --fail-on-empty");
    const policyGateHtml = await fs.readFile(path.join(result.session.outputPaths.html, "policy-gates.html"), "utf8");
    expect(policyGateHtml).toContain("Policy Gate Readiness");
    expect(policyGateHtml).toContain("policy-gate-card");
    expect(policyGateHtml).toContain("data-source-pattern=\"OPA\"");
    expect(policyGateHtml).toContain("Policy Gate Snapshot");
    expect(policyGateHtml).toContain("Recommended Commands");
    const policyGateMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "policy-gates.md"), "utf8");
    expect(policyGateMarkdown).toContain("# Policy Gate Readiness");
    expect(policyGateMarkdown).toContain("Source pattern: OPA");
    expect(policyGateMarkdown).toContain("## Policy Documents");
    expect(policyGateMarkdown).toContain("## Bundle Readiness");
    const apiContractText = await fs.readFile(path.join(result.session.outputPaths.analysis, "api-contract-report.json"), "utf8");
    expect(apiContractText).toContain("Schemathesis OpenAPI GraphQL schema generated cases Hypothesis checks stateful workflows JUnit Allure contract testing");
    expect(apiContractText).toContain("\"schemaDocuments\"");
    expect(apiContractText).toContain("\"operationTargets\"");
    expect(apiContractText).toContain("\"testPhases\"");
    expect(apiContractText).toContain("\"checkMatrix\"");
    expect(apiContractText).toContain("\"runtimeTargets\"");
    expect(apiContractText).toContain("\"reportingOutputs\"");
    expect(apiContractText).toContain("schemathesis run");
    expect(apiContractText).toContain("실제 버그 여부는 원본 API에서 별도 실행해 확인하세요.");
    expect(apiContractText).not.toContain("실제 버그 판정은 원본 API에서 별도 실행하세요.");
    const apiContractHtml = await fs.readFile(path.join(result.session.outputPaths.html, "api-contracts.html"), "utf8");
    expect(apiContractHtml).toContain("API Contract Readiness");
    expect(apiContractHtml).toContain("api-contract-card");
    expect(apiContractHtml).toContain("data-source-pattern=\"Schemathesis\"");
    expect(apiContractHtml).toContain("API Contract Snapshot");
    expect(apiContractHtml).toContain("Runtime Targets");
    expect(apiContractHtml).toContain("실제 버그 여부는 원본 API에서 별도 실행해 확인하세요.");
    expect(apiContractHtml).not.toContain("실제 버그 판정은 원본 API에서 별도 실행하세요.");
    const apiContractMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "api-contracts.md"), "utf8");
    expect(apiContractMarkdown).toContain("# API Contract Readiness");
    expect(apiContractMarkdown).toContain("Source pattern: Schemathesis");
    expect(apiContractMarkdown).toContain("## Schema Documents");
    expect(apiContractMarkdown).toContain("## Reporting Outputs");
    expect(apiContractMarkdown).toContain("실제 버그 여부는 원본 API에서 별도 실행해 확인하세요.");
    expect(apiContractMarkdown).not.toContain("실제 버그 판정은 원본 API에서 별도 실행하세요.");
    const consumerContractText = await fs.readFile(path.join(result.session.outputPaths.analysis, "consumer-contract-readiness-report.json"), "utf8");
    expect(consumerContractText).toContain("Pact consumer driven contracts interactions provider states verifier broker can-i-deploy matchers publish verification");
    expect(consumerContractText).toContain("\"contractSetups\"");
    expect(consumerContractText).toContain("\"interactionSignals\"");
    expect(consumerContractText).toContain("\"providerSignals\"");
    expect(consumerContractText).toContain("\"brokerSignals\"");
    expect(consumerContractText).toContain("\"matcherSignals\"");
    expect(consumerContractText).toContain("\"ciSignals\"");
    expect(consumerContractText).toContain("\"packageSignals\"");
    expect(consumerContractText).toContain("can-i-deploy");
    expect(consumerContractText).toContain("실제 contract pass/fail은 원본 프로젝트에서 Pact 도구를 실행해 확인하세요.");
    expect(consumerContractText).not.toContain("실제 contract pass/fail은 원본 프로젝트에서 Pact 도구를 실행해 판정하세요.");
    const consumerContractHtml = await fs.readFile(path.join(result.session.outputPaths.html, "consumer-contract-readiness.html"), "utf8");
    expect(consumerContractHtml).toContain("Consumer Contract Readiness");
    expect(consumerContractHtml).toContain("consumer-contract-readiness-card");
    expect(consumerContractHtml).toContain("data-source-pattern=\"Pact\"");
    expect(consumerContractHtml).toContain("Consumer Contract Snapshot");
    expect(consumerContractHtml).toContain("Broker Signals");
    expect(consumerContractHtml).toContain("실제 contract pass/fail은 원본 프로젝트에서 Pact 도구를 실행해 확인하세요.");
    expect(consumerContractHtml).not.toContain("실제 contract pass/fail은 원본 프로젝트에서 Pact 도구를 실행해 판정하세요.");
    const consumerContractMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "consumer-contract-readiness.md"), "utf8");
    expect(consumerContractMarkdown).toContain("# Consumer Contract Readiness");
    expect(consumerContractMarkdown).toContain("Source pattern: Pact");
    expect(consumerContractMarkdown).toContain("## Contract Setups");
    expect(consumerContractMarkdown).toContain("## Broker Signals");
    expect(consumerContractMarkdown).toContain("실제 contract pass/fail은 원본 프로젝트에서 Pact 도구를 실행해 확인하세요.");
    expect(consumerContractMarkdown).not.toContain("실제 contract pass/fail은 원본 프로젝트에서 Pact 도구를 실행해 판정하세요.");
    const observabilityText = await fs.readFile(path.join(result.session.outputPaths.analysis, "observability-report.json"), "utf8");
    expect(observabilityText).toContain("OpenTelemetry traces metrics logs resource context propagation exporter instrumentation semantic conventions diagnostics");
    expect(observabilityText).toContain("\"signalPipelines\"");
    expect(observabilityText).toContain("\"instrumentationSignals\"");
    expect(observabilityText).toContain("\"exporterTargets\"");
    expect(observabilityText).toContain("\"resourceAttributes\"");
    expect(observabilityText).toContain("\"propagationContext\"");
    expect(observabilityText).toContain("\"diagnostics\"");
    expect(observabilityText).toContain("OTEL_EXPORTER_OTLP_ENDPOINT");
    const observabilityHtml = await fs.readFile(path.join(result.session.outputPaths.html, "observability.html"), "utf8");
    expect(observabilityHtml).toContain("Observability Readiness");
    expect(observabilityHtml).toContain("observability-card");
    expect(observabilityHtml).toContain("data-source-pattern=\"OpenTelemetry\"");
    expect(observabilityHtml).toContain("Signal Pipelines");
    expect(observabilityHtml).toContain("Diagnostics");
    const observabilityMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "observability.md"), "utf8");
    expect(observabilityMarkdown).toContain("# Observability Readiness");
    expect(observabilityMarkdown).toContain("Source pattern: OpenTelemetry");
    expect(observabilityMarkdown).toContain("## Signal Pipelines");
    expect(observabilityMarkdown).toContain("## Diagnostics");
    const performanceText = await fs.readFile(path.join(result.session.outputPaths.analysis, "performance-report.json"), "utf8");
    expect(performanceText).toContain("k6 load testing scripts scenarios executors thresholds checks metrics outputs summaries cloud performance SLO");
    expect(performanceText).toContain("\"scriptTargets\"");
    expect(performanceText).toContain("\"workloadModels\"");
    expect(performanceText).toContain("\"thresholds\"");
    expect(performanceText).toContain("\"checks\"");
    expect(performanceText).toContain("\"metrics\"");
    expect(performanceText).toContain("\"outputs\"");
    expect(performanceText).toContain("\"runtimeControls\"");
    expect(performanceText).toContain("k6 run --summary-export");
    const performanceHtml = await fs.readFile(path.join(result.session.outputPaths.html, "performance.html"), "utf8");
    expect(performanceHtml).toContain("Performance Readiness");
    expect(performanceHtml).toContain("performance-card");
    expect(performanceHtml).toContain("data-source-pattern=\"k6\"");
    expect(performanceHtml).toContain("Workload Models");
    expect(performanceHtml).toContain("Runtime Controls");
    const performanceMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "performance.md"), "utf8");
    expect(performanceMarkdown).toContain("# Performance Readiness");
    expect(performanceMarkdown).toContain("Source pattern: k6");
    expect(performanceMarkdown).toContain("## Workload Models");
    expect(performanceMarkdown).toContain("## Runtime Controls");
    const profilingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "profiling-readiness-report.json"), "utf8");
    expect(profilingText).toContain("Profiling readiness Clinic.js py-spy Pyroscope pprof flamegraph speedscope heap CPU wall sampling tags permissions CI");
    expect(profilingText).toContain("\"profilingSetups\"");
    expect(profilingText).toContain("\"targetSignals\"");
    expect(profilingText).toContain("\"modeSignals\"");
    expect(profilingText).toContain("\"outputSignals\"");
    expect(profilingText).toContain("\"runtimeSignals\"");
    expect(profilingText).toContain("\"safetySignals\"");
    expect(profilingText).toContain("\"packageSignals\"");
    expect(profilingText).toContain("Run Clinic.js, py-spy, Pyroscope, pprof, eBPF, or profiling commands only in an authorized environment");
    const profilingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "profiling-readiness.html"), "utf8");
    expect(profilingHtml).toContain("Profiling Readiness");
    expect(profilingHtml).toContain("profiling-readiness-card");
    expect(profilingHtml).toContain("data-source-pattern=\"Profiling\"");
    expect(profilingHtml).toContain("does not attach to processes");
    const profilingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "profiling-readiness.md"), "utf8");
    expect(profilingMarkdown).toContain("# Profiling Readiness");
    expect(profilingMarkdown).toContain("## Mode Signals");
    expect(profilingMarkdown).toContain("## Safety Signals");
    const tracingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "tracing-readiness-report.json"), "utf8");
    expect(tracingText).toContain("Tracing readiness OpenTelemetry Jaeger Zipkin Tempo traceparent baggage spans exporters sampling resources backends quality");
    expect(tracingText).toContain("\"tracingSetups\"");
    expect(tracingText).toContain("\"instrumentationSignals\"");
    expect(tracingText).toContain("\"propagationSignals\"");
    expect(tracingText).toContain("\"exporterSignals\"");
    expect(tracingText).toContain("\"samplingSignals\"");
    expect(tracingText).toContain("\"resourceSignals\"");
    expect(tracingText).toContain("\"backendSignals\"");
    expect(tracingText).toContain("\"qualitySignals\"");
    expect(tracingText).toContain("\"packageSignals\"");
    expect(tracingText).toContain("RepoTutor records static tracing readiness only");
    const tracingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "tracing-readiness.html"), "utf8");
    expect(tracingHtml).toContain("Tracing Readiness");
    expect(tracingHtml).toContain("tracing-readiness-card");
    expect(tracingHtml).toContain("data-source-pattern=\"Tracing\"");
    expect(tracingHtml).toContain("does not start SDKs");
    const tracingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "tracing-readiness.md"), "utf8");
    expect(tracingMarkdown).toContain("# Tracing Readiness");
    expect(tracingMarkdown).toContain("## Propagation Signals");
    expect(tracingMarkdown).toContain("## Backend Signals");
    const debugText = await fs.readFile(path.join(result.session.outputPaths.analysis, "debug-readiness-report.json"), "utf8");
    expect(debugText).toContain("Debug readiness VS Code js-debug debugpy Delve DAP launch attach breakpoints source maps path mappings remote logs");
    expect(debugText).toContain("\"debugSetups\"");
    expect(debugText).toContain("\"adapterSignals\"");
    expect(debugText).toContain("\"modeSignals\"");
    expect(debugText).toContain("\"breakpointSignals\"");
    expect(debugText).toContain("\"mappingSignals\"");
    expect(debugText).toContain("\"runtimeSignals\"");
    expect(debugText).toContain("\"remoteSignals\"");
    expect(debugText).toContain("\"diagnosticSignals\"");
    expect(debugText).toContain("\"packageSignals\"");
    expect(debugText).toContain("RepoTutor records static debugging readiness only");
    const debugHtml = await fs.readFile(path.join(result.session.outputPaths.html, "debug-readiness.html"), "utf8");
    expect(debugHtml).toContain("Debug Readiness");
    expect(debugHtml).toContain("debug-readiness-card");
    expect(debugHtml).toContain("data-source-pattern=\"Debug\"");
    expect(debugHtml).toContain("does not launch debuggers");
    const debugMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "debug-readiness.md"), "utf8");
    expect(debugMarkdown).toContain("# Debug Readiness");
    expect(debugMarkdown).toContain("## Breakpoint Signals");
    expect(debugMarkdown).toContain("## Mapping Signals");
    const crashReportingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "crash-reporting-readiness-report.json"), "utf8");
    expect(crashReportingText).toContain("Crash reporting readiness Sentry Bugsnag Rollbar release source maps debug IDs dSYM ProGuard stacktrace breadcrumbs sessions privacy alerts");
    expect(crashReportingText).toContain("\"crashSetups\"");
    expect(crashReportingText).toContain("\"captureSignals\"");
    expect(crashReportingText).toContain("\"releaseSignals\"");
    expect(crashReportingText).toContain("\"symbolicationSignals\"");
    expect(crashReportingText).toContain("\"contextSignals\"");
    expect(crashReportingText).toContain("\"privacySignals\"");
    expect(crashReportingText).toContain("\"deliverySignals\"");
    expect(crashReportingText).toContain("\"workflowSignals\"");
    expect(crashReportingText).toContain("\"packageSignals\"");
    expect(crashReportingText).toContain("RepoTutor records static crash reporting readiness only");
    const crashReportingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "crash-reporting-readiness.html"), "utf8");
    expect(crashReportingHtml).toContain("Crash Reporting Readiness");
    expect(crashReportingHtml).toContain("crash-reporting-readiness-card");
    expect(crashReportingHtml).toContain("data-source-pattern=\"Crash\"");
    expect(crashReportingHtml).toContain("does not send crash events");
    const crashReportingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "crash-reporting-readiness.md"), "utf8");
    expect(crashReportingMarkdown).toContain("# Crash Reporting Readiness");
    expect(crashReportingMarkdown).toContain("## Symbolication Signals");
    expect(crashReportingMarkdown).toContain("## Privacy Signals");
    const incidentResponseText = await fs.readFile(path.join(result.session.outputPaths.analysis, "incident-response-readiness-report.json"), "utf8");
    expect(incidentResponseText).toContain("Incident response readiness PagerDuty Grafana OnCall FireHydrant alert routing escalation schedules runbooks status pages postmortems");
    expect(incidentResponseText).toContain("\"incidentSetups\"");
    expect(incidentResponseText).toContain("\"intakeSignals\"");
    expect(incidentResponseText).toContain("\"triageSignals\"");
    expect(incidentResponseText).toContain("\"onCallSignals\"");
    expect(incidentResponseText).toContain("\"communicationSignals\"");
    expect(incidentResponseText).toContain("\"runbookSignals\"");
    expect(incidentResponseText).toContain("\"lifecycleSignals\"");
    expect(incidentResponseText).toContain("\"governanceSignals\"");
    expect(incidentResponseText).toContain("\"workflowSignals\"");
    expect(incidentResponseText).toContain("\"packageSignals\"");
    expect(incidentResponseText).toContain("RepoTutor records static incident-response readiness only");
    const incidentResponseHtml = await fs.readFile(path.join(result.session.outputPaths.html, "incident-response-readiness.html"), "utf8");
    expect(incidentResponseHtml).toContain("Incident Response Readiness");
    expect(incidentResponseHtml).toContain("incident-response-readiness-card");
    expect(incidentResponseHtml).toContain("data-source-pattern=\"Incident\"");
    expect(incidentResponseHtml).toContain("does not page responders");
    const incidentResponseMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "incident-response-readiness.md"), "utf8");
    expect(incidentResponseMarkdown).toContain("# Incident Response Readiness");
    expect(incidentResponseMarkdown).toContain("## On-Call Signals");
    expect(incidentResponseMarkdown).toContain("## Runbook Signals");
    const sloText = await fs.readFile(path.join(result.session.outputPaths.analysis, "slo-readiness-report.json"), "utf8");
    expect(sloText).toContain("SLO readiness OpenSLO object model DataSource SLO SLI AlertPolicy AlertCondition AlertNotificationTarget Service duration shorthand service level objective SLI error budget burn rate Prometheus recording rules");
    expect(sloText).toContain("\"sloSetups\"");
    expect(sloText).toContain("\"specSignals\"");
    expect(sloText).toContain("\"openSloObjectSignals\"");
    expect(sloText).toContain("\"timeWindowSignals\"");
    expect(sloText).toContain("\"metricSourceSignals\"");
    expect(sloText).toContain("\"indicatorSignals\"");
    expect(sloText).toContain("\"objectiveSignals\"");
    expect(sloText).toContain("\"alertSignals\"");
    expect(sloText).toContain("\"ruleSignals\"");
    expect(sloText).toContain("\"governanceSignals\"");
    expect(sloText).toContain("\"workflowSignals\"");
    expect(sloText).toContain("\"packageSignals\"");
    expect(sloText).toContain("RepoTutor records static SLO readiness only");
    const sloHtml = await fs.readFile(path.join(result.session.outputPaths.html, "slo-readiness.html"), "utf8");
    expect(sloHtml).toContain("SLO Readiness");
    expect(sloHtml).toContain("slo-readiness-card");
    expect(sloHtml).toContain("data-source-pattern=\"SLO\"");
    expect(sloHtml).toContain("does not evaluate PromQL");
    const sloMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "slo-readiness.md"), "utf8");
    expect(sloMarkdown).toContain("# SLO Readiness");
    expect(sloMarkdown).toContain("## OpenSLO Object Signals");
    expect(sloMarkdown).toContain("## Time Window Signals");
    expect(sloMarkdown).toContain("## Metric Source Signals");
    expect(sloMarkdown).toContain("## Indicator Signals");
    expect(sloMarkdown).toContain("## Rule Signals");
    const costText = await fs.readFile(path.join(result.session.outputPaths.analysis, "cost-readiness-report.json"), "utf8");
    expect(costText).toContain("Cost readiness Infracost OpenCost Kubecost FinOps cost allocation cloud cost budget pricing Prometheus");
    expect(costText).toContain("\"costSetups\"");
    expect(costText).toContain("\"estimateSignals\"");
    expect(costText).toContain("\"allocationSignals\"");
    expect(costText).toContain("\"pricingSignals\"");
    expect(costText).toContain("\"budgetSignals\"");
    expect(costText).toContain("\"observabilitySignals\"");
    expect(costText).toContain("\"workflowSignals\"");
    expect(costText).toContain("\"packageSignals\"");
    expect(costText).toContain("RepoTutor records static cost readiness only");
    const costHtml = await fs.readFile(path.join(result.session.outputPaths.html, "cost-readiness.html"), "utf8");
    expect(costHtml).toContain("Cost Readiness");
    expect(costHtml).toContain("cost-readiness-card");
    expect(costHtml).toContain("data-source-pattern=\"Cost\"");
    expect(costHtml).toContain("does not run Infracost");
    const costMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "cost-readiness.md"), "utf8");
    expect(costMarkdown).toContain("# Cost Readiness");
    expect(costMarkdown).toContain("## Allocation Signals");
    expect(costMarkdown).toContain("## Observability Signals");
    const progressiveDeliveryText = await fs.readFile(path.join(result.session.outputPaths.analysis, "progressive-delivery-readiness-report.json"), "utf8");
    expect(progressiveDeliveryText).toContain("Progressive delivery readiness Argo Rollouts Flagger Kayenta canary blue-green traffic routing analysis promotion abort");
    expect(progressiveDeliveryText).toContain("\"rolloutSetups\"");
    expect(progressiveDeliveryText).toContain("\"strategySignals\"");
    expect(progressiveDeliveryText).toContain("\"trafficSignals\"");
    expect(progressiveDeliveryText).toContain("\"analysisSignals\"");
    expect(progressiveDeliveryText).toContain("\"safetySignals\"");
    expect(progressiveDeliveryText).toContain("\"notificationSignals\"");
    expect(progressiveDeliveryText).toContain("\"workflowSignals\"");
    expect(progressiveDeliveryText).toContain("\"packageSignals\"");
    expect(progressiveDeliveryText).toContain("RepoTutor records static progressive delivery readiness only");
    const progressiveDeliveryHtml = await fs.readFile(path.join(result.session.outputPaths.html, "progressive-delivery-readiness.html"), "utf8");
    expect(progressiveDeliveryHtml).toContain("Progressive Delivery Readiness");
    expect(progressiveDeliveryHtml).toContain("progressive-delivery-readiness-card");
    expect(progressiveDeliveryHtml).toContain("data-source-pattern=\"Progressive Delivery\"");
    expect(progressiveDeliveryHtml).toContain("does not apply Rollouts");
    const progressiveDeliveryMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "progressive-delivery-readiness.md"), "utf8");
    expect(progressiveDeliveryMarkdown).toContain("# Progressive Delivery Readiness");
    expect(progressiveDeliveryMarkdown).toContain("## Traffic Signals");
    expect(progressiveDeliveryMarkdown).toContain("## Analysis Signals");
    const loadTestingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "load-testing-readiness-report.json"), "utf8");
    expect(loadTestingText).toContain("k6 Artillery Locust load testing scenarios phases thresholds checks ensure HttpUser headless distributed reports");
    expect(loadTestingText).toContain("\"loadTestSetups\"");
    expect(loadTestingText).toContain("\"toolSignals\"");
    expect(loadTestingText).toContain("\"profileSignals\"");
    expect(loadTestingText).toContain("\"protocolSignals\"");
    expect(loadTestingText).toContain("\"assertionSignals\"");
    expect(loadTestingText).toContain("\"dataSignals\"");
    expect(loadTestingText).toContain("\"executionSignals\"");
    expect(loadTestingText).toContain("\"reportSignals\"");
    expect(loadTestingText).toContain("\"packageSignals\"");
    expect(loadTestingText).toContain("k6 run --summary-export");
    const loadTestingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "load-testing-readiness.html"), "utf8");
    expect(loadTestingHtml).toContain("Load Testing Readiness");
    expect(loadTestingHtml).toContain("load-testing-readiness-card");
    expect(loadTestingHtml).toContain("data-source-pattern=\"k6\"");
    expect(loadTestingHtml).toContain("Load Test Setups");
    expect(loadTestingHtml).toContain("Assertion Signals");
    const loadTestingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "load-testing-readiness.md"), "utf8");
    expect(loadTestingMarkdown).toContain("# Load Testing Readiness");
    expect(loadTestingMarkdown).toContain("Source pattern: k6 Artillery Locust");
    expect(loadTestingMarkdown).toContain("## Profile Signals");
    expect(loadTestingMarkdown).toContain("## Report Signals");
    const benchmarkReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "benchmark-readiness-report.json"), "utf8");
    expect(benchmarkReadinessText).toContain("Vitest bench Tinybench Benchmark.js Hyperfine Criterion pytest-benchmark Go benchmark warmup iterations samples ops/sec export-json regression threshold");
    expect(benchmarkReadinessText).toContain("\"benchmarkSuites\"");
    expect(benchmarkReadinessText).toContain("\"toolSignals\"");
    expect(benchmarkReadinessText).toContain("\"timingSignals\"");
    expect(benchmarkReadinessText).toContain("\"comparisonSignals\"");
    expect(benchmarkReadinessText).toContain("\"reportSignals\"");
    expect(benchmarkReadinessText).toContain("\"ciSignals\"");
    expect(benchmarkReadinessText).toContain("\"packageSignals\"");
    expect(benchmarkReadinessText).toContain("npx vitest bench --run");
    const benchmarkReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "benchmark-readiness.html"), "utf8");
    expect(benchmarkReadinessHtml).toContain("Benchmark Readiness");
    expect(benchmarkReadinessHtml).toContain("benchmark-readiness-card");
    expect(benchmarkReadinessHtml).toContain("data-source-pattern=\"Tinybench\"");
    expect(benchmarkReadinessHtml).toContain("Benchmark Suites");
    expect(benchmarkReadinessHtml).toContain("Comparison Signals");
    const benchmarkReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "benchmark-readiness.md"), "utf8");
    expect(benchmarkReadinessMarkdown).toContain("# Benchmark Readiness");
    expect(benchmarkReadinessMarkdown).toContain("Source pattern: Vitest bench Tinybench");
    expect(benchmarkReadinessMarkdown).toContain("## Timing Signals");
    expect(benchmarkReadinessMarkdown).toContain("## CI Signals");
    const e2eText = await fs.readFile(path.join(result.session.outputPaths.analysis, "e2e-report.json"), "utf8");
    expect(e2eText).toContain("Playwright browser E2E tests defineConfig fixtures projects devices locators assertions poll toPass traces screenshots video reporters CI webServer storageState APIRequestContext");
    expect(e2eText).toContain("\"testSuites\"");
    expect(e2eText).toContain("\"browserProjects\"");
    expect(e2eText).toContain("\"locatorSignals\"");
    expect(e2eText).toContain("\"assertions\"");
    expect(e2eText).toContain("\"artifacts\"");
    expect(e2eText).toContain("\"runtimeTargets\"");
    expect(e2eText).toContain("\"playwrightSignals\"");
    expect(e2eText).toContain("npx playwright test");
    const e2eHtml = await fs.readFile(path.join(result.session.outputPaths.html, "e2e.html"), "utf8");
    expect(e2eHtml).toContain("E2E Readiness");
    expect(e2eHtml).toContain("e2e-card");
    expect(e2eHtml).toContain("data-source-pattern=\"Playwright\"");
    expect(e2eHtml).toContain("Browser Projects");
    expect(e2eHtml).toContain("Runtime Targets");
    expect(e2eHtml).toContain("Playwright Signals");
    const e2eMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "e2e.md"), "utf8");
    expect(e2eMarkdown).toContain("# E2E Readiness");
    expect(e2eMarkdown).toContain("Source pattern: Playwright");
    expect(e2eMarkdown).toContain("## Browser Projects");
    expect(e2eMarkdown).toContain("## Runtime Targets");
    expect(e2eMarkdown).toContain("## Playwright Signals");
    const flakyTestText = await fs.readFile(path.join(result.session.outputPaths.analysis, "flaky-test-readiness-report.json"), "utf8");
    expect(flakyTestText).toContain("Flaky test readiness Playwright retries failOnFlakyTests trace on-first-retry pytest-rerunfailures --reruns --fail-on-flaky jest.retryTimes quarantine skip fixme xfail artifacts");
    expect(flakyTestText).toContain("\"flakyTestSetups\"");
    expect(flakyTestText).toContain("\"frameworkSignals\"");
    expect(flakyTestText).toContain("\"retrySignals\"");
    expect(flakyTestText).toContain("\"quarantineSignals\"");
    expect(flakyTestText).toContain("\"isolationSignals\"");
    expect(flakyTestText).toContain("\"artifactSignals\"");
    expect(flakyTestText).toContain("\"ciSignals\"");
    expect(flakyTestText).toContain("\"packageSignals\"");
    expect(flakyTestText).toContain("npx playwright test --retries=2");
    const flakyTestHtml = await fs.readFile(path.join(result.session.outputPaths.html, "flaky-test-readiness.html"), "utf8");
    expect(flakyTestHtml).toContain("Flaky Test Readiness");
    expect(flakyTestHtml).toContain("flaky-test-readiness-card");
    expect(flakyTestHtml).toContain("data-source-pattern=\"Flaky\"");
    expect(flakyTestHtml).toContain("Flaky Test Setups");
    expect(flakyTestHtml).toContain("Quarantine Signals");
    const flakyTestMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "flaky-test-readiness.md"), "utf8");
    expect(flakyTestMarkdown).toContain("# Flaky Test Readiness");
    expect(flakyTestMarkdown).toContain("Source pattern: Flaky test readiness");
    expect(flakyTestMarkdown).toContain("## Retry Signals");
    expect(flakyTestMarkdown).toContain("## Artifact Signals");
    const testImpactText = await fs.readFile(path.join(result.session.outputPaths.analysis, "test-impact-readiness-report.json"), "utf8");
    expect(testImpactText).toContain("Test impact readiness Nx affected Jest findRelatedTests onlyChanged changedSince pytest-testmon --testmon dependency graph base head changed files CI cache");
    expect(testImpactText).toContain("\"impactSetups\"");
    expect(testImpactText).toContain("\"toolSignals\"");
    expect(testImpactText).toContain("\"changeDetectionSignals\"");
    expect(testImpactText).toContain("\"selectionSignals\"");
    expect(testImpactText).toContain("\"cacheSignals\"");
    expect(testImpactText).toContain("\"ciSignals\"");
    expect(testImpactText).toContain("\"packageSignals\"");
    expect(testImpactText).toContain("npx nx affected -t test --base=origin/main --head=HEAD");
    const testImpactHtml = await fs.readFile(path.join(result.session.outputPaths.html, "test-impact-readiness.html"), "utf8");
    expect(testImpactHtml).toContain("Test Impact Readiness");
    expect(testImpactHtml).toContain("test-impact-readiness-card");
    expect(testImpactHtml).toContain("data-source-pattern=\"Test Impact\"");
    expect(testImpactHtml).toContain("Change Detection Signals");
    expect(testImpactHtml).toContain("Selection Signals");
    const testImpactMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "test-impact-readiness.md"), "utf8");
    expect(testImpactMarkdown).toContain("# Test Impact Readiness");
    expect(testImpactMarkdown).toContain("Source pattern: Test impact readiness");
    expect(testImpactMarkdown).toContain("## Selection Signals");
    expect(testImpactMarkdown).toContain("## Cache Signals");
    const testReportingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "test-reporting-readiness-report.json"), "utf8");
    expect(testReportingText).toContain("Test reporting readiness CTRF JSON Allure results JUnit XML GitHub annotations checks summaries artifacts history");
    expect(testReportingText).toContain("\"reportSetups\"");
    expect(testReportingText).toContain("\"formatSignals\"");
    expect(testReportingText).toContain("\"adapterSignals\"");
    expect(testReportingText).toContain("\"ciSignals\"");
    expect(testReportingText).toContain("\"outputSignals\"");
    expect(testReportingText).toContain("\"qualitySignals\"");
    expect(testReportingText).toContain("\"packageSignals\"");
    expect(testReportingText).toContain("npx jest --ci --reporters=default --reporters=jest-junit");
    const testReportingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "test-reporting-readiness.html"), "utf8");
    expect(testReportingHtml).toContain("Test Reporting Readiness");
    expect(testReportingHtml).toContain("test-reporting-readiness-card");
    expect(testReportingHtml).toContain("data-source-pattern=\"Test Reporting\"");
    expect(testReportingHtml).toContain("Adapter Signals");
    expect(testReportingHtml).toContain("Output Signals");
    const testReportingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "test-reporting-readiness.md"), "utf8");
    expect(testReportingMarkdown).toContain("# Test Reporting Readiness");
    expect(testReportingMarkdown).toContain("Source pattern: Test reporting readiness");
    expect(testReportingMarkdown).toContain("## Adapter Signals");
    expect(testReportingMarkdown).toContain("## Quality Signals");
    const snapshotReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "snapshot-readiness-report.json"), "utf8");
    expect(snapshotReadinessText).toContain("Snapshot testing readiness Jest Vitest Playwright toMatchSnapshot inline file visual ARIA snapshots update policy serializers baselines CI");
    expect(snapshotReadinessText).toContain("\"snapshotSetups\"");
    expect(snapshotReadinessText).toContain("\"assertionSignals\"");
    expect(snapshotReadinessText).toContain("\"storageSignals\"");
    expect(snapshotReadinessText).toContain("\"updateSignals\"");
    expect(snapshotReadinessText).toContain("\"serializerSignals\"");
    expect(snapshotReadinessText).toContain("\"visualSignals\"");
    expect(snapshotReadinessText).toContain("\"packageSignals\"");
    expect(snapshotReadinessText).toContain("npx playwright test --update-snapshots=changed");
    const snapshotReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "snapshot-readiness.html"), "utf8");
    expect(snapshotReadinessHtml).toContain("Snapshot Readiness");
    expect(snapshotReadinessHtml).toContain("snapshot-readiness-card");
    expect(snapshotReadinessHtml).toContain("data-source-pattern=\"Snapshot\"");
    expect(snapshotReadinessHtml).toContain("Update Signals");
    expect(snapshotReadinessHtml).toContain("Visual Signals");
    const snapshotReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "snapshot-readiness.md"), "utf8");
    expect(snapshotReadinessMarkdown).toContain("# Snapshot Readiness");
    expect(snapshotReadinessMarkdown).toContain("Source pattern: Snapshot testing readiness");
    expect(snapshotReadinessMarkdown).toContain("## Serializer Signals");
    expect(snapshotReadinessMarkdown).toContain("## Visual Signals");
    const propertyBasedTestingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "property-based-testing-readiness-report.json"), "utf8");
    expect(propertyBasedTestingText).toContain("Property-based testing fast-check Hypothesis jqwik generators arbitraries strategies shrinking seeds counterexamples stateful CI");
    expect(propertyBasedTestingText).toContain("\"propertySetups\"");
    expect(propertyBasedTestingText).toContain("\"generatorSignals\"");
    expect(propertyBasedTestingText).toContain("\"runnerSignals\"");
    expect(propertyBasedTestingText).toContain("\"reproductionSignals\"");
    expect(propertyBasedTestingText).toContain("\"statefulSignals\"");
    expect(propertyBasedTestingText).toContain("\"packageSignals\"");
    expect(propertyBasedTestingText).toContain("pytest -q --hypothesis-show-statistics");
    const propertyBasedTestingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "property-based-testing-readiness.html"), "utf8");
    expect(propertyBasedTestingHtml).toContain("Property-Based Testing Readiness");
    expect(propertyBasedTestingHtml).toContain("property-based-testing-readiness-card");
    expect(propertyBasedTestingHtml).toContain("data-source-pattern=\"Property-Based Testing\"");
    expect(propertyBasedTestingHtml).toContain("Generator Signals");
    expect(propertyBasedTestingHtml).toContain("Reproduction Signals");
    const propertyBasedTestingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "property-based-testing-readiness.md"), "utf8");
    expect(propertyBasedTestingMarkdown).toContain("# Property-Based Testing Readiness");
    expect(propertyBasedTestingMarkdown).toContain("Source pattern: Property-based testing");
    expect(propertyBasedTestingMarkdown).toContain("## Stateful Signals");
    expect(propertyBasedTestingMarkdown).toContain("## Package Signals");
    const fuzzText = await fs.readFile(path.join(result.session.outputPaths.analysis, "fuzz-readiness-report.json"), "utf8");
    expect(fuzzText).toContain("Fuzz readiness OSS-Fuzz libFuzzer AFL++ Jazzer ClusterFuzzLite fuzz targets corpus dictionary sanitizer coverage reproducer CI");
    expect(fuzzText).toContain("\"fuzzSetups\"");
    expect(fuzzText).toContain("\"harnessSignals\"");
    expect(fuzzText).toContain("\"engineSignals\"");
    expect(fuzzText).toContain("\"buildSignals\"");
    expect(fuzzText).toContain("\"runtimeSignals\"");
    expect(fuzzText).toContain("\"sanitizerSignals\"");
    expect(fuzzText).toContain("\"ciSignals\"");
    expect(fuzzText).toContain("\"packageSignals\"");
    expect(fuzzText).toContain("Run OSS-Fuzz, libFuzzer, AFL++, Jazzer, ClusterFuzzLite, or language fuzz commands only in an authorized environment");
    const fuzzHtml = await fs.readFile(path.join(result.session.outputPaths.html, "fuzz-readiness.html"), "utf8");
    expect(fuzzHtml).toContain("Fuzz Readiness");
    expect(fuzzHtml).toContain("fuzz-readiness-card");
    expect(fuzzHtml).toContain("data-source-pattern=\"Fuzz\"");
    expect(fuzzHtml).toContain("does not compile harnesses");
    expect(fuzzHtml).toContain("Sanitizer Signals");
    const fuzzMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "fuzz-readiness.md"), "utf8");
    expect(fuzzMarkdown).toContain("# Fuzz Readiness");
    expect(fuzzMarkdown).toContain("Source pattern: Fuzz readiness");
    expect(fuzzMarkdown).toContain("## Harness Signals");
    expect(fuzzMarkdown).toContain("## Sanitizer Signals");
    const testDataText = await fs.readFile(path.join(result.session.outputPaths.analysis, "test-data-readiness-report.json"), "utf8");
    expect(testDataText).toContain("Test data Factory Bot factory_boy Faker factories traits associations sequences seeds fixtures deterministic lint CI");
    expect(testDataText).toContain("\"dataSetups\"");
    expect(testDataText).toContain("\"factorySignals\"");
    expect(testDataText).toContain("\"relationshipSignals\"");
    expect(testDataText).toContain("\"generationSignals\"");
    expect(testDataText).toContain("\"reproducibilitySignals\"");
    expect(testDataText).toContain("\"lifecycleSignals\"");
    expect(testDataText).toContain("\"packageSignals\"");
    expect(testDataText).toContain("bundle exec rails runner 'FactoryBot.lint'");
    const testDataHtml = await fs.readFile(path.join(result.session.outputPaths.html, "test-data-readiness.html"), "utf8");
    expect(testDataHtml).toContain("Test Data Readiness");
    expect(testDataHtml).toContain("test-data-readiness-card");
    expect(testDataHtml).toContain("data-source-pattern=\"Test Data\"");
    expect(testDataHtml).toContain("Generation Signals");
    expect(testDataHtml).toContain("Reproducibility Signals");
    const testDataMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "test-data-readiness.md"), "utf8");
    expect(testDataMarkdown).toContain("# Test Data Readiness");
    expect(testDataMarkdown).toContain("Source pattern: Test data");
    expect(testDataMarkdown).toContain("## Lifecycle Signals");
    expect(testDataMarkdown).toContain("## Package Signals");
    const integrationTestEnvironmentText = await fs.readFile(path.join(result.session.outputPaths.analysis, "integration-test-environment-readiness-report.json"), "utf8");
    expect(integrationTestEnvironmentText).toContain("Testcontainers GenericContainer DockerContainer DockerComposeEnvironment DockerCompose wait strategies exposed ports env lifecycle stop Ryuk resource reaper pytest beforeAll afterAll");
    expect(integrationTestEnvironmentText).toContain("\"integrationSetups\"");
    expect(integrationTestEnvironmentText).toContain("\"containerSignals\"");
    expect(integrationTestEnvironmentText).toContain("\"waitSignals\"");
    expect(integrationTestEnvironmentText).toContain("\"lifecycleSignals\"");
    expect(integrationTestEnvironmentText).toContain("\"runtimeSignals\"");
    expect(integrationTestEnvironmentText).toContain("\"packageSignals\"");
    expect(integrationTestEnvironmentText).toContain("wait_for_logs");
    const integrationTestEnvironmentHtml = await fs.readFile(path.join(result.session.outputPaths.html, "integration-test-environment-readiness.html"), "utf8");
    expect(integrationTestEnvironmentHtml).toContain("Integration Test Environment Readiness");
    expect(integrationTestEnvironmentHtml).toContain("integration-test-environment-readiness-card");
    expect(integrationTestEnvironmentHtml).toContain("data-source-pattern=\"Testcontainers\"");
    expect(integrationTestEnvironmentHtml).toContain("Wait Signals");
    expect(integrationTestEnvironmentHtml).toContain("Lifecycle Signals");
    const integrationTestEnvironmentMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "integration-test-environment-readiness.md"), "utf8");
    expect(integrationTestEnvironmentMarkdown).toContain("# Integration Test Environment Readiness");
    expect(integrationTestEnvironmentMarkdown).toContain("Source pattern: Testcontainers");
    expect(integrationTestEnvironmentMarkdown).toContain("## Wait Signals");
    expect(integrationTestEnvironmentMarkdown).toContain("## Runtime Signals");
    const chaosEngineeringText = await fs.readFile(path.join(result.session.outputPaths.analysis, "chaos-engineering-readiness-report.json"), "utf8");
    expect(chaosEngineeringText).toContain("Chaos Mesh LitmusChaos Toxiproxy chaos experiments probes steady state blast radius schedules toxics cleanup");
    expect(chaosEngineeringText).toContain("\"chaosSetups\"");
    expect(chaosEngineeringText).toContain("\"experimentSignals\"");
    expect(chaosEngineeringText).toContain("\"faultSignals\"");
    expect(chaosEngineeringText).toContain("\"scopeSignals\"");
    expect(chaosEngineeringText).toContain("\"safetySignals\"");
    expect(chaosEngineeringText).toContain("\"observabilitySignals\"");
    expect(chaosEngineeringText).toContain("kubectl apply --dry-run=server");
    const chaosEngineeringHtml = await fs.readFile(path.join(result.session.outputPaths.html, "chaos-engineering-readiness.html"), "utf8");
    expect(chaosEngineeringHtml).toContain("Chaos Engineering Readiness");
    expect(chaosEngineeringHtml).toContain("chaos-engineering-readiness-card");
    expect(chaosEngineeringHtml).toContain("data-source-pattern=\"Chaos Engineering\"");
    expect(chaosEngineeringHtml).toContain("Safety Signals");
    expect(chaosEngineeringHtml).toContain("Observability Signals");
    const chaosEngineeringMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "chaos-engineering-readiness.md"), "utf8");
    expect(chaosEngineeringMarkdown).toContain("# Chaos Engineering Readiness");
    expect(chaosEngineeringMarkdown).toContain("Source pattern: Chaos Mesh LitmusChaos Toxiproxy");
    expect(chaosEngineeringMarkdown).toContain("## Fault Signals");
    expect(chaosEngineeringMarkdown).toContain("## Safety Signals");
    const accessibilityText = await fs.readFile(path.join(result.session.outputPaths.analysis, "accessibility-report.json"), "utf8");
    expect(accessibilityText).toContain("axe-core accessibility engine WCAG tags violations passes incomplete inapplicable impact selectors context configure reporter iframes");
    expect(accessibilityText).toContain("\"scanTargets\"");
    expect(accessibilityText).toContain("\"ruleTags\"");
    expect(accessibilityText).toContain("\"resultBuckets\"");
    expect(accessibilityText).toContain("\"impactLevels\"");
    expect(accessibilityText).toContain("\"integrationSignals\"");
    expect(accessibilityText).toContain("\"contextControls\"");
    expect(accessibilityText).toContain("npm install axe-core --save-dev");
    const accessibilityHtml = await fs.readFile(path.join(result.session.outputPaths.html, "accessibility.html"), "utf8");
    expect(accessibilityHtml).toContain("Accessibility Readiness");
    expect(accessibilityHtml).toContain("accessibility-card");
    expect(accessibilityHtml).toContain("data-source-pattern=\"axe-core\"");
    expect(accessibilityHtml).toContain("Result Buckets");
    expect(accessibilityHtml).toContain("Context Controls");
    const accessibilityMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "accessibility.md"), "utf8");
    expect(accessibilityMarkdown).toContain("# Accessibility Readiness");
    expect(accessibilityMarkdown).toContain("Source pattern: axe-core");
    expect(accessibilityMarkdown).toContain("## Result Buckets");
    expect(accessibilityMarkdown).toContain("## Context Controls");
    const storybookText = await fs.readFile(path.join(result.session.outputPaths.analysis, "storybook-report.json"), "utf8");
    expect(storybookText).toContain("Storybook Component Story Format stories Meta StoryObj satisfies Meta args argTypes decorators loaders play functions beforeEach autodocs MDX addons test-runner Vitest Chromatic portable stories composition MSW component workshop");
    expect(storybookText).toContain("\"storyFiles\"");
    expect(storybookText).toContain("\"configFiles\"");
    expect(storybookText).toContain("\"storyAnnotations\"");
    expect(storybookText).toContain("\"addonSignals\"");
    expect(storybookText).toContain("\"testSignals\"");
    expect(storybookText).toContain("\"publishSignals\"");
    expect(storybookText).toContain("\"storybookSignals\"");
    expect(storybookText).toContain("npx storybook@latest init");
    const storybookHtml = await fs.readFile(path.join(result.session.outputPaths.html, "storybook.html"), "utf8");
    expect(storybookHtml).toContain("Storybook Readiness");
    expect(storybookHtml).toContain("storybook-card");
    expect(storybookHtml).toContain("data-source-pattern=\"Storybook\"");
    expect(storybookHtml).toContain("Story Annotations");
    expect(storybookHtml).toContain("Publish Signals");
    expect(storybookHtml).toContain("Storybook Signals");
    const storybookMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "storybook.md"), "utf8");
    expect(storybookMarkdown).toContain("# Storybook Readiness");
    expect(storybookMarkdown).toContain("Source pattern: Storybook");
    expect(storybookMarkdown).toContain("## Story Annotations");
    expect(storybookMarkdown).toContain("## Publish Signals");
    expect(storybookMarkdown).toContain("## Storybook Signals");
    const designTokensText = await fs.readFile(path.join(result.session.outputPaths.analysis, "design-tokens-report.json"), "utf8");
    expect(designTokensText).toContain("Style Dictionary design tokens source include platforms transformGroup transforms buildPath files formats CTI aliases multi-platform CSS Android iOS");
    expect(designTokensText).toContain("\"tokenSources\"");
    expect(designTokensText).toContain("\"tokenCategories\"");
    expect(designTokensText).toContain("\"platformTargets\"");
    expect(designTokensText).toContain("\"transformSignals\"");
    expect(designTokensText).toContain("\"usageSignals\"");
    expect(designTokensText).toContain("\"governanceSignals\"");
    expect(designTokensText).toContain("npm install -D style-dictionary");
    const designTokensHtml = await fs.readFile(path.join(result.session.outputPaths.html, "design-tokens.html"), "utf8");
    expect(designTokensHtml).toContain("Design Tokens Readiness");
    expect(designTokensHtml).toContain("design-token-card");
    expect(designTokensHtml).toContain("data-source-pattern=\"Style Dictionary\"");
    expect(designTokensHtml).toContain("Platform Targets");
    expect(designTokensHtml).toContain("Governance Signals");
    const designTokensMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "design-tokens.md"), "utf8");
    expect(designTokensMarkdown).toContain("# Design Tokens Readiness");
    expect(designTokensMarkdown).toContain("Source pattern: Style Dictionary");
    expect(designTokensMarkdown).toContain("## Platform Targets");
    expect(designTokensMarkdown).toContain("## Governance Signals");
    const i18nText = await fs.readFile(path.join(result.session.outputPaths.analysis, "i18n-report.json"), "utf8");
    expect(i18nText).toContain("I18n readiness FormatJS React Intl next-intl useTranslations getTranslations NextIntlClientProvider createMiddleware defineRouting localePrefix pathnames requestLocale i18next init useTranslation I18nextProvider resources fallbackLng backend loadPath language detector saveMissing keyPrefix Lingui Trans useLingui I18nProvider lingui extract compile config vite plugin ESLint ICU messages extract compile verify IntlProvider polyfills locale data PO catalogs TMS");
    expect(i18nText).toContain("\"messageSources\"");
    expect(i18nText).toContain("\"localeAssets\"");
    expect(i18nText).toContain("\"runtimeSignals\"");
    expect(i18nText).toContain("\"extractionSignals\"");
    expect(i18nText).toContain("\"icuSignals\"");
    expect(i18nText).toContain("\"qaSignals\"");
    expect(i18nText).toContain("formatjs extract");
    const i18nHtml = await fs.readFile(path.join(result.session.outputPaths.html, "i18n.html"), "utf8");
    expect(i18nHtml).toContain("I18n Readiness");
    expect(i18nHtml).toContain("i18n-card");
    expect(i18nHtml).toContain("data-source-pattern=\"I18n\"");
    expect(i18nHtml).toContain("Extraction Signals");
    expect(i18nHtml).toContain("QA Signals");
    const i18nMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "i18n.md"), "utf8");
    expect(i18nMarkdown).toContain("# I18n Readiness");
    expect(i18nMarkdown).toContain("Source pattern: I18n readiness");
    expect(i18nMarkdown).toContain("## Extraction Signals");
    expect(i18nMarkdown).toContain("## QA Signals");
    const releaseText = await fs.readFile(path.join(result.session.outputPaths.analysis, "release-readiness-report.json"), "utf8");
    expect(releaseText).toContain("semantic-release branches tagFormat plugins verifyConditions analyzeCommits generateNotes prepare publish CI OIDC provenance");
    expect(releaseText).toContain("\"releaseConfigs\"");
    expect(releaseText).toContain("\"branchChannels\"");
    expect(releaseText).toContain("\"versionSignals\"");
    expect(releaseText).toContain("\"ciSignals\"");
    expect(releaseText).toContain("\"publishTargets\"");
    expect(releaseText).toContain("\"authSignals\"");
    expect(releaseText).toContain("\"pluginSteps\"");
    expect(releaseText).toContain("npx semantic-release --dry-run");
    const releaseHtml = await fs.readFile(path.join(result.session.outputPaths.html, "release-readiness.html"), "utf8");
    expect(releaseHtml).toContain("Release Readiness");
    expect(releaseHtml).toContain("release-card");
    expect(releaseHtml).toContain("data-source-pattern=\"semantic-release\"");
    expect(releaseHtml).toContain("Plugin Steps");
    expect(releaseHtml).toContain("Publish Targets");
    const releaseMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "release-readiness.md"), "utf8");
    expect(releaseMarkdown).toContain("# Release Readiness");
    expect(releaseMarkdown).toContain("Source pattern: semantic-release");
    expect(releaseMarkdown).toContain("## Plugin Steps");
    expect(releaseMarkdown).toContain("## Publish Targets");
    const secretText = await fs.readFile(path.join(result.session.outputPaths.analysis, "secret-readiness-report.json"), "utf8");
    expect(secretText).toContain("Gitleaks git dir stdin baseline config rules allowlists redaction report formats pre-commit staged secret scanning");
    expect(secretText).toContain("\"scanTargets\"");
    expect(secretText).toContain("\"secretSurfaces\"");
    expect(secretText).toContain("\"configSignals\"");
    expect(secretText).toContain("\"reportingSignals\"");
    expect(secretText).toContain("\"preventionSignals\"");
    expect(secretText).toContain("\"advancedSignals\"");
    expect(secretText).toContain("gitleaks git --redact");
    const secretHtml = await fs.readFile(path.join(result.session.outputPaths.html, "secret-readiness.html"), "utf8");
    expect(secretHtml).toContain("Secret Readiness");
    expect(secretHtml).toContain("secret-card");
    expect(secretHtml).toContain("data-source-pattern=\"Gitleaks\"");
    expect(secretHtml).toContain("Prevention Signals");
    expect(secretHtml).toContain("Reporting Signals");
    const secretMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "secret-readiness.md"), "utf8");
    expect(secretMarkdown).toContain("# Secret Readiness");
    expect(secretMarkdown).toContain("Source pattern: Gitleaks");
    expect(secretMarkdown).toContain("## Prevention Signals");
    expect(secretMarkdown).toContain("## Reporting Signals");
    const secretManagementText = await fs.readFile(path.join(result.session.outputPaths.analysis, "secret-management-readiness-report.json"), "utf8");
    expect(secretManagementText).toContain("Secrets management readiness Vault Infisical Doppler SOPS Sealed Secrets External Secrets secret engines auth methods policies tokens leases rotation transit kv env injection sync Kubernetes operator agent CLI SDK API audit logs dynamic secrets");
    expect(secretManagementText).toContain("\"secretManagementSetups\"");
    expect(secretManagementText).toContain("\"platformSignals\"");
    expect(secretManagementText).toContain("\"authSignals\"");
    expect(secretManagementText).toContain("\"storageSignals\"");
    expect(secretManagementText).toContain("\"deliverySignals\"");
    expect(secretManagementText).toContain("\"governanceSignals\"");
    expect(secretManagementText).toContain("\"packageSignals\"");
    const secretManagementHtml = await fs.readFile(path.join(result.session.outputPaths.html, "secret-management-readiness.html"), "utf8");
    expect(secretManagementHtml).toContain("Secret Management Readiness");
    expect(secretManagementHtml).toContain("secret-management-readiness-card");
    expect(secretManagementHtml).toContain("data-source-pattern=\"Secret Management\"");
    expect(secretManagementHtml).toContain("Governance Signals");
    const secretManagementMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "secret-management-readiness.md"), "utf8");
    expect(secretManagementMarkdown).toContain("# Secret Management Readiness");
    expect(secretManagementMarkdown).toContain("Source pattern: Secrets management readiness");
    expect(secretManagementMarkdown).toContain("## Delivery Signals");
    expect(secretManagementMarkdown).toContain("## Governance Signals");
    const containerText = await fs.readFile(path.join(result.session.outputPaths.analysis, "container-readiness-report.json"), "utf8");
    expect(containerText).toContain("Hadolint Dockerfile AST ShellCheck rules config ignored rules severity overrides trusted registries labels SARIF JUnit CI pre-commit");
    expect(containerText).toContain("\"dockerfiles\"");
    expect(containerText).toContain("\"composeFiles\"");
    expect(containerText).toContain("\"configSignals\"");
    expect(containerText).toContain("\"instructionRisks\"");
    expect(containerText).toContain("\"labelPolicy\"");
    expect(containerText).toContain("\"integrationSignals\"");
    expect(containerText).toContain("hadolint Dockerfile");
    const containerHtml = await fs.readFile(path.join(result.session.outputPaths.html, "container-readiness.html"), "utf8");
    expect(containerHtml).toContain("Container Readiness");
    expect(containerHtml).toContain("container-card");
    expect(containerHtml).toContain("data-source-pattern=\"Hadolint\"");
    expect(containerHtml).toContain("Instruction Risks");
    expect(containerHtml).toContain("Label Policy");
    const containerMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "container-readiness.md"), "utf8");
    expect(containerMarkdown).toContain("# Container Readiness");
    expect(containerMarkdown).toContain("Source pattern: Hadolint");
    expect(containerMarkdown).toContain("## Instruction Risks");
    expect(containerMarkdown).toContain("## Integration Signals");
    const containerScanText = await fs.readFile(path.join(result.session.outputPaths.analysis, "container-scan-readiness-report.json"), "utf8");
    expect(containerScanText).toContain("Container scan readiness Trivy Grype Dockle image filesystem SBOM vulnerability misconfig secret license CIS exit-code severity ignore-unfixed only-fixed fail-on exit-level SARIF CycloneDX SPDX JSON VEX trivyignore grype ignore dockleignore registry token docker-host");
    expect(containerScanText).toContain("\"containerScanSetups\"");
    expect(containerScanText).toContain("\"targetSignals\"");
    expect(containerScanText).toContain("\"scannerSignals\"");
    expect(containerScanText).toContain("\"gateSignals\"");
    expect(containerScanText).toContain("\"outputSignals\"");
    expect(containerScanText).toContain("\"policySignals\"");
    expect(containerScanText).toContain("\"registrySignals\"");
    expect(containerScanText).toContain("\"ciSignals\"");
    expect(containerScanText).toContain("\"packageSignals\"");
    expect(containerScanText).toContain("Run Trivy, Grype, Dockle, Docker, registry, vulnerability DB, and SARIF upload commands only in an authorized local or CI environment");
    const containerScanHtml = await fs.readFile(path.join(result.session.outputPaths.html, "container-scan-readiness.html"), "utf8");
    expect(containerScanHtml).toContain("Container Scan Readiness");
    expect(containerScanHtml).toContain("container-scan-readiness-card");
    expect(containerScanHtml).toContain("data-source-pattern=\"Container Scan\"");
    expect(containerScanHtml).toContain("does not build images");
    expect(containerScanHtml).toContain("Gate Signals");
    expect(containerScanHtml).toContain("Policy Signals");
    const containerScanMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "container-scan-readiness.md"), "utf8");
    expect(containerScanMarkdown).toContain("# Container Scan Readiness");
    expect(containerScanMarkdown).toContain("Source pattern: Container scan readiness");
    expect(containerScanMarkdown).toContain("## Gate Signals");
    expect(containerScanMarkdown).toContain("## Policy Signals");
    const codeQualityText = await fs.readFile(path.join(result.session.outputPaths.analysis, "code-quality-report.json"), "utf8");
    expect(codeQualityText).toContain("Biome formatter linter check ci biome.json assist organize imports diagnostics reporter editor LSP VCS ignore safe fixes");
    expect(codeQualityText).toContain("\"toolConfigs\"");
    expect(codeQualityText).toContain("\"formatterSignals\"");
    expect(codeQualityText).toContain("\"linterSignals\"");
    expect(codeQualityText).toContain("\"assistSignals\"");
    expect(codeQualityText).toContain("\"ciSignals\"");
    expect(codeQualityText).toContain("\"languageCoverage\"");
    expect(codeQualityText).toContain("npx @biomejs/biome check .");
    const codeQualityHtml = await fs.readFile(path.join(result.session.outputPaths.html, "code-quality.html"), "utf8");
    expect(codeQualityHtml).toContain("Code Quality");
    expect(codeQualityHtml).toContain("code-quality-card");
    expect(codeQualityHtml).toContain("data-source-pattern=\"Biome\"");
    expect(codeQualityHtml).toContain("Formatter Signals");
    expect(codeQualityHtml).toContain("Linter Signals");
    const codeQualityMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "code-quality.md"), "utf8");
    expect(codeQualityMarkdown).toContain("# Code Quality");
    expect(codeQualityMarkdown).toContain("Source pattern: Biome");
    expect(codeQualityMarkdown).toContain("## Formatter Signals");
    expect(codeQualityMarkdown).toContain("## Language Coverage");
    const documentationText = await fs.readFile(path.join(result.session.outputPaths.analysis, "documentation-report.json"), "utf8");
    expect(documentationText).toContain("Docusaurus docs blog pages sidebars docusaurus.config themeConfig navbar footer i18n versioning search build deploy");
    expect(documentationText).toContain("preset-classic plugin-content-docs plugin-content-blog theme-classic MDX admonitions swizzle plugin lifecycle");
    expect(documentationText).toContain("RepoAgent repository-level code documentation generation AST object docs bidirectional invocation relationships change detection Markdown replacement project hierarchy pre-commit GitBook chat-with-repo local model");
    expect(documentationText).toContain("\"siteConfigs\"");
    expect(documentationText).toContain("\"contentSurfaces\"");
    expect(documentationText).toContain("\"navigationSignals\"");
    expect(documentationText).toContain("\"qualitySignals\"");
    expect(documentationText).toContain("\"localizationSignals\"");
    expect(documentationText).toContain("\"releaseSignals\"");
    expect(documentationText).toContain("\"docusaurusSignals\"");
    expect(documentationText).toContain("\"objectDocumentationTargets\"");
    expect(documentationText).toContain("\"repoAgentAutomationSignals\"");
    expect(documentationText).toContain("npm run build");
    expect(documentationText).toContain("repoagent run --print-hierarchy");
    expect(documentationText).toContain("repoagent diff");
    const documentationHtml = await fs.readFile(path.join(result.session.outputPaths.html, "documentation.html"), "utf8");
    expect(documentationHtml).toContain("Documentation Readiness");
    expect(documentationHtml).toContain("documentation-card");
    expect(documentationHtml).toContain("data-source-pattern=\"Docusaurus\"");
    expect(documentationHtml).toContain("Content Surfaces");
    expect(documentationHtml).toContain("Release Signals");
    expect(documentationHtml).toContain("Docusaurus Signals");
    expect(documentationHtml).toContain("Object Documentation Targets");
    expect(documentationHtml).toContain("RepoAgent Automation Signals");
    expect(documentationHtml).toContain("data-doc-object-target=");
    const documentationMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "documentation.md"), "utf8");
    expect(documentationMarkdown).toContain("# Documentation Readiness");
    expect(documentationMarkdown).toContain("Source pattern: Docusaurus");
    expect(documentationMarkdown).toContain("## Content Surfaces");
    expect(documentationMarkdown).toContain("## Release Signals");
    expect(documentationMarkdown).toContain("## Docusaurus Signals");
    expect(documentationMarkdown).toContain("## Object Documentation Targets");
    expect(documentationMarkdown).toContain("## RepoAgent Automation Signals");
    const databaseText = await fs.readFile(path.join(result.session.outputPaths.analysis, "database-readiness-report.json"), "utf8");
    expect(databaseText).toContain("Prisma schema datasource generator model migrate generate db push seed PrismaClient DATABASE_URL driver adapter migrations");
    expect(databaseText).toContain("\"schemaFiles\"");
    expect(databaseText).toContain("\"datasourceSignals\"");
    expect(databaseText).toContain("\"migrationSignals\"");
    expect(databaseText).toContain("\"clientSignals\"");
    expect(databaseText).toContain("\"configSignals\"");
    expect(databaseText).toContain("\"modelSignals\"");
    expect(databaseText).toContain("npx prisma validate");
    const databaseHtml = await fs.readFile(path.join(result.session.outputPaths.html, "database-readiness.html"), "utf8");
    expect(databaseHtml).toContain("Database Readiness");
    expect(databaseHtml).toContain("database-card");
    expect(databaseHtml).toContain("data-source-pattern=\"Prisma\"");
    expect(databaseHtml).toContain("Schema Files");
    expect(databaseHtml).toContain("Migration Signals");
    const databaseMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "database-readiness.md"), "utf8");
    expect(databaseMarkdown).toContain("# Database Readiness");
    expect(databaseMarkdown).toContain("Source pattern: Prisma");
    expect(databaseMarkdown).toContain("## Schema Files");
    expect(databaseMarkdown).toContain("## Client Signals");
    const databaseMigrationText = await fs.readFile(path.join(result.session.outputPaths.analysis, "database-migration-readiness-report.json"), "utf8");
    expect(databaseMigrationText).toContain("Database migration readiness Flyway Liquibase Alembic");
    expect(databaseMigrationText).toContain("\"migrationSetups\"");
    expect(databaseMigrationText).toContain("\"fileSignals\"");
    expect(databaseMigrationText).toContain("\"lineageSignals\"");
    expect(databaseMigrationText).toContain("\"rollbackSignals\"");
    expect(databaseMigrationText).toContain("\"validationSignals\"");
    expect(databaseMigrationText).toContain("\"configSignals\"");
    expect(databaseMigrationText).toContain("\"ciSignals\"");
    expect(databaseMigrationText).toContain("\"packageSignals\"");
    expect(databaseMigrationText).toContain("flyway validate");
    expect(databaseMigrationText).toContain("liquibase updateSQL");
    expect(databaseMigrationText).toContain("alembic current");
    const databaseMigrationHtml = await fs.readFile(path.join(result.session.outputPaths.html, "database-migration-readiness.html"), "utf8");
    expect(databaseMigrationHtml).toContain("Database Migration Readiness");
    expect(databaseMigrationHtml).toContain("database-migration-readiness-card");
    expect(databaseMigrationHtml).toContain("data-source-pattern=\"Database Migration\"");
    expect(databaseMigrationHtml).toContain("Migration Setups");
    expect(databaseMigrationHtml).toContain("Validation Signals");
    const databaseMigrationMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "database-migration-readiness.md"), "utf8");
    expect(databaseMigrationMarkdown).toContain("# Database Migration Readiness");
    expect(databaseMigrationMarkdown).toContain("Source pattern: Database migration readiness");
    expect(databaseMigrationMarkdown).toContain("## Migration Setups");
    expect(databaseMigrationMarkdown).toContain("## Validation Signals");
    const databaseOrmText = await fs.readFile(path.join(result.session.outputPaths.analysis, "database-orm-readiness-report.json"), "utf8");
    expect(databaseOrmText).toContain("Database ORM readiness TypeORM Sequelize SQLAlchemy");
    expect(databaseOrmText).toContain("\"ormSetups\"");
    expect(databaseOrmText).toContain("\"entitySignals\"");
    expect(databaseOrmText).toContain("\"relationSignals\"");
    expect(databaseOrmText).toContain("\"repositorySignals\"");
    expect(databaseOrmText).toContain("\"transactionSignals\"");
    expect(databaseOrmText).toContain("\"loadingSignals\"");
    expect(databaseOrmText).toContain("\"configSignals\"");
    expect(databaseOrmText).toContain("\"ciSignals\"");
    expect(databaseOrmText).toContain("\"packageSignals\"");
    expect(databaseOrmText).toContain("npx typeorm migration:show");
    expect(databaseOrmText).toContain("npx sequelize-cli db:migrate:status");
    expect(databaseOrmText).toContain("alembic current");
    const databaseOrmHtml = await fs.readFile(path.join(result.session.outputPaths.html, "database-orm-readiness.html"), "utf8");
    expect(databaseOrmHtml).toContain("Database ORM Readiness");
    expect(databaseOrmHtml).toContain("database-orm-readiness-card");
    expect(databaseOrmHtml).toContain("data-source-pattern=\"Database ORM\"");
    expect(databaseOrmHtml).toContain("ORM Setups");
    expect(databaseOrmHtml).toContain("Transaction Signals");
    const databaseOrmMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "database-orm-readiness.md"), "utf8");
    expect(databaseOrmMarkdown).toContain("# Database ORM Readiness");
    expect(databaseOrmMarkdown).toContain("Source pattern: Database ORM readiness");
    expect(databaseOrmMarkdown).toContain("## ORM Setups");
    expect(databaseOrmMarkdown).toContain("## Transaction Signals");
    const dataTransformationText = await fs.readFile(path.join(result.session.outputPaths.analysis, "data-transformation-readiness-report.json"), "utf8");
    expect(dataTransformationText).toContain("Data transformation readiness dbt SQLMesh Dataform");
    expect(dataTransformationText).toContain("\"transformationSetups\"");
    expect(dataTransformationText).toContain("\"toolSignals\"");
    expect(dataTransformationText).toContain("\"modelSignals\"");
    expect(dataTransformationText).toContain("\"dependencySignals\"");
    expect(dataTransformationText).toContain("\"incrementalitySignals\"");
    expect(dataTransformationText).toContain("\"environmentSignals\"");
    expect(dataTransformationText).toContain("\"artifactSignals\"");
    expect(dataTransformationText).toContain("\"workflowSignals\"");
    expect(dataTransformationText).toContain("\"packageSignals\"");
    expect(dataTransformationText).toContain("RepoTutor records data transformation readiness only");
    const dataTransformationHtml = await fs.readFile(path.join(result.session.outputPaths.html, "data-transformation-readiness.html"), "utf8");
    expect(dataTransformationHtml).toContain("Data Transformation Readiness");
    expect(dataTransformationHtml).toContain("data-transformation-readiness-card");
    expect(dataTransformationHtml).toContain("data-source-pattern=\"DataTransformation\"");
    expect(dataTransformationHtml).toContain("Transformation Setups");
    expect(dataTransformationHtml).toContain("Incrementality Signals");
    const dataTransformationMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "data-transformation-readiness.md"), "utf8");
    expect(dataTransformationMarkdown).toContain("# Data Transformation Readiness");
    expect(dataTransformationMarkdown).toContain("Source pattern: Data transformation readiness");
    expect(dataTransformationMarkdown).toContain("## Transformation Setups");
    expect(dataTransformationMarkdown).toContain("## Workflow Signals");
    const ciCdText = await fs.readFile(path.join(result.session.outputPaths.analysis, "ci-cd-report.json"), "utf8");
    expect(ciCdText).toContain("GitHub Actions workflow syntax events jobs permissions GITHUB_TOKEN OIDC cache artifacts concurrency environments deployments");
    expect(ciCdText).toContain("\"workflowFiles\"");
    expect(ciCdText).toContain("\"triggerSignals\"");
    expect(ciCdText).toContain("\"jobSignals\"");
    expect(ciCdText).toContain("\"securitySignals\"");
    expect(ciCdText).toContain("\"deliverySignals\"");
    expect(ciCdText).toContain("\"platformSignals\"");
    expect(ciCdText).toContain("npx actionlint");
    const ciCdHtml = await fs.readFile(path.join(result.session.outputPaths.html, "ci-cd.html"), "utf8");
    expect(ciCdHtml).toContain("CI/CD Readiness");
    expect(ciCdHtml).toContain("ci-cd-card");
    expect(ciCdHtml).toContain("data-source-pattern=\"GitHub Actions\"");
    expect(ciCdHtml).toContain("Workflow Files");
    expect(ciCdHtml).toContain("Delivery Signals");
    const ciCdMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "ci-cd.md"), "utf8");
    expect(ciCdMarkdown).toContain("# CI/CD Readiness");
    expect(ciCdMarkdown).toContain("Source pattern: GitHub Actions");
    expect(ciCdMarkdown).toContain("## Workflow Files");
    expect(ciCdMarkdown).toContain("## Security Signals");
    const unitTestText = await fs.readFile(path.join(result.session.outputPaths.analysis, "unit-test-report.json"), "utf8");
    expect(unitTestText).toContain("Vitest test files config coverage v8 istanbul snapshots mocks jsdom happy-dom browser mode projects reporters");
    expect(unitTestText).toContain("\"testFiles\"");
    expect(unitTestText).toContain("\"configFiles\"");
    expect(unitTestText).toContain("\"assertionSignals\"");
    expect(unitTestText).toContain("\"mockSignals\"");
    expect(unitTestText).toContain("\"coverageSignals\"");
    expect(unitTestText).toContain("\"environmentSignals\"");
    expect(unitTestText).toContain("\"reportingSignals\"");
    expect(unitTestText).toContain("npx vitest run --coverage");
    const unitTestHtml = await fs.readFile(path.join(result.session.outputPaths.html, "unit-tests.html"), "utf8");
    expect(unitTestHtml).toContain("Unit Test Readiness");
    expect(unitTestHtml).toContain("unit-test-card");
    expect(unitTestHtml).toContain("data-source-pattern=\"Vitest\"");
    expect(unitTestHtml).toContain("Test Files");
    expect(unitTestHtml).toContain("Coverage Signals");
    const unitTestMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "unit-tests.md"), "utf8");
    expect(unitTestMarkdown).toContain("# Unit Test Readiness");
    expect(unitTestMarkdown).toContain("Source pattern: Vitest");
    expect(unitTestMarkdown).toContain("## Test Files");
    expect(unitTestMarkdown).toContain("## Coverage Signals");
    const coverageReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "coverage-readiness-report.json"), "utf8");
    expect(coverageReadinessText).toContain("nyc c8 Istanbul V8 coverage lcov cobertura coverage-final check-coverage thresholds Codecov OIDC flags");
    expect(coverageReadinessText).toContain("\"coverageSetups\"");
    expect(coverageReadinessText).toContain("\"instrumentationSignals\"");
    expect(coverageReadinessText).toContain("\"scopeSignals\"");
    expect(coverageReadinessText).toContain("\"thresholdSignals\"");
    expect(coverageReadinessText).toContain("\"reportSignals\"");
    expect(coverageReadinessText).toContain("\"ciUploadSignals\"");
    expect(coverageReadinessText).toContain("\"packageSignals\"");
    expect(coverageReadinessText).toContain("npx nyc --all");
    const coverageReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "coverage-readiness.html"), "utf8");
    expect(coverageReadinessHtml).toContain("Coverage Readiness");
    expect(coverageReadinessHtml).toContain("coverage-readiness-card");
    expect(coverageReadinessHtml).toContain("data-source-pattern=\"nyc\"");
    expect(coverageReadinessHtml).toContain("Coverage Setups");
    expect(coverageReadinessHtml).toContain("Threshold Signals");
    const coverageReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "coverage-readiness.md"), "utf8");
    expect(coverageReadinessMarkdown).toContain("# Coverage Readiness");
    expect(coverageReadinessMarkdown).toContain("Source pattern: nyc");
    expect(coverageReadinessMarkdown).toContain("## Instrumentation Signals");
    expect(coverageReadinessMarkdown).toContain("## CI Upload Signals");
    const mutationTestingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "mutation-testing-readiness-report.json"), "utf8");
    expect(mutationTestingText).toContain("Stryker mutation testing mutate patterns mutators testRunner coverageAnalysis reporters thresholds mutationScore killed survived timeout ignored incremental dashboard HTML JSON mutation-testing-report-schema Infection MSI covered MSI with-uncovered");
    expect(mutationTestingText).toContain("\"mutationSetups\"");
    expect(mutationTestingText).toContain("\"toolSignals\"");
    expect(mutationTestingText).toContain("\"configSignals\"");
    expect(mutationTestingText).toContain("\"qualitySignals\"");
    expect(mutationTestingText).toContain("\"reporterSignals\"");
    expect(mutationTestingText).toContain("\"scopeSignals\"");
    expect(mutationTestingText).toContain("\"packageSignals\"");
    expect(mutationTestingText).toContain("npx stryker run");
    const mutationTestingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "mutation-testing-readiness.html"), "utf8");
    expect(mutationTestingHtml).toContain("Mutation Testing Readiness");
    expect(mutationTestingHtml).toContain("mutation-testing-readiness-card");
    expect(mutationTestingHtml).toContain("data-source-pattern=\"Stryker\"");
    expect(mutationTestingHtml).toContain("Mutation Setups");
    expect(mutationTestingHtml).toContain("Quality Signals");
    const mutationTestingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "mutation-testing-readiness.md"), "utf8");
    expect(mutationTestingMarkdown).toContain("# Mutation Testing Readiness");
    expect(mutationTestingMarkdown).toContain("Source pattern: Stryker");
    expect(mutationTestingMarkdown).toContain("## Mutation Setups");
    expect(mutationTestingMarkdown).toContain("## Reporter Signals");
    const typecheckText = await fs.readFile(path.join(result.session.outputPaths.analysis, "typecheck-readiness-report.json"), "utf8");
    expect(typecheckText).toContain("TypeScript compilerOptions strict noImplicitAny strictNullChecks composite references declaration noEmit moduleResolution paths types skipLibCheck tsc build");
    expect(typecheckText).toContain("\"tsconfigFiles\"");
    expect(typecheckText).toContain("\"compilerOptionSignals\"");
    expect(typecheckText).toContain("\"projectSignals\"");
    expect(typecheckText).toContain("\"moduleResolutionSignals\"");
    expect(typecheckText).toContain("\"declarationSignals\"");
    expect(typecheckText).toContain("\"scriptSignals\"");
    expect(typecheckText).toContain("npx tsc --noEmit");
    const typecheckHtml = await fs.readFile(path.join(result.session.outputPaths.html, "typecheck-readiness.html"), "utf8");
    expect(typecheckHtml).toContain("Typecheck Readiness");
    expect(typecheckHtml).toContain("typecheck-card");
    expect(typecheckHtml).toContain("data-source-pattern=\"TypeScript\"");
    expect(typecheckHtml).toContain("TSConfig Files");
    expect(typecheckHtml).toContain("Module Resolution Signals");
    const typecheckMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "typecheck-readiness.md"), "utf8");
    expect(typecheckMarkdown).toContain("# Typecheck Readiness");
    expect(typecheckMarkdown).toContain("Source pattern: TypeScript");
    expect(typecheckMarkdown).toContain("## TSConfig Files");
    expect(typecheckMarkdown).toContain("## Script Signals");
    const packageManagerText = await fs.readFile(path.join(result.session.outputPaths.analysis, "package-manager-report.json"), "utf8");
    expect(packageManagerText).toContain("pnpm packageManager devEngines workspace packages catalog lockfile importers allowBuilds auditConfig pnpmfile hooks recursive filter frozen-lockfile");
    expect(packageManagerText).toContain("\"manifestFiles\"");
    expect(packageManagerText).toContain("\"workspaceSignals\"");
    expect(packageManagerText).toContain("\"lockfileSignals\"");
    expect(packageManagerText).toContain("\"scriptSignals\"");
    expect(packageManagerText).toContain("\"policySignals\"");
    expect(packageManagerText).toContain("pnpm install --frozen-lockfile");
    const packageManagerHtml = await fs.readFile(path.join(result.session.outputPaths.html, "package-manager.html"), "utf8");
    expect(packageManagerHtml).toContain("Package Manager Readiness");
    expect(packageManagerHtml).toContain("package-manager-card");
    expect(packageManagerHtml).toContain("data-source-pattern=\"pnpm\"");
    expect(packageManagerHtml).toContain("Manifest Files");
    expect(packageManagerHtml).toContain("Lockfile Signals");
    const packageManagerMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "package-manager.md"), "utf8");
    expect(packageManagerMarkdown).toContain("# Package Manager Readiness");
    expect(packageManagerMarkdown).toContain("Source pattern: pnpm");
    expect(packageManagerMarkdown).toContain("## Manifest Files");
    expect(packageManagerMarkdown).toContain("## Policy Signals");
    const gitHooksText = await fs.readFile(path.join(result.session.outputPaths.analysis, "git-hooks-report.json"), "utf8");
    expect(gitHooksText).toContain("Husky .husky hook files prepare core.hooksPath pre-commit pre-push commit-msg HUSKY=0 no-verify lint-staged POSIX shell; Lefthook lefthook.yml jobs commands scripts parallel group piped glob files root tags skip only stage_fixed runner output extends remotes local config run validate dump");
    expect(gitHooksText).toContain("\"hookFiles\"");
    expect(gitHooksText).toContain("\"installSignals\"");
    expect(gitHooksText).toContain("\"commandSignals\"");
    expect(gitHooksText).toContain("\"policySignals\"");
    expect(gitHooksText).toContain("\"toolConfigFiles\"");
    expect(gitHooksText).toContain("\"lefthookSignals\"");
    expect(gitHooksText).toContain("git config --get core.hooksPath");
    expect(gitHooksText).toContain("lefthook validate");
    const gitHooksHtml = await fs.readFile(path.join(result.session.outputPaths.html, "git-hooks.html"), "utf8");
    expect(gitHooksHtml).toContain("Git Hooks Readiness");
    expect(gitHooksHtml).toContain("git-hooks-card");
    expect(gitHooksHtml).toContain("data-source-pattern=\"Husky Lefthook\"");
    expect(gitHooksHtml).toContain("Hook Files");
    expect(gitHooksHtml).toContain("Policy Signals");
    expect(gitHooksHtml).toContain("Lefthook Signals");
    const gitHooksMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "git-hooks.md"), "utf8");
    expect(gitHooksMarkdown).toContain("# Git Hooks Readiness");
    expect(gitHooksMarkdown).toContain("## Lefthook Signals");
    expect(gitHooksMarkdown).toContain("Source pattern: Husky");
    expect(gitHooksMarkdown).toContain("## Hook Files");
    expect(gitHooksMarkdown).toContain("## Tool Config Files");
    const taskRunnerText = await fs.readFile(path.join(result.session.outputPaths.analysis, "task-runner-report.json"), "utf8");
    expect(taskRunnerText).toContain("Turborepo turbo.json tasks dependsOn outputs inputs cache env globalEnv passThroughEnv persistent turbo run filter remote cache");
    expect(taskRunnerText).toContain("\"configFiles\"");
    expect(taskRunnerText).toContain("\"taskSignals\"");
    expect(taskRunnerText).toContain("\"cacheSignals\"");
    expect(taskRunnerText).toContain("\"dependencySignals\"");
    expect(taskRunnerText).toContain("\"environmentSignals\"");
    expect(taskRunnerText).toContain("\"packageScriptSignals\"");
    expect(taskRunnerText).toContain("pnpm turbo run build --dry=json");
    const taskRunnerHtml = await fs.readFile(path.join(result.session.outputPaths.html, "task-runner.html"), "utf8");
    expect(taskRunnerHtml).toContain("Task Runner Readiness");
    expect(taskRunnerHtml).toContain("task-runner-card");
    expect(taskRunnerHtml).toContain("data-source-pattern=\"Turborepo\"");
    expect(taskRunnerHtml).toContain("Config Files");
    expect(taskRunnerHtml).toContain("Cache Signals");
    const taskRunnerMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "task-runner.md"), "utf8");
    expect(taskRunnerMarkdown).toContain("# Task Runner Readiness");
    expect(taskRunnerMarkdown).toContain("Source pattern: Turborepo");
    expect(taskRunnerMarkdown).toContain("## Config Files");
    expect(taskRunnerMarkdown).toContain("## Package Script Signals");
    const dependencyUpdateText = await fs.readFile(path.join(result.session.outputPaths.analysis, "dependency-update-report.json"), "utf8");
    expect(dependencyUpdateText).toContain("Renovate config presets packageRules automerge schedule dependencyDashboard enabledManagers hostRules rangeStrategy prConcurrentLimit configMigration");
    expect(dependencyUpdateText).toContain("\"configFiles\"");
    expect(dependencyUpdateText).toContain("\"managerSignals\"");
    expect(dependencyUpdateText).toContain("\"policySignals\"");
    expect(dependencyUpdateText).toContain("\"workflowSignals\"");
    expect(dependencyUpdateText).toContain("\"registrySignals\"");
    expect(dependencyUpdateText).toContain("\"packageFileSignals\"");
    expect(dependencyUpdateText).toContain("npx renovate-config-validator");
    const dependencyUpdateHtml = await fs.readFile(path.join(result.session.outputPaths.html, "dependency-updates.html"), "utf8");
    expect(dependencyUpdateHtml).toContain("Dependency Updates Readiness");
    expect(dependencyUpdateHtml).toContain("dependency-update-card");
    expect(dependencyUpdateHtml).toContain("data-source-pattern=\"Renovate\"");
    expect(dependencyUpdateHtml).toContain("Config Files");
    expect(dependencyUpdateHtml).toContain("Policy Signals");
    const dependencyUpdateMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "dependency-updates.md"), "utf8");
    expect(dependencyUpdateMarkdown).toContain("# Dependency Updates Readiness");
    expect(dependencyUpdateMarkdown).toContain("Source pattern: Renovate");
    expect(dependencyUpdateMarkdown).toContain("## Config Files");
    expect(dependencyUpdateMarkdown).toContain("## Package File Signals");
    const dependencyReviewText = await fs.readFile(path.join(result.session.outputPaths.analysis, "dependency-review-readiness-report.json"), "utf8");
    expect(dependencyReviewText).toContain("Dependency Review readiness actions/dependency-review-action fail-on-severity vulnerability-check license-check allow-licenses deny-licenses allow-dependencies-licenses deny-packages base-ref head-ref snapshot warnings OpenSSF scorecard Dependabot OSV Scanner lockfile license offline remediation PR summary artifact SARIF JSON HTML");
    expect(dependencyReviewText).toContain("\"dependencyReviewSetups\"");
    expect(dependencyReviewText).toContain("\"reviewSignals\"");
    expect(dependencyReviewText).toContain("\"vulnerabilitySignals\"");
    expect(dependencyReviewText).toContain("\"licenseSignals\"");
    expect(dependencyReviewText).toContain("\"packagePolicySignals\"");
    expect(dependencyReviewText).toContain("\"scorecardSignals\"");
    expect(dependencyReviewText).toContain("\"outputSignals\"");
    expect(dependencyReviewText).toContain("Run Dependency Review, Dependabot, OSV Scanner");
    const dependencyReviewHtml = await fs.readFile(path.join(result.session.outputPaths.html, "dependency-review-readiness.html"), "utf8");
    expect(dependencyReviewHtml).toContain("Dependency Review Readiness");
    expect(dependencyReviewHtml).toContain("dependency-review-readiness-card");
    expect(dependencyReviewHtml).toContain("data-source-pattern=\"Dependency Review\"");
    expect(dependencyReviewHtml).toContain("Package Policy Signals");
    expect(dependencyReviewHtml).toContain("does not call GitHub APIs");
    const dependencyReviewMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "dependency-review-readiness.md"), "utf8");
    expect(dependencyReviewMarkdown).toContain("# Dependency Review Readiness");
    expect(dependencyReviewMarkdown).toContain("Source pattern: Dependency Review");
    expect(dependencyReviewMarkdown).toContain("## Dependency Review Setups");
    expect(dependencyReviewMarkdown).toContain("## Package Policy Signals");
    const lintReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "lint-readiness-report.json"), "utf8");
    expect(lintReadinessText).toContain("ESLint flat config rules plugins parser ignores fix cache max-warnings report-unused-disable-directives print-config inspect-config");
    expect(lintReadinessText).toContain("\"configFiles\"");
    expect(lintReadinessText).toContain("\"ruleSignals\"");
    expect(lintReadinessText).toContain("\"scriptSignals\"");
    expect(lintReadinessText).toContain("\"scopeSignals\"");
    expect(lintReadinessText).toContain("\"outputSignals\"");
    expect(lintReadinessText).toContain("\"packageSignals\"");
    expect(lintReadinessText).toContain("npx eslint --print-config <file>");
    const lintReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "lint-readiness.html"), "utf8");
    expect(lintReadinessHtml).toContain("Lint Readiness");
    expect(lintReadinessHtml).toContain("lint-readiness-card");
    expect(lintReadinessHtml).toContain("data-source-pattern=\"ESLint\"");
    expect(lintReadinessHtml).toContain("Config Files");
    expect(lintReadinessHtml).toContain("Rule Signals");
    const lintReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "lint-readiness.md"), "utf8");
    expect(lintReadinessMarkdown).toContain("# Lint Readiness");
    expect(lintReadinessMarkdown).toContain("Source pattern: ESLint");
    expect(lintReadinessMarkdown).toContain("## Config Files");
    expect(lintReadinessMarkdown).toContain("## Package Signals");
    const formatReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "format-readiness-report.json"), "utf8");
    expect(formatReadinessText).toContain("Prettier config ignore options plugins parser check write list-different cache editorconfig file-info find-config-path");
    expect(formatReadinessText).toContain("\"configFiles\"");
    expect(formatReadinessText).toContain("\"ignoreFiles\"");
    expect(formatReadinessText).toContain("\"optionSignals\"");
    expect(formatReadinessText).toContain("\"scriptSignals\"");
    expect(formatReadinessText).toContain("\"scopeSignals\"");
    expect(formatReadinessText).toContain("\"packageSignals\"");
    expect(formatReadinessText).toContain("npx prettier --find-config-path <file>");
    const formatReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "format-readiness.html"), "utf8");
    expect(formatReadinessHtml).toContain("Format Readiness");
    expect(formatReadinessHtml).toContain("format-readiness-card");
    expect(formatReadinessHtml).toContain("data-source-pattern=\"Prettier\"");
    expect(formatReadinessHtml).toContain("Config Files");
    expect(formatReadinessHtml).toContain("Ignore Files");
    const formatReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "format-readiness.md"), "utf8");
    expect(formatReadinessMarkdown).toContain("# Format Readiness");
    expect(formatReadinessMarkdown).toContain("Source pattern: Prettier");
    expect(formatReadinessMarkdown).toContain("## Config Files");
    expect(formatReadinessMarkdown).toContain("## Package Signals");
    const commitConventionText = await fs.readFile(path.join(result.session.outputPaths.analysis, "commit-conventions-report.json"), "utf8");
    expect(commitConventionText).toContain("commitlint config conventional commits rules parserPreset commit-msg husky from to last edit strict verbose prompt");
    expect(commitConventionText).toContain("\"configFiles\"");
    expect(commitConventionText).toContain("\"ruleSignals\"");
    expect(commitConventionText).toContain("\"hookSignals\"");
    expect(commitConventionText).toContain("\"commandSignals\"");
    expect(commitConventionText).toContain("\"packageSignals\"");
    expect(commitConventionText).toContain("npx commitlint --from <base-sha> --to <head-sha> --verbose");
    const commitConventionHtml = await fs.readFile(path.join(result.session.outputPaths.html, "commit-conventions.html"), "utf8");
    expect(commitConventionHtml).toContain("Commit Conventions");
    expect(commitConventionHtml).toContain("commit-convention-card");
    expect(commitConventionHtml).toContain("data-source-pattern=\"commitlint\"");
    expect(commitConventionHtml).toContain("Config Files");
    expect(commitConventionHtml).toContain("Hook Signals");
    const commitConventionMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "commit-conventions.md"), "utf8");
    expect(commitConventionMarkdown).toContain("# Commit Conventions");
    expect(commitConventionMarkdown).toContain("Source pattern: commitlint");
    expect(commitConventionMarkdown).toContain("## Config Files");
    expect(commitConventionMarkdown).toContain("## Package Signals");
    const changelogReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "changelog-readiness-report.json"), "utf8");
    expect(changelogReadinessText).toContain("Changesets config changeset files changelog version publish status pre snapshot fixed linked private packages");
    expect(changelogReadinessText).toContain("\"configFiles\"");
    expect(changelogReadinessText).toContain("\"changesetFiles\"");
    expect(changelogReadinessText).toContain("\"workflowSignals\"");
    expect(changelogReadinessText).toContain("\"commandSignals\"");
    expect(changelogReadinessText).toContain("\"policySignals\"");
    expect(changelogReadinessText).toContain("npx changeset status --since=main --output=changeset-status.json");
    const changelogReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "changelog-readiness.html"), "utf8");
    expect(changelogReadinessHtml).toContain("Changelog Readiness");
    expect(changelogReadinessHtml).toContain("changelog-readiness-card");
    expect(changelogReadinessHtml).toContain("data-source-pattern=\"Changesets\"");
    expect(changelogReadinessHtml).toContain("Changeset Files");
    expect(changelogReadinessHtml).toContain("Policy Signals");
    const changelogReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "changelog-readiness.md"), "utf8");
    expect(changelogReadinessMarkdown).toContain("# Changelog Readiness");
    expect(changelogReadinessMarkdown).toContain("Source pattern: Changesets");
    expect(changelogReadinessMarkdown).toContain("## Changeset Files");
    expect(changelogReadinessMarkdown).toContain("## Policy Signals");
    const bundleAnalysisText = await fs.readFile(path.join(result.session.outputPaths.analysis, "bundle-analysis-report.json"), "utf8");
    expect(bundleAnalysisText).toContain("Webpack Bundle Analyzer stats json analyzerMode reportFilename defaultSizes gzip parsed stat source maps chunks assets");
    expect(bundleAnalysisText).toContain("\"configFiles\"");
    expect(bundleAnalysisText).toContain("\"bundleArtifacts\"");
    expect(bundleAnalysisText).toContain("\"sizeSignals\"");
    expect(bundleAnalysisText).toContain("\"scriptSignals\"");
    expect(bundleAnalysisText).toContain("\"packageSignals\"");
    expect(bundleAnalysisText).toContain("npx webpack-bundle-analyzer stats.json dist --mode static --report bundle-report.html --no-open");
    const bundleAnalysisHtml = await fs.readFile(path.join(result.session.outputPaths.html, "bundle-analysis.html"), "utf8");
    expect(bundleAnalysisHtml).toContain("Bundle Analysis");
    expect(bundleAnalysisHtml).toContain("bundle-analysis-card");
    expect(bundleAnalysisHtml).toContain("data-source-pattern=\"Webpack Bundle Analyzer\"");
    expect(bundleAnalysisHtml).toContain("Bundle Artifacts");
    expect(bundleAnalysisHtml).toContain("Size Signals");
    const bundleAnalysisMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "bundle-analysis.md"), "utf8");
    expect(bundleAnalysisMarkdown).toContain("# Bundle Analysis");
    expect(bundleAnalysisMarkdown).toContain("Source pattern: Webpack Bundle Analyzer");
    expect(bundleAnalysisMarkdown).toContain("## Bundle Artifacts");
    expect(bundleAnalysisMarkdown).toContain("## Package Signals");
    const mockingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "mocking-readiness-report.json"), "utf8");
    expect(mockingReadinessText).toContain("Mock Service Worker setupWorker setupServer http graphql ws sse HttpResponse handlers onUnhandledRequest resetHandlers restoreHandlers passthrough bypass boundary events listHandlers serviceWorker findWorker quiet waitUntilReady cookieStore delay");
    expect(mockingReadinessText).toContain("\"handlerFiles\"");
    expect(mockingReadinessText).toContain("\"serverSetups\"");
    expect(mockingReadinessText).toContain("\"protocolSignals\"");
    expect(mockingReadinessText).toContain("\"lifecycleSignals\"");
    expect(mockingReadinessText).toContain("\"mswSignals\"");
    expect(mockingReadinessText).toContain("\"packageSignals\"");
    expect(mockingReadinessText).toContain("npx msw init public/ --save");
    const mockingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "mocking-readiness.html"), "utf8");
    expect(mockingReadinessHtml).toContain("Mocking Readiness");
    expect(mockingReadinessHtml).toContain("mocking-readiness-card");
    expect(mockingReadinessHtml).toContain("data-source-pattern=\"Mock Service Worker\"");
    expect(mockingReadinessHtml).toContain("Handler Files");
    expect(mockingReadinessHtml).toContain("Lifecycle Signals");
    expect(mockingReadinessHtml).toContain("MSW Signals");
    const mockingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "mocking-readiness.md"), "utf8");
    expect(mockingReadinessMarkdown).toContain("# Mocking Readiness");
    expect(mockingReadinessMarkdown).toContain("Source pattern: Mock Service Worker");
    expect(mockingReadinessMarkdown).toContain("## Handler Files");
    expect(mockingReadinessMarkdown).toContain("## MSW Signals");
    expect(mockingReadinessMarkdown).toContain("## Package Signals");
    const dataFetchingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "data-fetching-readiness-report.json"), "utf8");
    expect(dataFetchingText).toContain("TanStack Query QueryClient QueryClientProvider useQuery useMutation queryOptions infiniteQueryOptions mutationOptions useQueries useSuspenseQuery useSuspenseInfiniteQuery usePrefetchQuery queryKey queryFn invalidateQueries refetchQueries cancelQueries removeQueries setQueryData setQueriesData getQueryData getQueryState ensureQueryData staleTime gcTime retry retryDelay networkMode structuralSharing hydrate dehydrate HydrationBoundary persistQueryClient PersistQueryClientProvider createPersister broadcastQueryClient focusManager onlineManager notifyManager streamedQuery devtools");
    expect(dataFetchingText).toContain("\"clientSetups\"");
    expect(dataFetchingText).toContain("\"queryUsages\"");
    expect(dataFetchingText).toContain("\"cacheSignals\"");
    expect(dataFetchingText).toContain("\"dataFlowSignals\"");
    expect(dataFetchingText).toContain("\"tanstackQuerySignals\"");
    expect(dataFetchingText).toContain("\"packageSignals\"");
    expect(dataFetchingText).toContain("npx eslint . --rule '@tanstack/query/stable-query-client:error'");
    const dataFetchingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "data-fetching-readiness.html"), "utf8");
    expect(dataFetchingHtml).toContain("Data Fetching Readiness");
    expect(dataFetchingHtml).toContain("data-fetching-card");
    expect(dataFetchingHtml).toContain("data-source-pattern=\"TanStack Query\"");
    expect(dataFetchingHtml).toContain("Client Setups");
    expect(dataFetchingHtml).toContain("Data Flow Signals");
    expect(dataFetchingHtml).toContain("TanStack Query Signals");
    const dataFetchingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "data-fetching-readiness.md"), "utf8");
    expect(dataFetchingMarkdown).toContain("# Data Fetching Readiness");
    expect(dataFetchingMarkdown).toContain("Source pattern: TanStack Query");
    expect(dataFetchingMarkdown).toContain("## Query Usages");
    expect(dataFetchingMarkdown).toContain("## Cache Signals");
    expect(dataFetchingMarkdown).toContain("## TanStack Query Signals");
    const routingText = await fs.readFile(path.join(result.session.outputPaths.analysis, "routing-readiness-report.json"), "utf8");
    expect(routingText).toContain("React Router TanStack Router BrowserRouter createBrowserRouter RouterProvider routes.ts route index Link NavLink Outlet loader action ErrorBoundary useNavigate useParams useSearchParams createRouter routeTree routeTree.gen createFileRoute createRootRoute createRoute Route.useParams validateSearch beforeLoad SearchSchemaInput linkOptions createRouteMask preload notFound TanStackRouterVite TanStackRouterDevtools");
    expect(routingText).toContain("\"routingSetups\"");
    expect(routingText).toContain("\"routeDefinitions\"");
    expect(routingText).toContain("\"navigationSignals\"");
    expect(routingText).toContain("\"dataRouteSignals\"");
    expect(routingText).toContain("\"fileRouteSignals\"");
    expect(routingText).toContain("\"tanstackSignals\"");
    expect(routingText).toContain("\"packageSignals\"");
    expect(routingText).toContain("npx react-router typegen");
    const routingHtml = await fs.readFile(path.join(result.session.outputPaths.html, "routing-readiness.html"), "utf8");
    expect(routingHtml).toContain("Routing Readiness");
    expect(routingHtml).toContain("routing-readiness-card");
    expect(routingHtml).toContain("data-source-pattern=\"React Router TanStack Router\"");
    expect(routingHtml).toContain("Route Definitions");
    expect(routingHtml).toContain("Data Route Signals");
    expect(routingHtml).toContain("TanStack Router Signals");
    const routingMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "routing-readiness.md"), "utf8");
    expect(routingMarkdown).toContain("# Routing Readiness");
    expect(routingMarkdown).toContain("Source pattern: React Router TanStack Router");
    expect(routingMarkdown).toContain("## Route Definitions");
    expect(routingMarkdown).toContain("## Navigation Signals");
    expect(routingMarkdown).toContain("## TanStack Router Signals");
    const stateManagementText = await fs.readFile(path.join(result.session.outputPaths.analysis, "state-management-readiness-report.json"), "utf8");
    expect(stateManagementText).toContain("Redux Toolkit configureStore createSlice reducers actions selectors Provider useSelector useDispatch createAsyncThunk createListenerMiddleware createEntityAdapter middleware devTools RTK Query Zustand create createStore useStore useShallow shallow subscribeWithSelector persist createJSONStorage devtools immer redux combine setState getState getInitialState subscribe StateCreator StoreApi Mutate StoreMutatorIdentifier Jotai atom primitive atom derived atom useAtom useAtomValue useSetAtom Provider createStore getDefaultStore store.get store.set store.sub onMount debugLabel atomWithStorage atomFamily selectAtom splitAtom focusAtom loadable unwrap useHydrateAtoms useAtomCallback devtools atomEffect atomWithImmer Valtio proxy useSnapshot snapshot subscribe subscribeKey watch ref devtools proxyMap proxySet useProxy derive deepClone unstable_deepProxy sync Snapshot MobX makeAutoObservable makeObservable observable computed action runInAction flow autorun reaction when configure observer useLocalObservable Provider inject spy trace toJS");
    expect(stateManagementText).toContain("\"storeSetups\"");
    expect(stateManagementText).toContain("\"sliceDefinitions\"");
    expect(stateManagementText).toContain("\"selectorSignals\"");
    expect(stateManagementText).toContain("\"sideEffectSignals\"");
    expect(stateManagementText).toContain("\"entitySignals\"");
    expect(stateManagementText).toContain("\"middlewareSignals\"");
    expect(stateManagementText).toContain("\"rtkQuerySignals\"");
    expect(stateManagementText).toContain("\"zustandSignals\"");
    expect(stateManagementText).toContain("\"jotaiSignals\"");
    expect(stateManagementText).toContain("\"valtioSignals\"");
    expect(stateManagementText).toContain("\"mobxSignals\"");
    expect(stateManagementText).toContain("\"packageSignals\"");
    expect(stateManagementText).toContain("npx vitest run");
    const stateManagementHtml = await fs.readFile(path.join(result.session.outputPaths.html, "state-management-readiness.html"), "utf8");
    expect(stateManagementHtml).toContain("State Management Readiness");
    expect(stateManagementHtml).toContain("state-management-card");
    expect(stateManagementHtml).toContain("data-source-pattern=\"Redux Toolkit\"");
    expect(stateManagementHtml).toContain("Store Setups");
    expect(stateManagementHtml).toContain("RTK Query Signals");
    expect(stateManagementHtml).toContain("Zustand Signals");
    expect(stateManagementHtml).toContain("Jotai Signals");
    expect(stateManagementHtml).toContain("Valtio Signals");
    expect(stateManagementHtml).toContain("MobX Signals");
    const stateManagementMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "state-management-readiness.md"), "utf8");
    expect(stateManagementMarkdown).toContain("# State Management Readiness");
    expect(stateManagementMarkdown).toContain("Source pattern: Redux Toolkit");
    expect(stateManagementMarkdown).toContain("## Slice Definitions");
    expect(stateManagementMarkdown).toContain("## Middleware Signals");
    expect(stateManagementMarkdown).toContain("## Zustand Signals");
    expect(stateManagementMarkdown).toContain("## Jotai Signals");
    expect(stateManagementMarkdown).toContain("## Valtio Signals");
    expect(stateManagementMarkdown).toContain("## MobX Signals");
    const formReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "form-readiness-report.json"), "utf8");
    expect(formReadinessText).toContain("React Hook Form useForm register handleSubmit Controller useController FormProvider useFormContext useFieldArray append remove move insert update replace swap resolver mode reValidateMode criteriaMode errors defaultValues values watch useWatch useFormState formState reset resetField setValue getValues getFieldState setError clearErrors trigger shouldUnregister disabled delayError shouldFocusError context control RegisterOptions FieldValues FieldPath SubmitHandler UseFormReturn ControllerRenderProps Form component FormStateSubscribe createFormControl validation");
    expect(formReadinessText).toContain("\"formSetups\"");
    expect(formReadinessText).toContain("\"fieldRegistrations\"");
    expect(formReadinessText).toContain("\"validationSignals\"");
    expect(formReadinessText).toContain("\"errorSignals\"");
    expect(formReadinessText).toContain("\"valueFlowSignals\"");
    expect(formReadinessText).toContain("\"reactHookFormSignals\"");
    expect(formReadinessText).toContain("\"packageSignals\"");
    expect(formReadinessText).toContain("npx vitest run");
    const formReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "form-readiness.html"), "utf8");
    expect(formReadinessHtml).toContain("Form Readiness");
    expect(formReadinessHtml).toContain("form-readiness-card");
    expect(formReadinessHtml).toContain("data-source-pattern=\"React Hook Form\"");
    expect(formReadinessHtml).toContain("Form Setups");
    expect(formReadinessHtml).toContain("Validation Signals");
    expect(formReadinessHtml).toContain("React Hook Form Signals");
    const formReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "form-readiness.md"), "utf8");
    expect(formReadinessMarkdown).toContain("# Form Readiness");
    expect(formReadinessMarkdown).toContain("Source pattern: React Hook Form");
    expect(formReadinessMarkdown).toContain("## Field Registrations");
    expect(formReadinessMarkdown).toContain("## Error Signals");
    expect(formReadinessMarkdown).toContain("## React Hook Form Signals");
    const authReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "auth-readiness-report.json"), "utf8");
    expect(authReadinessText).toContain("Auth.js NextAuth auth handlers auth signIn signOut exports providers callbacks session strategy maxAge updateAge jwt middleware protected routes trustHost basePath raw env secrets adapter WebAuthn experimental useSession SessionProvider");
    expect(authReadinessText).toContain("\"authSetups\"");
    expect(authReadinessText).toContain("\"sessionSurfaces\"");
    expect(authReadinessText).toContain("\"runtimeSignals\"");
    expect(authReadinessText).toContain("\"protectionSignals\"");
    expect(authReadinessText).toContain("\"providerSignals\"");
    expect(authReadinessText).toContain("\"callbackSignals\"");
    expect(authReadinessText).toContain("\"credentialSignals\"");
    expect(authReadinessText).toContain("\"packageSignals\"");
    expect(authReadinessText).toContain("npx vitest run");
    const authReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "auth-readiness.html"), "utf8");
    expect(authReadinessHtml).toContain("Auth Readiness");
    expect(authReadinessHtml).toContain("auth-readiness-card");
    expect(authReadinessHtml).toContain("data-source-pattern=\"Auth.js\"");
    expect(authReadinessHtml).toContain("Auth Setups");
    expect(authReadinessHtml).toContain("Runtime Signals");
    expect(authReadinessHtml).toContain("Provider Signals");
    const authReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "auth-readiness.md"), "utf8");
    expect(authReadinessMarkdown).toContain("# Auth Readiness");
    expect(authReadinessMarkdown).toContain("Source pattern: Auth.js");
    expect(authReadinessMarkdown).toContain("## Session Surfaces");
    expect(authReadinessMarkdown).toContain("## Runtime Signals");
    expect(authReadinessMarkdown).toContain("## Credential Signals");
    const authorizationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "authorization-readiness-report.json"), "utf8");
    expect(authorizationReadinessText).toContain("Authorization readiness OpenFGA Casbin CASL Oso RBAC ABAC ReBAC ACL relationship tuples policy model roles permissions resources actions guards middleware can checks deny by default ownership tenants organizations audit decision logs tests");
    expect(authorizationReadinessText).toContain("\"authorizationSetups\"");
    expect(authorizationReadinessText).toContain("\"modelSignals\"");
    expect(authorizationReadinessText).toContain("\"enforcementSignals\"");
    expect(authorizationReadinessText).toContain("\"identitySignals\"");
    expect(authorizationReadinessText).toContain("\"resourceSignals\"");
    expect(authorizationReadinessText).toContain("\"governanceSignals\"");
    expect(authorizationReadinessText).toContain("\"testSignals\"");
    expect(authorizationReadinessText).toContain("\"packageSignals\"");
    expect(authorizationReadinessText).toContain("npx vitest run");
    const authorizationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "authorization-readiness.html"), "utf8");
    expect(authorizationReadinessHtml).toContain("Authorization Readiness");
    expect(authorizationReadinessHtml).toContain("authorization-readiness-card");
    expect(authorizationReadinessHtml).toContain("data-source-pattern=\"OpenFGA\"");
    expect(authorizationReadinessHtml).toContain("Authorization Setups");
    expect(authorizationReadinessHtml).toContain("Enforcement Signals");
    const authorizationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "authorization-readiness.md"), "utf8");
    expect(authorizationReadinessMarkdown).toContain("# Authorization Readiness");
    expect(authorizationReadinessMarkdown).toContain("Source pattern: Authorization readiness");
    expect(authorizationReadinessMarkdown).toContain("## Model Signals");
    expect(authorizationReadinessMarkdown).toContain("## Governance Signals");
    const paymentReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "payment-readiness-report.json"), "utf8");
    expect(paymentReadinessText).toContain("Stripe new Stripe checkout sessions payment intents subscriptions customers invoices billing portal webhooks constructEvent raw body signature idempotency apiVersion env price");
    expect(paymentReadinessText).toContain("\"paymentSetups\"");
    expect(paymentReadinessText).toContain("\"checkoutSignals\"");
    expect(paymentReadinessText).toContain("\"webhookSignals\"");
    expect(paymentReadinessText).toContain("\"customerSignals\"");
    expect(paymentReadinessText).toContain("\"credentialSignals\"");
    expect(paymentReadinessText).toContain("\"packageSignals\"");
    expect(paymentReadinessText).toContain("npx vitest run");
    const paymentReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "payment-readiness.html"), "utf8");
    expect(paymentReadinessHtml).toContain("Payment Readiness");
    expect(paymentReadinessHtml).toContain("payment-readiness-card");
    expect(paymentReadinessHtml).toContain("data-source-pattern=\"Stripe\"");
    expect(paymentReadinessHtml).toContain("Payment Setups");
    expect(paymentReadinessHtml).toContain("Webhook Signals");
    const paymentReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "payment-readiness.md"), "utf8");
    expect(paymentReadinessMarkdown).toContain("# Payment Readiness");
    expect(paymentReadinessMarkdown).toContain("Source pattern: Stripe");
    expect(paymentReadinessMarkdown).toContain("## Checkout Signals");
    expect(paymentReadinessMarkdown).toContain("## Credential Signals");
    const emailReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "email-readiness-report.json"), "utf8");
    expect(emailReadinessText).toContain("Resend new Resend emails.send batch.send domains verify webhooks verify standardwebhooks apiKeys contacts audiences segments broadcasts automations templates events logs receiving from to subject html react attachments replyTo RESEND_API_KEY idempotency");
    expect(emailReadinessText).toContain("\"emailSetups\"");
    expect(emailReadinessText).toContain("\"recipientSignals\"");
    expect(emailReadinessText).toContain("\"deliverySignals\"");
    expect(emailReadinessText).toContain("\"templateSignals\"");
    expect(emailReadinessText).toContain("\"providerSignals\"");
    expect(emailReadinessText).toContain("\"credentialSignals\"");
    expect(emailReadinessText).toContain("\"packageSignals\"");
    expect(emailReadinessText).toContain("npx vitest run");
    const emailReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "email-readiness.html"), "utf8");
    expect(emailReadinessHtml).toContain("Email Readiness");
    expect(emailReadinessHtml).toContain("email-readiness-card");
    expect(emailReadinessHtml).toContain("data-source-pattern=\"Resend\"");
    expect(emailReadinessHtml).toContain("Email Setups");
    expect(emailReadinessHtml).toContain("Delivery Signals");
    expect(emailReadinessHtml).toContain("Provider Signals");
    const emailReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "email-readiness.md"), "utf8");
    expect(emailReadinessMarkdown).toContain("# Email Readiness");
    expect(emailReadinessMarkdown).toContain("Source pattern: Resend");
    expect(emailReadinessMarkdown).toContain("## Recipient Signals");
    expect(emailReadinessMarkdown).toContain("## Template Signals");
    expect(emailReadinessMarkdown).toContain("## Provider Signals");
    const queueReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "queue-readiness-report.json"), "utf8");
    expect(queueReadinessText).toContain("BullMQ Queue Worker QueueEvents FlowProducer JobScheduler queue.add addBulk repeat attempts backoff removeOnComplete removeOnFail Redis connection concurrency limiter stalled failed completed metrics telemetry");
    expect(queueReadinessText).toContain("\"queueSetups\"");
    expect(queueReadinessText).toContain("\"producerSignals\"");
    expect(queueReadinessText).toContain("\"workerSignals\"");
    expect(queueReadinessText).toContain("\"reliabilitySignals\"");
    expect(queueReadinessText).toContain("\"connectionSignals\"");
    expect(queueReadinessText).toContain("\"packageSignals\"");
    expect(queueReadinessText).toContain("npx vitest run");
    const queueReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "queue-readiness.html"), "utf8");
    expect(queueReadinessHtml).toContain("Queue Readiness");
    expect(queueReadinessHtml).toContain("queue-readiness-card");
    expect(queueReadinessHtml).toContain("data-source-pattern=\"BullMQ\"");
    expect(queueReadinessHtml).toContain("Queue Setups");
    expect(queueReadinessHtml).toContain("Reliability Signals");
    const queueReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "queue-readiness.md"), "utf8");
    expect(queueReadinessMarkdown).toContain("# Queue Readiness");
    expect(queueReadinessMarkdown).toContain("Source pattern: BullMQ");
    expect(queueReadinessMarkdown).toContain("## Producer Signals");
    expect(queueReadinessMarkdown).toContain("## Connection Signals");
    const eventStreamReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "event-stream-readiness-report.json"), "utf8");
    expect(eventStreamReadinessText).toContain("Event stream readiness Apache Kafka Redpanda Apache Pulsar KafkaProducer KafkaConsumer AdminClient NewTopic consumer group group.protocol consumer streams classic group coordinator __consumer_offsets auto.offset.reset enable.auto.commit isolation.level partition assignment rebalance metrics offset commit rebalance schema registry DLQ retention compaction idempotence transactions ACL SASL PulsarClient SubscriptionType BookKeeper tenant namespace CI");
    expect(eventStreamReadinessText).toContain("\"eventStreamSetups\"");
    expect(eventStreamReadinessText).toContain("\"platformSignals\"");
    expect(eventStreamReadinessText).toContain("\"brokerSignals\"");
    expect(eventStreamReadinessText).toContain("\"topicSignals\"");
    expect(eventStreamReadinessText).toContain("\"schemaSignals\"");
    expect(eventStreamReadinessText).toContain("\"reliabilitySignals\"");
    expect(eventStreamReadinessText).toContain("\"securitySignals\"");
    expect(eventStreamReadinessText).toContain("\"opsSignals\"");
    expect(eventStreamReadinessText).toContain("\"ciSignals\"");
    expect(eventStreamReadinessText).toContain("\"packageSignals\"");
    const eventStreamReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "event-stream-readiness.html"), "utf8");
    expect(eventStreamReadinessHtml).toContain("Event Stream Readiness");
    expect(eventStreamReadinessHtml).toContain("event-stream-readiness-card");
    expect(eventStreamReadinessHtml).toContain("data-source-pattern=\"EventStream\"");
    expect(eventStreamReadinessHtml).toContain("Schema Signals");
    expect(eventStreamReadinessHtml).toContain("Reliability Signals");
    const eventStreamReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "event-stream-readiness.md"), "utf8");
    expect(eventStreamReadinessMarkdown).toContain("# Event Stream Readiness");
    expect(eventStreamReadinessMarkdown).toContain("Source pattern: Event stream readiness");
    expect(eventStreamReadinessMarkdown).toContain("## Schema Signals");
    expect(eventStreamReadinessMarkdown).toContain("## Ops Signals");
    const dataConnectorReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "data-connector-readiness-report.json"), "utf8");
    expect(dataConnectorReadinessText).toContain("Data connector readiness Debezium Kafka Connect Airbyte Spec Check Discover Read AirbyteCatalog ConfiguredAirbyteCatalog AirbyteStream ConfiguredAirbyteStream SyncMode DestinationSyncMode primary_key cursor_field AirbyteRecordMessage AirbyteStateMessage AirbyteTraceMessage stream status SourceConnector SinkConnector connector.class tasks.max plugin.path transforms predicates offset.storage.topic status.storage.topic CDC snapshot schema history sync catalog state");
    expect(dataConnectorReadinessText).toContain("\"connectorSetups\"");
    expect(dataConnectorReadinessText).toContain("\"platformSignals\"");
    expect(dataConnectorReadinessText).toContain("\"connectorKindSignals\"");
    expect(dataConnectorReadinessText).toContain("\"configSignals\"");
    expect(dataConnectorReadinessText).toContain("\"stateSignals\"");
    expect(dataConnectorReadinessText).toContain("\"transformSignals\"");
    expect(dataConnectorReadinessText).toContain("\"opsSignals\"");
    expect(dataConnectorReadinessText).toContain("\"workflowSignals\"");
    expect(dataConnectorReadinessText).toContain("\"packageSignals\"");
    const dataConnectorReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "data-connector-readiness.html"), "utf8");
    expect(dataConnectorReadinessHtml).toContain("Data Connector Readiness");
    expect(dataConnectorReadinessHtml).toContain("data-connector-readiness-card");
    expect(dataConnectorReadinessHtml).toContain("data-source-pattern=\"DataConnector\"");
    expect(dataConnectorReadinessHtml).toContain("Config Signals");
    expect(dataConnectorReadinessHtml).toContain("State Signals");
    const dataConnectorReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "data-connector-readiness.md"), "utf8");
    expect(dataConnectorReadinessMarkdown).toContain("# Data Connector Readiness");
    expect(dataConnectorReadinessMarkdown).toContain("Source pattern: Data connector readiness");
    expect(dataConnectorReadinessMarkdown).toContain("## Config Signals");
    expect(dataConnectorReadinessMarkdown).toContain("## State Signals");
    const semanticLayerReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "semantic-layer-readiness-report.json"), "utf8");
    expect(semanticLayerReadinessText).toContain("Semantic layer readiness MetricFlow dbt Semantic Layer Cube semantic_models metrics measures dimensions entities saved_queries TimeDimension Dimension agg_time_dimension type_params ratio derived cumulative cubes joins pre_aggregations access_policy query_rewrite SQL REST GraphQL");
    expect(semanticLayerReadinessText).toContain("\"semanticLayerSetups\"");
    expect(semanticLayerReadinessText).toContain("\"metricSignals\"");
    expect(semanticLayerReadinessText).toContain("\"querySignals\"");
    expect(semanticLayerReadinessText).toContain("\"accessSignals\"");
    const semanticLayerReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "semantic-layer-readiness.html"), "utf8");
    expect(semanticLayerReadinessHtml).toContain("Semantic Layer Readiness");
    expect(semanticLayerReadinessHtml).toContain("semantic-layer-readiness-card");
    expect(semanticLayerReadinessHtml).toContain("data-source-pattern=\"SemanticLayer\"");
    expect(semanticLayerReadinessHtml).toContain("Metric Signals");
    expect(semanticLayerReadinessHtml).toContain("Query Signals");
    const semanticLayerReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "semantic-layer-readiness.md"), "utf8");
    expect(semanticLayerReadinessMarkdown).toContain("# Semantic Layer Readiness");
    expect(semanticLayerReadinessMarkdown).toContain("Source pattern: Semantic layer readiness");
    expect(semanticLayerReadinessMarkdown).toContain("## Metric Signals");
    expect(semanticLayerReadinessMarkdown).toContain("## Query Signals");
    const schemaRegistryReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "schema-registry-readiness-report.json"), "utf8");
    expect(schemaRegistryReadinessText).toContain("Schema registry readiness Confluent Apicurio Buf subject artifact version compatibility Avro Protobuf JSON Schema lint breaking generate push");
    expect(schemaRegistryReadinessText).toContain("\"registrySetups\"");
    expect(schemaRegistryReadinessText).toContain("\"registrySignals\"");
    expect(schemaRegistryReadinessText).toContain("\"schemaFormatSignals\"");
    expect(schemaRegistryReadinessText).toContain("\"identitySignals\"");
    expect(schemaRegistryReadinessText).toContain("\"compatibilitySignals\"");
    expect(schemaRegistryReadinessText).toContain("\"governanceSignals\"");
    expect(schemaRegistryReadinessText).toContain("\"workflowSignals\"");
    expect(schemaRegistryReadinessText).toContain("\"packageSignals\"");
    const schemaRegistryReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "schema-registry-readiness.html"), "utf8");
    expect(schemaRegistryReadinessHtml).toContain("Schema Registry Readiness");
    expect(schemaRegistryReadinessHtml).toContain("schema-registry-readiness-card");
    expect(schemaRegistryReadinessHtml).toContain("data-source-pattern=\"Schema Registry\"");
    expect(schemaRegistryReadinessHtml).toContain("Compatibility Signals");
    expect(schemaRegistryReadinessHtml).toContain("Governance Signals");
    const schemaRegistryReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "schema-registry-readiness.md"), "utf8");
    expect(schemaRegistryReadinessMarkdown).toContain("# Schema Registry Readiness");
    expect(schemaRegistryReadinessMarkdown).toContain("Source pattern: Schema registry readiness");
    expect(schemaRegistryReadinessMarkdown).toContain("## Compatibility Signals");
    expect(schemaRegistryReadinessMarkdown).toContain("## Governance Signals");
    const streamProcessingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "stream-processing-readiness-report.json"), "utf8");
    expect(streamProcessingReadinessText).toContain("Stream processing readiness Apache Flink Apache Beam Spark Structured Streaming StreamExecutionEnvironment DataStream Pipeline PCollection readStream writeStream checkpointing savepoint state backend WatermarkStrategy window trigger exactly-once sink runner deployment metrics CI");
    expect(streamProcessingReadinessText).toContain("\"streamProcessingSetups\"");
    expect(streamProcessingReadinessText).toContain("\"engineSignals\"");
    expect(streamProcessingReadinessText).toContain("\"jobSignals\"");
    expect(streamProcessingReadinessText).toContain("\"watermarkSignals\"");
    expect(streamProcessingReadinessText).toContain("\"checkpointSignals\"");
    expect(streamProcessingReadinessText).toContain("\"sinkSignals\"");
    expect(streamProcessingReadinessText).toContain("\"monitoringSignals\"");
    expect(streamProcessingReadinessText).toContain("\"ciSignals\"");
    expect(streamProcessingReadinessText).toContain("\"packageSignals\"");
    const streamProcessingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "stream-processing-readiness.html"), "utf8");
    expect(streamProcessingReadinessHtml).toContain("Stream Processing Readiness");
    expect(streamProcessingReadinessHtml).toContain("stream-processing-readiness-card");
    expect(streamProcessingReadinessHtml).toContain("data-source-pattern=\"StreamProcessing\"");
    expect(streamProcessingReadinessHtml).toContain("Watermark Signals");
    expect(streamProcessingReadinessHtml).toContain("Checkpoint Signals");
    const streamProcessingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "stream-processing-readiness.md"), "utf8");
    expect(streamProcessingReadinessMarkdown).toContain("# Stream Processing Readiness");
    expect(streamProcessingReadinessMarkdown).toContain("Source pattern: Stream processing readiness");
    expect(streamProcessingReadinessMarkdown).toContain("## Watermark Signals");
    expect(streamProcessingReadinessMarkdown).toContain("## Checkpoint Signals");
    const pipelineOrchestrationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "pipeline-orchestration-readiness-report.json"), "utf8");
    expect(pipelineOrchestrationReadinessText).toContain("Pipeline orchestration readiness Apache Airflow Dagster Prefect airflow.sdk stable authoring interface DAG dag task task_group setup teardown Param Context TriggerRule Asset flow asset sensor schedule backfill catchup partition retry SLA XCom executor worker deployment run history CI");
    expect(pipelineOrchestrationReadinessText).toContain("\"pipelineOrchestrationSetups\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"orchestratorSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"dagSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"taskSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"scheduleSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"sensorSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"assetSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"partitionSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"reliabilitySignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"executorSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"deploymentSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"observabilitySignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"ciSignals\"");
    expect(pipelineOrchestrationReadinessText).toContain("\"packageSignals\"");
    const pipelineOrchestrationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "pipeline-orchestration-readiness.html"), "utf8");
    expect(pipelineOrchestrationReadinessHtml).toContain("Pipeline Orchestration Readiness");
    expect(pipelineOrchestrationReadinessHtml).toContain("pipeline-orchestration-readiness-card");
    expect(pipelineOrchestrationReadinessHtml).toContain("data-source-pattern=\"PipelineOrchestration\"");
    expect(pipelineOrchestrationReadinessHtml).toContain("Reliability Signals");
    expect(pipelineOrchestrationReadinessHtml).toContain("Executor Signals");
    const pipelineOrchestrationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "pipeline-orchestration-readiness.md"), "utf8");
    expect(pipelineOrchestrationReadinessMarkdown).toContain("# Pipeline Orchestration Readiness");
    expect(pipelineOrchestrationReadinessMarkdown).toContain("Source pattern: Pipeline orchestration readiness");
    expect(pipelineOrchestrationReadinessMarkdown).toContain("## Orchestrator Signals");
    expect(pipelineOrchestrationReadinessMarkdown).toContain("## Reliability Signals");
    const serviceMeshReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "service-mesh-readiness-report.json"), "utf8");
    expect(serviceMeshReadinessText).toContain("Service mesh readiness Istio Linkerd Consul Envoy Gateway API VirtualService DestinationRule Gateway Sidecar EnvoyFilter PeerAuthentication AuthorizationPolicy RequestAuthentication ServiceEntry HTTPRoute GRPCRoute TrafficSplit ServerAuthorization MeshTLSAuthentication service-defaults service-router service-splitter service-resolver proxy-defaults intentions mTLS SPIFFE telemetry proxy-config CI");
    expect(serviceMeshReadinessText).toContain("\"serviceMeshSetups\"");
    expect(serviceMeshReadinessText).toContain("\"meshSignals\"");
    expect(serviceMeshReadinessText).toContain("\"controlPlaneSignals\"");
    expect(serviceMeshReadinessText).toContain("\"injectionSignals\"");
    expect(serviceMeshReadinessText).toContain("\"trafficSignals\"");
    expect(serviceMeshReadinessText).toContain("\"securitySignals\"");
    expect(serviceMeshReadinessText).toContain("\"mtlsSignals\"");
    expect(serviceMeshReadinessText).toContain("\"telemetrySignals\"");
    expect(serviceMeshReadinessText).toContain("\"packageSignals\"");
    const serviceMeshReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "service-mesh-readiness.html"), "utf8");
    expect(serviceMeshReadinessHtml).toContain("Service Mesh Readiness");
    expect(serviceMeshReadinessHtml).toContain("service-mesh-readiness-card");
    expect(serviceMeshReadinessHtml).toContain("data-source-pattern=\"ServiceMesh\"");
    expect(serviceMeshReadinessHtml).toContain("Traffic Signals");
    expect(serviceMeshReadinessHtml).toContain("Security Signals");
    expect(serviceMeshReadinessHtml).toContain("mTLS Signals");
    const serviceMeshReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "service-mesh-readiness.md"), "utf8");
    expect(serviceMeshReadinessMarkdown).toContain("# Service Mesh Readiness");
    expect(serviceMeshReadinessMarkdown).toContain("Source pattern: Service mesh readiness");
    expect(serviceMeshReadinessMarkdown).toContain("## Mesh Signals");
    expect(serviceMeshReadinessMarkdown).toContain("## Traffic Signals");
    expect(serviceMeshReadinessMarkdown).toContain("## Security Signals");
    const ingressControllerReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "ingress-controller-readiness-report.json"), "utf8");
    expect(ingressControllerReadinessText).toContain("Ingress controller readiness ingress-nginx Traefik Envoy Gateway IngressClass IngressRoute Middleware GatewayClass Gateway HTTPRoute GRPCRoute BackendTrafficPolicy SecurityPolicy ClientTrafficPolicy TLSOption TLSStore LoadBalancer NodePort admission webhook cert-manager Prometheus access logs rate limit CI");
    expect(ingressControllerReadinessText).toContain("\"ingressControllerSetups\"");
    expect(ingressControllerReadinessText).toContain("\"controllerSignals\"");
    expect(ingressControllerReadinessText).toContain("\"ingressClassSignals\"");
    expect(ingressControllerReadinessText).toContain("\"routeSignals\"");
    expect(ingressControllerReadinessText).toContain("\"serviceExposureSignals\"");
    expect(ingressControllerReadinessText).toContain("\"tlsSignals\"");
    expect(ingressControllerReadinessText).toContain("\"middlewareSignals\"");
    expect(ingressControllerReadinessText).toContain("\"policySignals\"");
    expect(ingressControllerReadinessText).toContain("\"loadBalancingSignals\"");
    expect(ingressControllerReadinessText).toContain("\"observabilitySignals\"");
    expect(ingressControllerReadinessText).toContain("\"admissionSignals\"");
    expect(ingressControllerReadinessText).toContain("\"ciSignals\"");
    expect(ingressControllerReadinessText).toContain("\"packageSignals\"");
    const ingressControllerReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "ingress-controller-readiness.html"), "utf8");
    expect(ingressControllerReadinessHtml).toContain("Ingress Controller Readiness");
    expect(ingressControllerReadinessHtml).toContain("ingress-controller-readiness-card");
    expect(ingressControllerReadinessHtml).toContain("data-source-pattern=\"IngressController\"");
    expect(ingressControllerReadinessHtml).toContain("Route Signals");
    expect(ingressControllerReadinessHtml).toContain("TLS Signals");
    expect(ingressControllerReadinessHtml).toContain("Admission Signals");
    const ingressControllerReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "ingress-controller-readiness.md"), "utf8");
    expect(ingressControllerReadinessMarkdown).toContain("# Ingress Controller Readiness");
    expect(ingressControllerReadinessMarkdown).toContain("Source pattern: Ingress controller readiness");
    expect(ingressControllerReadinessMarkdown).toContain("## Controller Signals");
    expect(ingressControllerReadinessMarkdown).toContain("## Route Signals");
    expect(ingressControllerReadinessMarkdown).toContain("## Admission Signals");
    const dnsReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "dns-readiness-report.json"), "utf8");
    expect(dnsReadinessText).toContain("DNS readiness ExternalDNS CoreDNS octoDNS Route53 Cloudflare Google Cloud DNS Azure DNS source provider zone record TXT registry Corefile forward cache kubernetes plugin octodns-sync dry-run dig");
    expect(dnsReadinessText).toContain("\"dnsSetups\"");
    expect(dnsReadinessText).toContain("\"providerSignals\"");
    expect(dnsReadinessText).toContain("\"sourceSignals\"");
    expect(dnsReadinessText).toContain("\"zoneSignals\"");
    expect(dnsReadinessText).toContain("\"recordSignals\"");
    expect(dnsReadinessText).toContain("\"ownershipSignals\"");
    expect(dnsReadinessText).toContain("\"coreDnsSignals\"");
    expect(dnsReadinessText).toContain("\"automationSignals\"");
    expect(dnsReadinessText).toContain("\"observabilitySignals\"");
    expect(dnsReadinessText).toContain("\"ciSignals\"");
    expect(dnsReadinessText).toContain("\"packageSignals\"");
    const dnsReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "dns-readiness.html"), "utf8");
    expect(dnsReadinessHtml).toContain("DNS Readiness");
    expect(dnsReadinessHtml).toContain("dns-readiness-card");
    expect(dnsReadinessHtml).toContain("data-source-pattern=\"DNS\"");
    expect(dnsReadinessHtml).toContain("Provider Signals");
    expect(dnsReadinessHtml).toContain("CoreDNS Signals");
    expect(dnsReadinessHtml).toContain("Ownership Signals");
    const dnsReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "dns-readiness.md"), "utf8");
    expect(dnsReadinessMarkdown).toContain("# DNS Readiness");
    expect(dnsReadinessMarkdown).toContain("Source pattern: DNS readiness");
    expect(dnsReadinessMarkdown).toContain("## Provider Signals");
    expect(dnsReadinessMarkdown).toContain("## CoreDNS Signals");
    expect(dnsReadinessMarkdown).toContain("## Ownership Signals");
    const certificateReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "certificate-readiness-report.json"), "utf8");
    expect(certificateReadinessText).toContain("Certificate readiness cert-manager step-ca CertMagic ACME Certificate Issuer ClusterIssuer CertificateRequest Order Challenge DNS01 HTTP01 TLS-ALPN renewBefore duration privateKey rotation Secret cainjector trust-manager root intermediate CRL OCSP cmctl step ca renew");
    expect(certificateReadinessText).toContain("\"certificateSetups\"");
    expect(certificateReadinessText).toContain("\"platformSignals\"");
    expect(certificateReadinessText).toContain("\"resourceSignals\"");
    expect(certificateReadinessText).toContain("\"issuerSignals\"");
    expect(certificateReadinessText).toContain("\"challengeSignals\"");
    expect(certificateReadinessText).toContain("\"lifecycleSignals\"");
    expect(certificateReadinessText).toContain("\"trustSignals\"");
    expect(certificateReadinessText).toContain("\"revocationSignals\"");
    expect(certificateReadinessText).toContain("\"automationSignals\"");
    expect(certificateReadinessText).toContain("\"observabilitySignals\"");
    expect(certificateReadinessText).toContain("\"ciSignals\"");
    expect(certificateReadinessText).toContain("\"packageSignals\"");
    const certificateReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "certificate-readiness.html"), "utf8");
    expect(certificateReadinessHtml).toContain("Certificate Readiness");
    expect(certificateReadinessHtml).toContain("certificate-readiness-card");
    expect(certificateReadinessHtml).toContain("data-source-pattern=\"Certificate\"");
    expect(certificateReadinessHtml).toContain("Issuer Signals");
    expect(certificateReadinessHtml).toContain("Lifecycle Signals");
    expect(certificateReadinessHtml).toContain("Trust Signals");
    const certificateReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "certificate-readiness.md"), "utf8");
    expect(certificateReadinessMarkdown).toContain("# Certificate Readiness");
    expect(certificateReadinessMarkdown).toContain("Source pattern: Certificate readiness");
    expect(certificateReadinessMarkdown).toContain("## Issuer Signals");
    expect(certificateReadinessMarkdown).toContain("## Lifecycle Signals");
    expect(certificateReadinessMarkdown).toContain("## Trust Signals");
    const cacheReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "cache-readiness-report.json"), "utf8");
    expect(cacheReadinessText).toContain("Node Redis createClient connect get set EX NX expire ttl del mGet mSet scanIterator multi watch clientSideCache RESP socket reconnect isReady");
    expect(cacheReadinessText).toContain("\"cacheSetups\"");
    expect(cacheReadinessText).toContain("\"operationSignals\"");
    expect(cacheReadinessText).toContain("\"policySignals\"");
    expect(cacheReadinessText).toContain("\"connectionSignals\"");
    expect(cacheReadinessText).toContain("\"advancedSignals\"");
    expect(cacheReadinessText).toContain("\"packageSignals\"");
    expect(cacheReadinessText).toContain("npx vitest run");
    const cacheReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "cache-readiness.html"), "utf8");
    expect(cacheReadinessHtml).toContain("Cache Readiness");
    expect(cacheReadinessHtml).toContain("cache-readiness-card");
    expect(cacheReadinessHtml).toContain("data-source-pattern=\"Node Redis\"");
    expect(cacheReadinessHtml).toContain("Cache Setups");
    expect(cacheReadinessHtml).toContain("Policy Signals");
    const cacheReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "cache-readiness.md"), "utf8");
    expect(cacheReadinessMarkdown).toContain("# Cache Readiness");
    expect(cacheReadinessMarkdown).toContain("Source pattern: Node Redis");
    expect(cacheReadinessMarkdown).toContain("## Operation Signals");
    expect(cacheReadinessMarkdown).toContain("## Advanced Signals");
    const loggingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "logging-readiness-report.json"), "utf8");
    expect(loggingReadinessText).toContain("Pino Zap Zerolog pino logger.info logger.error child logger level transport destination redact serializers pino-pretty multistream timestamp formatters mixin bindings go.uber.org/zap zap.NewProduction zap.NewDevelopment zap.Config zap.AtomicLevel zap.Logger zap.SugaredLogger zap.String zap.Error zap.Any zapcore.NewCore EncoderConfig WriteSyncer Sync AddCaller AddStacktrace Sampling github.com/rs/zerolog zerolog.New log.Info Msg Msgf With Timestamp SetGlobalLevel ConsoleWriter MultiLevelWriter hlog diode journald syslog");
    expect(loggingReadinessText).toContain("\"loggingSetups\"");
    expect(loggingReadinessText).toContain("\"levelSignals\"");
    expect(loggingReadinessText).toContain("\"contextSignals\"");
    expect(loggingReadinessText).toContain("\"safetySignals\"");
    expect(loggingReadinessText).toContain("\"transportSignals\"");
    expect(loggingReadinessText).toContain("\"packageSignals\"");
    expect(loggingReadinessText).toContain("npx vitest run");
    const loggingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "logging-readiness.html"), "utf8");
    expect(loggingReadinessHtml).toContain("Logging Readiness");
    expect(loggingReadinessHtml).toContain("logging-readiness-card");
    expect(loggingReadinessHtml).toContain("data-source-pattern=\"Pino Zap Zerolog\"");
    expect(loggingReadinessHtml).toContain("Logging Setups");
    expect(loggingReadinessHtml).toContain("Safety Signals");
    const loggingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "logging-readiness.md"), "utf8");
    expect(loggingReadinessMarkdown).toContain("# Logging Readiness");
    expect(loggingReadinessMarkdown).toContain("Source pattern: Pino Zap Zerolog");
    expect(loggingReadinessMarkdown).toContain("## Context Signals");
    expect(loggingReadinessMarkdown).toContain("## Transport Signals");
    const featureFlagReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "feature-flag-readiness-report.json"), "utf8");
    expect(featureFlagReadinessText).toContain("OpenFeature setProviderAndWait setProvider getClient getBooleanValue getStringValue getNumberValue getObjectValue getBooleanDetails EvaluationContext targetingKey hooks events tracking shutdown MultiProvider Unleash flexibleRollout constraints segments variants impressionData stickiness stale archived GrowthBook isOn isOff getFeatureValue trackingCallback stickyBucket remoteEval subscribeToChanges EventSource encryptedFeatures safe rollout");
    expect(featureFlagReadinessText).toContain("\"featureFlagSetups\"");
    expect(featureFlagReadinessText).toContain("\"evaluationSignals\"");
    expect(featureFlagReadinessText).toContain("\"contextSignals\"");
    expect(featureFlagReadinessText).toContain("\"lifecycleSignals\"");
    expect(featureFlagReadinessText).toContain("\"packageSignals\"");
    expect(featureFlagReadinessText).toContain("npx vitest run");
    const featureFlagReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "feature-flag-readiness.html"), "utf8");
    expect(featureFlagReadinessHtml).toContain("Feature Flag Readiness");
    expect(featureFlagReadinessHtml).toContain("feature-flag-readiness-card");
    expect(featureFlagReadinessHtml).toContain("data-source-pattern=\"OpenFeature Unleash GrowthBook\"");
    expect(featureFlagReadinessHtml).toContain("Feature Flag Setups");
    expect(featureFlagReadinessHtml).toContain("Lifecycle Signals");
    const featureFlagReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "feature-flag-readiness.md"), "utf8");
    expect(featureFlagReadinessMarkdown).toContain("# Feature Flag Readiness");
    expect(featureFlagReadinessMarkdown).toContain("Source pattern: OpenFeature");
    expect(featureFlagReadinessMarkdown).toContain("## Evaluation Signals");
    expect(featureFlagReadinessMarkdown).toContain("## Lifecycle Signals");
    const rateLimitReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "rate-limit-readiness-report.json"), "utf8");
    expect(rateLimitReadinessText).toContain("rate-limiter-flexible RateLimiterMemory RateLimiterRedis points duration blockDuration keyPrefix storeClient consume penalty reward insuranceLimiter msBeforeNext remainingPoints Retry-After X-RateLimit");
    expect(rateLimitReadinessText).toContain("\"rateLimitSetups\"");
    expect(rateLimitReadinessText).toContain("\"quotaSignals\"");
    expect(rateLimitReadinessText).toContain("\"identitySignals\"");
    expect(rateLimitReadinessText).toContain("\"storeSignals\"");
    expect(rateLimitReadinessText).toContain("\"responseSignals\"");
    expect(rateLimitReadinessText).toContain("\"resilienceSignals\"");
    expect(rateLimitReadinessText).toContain("\"packageSignals\"");
    expect(rateLimitReadinessText).toContain("npx vitest run");
    const rateLimitReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "rate-limit-readiness.html"), "utf8");
    expect(rateLimitReadinessHtml).toContain("Rate Limit Readiness");
    expect(rateLimitReadinessHtml).toContain("rate-limit-readiness-card");
    expect(rateLimitReadinessHtml).toContain("data-source-pattern=\"rate-limiter-flexible\"");
    expect(rateLimitReadinessHtml).toContain("Rate Limit Setups");
    expect(rateLimitReadinessHtml).toContain("Resilience Signals");
    const rateLimitReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "rate-limit-readiness.md"), "utf8");
    expect(rateLimitReadinessMarkdown).toContain("# Rate Limit Readiness");
    expect(rateLimitReadinessMarkdown).toContain("Source pattern: rate-limiter-flexible");
    expect(rateLimitReadinessMarkdown).toContain("## Quota Signals");
    expect(rateLimitReadinessMarkdown).toContain("## Store Signals");
    const errorTrackingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "error-tracking-readiness-report.json"), "utf8");
    expect(errorTrackingReadinessText).toContain("Sentry.init dsn captureException captureMessage captureEvent withScope setUser setContext setTag beforeSend ignoreErrors tracesSampleRate tracePropagationTargets replayIntegration ErrorBoundary");
    expect(errorTrackingReadinessText).toContain("\"errorTrackingSetups\"");
    expect(errorTrackingReadinessText).toContain("\"captureSignals\"");
    expect(errorTrackingReadinessText).toContain("\"contextSignals\"");
    expect(errorTrackingReadinessText).toContain("\"filteringSignals\"");
    expect(errorTrackingReadinessText).toContain("\"observabilitySignals\"");
    expect(errorTrackingReadinessText).toContain("\"packageSignals\"");
    expect(errorTrackingReadinessText).toContain("npx vitest run");
    const errorTrackingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "error-tracking-readiness.html"), "utf8");
    expect(errorTrackingReadinessHtml).toContain("Error Tracking Readiness");
    expect(errorTrackingReadinessHtml).toContain("error-tracking-readiness-card");
    expect(errorTrackingReadinessHtml).toContain("data-source-pattern=\"Sentry\"");
    expect(errorTrackingReadinessHtml).toContain("Error Tracking Setups");
    expect(errorTrackingReadinessHtml).toContain("Filtering Signals");
    const errorTrackingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "error-tracking-readiness.md"), "utf8");
    expect(errorTrackingReadinessMarkdown).toContain("# Error Tracking Readiness");
    expect(errorTrackingReadinessMarkdown).toContain("Source pattern: Sentry");
    expect(errorTrackingReadinessMarkdown).toContain("## Capture Signals");
    expect(errorTrackingReadinessMarkdown).toContain("## Observability Signals");
    const analyticsReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "analytics-readiness-report.json"), "utf8");
    expect(analyticsReadinessText).toContain("posthog.init posthog.capture posthog.identify posthog.alias posthog.group posthog.reset autocapture capture_pageview opt_in_capturing opt_out_capturing before_send getFeatureFlag onFeatureFlags session_recording");
    expect(analyticsReadinessText).toContain("\"analyticsSetups\"");
    expect(analyticsReadinessText).toContain("\"eventSignals\"");
    expect(analyticsReadinessText).toContain("\"identitySignals\"");
    expect(analyticsReadinessText).toContain("\"privacySignals\"");
    expect(analyticsReadinessText).toContain("\"productSignals\"");
    expect(analyticsReadinessText).toContain("\"packageSignals\"");
    expect(analyticsReadinessText).toContain("npx vitest run");
    const analyticsReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "analytics-readiness.html"), "utf8");
    expect(analyticsReadinessHtml).toContain("Analytics Readiness");
    expect(analyticsReadinessHtml).toContain("analytics-readiness-card");
    expect(analyticsReadinessHtml).toContain("data-source-pattern=\"PostHog\"");
    expect(analyticsReadinessHtml).toContain("Analytics Setups");
    expect(analyticsReadinessHtml).toContain("Privacy Signals");
    const analyticsReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "analytics-readiness.md"), "utf8");
    expect(analyticsReadinessMarkdown).toContain("# Analytics Readiness");
    expect(analyticsReadinessMarkdown).toContain("Source pattern: posthog.init");
    expect(analyticsReadinessMarkdown).toContain("## Event Signals");
    expect(analyticsReadinessMarkdown).toContain("## Product Signals");
    const httpClientReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "http-client-readiness-report.json"), "utf8");
    expect(httpClientReadinessText).toContain("got timeout retry limit methods statusCodes hooks beforeRequest afterResponse beforeRetry beforeError prefixUrl searchParams responseType throwHttpErrors HTTPError RequestError agent cache http2 pagination");
    expect(httpClientReadinessText).toContain("\"httpClientSetups\"");
    expect(httpClientReadinessText).toContain("\"requestSignals\"");
    expect(httpClientReadinessText).toContain("\"resilienceSignals\"");
    expect(httpClientReadinessText).toContain("\"configurationSignals\"");
    expect(httpClientReadinessText).toContain("\"transportSignals\"");
    expect(httpClientReadinessText).toContain("\"errorSignals\"");
    expect(httpClientReadinessText).toContain("\"packageSignals\"");
    expect(httpClientReadinessText).toContain("npx vitest run");
    const httpClientReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "http-client-readiness.html"), "utf8");
    expect(httpClientReadinessHtml).toContain("HTTP Client Readiness");
    expect(httpClientReadinessHtml).toContain("http-client-readiness-card");
    expect(httpClientReadinessHtml).toContain("data-source-pattern=\"Got\"");
    expect(httpClientReadinessHtml).toContain("HTTP Client Setups");
    expect(httpClientReadinessHtml).toContain("Transport Signals");
    const httpClientReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "http-client-readiness.md"), "utf8");
    expect(httpClientReadinessMarkdown).toContain("# HTTP Client Readiness");
    expect(httpClientReadinessMarkdown).toContain("Source pattern: got timeout");
    expect(httpClientReadinessMarkdown).toContain("## Resilience Signals");
    expect(httpClientReadinessMarkdown).toContain("## Error Signals");
    const schemaValidationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "schema-validation-readiness-report.json"), "utf8");
    expect(schemaValidationReadinessText).toContain("z.object z.array z.union z.discriminatedUnion parse safeParse parseAsync safeParseAsync z.infer z.input z.output refine superRefine transform preprocess coerce ZodError flatten treeifyError toJSONSchema");
    expect(schemaValidationReadinessText).toContain("Valibot v.object v.pipe v.variant v.picklist parse safeParse parser safeParser InferInput InferOutput InferIssue ValiError issues flatten forward partialCheck rawCheck metadata @valibot/to-json-schema zod-to-valibot Standard Schema");
    expect(schemaValidationReadinessText).toContain("\"schemaSetups\"");
    expect(schemaValidationReadinessText).toContain("\"shapeSignals\"");
    expect(schemaValidationReadinessText).toContain("\"parserSignals\"");
    expect(schemaValidationReadinessText).toContain("\"typeSignals\"");
    expect(schemaValidationReadinessText).toContain("\"refinementSignals\"");
    expect(schemaValidationReadinessText).toContain("\"errorSignals\"");
    expect(schemaValidationReadinessText).toContain("\"integrationSignals\"");
    expect(schemaValidationReadinessText).toContain("\"valibotSignals\"");
    expect(schemaValidationReadinessText).toContain("\"packageSignals\"");
    expect(schemaValidationReadinessText).toContain("npx vitest run");
    const schemaValidationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "schema-validation-readiness.html"), "utf8");
    expect(schemaValidationReadinessHtml).toContain("Schema Validation Readiness");
    expect(schemaValidationReadinessHtml).toContain("schema-validation-readiness-card");
    expect(schemaValidationReadinessHtml).toContain("data-source-pattern=\"Zod Valibot\"");
    expect(schemaValidationReadinessHtml).toContain("Schema Setups");
    expect(schemaValidationReadinessHtml).toContain("Refinement Signals");
    expect(schemaValidationReadinessHtml).toContain("Valibot Signals");
    const schemaValidationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "schema-validation-readiness.md"), "utf8");
    expect(schemaValidationReadinessMarkdown).toContain("# Schema Validation Readiness");
    expect(schemaValidationReadinessMarkdown).toContain("Source pattern: z.object");
    expect(schemaValidationReadinessMarkdown).toContain("## Parser Signals");
    expect(schemaValidationReadinessMarkdown).toContain("## Valibot Signals");
    expect(schemaValidationReadinessMarkdown).toContain("## Error Signals");
    const dateTimeReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "datetime-readiness-report.json"), "utf8");
    expect(dateTimeReadinessText).toContain("DateTime Duration Interval Zone setZone fromISO fromFormat fromJSDate toISO toFormat toLocaleString diff plus minus startOf endOf isValid invalidReason Settings defaultZone");
    expect(dateTimeReadinessText).toContain("\"dateTimeSetups\"");
    expect(dateTimeReadinessText).toContain("\"constructionSignals\"");
    expect(dateTimeReadinessText).toContain("\"parsingSignals\"");
    expect(dateTimeReadinessText).toContain("\"formattingSignals\"");
    expect(dateTimeReadinessText).toContain("\"zoneSignals\"");
    expect(dateTimeReadinessText).toContain("\"durationSignals\"");
    expect(dateTimeReadinessText).toContain("\"validitySignals\"");
    expect(dateTimeReadinessText).toContain("\"luxonSignals\"");
    expect(dateTimeReadinessText).toContain("\"packageSignals\"");
    expect(dateTimeReadinessText).toContain("npx vitest run");
    const dateTimeReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "datetime-readiness.html"), "utf8");
    expect(dateTimeReadinessHtml).toContain("Datetime Readiness");
    expect(dateTimeReadinessHtml).toContain("datetime-readiness-card");
    expect(dateTimeReadinessHtml).toContain("data-source-pattern=\"Luxon\"");
    expect(dateTimeReadinessHtml).toContain("DateTime Setups");
    expect(dateTimeReadinessHtml).toContain("Zone Signals");
    expect(dateTimeReadinessHtml).toContain("Luxon Signals");
    const dateTimeReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "datetime-readiness.md"), "utf8");
    expect(dateTimeReadinessMarkdown).toContain("# Datetime Readiness");
    expect(dateTimeReadinessMarkdown).toContain("Source pattern: DateTime");
    expect(dateTimeReadinessMarkdown).toContain("## Parsing Signals");
    expect(dateTimeReadinessMarkdown).toContain("## Validity Signals");
    expect(dateTimeReadinessMarkdown).toContain("## Luxon Signals");
    const idGenerationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "id-generation-readiness-report.json"), "utf8");
    expect(idGenerationReadinessText).toContain("nanoid customAlphabet customRandom urlAlphabet random nanoid/non-secure crypto.getRandomValues Math.random --size --alphabet react-native-get-random-values");
    expect(idGenerationReadinessText).toContain("\"idGeneratorSetups\"");
    expect(idGenerationReadinessText).toContain("\"generationSignals\"");
    expect(idGenerationReadinessText).toContain("\"entropySignals\"");
    expect(idGenerationReadinessText).toContain("\"alphabetSignals\"");
    expect(idGenerationReadinessText).toContain("\"runtimeSignals\"");
    expect(idGenerationReadinessText).toContain("\"usageSignals\"");
    expect(idGenerationReadinessText).toContain("\"validationSignals\"");
    expect(idGenerationReadinessText).toContain("\"packageSignals\"");
    expect(idGenerationReadinessText).toContain("npx vitest run");
    const idGenerationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "id-generation-readiness.html"), "utf8");
    expect(idGenerationReadinessHtml).toContain("ID Generation Readiness");
    expect(idGenerationReadinessHtml).toContain("id-generation-readiness-card");
    expect(idGenerationReadinessHtml).toContain("data-source-pattern=\"Nano ID\"");
    expect(idGenerationReadinessHtml).toContain("ID Generator Setups");
    expect(idGenerationReadinessHtml).toContain("Entropy Signals");
    const idGenerationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "id-generation-readiness.md"), "utf8");
    expect(idGenerationReadinessMarkdown).toContain("# ID Generation Readiness");
    expect(idGenerationReadinessMarkdown).toContain("Source pattern: nanoid");
    expect(idGenerationReadinessMarkdown).toContain("## Entropy Signals");
    expect(idGenerationReadinessMarkdown).toContain("## Validation Signals");
    const imageProcessingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "image-processing-readiness-report.json"), "utf8");
    expect(imageProcessingReadinessText).toContain("sharp resize toFormat jpeg png webp avif metadata rotate composite pipeline stream toBuffer toFile cache concurrency limitInputPixels");
    expect(imageProcessingReadinessText).toContain("\"imageProcessingSetups\"");
    expect(imageProcessingReadinessText).toContain("\"inputSignals\"");
    expect(imageProcessingReadinessText).toContain("\"transformSignals\"");
    expect(imageProcessingReadinessText).toContain("\"outputSignals\"");
    expect(imageProcessingReadinessText).toContain("\"safetySignals\"");
    expect(imageProcessingReadinessText).toContain("\"performanceSignals\"");
    expect(imageProcessingReadinessText).toContain("\"packageSignals\"");
    expect(imageProcessingReadinessText).toContain("npx vitest run");
    const imageProcessingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "image-processing-readiness.html"), "utf8");
    expect(imageProcessingReadinessHtml).toContain("Image Processing Readiness");
    expect(imageProcessingReadinessHtml).toContain("image-processing-readiness-card");
    expect(imageProcessingReadinessHtml).toContain("data-source-pattern=\"Sharp\"");
    expect(imageProcessingReadinessHtml).toContain("Image Processing Setups");
    expect(imageProcessingReadinessHtml).toContain("Safety Signals");
    const imageProcessingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "image-processing-readiness.md"), "utf8");
    expect(imageProcessingReadinessMarkdown).toContain("# Image Processing Readiness");
    expect(imageProcessingReadinessMarkdown).toContain("Source pattern: sharp");
    expect(imageProcessingReadinessMarkdown).toContain("## Transform Signals");
    expect(imageProcessingReadinessMarkdown).toContain("## Safety Signals");
    const fileUploadReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "file-upload-readiness-report.json"), "utf8");
    expect(fileUploadReadinessText).toContain("Uppy Dashboard DragDrop FileInput restrictions allowedFileTypes maxFileSize maxNumberOfFiles metaFields XHRUpload Tus AwsS3 Companion upload-progress complete error cancel retry");
    expect(fileUploadReadinessText).toContain("\"fileUploadSetups\"");
    expect(fileUploadReadinessText).toContain("\"inputSignals\"");
    expect(fileUploadReadinessText).toContain("\"restrictionSignals\"");
    expect(fileUploadReadinessText).toContain("\"transportSignals\"");
    expect(fileUploadReadinessText).toContain("\"lifecycleSignals\"");
    expect(fileUploadReadinessText).toContain("\"safetySignals\"");
    expect(fileUploadReadinessText).toContain("\"packageSignals\"");
    expect(fileUploadReadinessText).toContain("npx vitest run");
    const fileUploadReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "file-upload-readiness.html"), "utf8");
    expect(fileUploadReadinessHtml).toContain("File Upload Readiness");
    expect(fileUploadReadinessHtml).toContain("file-upload-readiness-card");
    expect(fileUploadReadinessHtml).toContain("data-source-pattern=\"Uppy\"");
    expect(fileUploadReadinessHtml).toContain("File Upload Setups");
    expect(fileUploadReadinessHtml).toContain("Transport Signals");
    const fileUploadReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "file-upload-readiness.md"), "utf8");
    expect(fileUploadReadinessMarkdown).toContain("# File Upload Readiness");
    expect(fileUploadReadinessMarkdown).toContain("Source pattern: Uppy");
    expect(fileUploadReadinessMarkdown).toContain("## Restriction Signals");
    expect(fileUploadReadinessMarkdown).toContain("## Lifecycle Signals");
    const webSocketReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "websocket-readiness-report.json"), "utf8");
    expect(webSocketReadinessText).toContain("ws WebSocket WebSocketServer upgrade connection message send close error ping pong perMessageDeflate backpressure maxPayload");
    expect(webSocketReadinessText).toContain("\"webSocketSetups\"");
    expect(webSocketReadinessText).toContain("\"connectionSignals\"");
    expect(webSocketReadinessText).toContain("\"messageSignals\"");
    expect(webSocketReadinessText).toContain("\"lifecycleSignals\"");
    expect(webSocketReadinessText).toContain("\"safetySignals\"");
    expect(webSocketReadinessText).toContain("\"packageSignals\"");
    expect(webSocketReadinessText).toContain("npx vitest run");
    const webSocketReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "websocket-readiness.html"), "utf8");
    expect(webSocketReadinessHtml).toContain("WebSocket Readiness");
    expect(webSocketReadinessHtml).toContain("websocket-readiness-card");
    expect(webSocketReadinessHtml).toContain("data-source-pattern=\"ws\"");
    expect(webSocketReadinessHtml).toContain("WebSocket Setups");
    expect(webSocketReadinessHtml).toContain("Message Signals");
    const webSocketReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "websocket-readiness.md"), "utf8");
    expect(webSocketReadinessMarkdown).toContain("# WebSocket Readiness");
    expect(webSocketReadinessMarkdown).toContain("Source pattern: ws");
    expect(webSocketReadinessMarkdown).toContain("## Connection Signals");
    expect(webSocketReadinessMarkdown).toContain("## Safety Signals");
    const pdfGenerationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "pdf-generation-readiness-report.json"), "utf8");
    expect(pdfGenerationReadinessText).toContain("pdf-lib PDFDocument create load addPage drawText drawImage embedFont embedPng embedJpg getForm createTextField setText copyPages save saveAsBase64");
    expect(pdfGenerationReadinessText).toContain("\"pdfGenerationSetups\"");
    expect(pdfGenerationReadinessText).toContain("\"documentSignals\"");
    expect(pdfGenerationReadinessText).toContain("\"pageSignals\"");
    expect(pdfGenerationReadinessText).toContain("\"assetSignals\"");
    expect(pdfGenerationReadinessText).toContain("\"formSignals\"");
    expect(pdfGenerationReadinessText).toContain("\"outputSignals\"");
    expect(pdfGenerationReadinessText).toContain("\"safetySignals\"");
    expect(pdfGenerationReadinessText).toContain("\"packageSignals\"");
    expect(pdfGenerationReadinessText).toContain("npx vitest run");
    const pdfGenerationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "pdf-generation-readiness.html"), "utf8");
    expect(pdfGenerationReadinessHtml).toContain("PDF Generation Readiness");
    expect(pdfGenerationReadinessHtml).toContain("pdf-generation-readiness-card");
    expect(pdfGenerationReadinessHtml).toContain("data-source-pattern=\"pdf-lib\"");
    expect(pdfGenerationReadinessHtml).toContain("PDF Generation Setups");
    expect(pdfGenerationReadinessHtml).toContain("Output Signals");
    const pdfGenerationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "pdf-generation-readiness.md"), "utf8");
    expect(pdfGenerationReadinessMarkdown).toContain("# PDF Generation Readiness");
    expect(pdfGenerationReadinessMarkdown).toContain("Source pattern: pdf-lib");
    expect(pdfGenerationReadinessMarkdown).toContain("## Document Signals");
    expect(pdfGenerationReadinessMarkdown).toContain("## Output Signals");
    const spreadsheetReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "spreadsheet-readiness-report.json"), "utf8");
    expect(spreadsheetReadinessText).toContain("SheetJS XLSX readFile writeFile read write book_new book_append_sheet json_to_sheet sheet_to_json aoa_to_sheet table_to_sheet CSV workbook worksheet");
    expect(spreadsheetReadinessText).toContain("\"spreadsheetSetups\"");
    expect(spreadsheetReadinessText).toContain("\"workbookSignals\"");
    expect(spreadsheetReadinessText).toContain("\"sheetSignals\"");
    expect(spreadsheetReadinessText).toContain("\"formatSignals\"");
    expect(spreadsheetReadinessText).toContain("\"inputSignals\"");
    expect(spreadsheetReadinessText).toContain("\"outputSignals\"");
    expect(spreadsheetReadinessText).toContain("\"safetySignals\"");
    expect(spreadsheetReadinessText).toContain("\"packageSignals\"");
    expect(spreadsheetReadinessText).toContain("npx vitest run");
    const spreadsheetReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "spreadsheet-readiness.html"), "utf8");
    expect(spreadsheetReadinessHtml).toContain("Spreadsheet Readiness");
    expect(spreadsheetReadinessHtml).toContain("spreadsheet-readiness-card");
    expect(spreadsheetReadinessHtml).toContain("data-source-pattern=\"SheetJS\"");
    expect(spreadsheetReadinessHtml).toContain("Spreadsheet Setups");
    expect(spreadsheetReadinessHtml).toContain("Output Signals");
    const spreadsheetReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "spreadsheet-readiness.md"), "utf8");
    expect(spreadsheetReadinessMarkdown).toContain("# Spreadsheet Readiness");
    expect(spreadsheetReadinessMarkdown).toContain("Source pattern: SheetJS");
    expect(spreadsheetReadinessMarkdown).toContain("## Workbook Signals");
    expect(spreadsheetReadinessMarkdown).toContain("## Output Signals");
    const chartVisualizationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "chart-visualization-readiness-report.json"), "utf8");
    expect(chartVisualizationReadinessText).toContain("Chart.js new Chart Chart.register registerables datasets scales tooltip legend responsive canvas update resize destroy toBase64Image decimation");
    expect(chartVisualizationReadinessText).toContain("\"chartSetups\"");
    expect(chartVisualizationReadinessText).toContain("\"chartTypeSignals\"");
    expect(chartVisualizationReadinessText).toContain("\"dataSignals\"");
    expect(chartVisualizationReadinessText).toContain("\"scaleSignals\"");
    expect(chartVisualizationReadinessText).toContain("\"interactionSignals\"");
    expect(chartVisualizationReadinessText).toContain("\"renderSignals\"");
    expect(chartVisualizationReadinessText).toContain("\"lifecycleSignals\"");
    expect(chartVisualizationReadinessText).toContain("\"safetySignals\"");
    expect(chartVisualizationReadinessText).toContain("\"packageSignals\"");
    expect(chartVisualizationReadinessText).toContain("npx vitest run");
    const chartVisualizationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "chart-visualization-readiness.html"), "utf8");
    expect(chartVisualizationReadinessHtml).toContain("Chart Visualization Readiness");
    expect(chartVisualizationReadinessHtml).toContain("chart-visualization-readiness-card");
    expect(chartVisualizationReadinessHtml).toContain("data-source-pattern=\"Chart.js\"");
    expect(chartVisualizationReadinessHtml).toContain("Chart Setups");
    expect(chartVisualizationReadinessHtml).toContain("Render Signals");
    const chartVisualizationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "chart-visualization-readiness.md"), "utf8");
    expect(chartVisualizationReadinessMarkdown).toContain("# Chart Visualization Readiness");
    expect(chartVisualizationReadinessMarkdown).toContain("Source pattern: Chart.js");
    expect(chartVisualizationReadinessMarkdown).toContain("## Chart Type Signals");
    expect(chartVisualizationReadinessMarkdown).toContain("## Render Signals");
    const diagramRenderingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "diagram-rendering-readiness-report.json"), "utf8");
    expect(diagramRenderingReadinessText).toContain("Mermaid mermaid.initialize mermaid.run mermaid.render mermaid.parse flowchart sequenceDiagram classDiagram stateDiagram erDiagram gantt journey mindmap securityLevel theme svg sandbox");
    expect(diagramRenderingReadinessText).toContain("\"diagramSetups\"");
    expect(diagramRenderingReadinessText).toContain("\"diagramTypeSignals\"");
    expect(diagramRenderingReadinessText).toContain("\"renderSignals\"");
    expect(diagramRenderingReadinessText).toContain("\"themeSignals\"");
    expect(diagramRenderingReadinessText).toContain("\"securitySignals\"");
    expect(diagramRenderingReadinessText).toContain("\"layoutSignals\"");
    expect(diagramRenderingReadinessText).toContain("\"outputSignals\"");
    expect(diagramRenderingReadinessText).toContain("\"packageSignals\"");
    expect(diagramRenderingReadinessText).toContain("npx vitest run");
    const diagramRenderingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "diagram-rendering-readiness.html"), "utf8");
    expect(diagramRenderingReadinessHtml).toContain("Diagram Rendering Readiness");
    expect(diagramRenderingReadinessHtml).toContain("diagram-rendering-readiness-card");
    expect(diagramRenderingReadinessHtml).toContain("data-source-pattern=\"Mermaid\"");
    expect(diagramRenderingReadinessHtml).toContain("Diagram Setups");
    expect(diagramRenderingReadinessHtml).toContain("Security Signals");
    const diagramRenderingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "diagram-rendering-readiness.md"), "utf8");
    expect(diagramRenderingReadinessMarkdown).toContain("# Diagram Rendering Readiness");
    expect(diagramRenderingReadinessMarkdown).toContain("Source pattern: Mermaid");
    expect(diagramRenderingReadinessMarkdown).toContain("## Diagram Type Signals");
    expect(diagramRenderingReadinessMarkdown).toContain("## Security Signals");
    const linkIntegrityReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "link-integrity-readiness-report.json"), "utf8");
    expect(linkIntegrityReadinessText).toContain("Lychee link checker markdown html reStructuredText website mail sitemap accept status exclude include scheme timeout retry headers github-token offline output cache");
    expect(linkIntegrityReadinessText).toContain("\"linkSetups\"");
    expect(linkIntegrityReadinessText).toContain("\"targetSignals\"");
    expect(linkIntegrityReadinessText).toContain("\"policySignals\"");
    expect(linkIntegrityReadinessText).toContain("\"networkSignals\"");
    expect(linkIntegrityReadinessText).toContain("\"outputSignals\"");
    expect(linkIntegrityReadinessText).toContain("\"ciSignals\"");
    expect(linkIntegrityReadinessText).toContain("\"packageSignals\"");
    expect(linkIntegrityReadinessText).toContain("npx vitest run");
    const linkIntegrityReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "link-integrity-readiness.html"), "utf8");
    expect(linkIntegrityReadinessHtml).toContain("Link Integrity Readiness");
    expect(linkIntegrityReadinessHtml).toContain("link-integrity-readiness-card");
    expect(linkIntegrityReadinessHtml).toContain("data-source-pattern=\"Lychee\"");
    expect(linkIntegrityReadinessHtml).toContain("Link Setups");
    expect(linkIntegrityReadinessHtml).toContain("Network Signals");
    const linkIntegrityReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "link-integrity-readiness.md"), "utf8");
    expect(linkIntegrityReadinessMarkdown).toContain("# Link Integrity Readiness");
    expect(linkIntegrityReadinessMarkdown).toContain("Source pattern: Lychee");
    expect(linkIntegrityReadinessMarkdown).toContain("## Target Signals");
    expect(linkIntegrityReadinessMarkdown).toContain("## Network Signals");
    const seoMetadataReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "seo-metadata-readiness-report.json"), "utf8");
    expect(seoMetadataReadinessText).toContain("Nuxt SEO robots sitemap Schema.org OpenGraph meta tags canonical siteUrl indexable i18n hreflang JSON-LD AEO llms");
    expect(seoMetadataReadinessText).toContain("\"seoSetups\"");
    expect(seoMetadataReadinessText).toContain("\"crawlSignals\"");
    expect(seoMetadataReadinessText).toContain("\"sitemapSignals\"");
    expect(seoMetadataReadinessText).toContain("\"metadataSignals\"");
    expect(seoMetadataReadinessText).toContain("\"structuredDataSignals\"");
    expect(seoMetadataReadinessText).toContain("\"aiReadinessSignals\"");
    expect(seoMetadataReadinessText).toContain("\"packageSignals\"");
    expect(seoMetadataReadinessText).toContain("npx vitest run");
    const seoMetadataReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "seo-metadata-readiness.html"), "utf8");
    expect(seoMetadataReadinessHtml).toContain("SEO Metadata Readiness");
    expect(seoMetadataReadinessHtml).toContain("seo-metadata-readiness-card");
    expect(seoMetadataReadinessHtml).toContain("data-source-pattern=\"Nuxt SEO\"");
    expect(seoMetadataReadinessHtml).toContain("SEO Setups");
    expect(seoMetadataReadinessHtml).toContain("Structured Data Signals");
    const seoMetadataReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "seo-metadata-readiness.md"), "utf8");
    expect(seoMetadataReadinessMarkdown).toContain("# SEO Metadata Readiness");
    expect(seoMetadataReadinessMarkdown).toContain("Source pattern: Nuxt SEO");
    expect(seoMetadataReadinessMarkdown).toContain("## Crawl Signals");
    expect(seoMetadataReadinessMarkdown).toContain("## Structured Data Signals");
    const pwaReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "pwa-readiness-report.json"), "utf8");
    expect(pwaReadinessText).toContain("Vite PWA manifest webmanifest service worker registerSW Workbox generateSW injectManifest precache runtimeCaching offline icons theme_color start_url display");
    expect(pwaReadinessText).toContain("\"pwaSetups\"");
    expect(pwaReadinessText).toContain("\"manifestSignals\"");
    expect(pwaReadinessText).toContain("\"serviceWorkerSignals\"");
    expect(pwaReadinessText).toContain("\"cachingSignals\"");
    expect(pwaReadinessText).toContain("\"updateSignals\"");
    expect(pwaReadinessText).toContain("\"installSignals\"");
    expect(pwaReadinessText).toContain("\"packageSignals\"");
    expect(pwaReadinessText).toContain("npx vitest run");
    const pwaReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "pwa-readiness.html"), "utf8");
    expect(pwaReadinessHtml).toContain("PWA Readiness");
    expect(pwaReadinessHtml).toContain("pwa-readiness-card");
    expect(pwaReadinessHtml).toContain("data-source-pattern=\"Vite PWA\"");
    expect(pwaReadinessHtml).toContain("PWA Setups");
    expect(pwaReadinessHtml).toContain("Service Worker Signals");
    const pwaReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "pwa-readiness.md"), "utf8");
    expect(pwaReadinessMarkdown).toContain("# PWA Readiness");
    expect(pwaReadinessMarkdown).toContain("Source pattern: Vite PWA");
    expect(pwaReadinessMarkdown).toContain("## Manifest Signals");
    expect(pwaReadinessMarkdown).toContain("## Service Worker Signals");
    const browserCompatibilityReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "browser-compat-readiness-report.json"), "utf8");
    expect(browserCompatibilityReadinessText).toContain("Browserslist target browsers config queries coverage caniuse-lite update-browserslist-db mobile-to-desktop env stats");
    expect(browserCompatibilityReadinessText).toContain("\"compatibilitySetups\"");
    expect(browserCompatibilityReadinessText).toContain("\"configSignals\"");
    expect(browserCompatibilityReadinessText).toContain("\"querySignals\"");
    expect(browserCompatibilityReadinessText).toContain("\"coverageSignals\"");
    expect(browserCompatibilityReadinessText).toContain("\"featureSignals\"");
    expect(browserCompatibilityReadinessText).toContain("\"updateSignals\"");
    expect(browserCompatibilityReadinessText).toContain("\"packageSignals\"");
    expect(browserCompatibilityReadinessText).toContain("npx browserslist");
    const browserCompatibilityReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "browser-compat-readiness.html"), "utf8");
    expect(browserCompatibilityReadinessHtml).toContain("Browser Compatibility Readiness");
    expect(browserCompatibilityReadinessHtml).toContain("browser-compat-readiness-card");
    expect(browserCompatibilityReadinessHtml).toContain("data-source-pattern=\"Browserslist\"");
    expect(browserCompatibilityReadinessHtml).toContain("Compatibility Setups");
    expect(browserCompatibilityReadinessHtml).toContain("Query Signals");
    const browserCompatibilityReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "browser-compat-readiness.md"), "utf8");
    expect(browserCompatibilityReadinessMarkdown).toContain("# Browser Compatibility Readiness");
    expect(browserCompatibilityReadinessMarkdown).toContain("Source pattern: Browserslist");
    expect(browserCompatibilityReadinessMarkdown).toContain("## Config Signals");
    expect(browserCompatibilityReadinessMarkdown).toContain("## Query Signals");
    const browserExtensionReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "browser-extension-readiness-report.json"), "utf8");
    expect(browserExtensionReadinessText).toContain("WXT Plasmo CRXJS Manifest V3 manifest.json background service_worker content_scripts permissions host_permissions web_accessible_resources chrome.runtime browser.runtime web-ext zip publish");
    expect(browserExtensionReadinessText).toContain("\"extensionSetups\"");
    expect(browserExtensionReadinessText).toContain("\"manifestSignals\"");
    expect(browserExtensionReadinessText).toContain("\"permissionSignals\"");
    expect(browserExtensionReadinessText).toContain("\"publishSignals\"");
    const browserExtensionReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "browser-extension-readiness.html"), "utf8");
    expect(browserExtensionReadinessHtml).toContain("Browser Extension Readiness");
    expect(browserExtensionReadinessHtml).toContain("browser-extension-readiness-card");
    expect(browserExtensionReadinessHtml).toContain("data-source-pattern=\"WXT\"");
    expect(browserExtensionReadinessHtml).toContain("Extension Setups");
    expect(browserExtensionReadinessHtml).toContain("Permission Signals");
    const browserExtensionReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "browser-extension-readiness.md"), "utf8");
    expect(browserExtensionReadinessMarkdown).toContain("# Browser Extension Readiness");
    expect(browserExtensionReadinessMarkdown).toContain("Source pattern: WXT");
    expect(browserExtensionReadinessMarkdown).toContain("## Entrypoint Signals");
    expect(browserExtensionReadinessMarkdown).toContain("## Publish Signals");
    const envValidationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "env-validation-readiness-report.json"), "utf8");
    expect(envValidationReadinessText).toContain("t3-env createEnv server client shared runtimeEnv runtimeEnvStrict clientPrefix Standard Schema process.env import.meta.env emptyStringAsUndefined skipValidation @t3-oss/env-core @t3-oss/env-nextjs @t3-oss/env-nuxt Astro Vite extends isServer");
    expect(envValidationReadinessText).toContain("\"envSetups\"");
    expect(envValidationReadinessText).toContain("\"schemaSignals\"");
    expect(envValidationReadinessText).toContain("\"runtimeSignals\"");
    expect(envValidationReadinessText).toContain("\"boundarySignals\"");
    expect(envValidationReadinessText).toContain("\"frameworkSignals\"");
    expect(envValidationReadinessText).toContain("\"validationSignals\"");
    expect(envValidationReadinessText).toContain("\"documentationSignals\"");
    expect(envValidationReadinessText).toContain("\"packageSignals\"");
    expect(envValidationReadinessText).toContain("pnpm build");
    const envValidationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "env-validation-readiness.html"), "utf8");
    expect(envValidationReadinessHtml).toContain("Env Validation Readiness");
    expect(envValidationReadinessHtml).toContain("env-validation-readiness-card");
    expect(envValidationReadinessHtml).toContain("data-source-pattern=\"t3-env\"");
    expect(envValidationReadinessHtml).toContain("Env Setups");
    expect(envValidationReadinessHtml).toContain("Runtime Signals");
    expect(envValidationReadinessHtml).toContain("Framework Signals");
    const envValidationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "env-validation-readiness.md"), "utf8");
    expect(envValidationReadinessMarkdown).toContain("# Env Validation Readiness");
    expect(envValidationReadinessMarkdown).toContain("Source pattern: t3-env");
    expect(envValidationReadinessMarkdown).toContain("## Schema Signals");
    expect(envValidationReadinessMarkdown).toContain("## Runtime Signals");
    expect(envValidationReadinessMarkdown).toContain("## Framework Signals");
    const securityHeadersReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "security-headers-readiness-report.json"), "utf8");
    expect(securityHeadersReadinessText).toContain("Helmet Content-Security-Policy Strict-Transport-Security Cross-Origin-Opener-Policy Cross-Origin-Resource-Policy X-Frame-Options Referrer-Policy X-Content-Type-Options X-Powered-By");
    expect(securityHeadersReadinessText).toContain("\"headerSetups\"");
    expect(securityHeadersReadinessText).toContain("\"cspSignals\"");
    expect(securityHeadersReadinessText).toContain("\"transportSignals\"");
    expect(securityHeadersReadinessText).toContain("\"crossOriginSignals\"");
    expect(securityHeadersReadinessText).toContain("\"legacyHeaderSignals\"");
    expect(securityHeadersReadinessText).toContain("\"middlewareSignals\"");
    expect(securityHeadersReadinessText).toContain("\"packageSignals\"");
    expect(securityHeadersReadinessText).toContain("curl -I <preview-url>");
    const securityHeadersReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "security-headers-readiness.html"), "utf8");
    expect(securityHeadersReadinessHtml).toContain("Security Headers Readiness");
    expect(securityHeadersReadinessHtml).toContain("security-headers-readiness-card");
    expect(securityHeadersReadinessHtml).toContain("data-source-pattern=\"Helmet\"");
    expect(securityHeadersReadinessHtml).toContain("Header Setups");
    expect(securityHeadersReadinessHtml).toContain("CSP Signals");
    const securityHeadersReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "security-headers-readiness.md"), "utf8");
    expect(securityHeadersReadinessMarkdown).toContain("# Security Headers Readiness");
    expect(securityHeadersReadinessMarkdown).toContain("Source pattern: Helmet");
    expect(securityHeadersReadinessMarkdown).toContain("## CSP Signals");
    expect(securityHeadersReadinessMarkdown).toContain("## Transport Signals");
    const graphqlReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "graphql-readiness-report.json"), "utf8");
    expect(graphqlReadinessText).toContain("GraphQL.js GraphQLSchema GraphQLObjectType buildSchema parse validate visit TypeInfo visitWithTypeInfo separateOperations concatAST stripIgnoredCharacters extendSchema lexicographicSortSchema typeFromAST valueFromAST coerceInputValue execute subscribe introspection typed documents resolvers");
    expect(graphqlReadinessText).toContain("\"graphqlSetups\"");
    expect(graphqlReadinessText).toContain("\"schemaSignals\"");
    expect(graphqlReadinessText).toContain("\"operationSignals\"");
    expect(graphqlReadinessText).toContain("\"resolverSignals\"");
    expect(graphqlReadinessText).toContain("\"validationSignals\"");
    expect(graphqlReadinessText).toContain("\"documentSignals\"");
    expect(graphqlReadinessText).toContain("\"executionSignals\"");
    expect(graphqlReadinessText).toContain("\"clientSignals\"");
    expect(graphqlReadinessText).toContain("\"codegenSignals\"");
    expect(graphqlReadinessText).toContain("TypedDocumentNode");
    const graphqlReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "graphql-readiness.html"), "utf8");
    expect(graphqlReadinessHtml).toContain("GraphQL Readiness");
    expect(graphqlReadinessHtml).toContain("graphql-readiness-card");
    expect(graphqlReadinessHtml).toContain("data-source-pattern=\"GraphQL.js\"");
    expect(graphqlReadinessHtml).toContain("GraphQL Setups");
    expect(graphqlReadinessHtml).toContain("Validation Signals");
    expect(graphqlReadinessHtml).toContain("Document Signals");
    const graphqlReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "graphql-readiness.md"), "utf8");
    expect(graphqlReadinessMarkdown).toContain("# GraphQL Readiness");
    expect(graphqlReadinessMarkdown).toContain("Source pattern: GraphQL.js");
    expect(graphqlReadinessMarkdown).toContain("## Schema Signals");
    expect(graphqlReadinessMarkdown).toContain("## Document Signals");
    expect(graphqlReadinessMarkdown).toContain("## Execution Signals");
    const cliReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "cli-readiness-report.json"), "utf8");
    expect(cliReadinessText).toContain("Commander.js Command option requiredOption argument action parseAsync help usage exitOverride showHelpAfterError");
    expect(cliReadinessText).toContain("\"cliSetups\"");
    expect(cliReadinessText).toContain("\"commandSignals\"");
    expect(cliReadinessText).toContain("\"optionSignals\"");
    expect(cliReadinessText).toContain("\"parseSignals\"");
    expect(cliReadinessText).toContain("\"actionSignals\"");
    expect(cliReadinessText).toContain("\"helpSignals\"");
    expect(cliReadinessText).toContain("\"errorSignals\"");
    expect(cliReadinessText).toContain("\"packageSignals\"");
    expect(cliReadinessText).toContain("Commander.js");
    const cliReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "cli-readiness.html"), "utf8");
    expect(cliReadinessHtml).toContain("CLI Readiness");
    expect(cliReadinessHtml).toContain("cli-readiness-card");
    expect(cliReadinessHtml).toContain("data-source-pattern=\"Commander.js\"");
    expect(cliReadinessHtml).toContain("CLI Setups");
    expect(cliReadinessHtml).toContain("Help Signals");
    const cliReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "cli-readiness.md"), "utf8");
    expect(cliReadinessMarkdown).toContain("# CLI Readiness");
    expect(cliReadinessMarkdown).toContain("Source pattern: Commander.js");
    expect(cliReadinessMarkdown).toContain("## Command Signals");
    expect(cliReadinessMarkdown).toContain("## Error Signals");
    const llmReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "llm-readiness-report.json"), "utf8");
    expect(llmReadinessText).toContain("LangChain.js ModelProfile maxInputTokens maxOutputTokens imageInputs imageUrlInputs pdfInputs audioInputs videoInputs imageToolMessage pdfToolMessage reasoningOutput imageOutputs audioOutputs videoOutputs toolCalling toolChoice structuredOutput BaseChatModel BaseChatModelParams BaseChatModelCallOptions BaseLanguageModel BaseLanguageModelCallOptions BaseLanguageModelInput BaseLanguageModelParams LangSmithParams BindToolsInput ToolChoice disableStreaming outputVersion LC_OUTPUT_VERSION MessageOutputVersion streamV2 _streamChatModelEvents _streamResponseChunks _streamIterator _generateUncached _generateCached generatePrompt generate invocationParams _modelType _llmType _combineLLMOutput _separateRunnableConfigFromCallOptionsCompat handleChatModelStart handleChatModelStreamEvent handleLLMEnd handleLLMError callbackHandlerPrefersChatModelStreamEvents callbackHandlerPrefersStreaming _getSerializedCacheKeyParametersForCall cache.lookup cache.update BaseCache InMemoryCache defaultHashKeyEncoder HashKeyEncoder sha256 serializeGeneration deserializeStoredGeneration StoredGeneration Generation ChatGeneration mapStoredMessageToChatMessage toDict makeDefaultKeyEncoder keyEncoder lookup update prompt llmKey Promise<T | null> GLOBAL_MAP global() Map<string, T> ContentBlock missingPromptIndices RUN_KEY castStandardMessageContent ModelAbortError LLMResult Generation GenerationChunk GenerationChunkFields ChatGeneration ChatGenerationChunk ChatGenerationChunkFields ChatResult RUN_KEY generationInfo FakeBuiltModel fakeModel ResponseFactory QueueEntry FakeModelCall FakeModelState respond respondWithTools alwaysThrow structuredResponse bindTools callCount calls fake-model-builder no response queued llmOutput generations tokenUsage promptTokens completionTokens totalTokens ChatOpenAI ChatPromptTemplate RunnableSequence RunnableLambda RunnablePassthrough RunnableBranch Branch BranchLike condition branch default branch:default RouterRunnable RouterInput runnables key actualInput missingKey No runnable associated with key _getOptionsList returnExceptions batchSize RunnableBinding RunnableBindingArgs configFactories withConfig withListeners RootListenersTracer RunnableEach RunnableRetry RunnableRetryFailedAttemptHandler stopAfterAttempt onFailedAttempt maxAttemptNumber retry:attempt RunnableWithFallbacks handledExceptions exceptionKey RunnableAssign mapper RunnablePick keys map:key RunnableMapLike _coerceToRunnable _coerceToDict streamLog RunLogPatch streamed_output streamEvents StreamEvent on_chain_start on_chain_stream on_chain_end convertChunksToEvents ChatModelStreamEvent ContentBlockDelta ChatGenerationChunk AIMessageChunk _streamResponseChunks activeBlocks nextBlockIndex getAdditionalKwargs extractImageBlocksFromToolOutputs getAudioPayload MIME_TYPE_BY_AUDIO_FORMAT MIME_TYPE_BY_IMAGE_FORMAT AudioStreamState usage_metadata input_tokens output_tokens total_tokens options?.signal?.throwIfAborted ChatModelStream TextContentStream ToolCallsStream ReasoningContentStream UsageMetadataStream ReplayBuffer applyDelta getEventDelta getReasoningDelta isReasoningContent normalizeUsage parseToolArgs standardizeToolBlock content-block-start content-block-delta text-delta reasoning-delta data-delta block-delta content-block-finish message-start message-finish usage output_version v1 finish_reason usage_metadata response_metadata toolCalls text reasoning output ContentBlock.Tools.ToolCall pipe invoke batch stream withRetry withFallbacks tool createAgent MCP adapters ToolHooks DynamicStructuredTool VectorStore Retriever StructuredOutputParser createContentParser createFunctionCallingParser FunctionCallingParserConstructor assembleStructuredOutputPipeline includeRaw raw parsed parserAssign parserNone parsedWithFallback RunnablePassthrough.assign RunnableSequence.from BaseLanguageModelInput JsonOutputKeyToolsParser returnSingle StandardSchemaOutputParser SerializableSchema isSerializableSchema InteropZodType isInteropZodSchema BaseLLMOutputParser BaseOutputParser FormatInstructionsOptions parseResult parseResultWithPrompt parseWithPrompt getFormatInstructions OutputParserException OUTPUT_PARSING_FAILURE BaseTransformOutputParser BaseCumulativeTransformOutputParser parsePartialResult JsonOutputParser parseJsonMarkdown parsePartialJson StringOutputParser StrOutputParser BytesOutputParser TextEncoder ListOutputParser CommaSeparatedListOutputParser CustomListOutputParser NumberedListOutputParser MarkdownListOutputParser XMLOutputParser XML_FORMAT_INSTRUCTIONS parseXMLMarkdown StandardSchemaOutputParser fromSerializableSchema OutputFunctionsParser JsonOutputFunctionsParser JsonKeyOutputFunctionsParser JsonOutputToolsParser JsonOutputKeyToolsParser ParsedToolCall parseToolCall convertLangChainToolCallToOpenAI makeInvalidToolCall returnId returnSingle keyName argsOnly stream callbacks BaseCallbackHandler BaseCallbackHandlerInput ignoreLLM ignoreChain ignoreAgent ignoreRetriever ignoreCustomEvent _awaitHandler raiseError HandleLLMNewTokenCallbackFields handleLLMNewToken handleChatModelStreamEvent CallbackManagerOptions BaseCallbackConfig parseCallbackConfigArg BaseCallbackManager BaseRunManager CallbackManagerForLLMRun CallbackManagerForChainRun CallbackManagerForToolRun CallbackManagerForRetrieverRun CallbackManager.configure CallbackManager.fromHandlers addHandler removeHandler setHandlers inheritableHandlers inheritableTags inheritableMetadata getParentRunId getChild handleCustomEvent dispatchCustomEvent EventStreamCallbackHandler EventStreamCallbackHandlerInput StreamEvent StreamEventData includeNames includeTypes includeTags excludeNames excludeTypes excludeTags isStreamEventsHandler LogStreamCallbackHandler LogStreamCallbackHandlerInput RunLogPatch RunLog RunState LogEntry SchemaFormat isLogStreamHandler RunCollectorCallbackHandler tracedRuns RootListenersTracer onRunCreate onRunUpdate LangSmith createMiddleware wrapModelCall wrapToolCall humanInTheLoopMiddleware modelRetryMiddleware toolRetryMiddleware dynamic tools stateSchema contextSchema interruptOn piiMiddleware PIIDetectionError applyToToolResults redaction mask hash OpenAIModerationMiddleware openAIModerationMiddleware canJumpTo exitBehavior anthropicPromptCachingMiddleware cache_control ttl unsupportedModelBehavior dynamicSystemPromptMiddleware summarizationMiddleware contextEditingMiddleware ClearToolUsesEdit llmToolSelectorMiddleware modelCallLimitMiddleware toolCallLimitMiddleware threadLimit runLimit maxTools alwaysInclude REMOVE_ALL_MESSAGES trimMessages ToolCallLimitExceededError ModelCallLimitMiddlewareError initChatModel ConfigurableModel MODEL_PROVIDER_CONFIG SUPPORTED_PROVIDERS ChatModelProvider getChatModelByClassName _initChatModelHelper _inferModelProvider modelProvider configurableFields configPrefix configurable RunnableConfig DEFAULT_RECURSION_LIMIT _getTracingInheritableMetadataFromConfig CONFIGURABLE_TO_TRACING_METADATA_EXCLUDED_KEYS PRIMITIVES getCallbackManagerForConfig mergeConfigs ensureConfig patchConfig pickRunnableConfigKeys AsyncLocalStorageProviderSingleton recursionLimit runId runName maxConcurrency timeout AbortSignal.timeout signal timeoutMs metadata tags configurable store BaseStore InMemoryStore mget mset mdelete yieldKeys AsyncGenerator keyValuePairs langchain storage consumeIteratorInContext consumeAsyncIterableInContext runWithConfig getRunnableConfig LC_CHILD_KEY lc:child_config AsyncLocalStorageProvider getInstance avoidCreatingRootRunTree CallbackManager._configureSync parentRunId LangChainTracer getRunTreeWithTracingConfig RunTree <runnable_lambda> tracingEnabled false runTree.extra _CONTEXT_VARIABLES_KEY previousValue storage.getStore storage.run initializeGlobalInstance getGlobalAsyncLocalStorageInstance setGlobalAsyncLocalStorageInstance MockAsyncLocalStorage AgentRunStream GraphRunStream Graph nodeDataStr nodeDataJson toJsonSchema toJSON stableNodeIds addNode removeNode addEdge firstNode lastNode extend trimFirstNode trimLastNode reid drawMermaid drawMermaidPng drawMermaidImage _firstNode _lastNode _escapeNodeLabel MARKDOWN_SPECIAL_CHARS _generateMermaidGraphStyles curveStyle withStyles nodeColors wrapLabelNWords mermaid.ink toBase64Url backgroundColor imageType streamTransformers StreamTransformer StreamChannel createToolCallTransformer ToolCallProjection ToolCallStream isOwnEvent isHeadlessToolInterruptError isSerializedToolMessage normalizeToolOutput pendingCalls resolveOutput rejectOutput resolveStatus resolveError toolCallsLog.close toolCallsLog.fail ProtocolEvent streamMode text/event-stream convertToHttpEventStream IterableReadableStream TextEncoder ReadableStream<Uint8Array> controller.enqueue event: data event: end JSON.stringify(chunk) fromReadableStream EventStreamContentType text/event-stream EventSourceMessage getBytes getLines getMessages ControlChars NewLine CarriageReturn Space Colon fieldLength discardTrailingNewline TextDecoder onId onRetry parseInt Number.isNaN newMessage isEmpty convertEventStreamToIterableReadableDataStream onMetadataEvent event error metadata controller.close JSONPatchOperation applyPatch RunLogPatch RunLog fromRunLogPatch concat states[states.length - 1].newDocument LogEntry RunState id name type tags metadata start_time streamed_output streamed_output_str inputs final_output end_time logs SchemaFormat original streaming_events LogStreamCallbackHandlerInput autoClose includeNames includeTypes includeTags excludeNames excludeTypes excludeTags _schemaFormat isLogStreamHandler log_stream_tracer lc_prefer_streaming TransformStream writable.getWriter writer receiveStream IterableReadableStream.fromReadableStream Symbol.asyncIterator _includeRun keyMapByRunId counterMapByRunName tapOutputIterable onRunCreate onRunUpdate onLLMNewToken /logs/${key}/streamed_output/- /logs/${runName}/streamed_output_str/- /logs/${runName}/streamed_output/- /logs/${runName}/inputs /logs/${runName}/final_output /logs/${runName}/end_time /final_output _getStandardizedInputs _getStandardizedOutputs isChatGenerationChunk AIMessageChunk writer.close content-block-delta content-block-finish tool-started tool-finished tool-error responseFormat structuredResponse ToolStrategy ProviderStrategy TypedToolStrategy toolStrategy providerStrategy transformResponseFormat ResponseFormatUndefined hasSupportForJsonSchemaOutput StructuredOutputParsingError MultipleStructuredOutputsError ToolStrategyOptions handleError toolMessageContent ToolMessageFields ToolMessageChunk DirectToolOutput isDirectToolOutput lc_direct_tool_output tool_call_id status artifact metadata ResponseFormat content_and_artifact ToolOutputType ToolEventType InferToolEventFromFunc InferToolOutputFromFunc ContentAndArtifact ToolReturnType StructuredTool DynamicTool DynamicStructuredTool ToolWrapperParams ToolInputParsingException interopParseAsync validate verboseParsingErrors ToolInputSchemaBase ToolInputSchemaInputType ToolInputSchemaOutputType StructuredToolCallInput ToolCallInput StructuredToolInterface responseFormat defaultConfig verboseParsingErrors extras _formatToolOutput returnDirect toolCallId config.toolCall Tool response format handleToolStart handleToolEvent handleToolError handleToolEnd isSimpleStringZodSchema validatesOnlyStrings AsyncLocalStorageProviderSingleton runWithConfig patchConfig pickRunnableConfigKeys getAbortSignalError convertToOpenAIFunction convertToOpenAITool FunctionDefinition ToolDefinition RunnableToolLike isLangChainTool isStructuredTool isStructuredToolParams isRunnableToolLike strict fieldsCopy strict !== undefined parameters toJsonSchema ToJSONSchemaParams _jsonSchemaCache WeakMap canCache cached StandardJSONSchemaV1 isStandardJsonSchema isZodSchemaV4 isZodSchemaV3 interopZodTransformInputSchema interopZodObjectStrict zodToJsonSchema toJSONSchema ToolCall ToolCallChunk InvalidToolCall tool_calls invalid_tool_calls defaultToolCallParser collapseToolCallChunks contentBlocks missingContentBlockToolCalls missingToolCalls tool_call tool_call_chunk invalid_tool_call server_tool_call server_tool_call_chunk server_tool_call_result HeadlessTool HeadlessToolFields HeadlessToolImplementation createHeadlessTool HeadlessToolOverload headlessTool implement useStream ToolRunnableConfig createRetrieverTool BaseRetrieverInterface BaseRetriever BaseRetrieverInput _getRelevantDocuments handleRetrieverStart handleRetrieverEnd handleRetrieverError CallbackManagerForRetrieverRun parseCallbackConfigArg ensureConfig FakeRetriever BaseDocumentTransformer MappingDocumentTransformer transformDocuments _transformDocument BaseDocumentCompressor compressDocuments isBaseDocumentCompressor BaseDocumentLoader DocumentLoader load CallbackManagerForToolRun formatDocumentsAsString DynamicStructuredToolInput retriever.getChild RunnableWithMessageHistory RunnableWithMessageHistoryInputs GetSessionHistoryCallable _getInputMessages _getOutputMessages _enterHistory _exitHistory _mergeConfig configurable.messageHistory existingMessages.length inputMessages.slice HumanMessage AIMessage isBaseMessage generations[0][0].message BaseChatMessageHistory BaseListChatMessageHistory InMemoryChatMessageHistory getMessageHistory inputMessagesKey outputMessagesKey historyMessagesKey messageHistory sessionId loadHistory insertHistory addMessages _coerceToolCall isSerializedConstructor SerializedConstructor _constructMessageFromParams coerceMessageLikeToMessage _contentBlockToString getBufferString mapV1MessageToStoredMessage StoredMessage StoredMessageV1 mapStoredMessageToChatMessage mapStoredMessagesToChatMessages mapChatMessagesToStoredMessages toDict filterMessages FilterMessagesFields includeNames excludeNames includeTypes excludeTypes includeIds excludeIds _filterMessages _isMessageType mergeMessageRuns _mergeMessageRuns convertToChunk _chunkToMsg trimMessages TrimMessagesFields maxTokens tokenCounter strategy allowPartial endOn startOn includeSystem textSplitter _trimMessagesHelper _firstMaxTokens _lastMaxTokens _switchTypeToMessage _MSG_CHUNK_MAP BaseMessageChunk isBaseMessageChunk AIMessageChunk AIMessageChunkFields HumanMessageChunk SystemMessageChunk FunctionMessageChunk ChatMessageChunk mergeResponseMetadata mergeUsageMetadata UsageMetadata ModalitiesTokenDetails input_token_details output_token_details FewShotPromptTemplate FewShotChatMessagePromptTemplate BaseExampleSelector LengthBasedExampleSelector SemanticSimilarityExampleSelector BasePromptSelector ConditionalPromptSelector BaseGetPromptAsyncOptions getPrompt getPromptAsync defaultPrompt conditionals partialVariables isLLM isChatModel BaseLanguageModelInterface exampleSelector examplePrompt exampleSeparator partialVariables inputKeys exampleKeys maxLength getTextLength selectExamples TemplateFormat ParsedTemplateNode ParsedFStringNode parseFString parseMustache interpolateFString interpolateMustache DEFAULT_FORMATTER_MAPPING DEFAULT_PARSER_MAPPING renderTemplate parseTemplate checkValidTemplate INVALID_PROMPT_INPUT templateFormat validateTemplate mustache f-string image_url ImagePromptTemplateInput ImagePromptValue ImageContent ContentBlock additionalContentFields detail Must provide either an image URL url must be a string MessageContentComplex DataContentBlock BaseDataContentBlock URLContentBlock Base64ContentBlock PlainTextContentBlock IDContentBlock isDataContentBlock isURLContentBlock isBase64ContentBlock isPlainTextContentBlock isIDContentBlock convertToOpenAIImageBlock parseMimeType parseBase64DataUrl ProviderFormatTypes StandardContentBlockConverter convertToProviderContentBlock convertToStandardContentBlock convertToV1FromDataContentBlock convertToV1FromDataContent isOpenAIDataBlock convertToV1FromOpenAIDataBlock convertToV1FromChatCompletions convertToV1FromChatCompletionsChunk convertToV1FromChatCompletionsInput convertToV1FromResponses convertToV1FromResponsesChunk convertToV1FromAnthropicContentBlock convertToV1FromAnthropicInput convertToV1FromAnthropicMessage convertAnthropicAnnotation StandardContentBlockTranslator contentBlocksFromNonStringFirst mergeContent tool_call server_tool_call reasoning citation non_standard mime_type source_type fileId metadata convertToV1FromOpenRouterMessage ChatOpenRouterTranslator reasoning_content reasoning_details reasoning.summary reasoning.text reasoning.encrypted convertToV1FromGroqMessage ChatGroqTranslator <think> convertToV1FromOllamaMessage ChatOllamaTranslator convertToV1FromDeepSeekMessage ChatDeepSeekTranslator convertToV1FromXAIMessage ChatXAITranslator ChatGoogleGenAITranslator ChatGoogleTranslator thinking thoughtSignature thought inlineData functionCall functionResponse fileData executableCode codeExecutionResult convertToV1FromChatBedrockConverseInput convertToV1FromChatBedrockConverseMessage ChatBedrockConverseTranslator citations_content citationsContent reasoning_content guard_content cache_point documentChar documentPage documentChunk BaseMessagePromptTemplate BaseChatPromptTemplate BaseMessageStringPromptTemplate ChatMessagePromptTemplate HumanMessagePromptTemplate AIMessagePromptTemplate SystemMessagePromptTemplate ImagePromptTemplate _StringImageMessagePromptTemplate MessagesPlaceholderFields BaseMessagePromptTemplateLike _coerceMessagePromptTemplateLike isMessagesPlaceholder _parseImagePrompts promptMessages flattenedMessages flattenedPartialVariables PipelinePromptTemplate PipelinePromptParams PipelinePromptTemplateInput pipelinePrompts finalPrompt computeInputValues intermediateValues extractRequiredInputValues formatPipelinePrompts StructuredPrompt StructuredPromptInput fromMessagesAndSchema schema method jsonMode jsonSchema functionMode withStructuredOutput RunnableBinding isWithStructuredOutput isRunnableBinding lc_namespace lc_aliases schema_ DictPromptTemplate TypedPromptInputValues _getInputVariables _insertInputVariables templateFormat inputVariables renderTemplate parseTemplate runType prompt lc_serializable BasePromptTemplate BasePromptTemplateInput BaseStringPromptTemplate PromptValueReturnType formatPromptValue mergePartialAndUserVariables lc_attributes outputParser metadata tags StringPromptValue SerializedPromptTemplate SerializedFewShotTemplate SerializedBasePromptTemplate input_variables template_format serialize deserialize load LoadOptions secretsMap secretsFromEnv optionalImportsMap optionalImportEntrypoints importMap maxDepth DEFAULT_MAX_DEPTH reviver SerializedConstructor SerializedSecret SerializedNotImplemented getEnvironmentVariable isEscapedObject unescapeValue LC_ESCAPED_KEY escapeObject needsEscaping serializeValue serializeLcObject lc_serializable lc_secrets lc_aliases lc_attributes lc_serializable_keys toJSON toJSONNotImplemented replaceSecrets keyToJson keyFromJson mapKeys");
    expect(llmReadinessText).toContain("\"llmSetups\"");
    expect(llmReadinessText).toContain("\"modelSignals\"");
    expect(llmReadinessText).toContain("\"promptSignals\"");
    expect(llmReadinessText).toContain("\"runnableSignals\"");
    expect(llmReadinessText).toContain("\"toolSignals\"");
    expect(llmReadinessText).toContain("\"retrievalSignals\"");
    expect(llmReadinessText).toContain("\"structuredOutputSignals\"");
    expect(llmReadinessText).toContain("\"streamingSignals\"");
    expect(llmReadinessText).toContain("\"safetySignals\"");
    expect(llmReadinessText).toContain("\"packageSignals\"");
    expect(llmReadinessText).toContain("LangChain.js");
    const llmReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "llm-readiness.html"), "utf8");
    expect(llmReadinessHtml).toContain("LLM Readiness");
    expect(llmReadinessHtml).toContain("llm-readiness-card");
    expect(llmReadinessHtml).toContain("data-source-pattern=\"LangChain.js\"");
    expect(llmReadinessHtml).toContain("LLM Setups");
    expect(llmReadinessHtml).toContain("Runnable Signals");
    expect(llmReadinessHtml).toContain("Structured Output Signals");
    const llmReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "llm-readiness.md"), "utf8");
    expect(llmReadinessMarkdown).toContain("# LLM Readiness");
    expect(llmReadinessMarkdown).toContain("Source pattern: LangChain.js");
    expect(llmReadinessMarkdown).toContain("## Model Signals");
    expect(llmReadinessMarkdown).toContain("## Runnable Signals");
    expect(llmReadinessMarkdown).toContain("## Streaming Signals");
    const llmEvalReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "llm-eval-readiness-report.json"), "utf8");
    expect(llmEvalReadinessText).toContain("LLM eval readiness promptfoo promptfooconfig providers prompts tests assert llm-rubric redteam plugins strategies OpenAI evals evals registry samples_jsonl modelgraded_spec completion_fns oaieval OpenEvals create_llm_as_judge createLLMAsJudge correctness hallucination feedbackKey score reference_outputs datasets reports");
    expect(llmEvalReadinessText).toContain("\"evalSetups\"");
    expect(llmEvalReadinessText).toContain("\"configSignals\"");
    expect(llmEvalReadinessText).toContain("\"promptSignals\"");
    expect(llmEvalReadinessText).toContain("\"providerSignals\"");
    expect(llmEvalReadinessText).toContain("\"testCaseSignals\"");
    expect(llmEvalReadinessText).toContain("\"judgeSignals\"");
    expect(llmEvalReadinessText).toContain("\"datasetSignals\"");
    expect(llmEvalReadinessText).toContain("\"redteamSignals\"");
    expect(llmEvalReadinessText).toContain("\"workflowSignals\"");
    expect(llmEvalReadinessText).toContain("\"packageSignals\"");
    const llmEvalReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "llm-eval-readiness.html"), "utf8");
    expect(llmEvalReadinessHtml).toContain("LLM Eval Readiness");
    expect(llmEvalReadinessHtml).toContain("llm-eval-readiness-card");
    expect(llmEvalReadinessHtml).toContain("data-source-pattern=\"LLM Eval\"");
    expect(llmEvalReadinessHtml).toContain("Eval Setups");
    expect(llmEvalReadinessHtml).toContain("Judge Signals");
    const llmEvalReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "llm-eval-readiness.md"), "utf8");
    expect(llmEvalReadinessMarkdown).toContain("# LLM Eval Readiness");
    expect(llmEvalReadinessMarkdown).toContain("Source pattern: LLM eval readiness");
    expect(llmEvalReadinessMarkdown).toContain("## Config Signals");
    expect(llmEvalReadinessMarkdown).toContain("## Red-Team Signals");
    const llmObservabilityReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "llm-observability-readiness-report.json"), "utf8");
    expect(llmObservabilityReadinessText).toContain("LLM observability readiness Langfuse Phoenix Helicone LangChainTracer RunCollectorCallbackHandler LogStreamCallbackHandler EventStreamCallbackHandler RootListenersTracer BaseTracer isBaseTracer convertRunTreeToRun convertRunToRunTree _addRunToRunMap runMap runTreeMap usesRunTreeMap getRunById persistRun _endTrace _getExecutionOrder _createRunForLLMStart parent_run_id child_runs child_execution_order trace_id dotted_order _serialized_start_time convertToDottedOrderFormat consumeCallback awaitAllCallbacks getQueue createQueue PQueue autoStart concurrency awaitHandlers getGlobalAsyncLocalStorageInstance asyncLocalStorageInstance.run awaitPendingTraceBatches Promise.allSettled queue.onIdle RunTree traces spans observations generations sessions userId sessionId metadata release tags scores feedback annotations datasets experiments prompt versions playground OpenInference OpenTelemetry OTLP exporter token usage promptTokens completionTokens totalTokens cost latency model provider gateway baseURL Helicone headers rate limit retry fallback redaction telemetry opt-out ingestion queues ClickHouse S3 blob storage data retention OpenAPI SDK integrations annotation queues LLM-as-a-judge usage metering event replay safe URL SSRF IO size limit");
    expect(llmObservabilityReadinessText).toContain("\"observabilitySetups\"");
    expect(llmObservabilityReadinessText).toContain("\"traceSignals\"");
    expect(llmObservabilityReadinessText).toContain("\"instrumentationSignals\"");
    expect(llmObservabilityReadinessText).toContain("\"identitySignals\"");
    expect(llmObservabilityReadinessText).toContain("\"llmMetricSignals\"");
    expect(llmObservabilityReadinessText).toContain("\"feedbackSignals\"");
    expect(llmObservabilityReadinessText).toContain("\"datasetExperimentSignals\"");
    expect(llmObservabilityReadinessText).toContain("\"gatewaySignals\"");
    expect(llmObservabilityReadinessText).toContain("\"privacySignals\"");
    expect(llmObservabilityReadinessText).toContain("\"workflowSignals\"");
    expect(llmObservabilityReadinessText).toContain("\"packageSignals\"");
    const llmObservabilityReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "llm-observability-readiness.html"), "utf8");
    expect(llmObservabilityReadinessHtml).toContain("LLM Observability Readiness");
    expect(llmObservabilityReadinessHtml).toContain("llm-observability-readiness-card");
    expect(llmObservabilityReadinessHtml).toContain("data-source-pattern=\"LLM Observability\"");
    expect(llmObservabilityReadinessHtml).toContain("Observability Setups");
    expect(llmObservabilityReadinessHtml).toContain("Gateway Signals");
    const llmObservabilityReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "llm-observability-readiness.md"), "utf8");
    expect(llmObservabilityReadinessMarkdown).toContain("# LLM Observability Readiness");
    expect(llmObservabilityReadinessMarkdown).toContain("Source pattern: LLM observability readiness");
    expect(llmObservabilityReadinessMarkdown).toContain("## Trace Signals");
    expect(llmObservabilityReadinessMarkdown).toContain("## Gateway Signals");
    const vectorDbReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "vector-db-readiness-report.json"), "utf8");
    expect(vectorDbReadinessText).toContain("Vector DB readiness Qdrant Weaviate Chroma LangChain VectorStore VectorStoreRetriever MemoryVectorStore FakeVectorStore FakeVectorStoreArgs EmbeddingsInterface Embeddings EmbeddingsParams AsyncCaller AsyncCallerParams caller maxConcurrency maxRetries MemoryVector memoryVectors _queryVectors filterFunction filteredMemoryVectors maximalMarginalRelevance queryEmbedding embeddingList mmrIndexes selectedEmbeddingsIndexes fromExistingIndex id DocumentInterface SyntheticEmbeddings similarityCalledCount custom similarity MMR similaritySearchWithScore addVectors addDocuments asRetriever RecordManagerInterface IndexingResult _HashedDocument HashedDocumentInterface CleanupMode IndexOptions sourceIdKey cleanupBatchSize forceUpdate _batch _deduplicateInOrder _getSourceIdAssigner _isBaseDocumentLoader indexStartDt timeAtLeast groupIds docsToIndex docsToUpdate seenDocs listKeys deleteKeys numAdded numDeleted numUpdated numSkipped UUIDV5_NAMESPACE hash_ contentHash metadataHash calculateHashes toDocument collections classes schema vector config embeddings vectorizer distance dimensions HNSW payload metadata filters StructuredQuery FilterDirective Comparison Operation Operators Comparators Visitor VisitorResult VisitorOperationResult VisitorComparisonResult VisitorStructuredQueryResult BaseTranslator BasicTranslator FunctionalTranslator FunctionFilter ValueType getAllowedComparatorsForType getComparatorFunction getOperatorFunction undefinedTrue TranslatorOpts allowedOperators allowedComparators visitOperation visitComparison visitStructuredQuery formatFunction mergeFilters isFilterEmpty castValue forceDefaultFilter mergeType hybrid search BM25 sparse vectors upsert add query search nearest neighbors score limit snapshots backup restore sharding replication tenancy ttl clients endpoints API keys persistence");
    expect(vectorDbReadinessText).toContain("\"vectorSetups\"");
    expect(vectorDbReadinessText).toContain("\"collectionSignals\"");
    expect(vectorDbReadinessText).toContain("\"clientSignals\"");
    expect(vectorDbReadinessText).toContain("\"ingestionSignals\"");
    expect(vectorDbReadinessText).toContain("\"querySignals\"");
    expect(vectorDbReadinessText).toContain("\"embeddingSignals\"");
    expect(vectorDbReadinessText).toContain("\"indexSignals\"");
    expect(vectorDbReadinessText).toContain("\"opsSignals\"");
    expect(vectorDbReadinessText).toContain("\"packageSignals\"");
    const vectorDbReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "vector-db-readiness.html"), "utf8");
    expect(vectorDbReadinessHtml).toContain("Vector DB Readiness");
    expect(vectorDbReadinessHtml).toContain("vector-db-readiness-card");
    expect(vectorDbReadinessHtml).toContain("data-source-pattern=\"Vector DB\"");
    expect(vectorDbReadinessHtml).toContain("Vector Setups");
    expect(vectorDbReadinessHtml).toContain("Query Signals");
    const vectorDbReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "vector-db-readiness.md"), "utf8");
    expect(vectorDbReadinessMarkdown).toContain("# Vector DB Readiness");
    expect(vectorDbReadinessMarkdown).toContain("Source pattern: Vector DB readiness");
    expect(vectorDbReadinessMarkdown).toContain("## Collection Signals");
    expect(vectorDbReadinessMarkdown).toContain("## Ops Signals");
    const searchServiceReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "search-service-readiness-report.json"), "utf8");
    expect(searchServiceReadinessText).toContain("Search service readiness Meilisearch Typesense OpenSearch indexes collections schema mappings fields primary key settings documents add import bulk upsert batch search q query_by filter_by sort_by facet_by ranking rules typo tolerance synonyms stop words distinct geosearch hybrid semantic highlight crop pagination tasks health dumps snapshots aliases replicas cluster analytics API keys hosts nodes");
    expect(searchServiceReadinessText).toContain("\"searchSetups\"");
    expect(searchServiceReadinessText).toContain("\"indexSignals\"");
    expect(searchServiceReadinessText).toContain("\"ingestionSignals\"");
    expect(searchServiceReadinessText).toContain("\"querySignals\"");
    expect(searchServiceReadinessText).toContain("\"relevanceSignals\"");
    expect(searchServiceReadinessText).toContain("\"clientSignals\"");
    expect(searchServiceReadinessText).toContain("\"opsSignals\"");
    expect(searchServiceReadinessText).toContain("\"packageSignals\"");
    const searchServiceReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "search-service-readiness.html"), "utf8");
    expect(searchServiceReadinessHtml).toContain("Search Service Readiness");
    expect(searchServiceReadinessHtml).toContain("search-service-readiness-card");
    expect(searchServiceReadinessHtml).toContain("data-source-pattern=\"Search Service\"");
    expect(searchServiceReadinessHtml).toContain("Search Setups");
    expect(searchServiceReadinessHtml).toContain("Query Signals");
    const searchServiceReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "search-service-readiness.md"), "utf8");
    expect(searchServiceReadinessMarkdown).toContain("# Search Service Readiness");
    expect(searchServiceReadinessMarkdown).toContain("Source pattern: Search service readiness");
    expect(searchServiceReadinessMarkdown).toContain("## Index Signals");
    expect(searchServiceReadinessMarkdown).toContain("## Ops Signals");
    const objectStorageReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "object-storage-readiness-report.json"), "utf8");
    expect(objectStorageReadinessText).toContain("Object storage readiness S3 MinIO R2 Supabase Storage buckets regions endpoints path-style credentials PutObject GetObject ListObjects DeleteObject CopyObject multipart upload download metadata tags presigned URL signed URL policy ACL CORS RLS RBAC versioning lifecycle retention object lock replication checksum ETag SSE KMS encryption event notifications queues CDN cache health metrics backup restore migration");
    expect(objectStorageReadinessText).toContain("\"storageSetups\"");
    expect(objectStorageReadinessText).toContain("\"bucketSignals\"");
    expect(objectStorageReadinessText).toContain("\"clientSignals\"");
    expect(objectStorageReadinessText).toContain("\"objectSignals\"");
    expect(objectStorageReadinessText).toContain("\"accessSignals\"");
    expect(objectStorageReadinessText).toContain("\"reliabilitySignals\"");
    expect(objectStorageReadinessText).toContain("\"securitySignals\"");
    expect(objectStorageReadinessText).toContain("\"opsSignals\"");
    expect(objectStorageReadinessText).toContain("\"packageSignals\"");
    const objectStorageReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "object-storage-readiness.html"), "utf8");
    expect(objectStorageReadinessHtml).toContain("Object Storage Readiness");
    expect(objectStorageReadinessHtml).toContain("object-storage-readiness-card");
    expect(objectStorageReadinessHtml).toContain("data-source-pattern=\"Object Storage\"");
    expect(objectStorageReadinessHtml).toContain("Storage Setups");
    expect(objectStorageReadinessHtml).toContain("Object Signals");
    const objectStorageReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "object-storage-readiness.md"), "utf8");
    expect(objectStorageReadinessMarkdown).toContain("# Object Storage Readiness");
    expect(objectStorageReadinessMarkdown).toContain("Source pattern: Object storage readiness");
    expect(objectStorageReadinessMarkdown).toContain("## Bucket Signals");
    expect(objectStorageReadinessMarkdown).toContain("## Ops Signals");
    const realtimeCollaborationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "realtime-collaboration-readiness-report.json"), "utf8");
    expect(realtimeCollaborationReadinessText).toContain("Realtime collaboration readiness Yjs Y.Doc shared types WebsocketProvider awareness UndoManager encodeStateAsUpdate applyUpdate Automerge change merge sync save load conflicts heads patches Liveblocks LiveblocksProvider RoomProvider useOthers useMyPresence useMutation storage presence comments threads authEndpoint room permissions");
    expect(realtimeCollaborationReadinessText).toContain("\"collaborationSetups\"");
    expect(realtimeCollaborationReadinessText).toContain("\"crdtSignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"providerSignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"presenceSignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"syncSignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"persistenceSignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"historySignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"accessSignals\"");
    expect(realtimeCollaborationReadinessText).toContain("\"packageSignals\"");
    const realtimeCollaborationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "realtime-collaboration-readiness.html"), "utf8");
    expect(realtimeCollaborationReadinessHtml).toContain("Realtime Collaboration Readiness");
    expect(realtimeCollaborationReadinessHtml).toContain("realtime-collaboration-readiness-card");
    expect(realtimeCollaborationReadinessHtml).toContain("data-source-pattern=\"Realtime Collaboration\"");
    expect(realtimeCollaborationReadinessHtml).toContain("Collaboration Setups");
    expect(realtimeCollaborationReadinessHtml).toContain("Provider Signals");
    const realtimeCollaborationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "realtime-collaboration-readiness.md"), "utf8");
    expect(realtimeCollaborationReadinessMarkdown).toContain("# Realtime Collaboration Readiness");
    expect(realtimeCollaborationReadinessMarkdown).toContain("Source pattern: Realtime collaboration readiness");
    expect(realtimeCollaborationReadinessMarkdown).toContain("## CRDT Signals");
    expect(realtimeCollaborationReadinessMarkdown).toContain("## Sync Signals");
    const workflowOrchestrationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "workflow-orchestration-readiness-report.json"), "utf8");
    expect(workflowOrchestrationReadinessText).toContain("Workflow orchestration readiness Temporal workflows activities Worker taskQueue schedules signals queries updates setHandler condition CancellationScope workflowInfo patched deprecatePatch ApplicationFailure ActivityFailure NativeConnection TestWorkflowEnvironment proxySinks interceptors @temporalio/activity @temporalio/common @temporalio/testing @temporalio/openai-agents continueAsNew Inngest createFunction events cron step.run step.sleep waitForEvent invoke cancelOn concurrency throttle debounce rate limit Trigger.dev task schemaTask schedules cron wait queue retry maxDuration idempotency metadata logger deploy runs LangGraph StateGraph START END addNode addEdge addConditionalEdges compile checkpointer MemorySaver Command resume graph.getState streamEvents");
    expect(workflowOrchestrationReadinessText).toContain("\"workflowSetups\"");
    expect(workflowOrchestrationReadinessText).toContain("\"triggerSignals\"");
    expect(workflowOrchestrationReadinessText).toContain("\"executionSignals\"");
    expect(workflowOrchestrationReadinessText).toContain("\"durabilitySignals\"");
    expect(workflowOrchestrationReadinessText).toContain("\"flowSignals\"");
    expect(workflowOrchestrationReadinessText).toContain("\"runtimeSignals\"");
    expect(workflowOrchestrationReadinessText).toContain("\"observabilitySignals\"");
    expect(workflowOrchestrationReadinessText).toContain("\"packageSignals\"");
    const workflowOrchestrationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "workflow-orchestration-readiness.html"), "utf8");
    expect(workflowOrchestrationReadinessHtml).toContain("Workflow Orchestration Readiness");
    expect(workflowOrchestrationReadinessHtml).toContain("workflow-orchestration-readiness-card");
    expect(workflowOrchestrationReadinessHtml).toContain("data-source-pattern=\"Workflow Orchestration\"");
    expect(workflowOrchestrationReadinessHtml).toContain("Workflow Setups");
    expect(workflowOrchestrationReadinessHtml).toContain("Durability Signals");
    const workflowOrchestrationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "workflow-orchestration-readiness.md"), "utf8");
    expect(workflowOrchestrationReadinessMarkdown).toContain("# Workflow Orchestration Readiness");
    expect(workflowOrchestrationReadinessMarkdown).toContain("Source pattern: Workflow orchestration readiness");
    expect(workflowOrchestrationReadinessMarkdown).toContain("## Execution Signals");
    expect(workflowOrchestrationReadinessMarkdown).toContain("## Flow Signals");
    const openApiClientReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "openapi-client-readiness-report.json"), "utf8");
    expect(openApiClientReadinessText).toContain("OpenAPI client readiness openapi-typescript openapi-fetch Hey API @hey-api/openapi-ts createClient plugins @hey-api/client-fetch @hey-api/client-axios @hey-api/client-ky @hey-api/client-next @hey-api/client-nuxt @hey-api/client-ofetch @hey-api/sdk @hey-api/schemas @hey-api/transformers Orval OpenAPI Generator input spec output schemas client hooks mocks MSW zod valibot arktype mutator interceptors auth axios fetch ky ofetch next nuxt react-query preact-query SWR Vue Query Svelte Query Solid Query Angular Query Pinia Colada Hono Fastify NestJS oRPC Effect native fetch MCP Vite plugin Nuxt module watch update-samples test:samples test:snapshots test:cli generatorName config validate lint generated diff typecheck templates");
    expect(openApiClientReadinessText).toContain("\"clientSetups\"");
    expect(openApiClientReadinessText).toContain("\"specSignals\"");
    expect(openApiClientReadinessText).toContain("\"generatorSignals\"");
    expect(openApiClientReadinessText).toContain("\"outputSignals\"");
    expect(openApiClientReadinessText).toContain("\"runtimeSignals\"");
    expect(openApiClientReadinessText).toContain("\"clientTargetSignals\"");
    expect(openApiClientReadinessText).toContain("\"generationWorkflowSignals\"");
    expect(openApiClientReadinessText).toContain("\"qualitySignals\"");
    expect(openApiClientReadinessText).toContain("\"packageSignals\"");
    expect(openApiClientReadinessText).toContain("실제 drift 여부는 원본 저장소에서 validate/generate/typecheck를 별도 실행해 확인하세요.");
    expect(openApiClientReadinessText).not.toContain("실제 drift 판정은 원본 저장소에서 validate/generate/typecheck를 별도 실행하세요.");
    const openApiClientReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "openapi-client-readiness.html"), "utf8");
    expect(openApiClientReadinessHtml).toContain("OpenAPI Client Readiness");
    expect(openApiClientReadinessHtml).toContain("openapi-client-readiness-card");
    expect(openApiClientReadinessHtml).toContain("data-source-pattern=\"OpenAPI Client\"");
    expect(openApiClientReadinessHtml).toContain("Client Target Signals");
    expect(openApiClientReadinessHtml).toContain("Generation Workflow Signals");
    expect(openApiClientReadinessHtml).toContain("Quality Signals");
    expect(openApiClientReadinessHtml).toContain("실제 drift 여부는 원본 저장소에서 validate/generate/typecheck를 별도 실행해 확인하세요.");
    expect(openApiClientReadinessHtml).not.toContain("실제 drift 판정은 원본 저장소에서 validate/generate/typecheck를 별도 실행하세요.");
    const openApiClientReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "openapi-client-readiness.md"), "utf8");
    expect(openApiClientReadinessMarkdown).toContain("# OpenAPI Client Readiness");
    expect(openApiClientReadinessMarkdown).toContain("Source pattern: OpenAPI client readiness");
    expect(openApiClientReadinessMarkdown).toContain("## Generator Signals");
    expect(openApiClientReadinessMarkdown).toContain("## Client Target Signals");
    expect(openApiClientReadinessMarkdown).toContain("## Generation Workflow Signals");
    expect(openApiClientReadinessMarkdown).toContain("## Quality Signals");
    expect(openApiClientReadinessMarkdown).toContain("실제 drift 여부는 원본 저장소에서 validate/generate/typecheck를 별도 실행해 확인하세요.");
    expect(openApiClientReadinessMarkdown).not.toContain("실제 drift 판정은 원본 저장소에서 validate/generate/typecheck를 별도 실행하세요.");
    const webhookReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "webhook-readiness-report.json"), "utf8");
    expect(webhookReadinessText).toContain("Webhook readiness Svix Standard Webhooks Hookdeck signature webhook-id webhook-timestamp webhook-signature HMAC ed25519 verification msg_id.timestamp.payload signed content versioned signature multiple signatures base64 secret required headers payload schema thin full payload payload size replay idempotency event types endpoints retry attempts delivery logs replay fan-out filtering source destination source auth destination auth transformations rate limit healthcheck localhost CLI Event Gateway MCP tools failures metrics SSRF proxy retry-after legacy migration telemetry opt-out bookmarks profiles");
    expect(webhookReadinessText).toContain("\"webhookSetups\"");
    expect(webhookReadinessText).toContain("\"endpointSignals\"");
    expect(webhookReadinessText).toContain("\"signatureSignals\"");
    expect(webhookReadinessText).toContain("\"verificationSignals\"");
    expect(webhookReadinessText).toContain("\"reliabilitySignals\"");
    expect(webhookReadinessText).toContain("\"operationsSignals\"");
    expect(webhookReadinessText).toContain("\"packageSignals\"");
    const webhookReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "webhook-readiness.html"), "utf8");
    expect(webhookReadinessHtml).toContain("Webhook Readiness");
    expect(webhookReadinessHtml).toContain("webhook-readiness-card");
    expect(webhookReadinessHtml).toContain("data-source-pattern=\"Webhook\"");
    expect(webhookReadinessHtml).toContain("Signature Signals");
    expect(webhookReadinessHtml).toContain("Verification Signals");
    const webhookReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "webhook-readiness.md"), "utf8");
    expect(webhookReadinessMarkdown).toContain("# Webhook Readiness");
    expect(webhookReadinessMarkdown).toContain("Source pattern: Webhook readiness");
    expect(webhookReadinessMarkdown).toContain("## Signature Signals");
    expect(webhookReadinessMarkdown).toContain("## Verification Signals");
    expect(webhookReadinessMarkdown).toContain("## Reliability Signals");
    const notificationReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "notification-readiness-report.json"), "utf8");
    expect(notificationReadinessText).toContain("Notification readiness Novu workflows trigger subscriberId subscribers topics subscriptions preferences Inbox email SMS push chat Slack Teams Telegram WhatsApp digest delay conditions payload tenant templates variables API key delivery logs retries dashboard analytics");
    expect(notificationReadinessText).toContain("\"notificationSetups\"");
    expect(notificationReadinessText).toContain("\"workflowSignals\"");
    expect(notificationReadinessText).toContain("\"audienceSignals\"");
    expect(notificationReadinessText).toContain("\"channelSignals\"");
    expect(notificationReadinessText).toContain("\"templateSignals\"");
    expect(notificationReadinessText).toContain("\"operationsSignals\"");
    expect(notificationReadinessText).toContain("\"packageSignals\"");
    const notificationReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "notification-readiness.html"), "utf8");
    expect(notificationReadinessHtml).toContain("Notification Readiness");
    expect(notificationReadinessHtml).toContain("notification-readiness-card");
    expect(notificationReadinessHtml).toContain("data-source-pattern=\"Notifications\"");
    expect(notificationReadinessHtml).toContain("Audience Signals");
    const notificationReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "notification-readiness.md"), "utf8");
    expect(notificationReadinessMarkdown).toContain("# Notification Readiness");
    expect(notificationReadinessMarkdown).toContain("Source pattern: Notification readiness");
    expect(notificationReadinessMarkdown).toContain("## Audience Signals");
    expect(notificationReadinessMarkdown).toContain("## Channel Signals");
    const consentReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "consent-readiness-report.json"), "utf8");
    expect(consentReadinessText).toContain("Consent readiness CookieConsent Klaro IAB TCF CMP banner modal categories services purposes vendors accept all accept selected reject all withdraw privacy policy data-src text/plain data-type data-name autoclear cookies localStorage revision translations __tcfapi TCString cmpId GVL purposeConsents vendorConsents legitimateInterests");
    expect(consentReadinessText).toContain("\"consentSetups\"");
    expect(consentReadinessText).toContain("\"bannerSignals\"");
    expect(consentReadinessText).toContain("\"categorySignals\"");
    expect(consentReadinessText).toContain("\"scriptSignals\"");
    expect(consentReadinessText).toContain("\"privacySignals\"");
    expect(consentReadinessText).toContain("\"tcfSignals\"");
    expect(consentReadinessText).toContain("\"packageSignals\"");
    const consentReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "consent-readiness.html"), "utf8");
    expect(consentReadinessHtml).toContain("Consent Readiness");
    expect(consentReadinessHtml).toContain("consent-readiness-card");
    expect(consentReadinessHtml).toContain("data-source-pattern=\"Consent\"");
    expect(consentReadinessHtml).toContain("TCF Signals");
    const consentReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "consent-readiness.md"), "utf8");
    expect(consentReadinessMarkdown).toContain("# Consent Readiness");
    expect(consentReadinessMarkdown).toContain("Source pattern: Consent readiness");
    expect(consentReadinessMarkdown).toContain("## Script Signals");
    expect(consentReadinessMarkdown).toContain("## TCF Signals");
    const serverFrameworkReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "server-framework-readiness-report.json"), "utf8");
    expect(serverFrameworkReadinessText).toContain("Fastify Express Koa NestJS Hono Hapi Elysia AdonisJS Sails Meteor Rails Django Laravel");
    expect(serverFrameworkReadinessText).toContain("sails config/routes sails.config.routes sails.router.bind sails.lift sails.load actions2 inputs exits fn policies blueprints hooks helpers Waterline res.view res.json sails.request");
    expect(serverFrameworkReadinessText).toContain("\"serverSetups\"");
    expect(serverFrameworkReadinessText).toContain("\"routeSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"schemaSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"pluginSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"lifecycleSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"runtimeSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"errorSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"testSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"fastifySignals\"");
    expect(serverFrameworkReadinessText).toContain("\"expressSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"koaSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"nestjsSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"honoSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"hapiSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"elysiaSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"adonisSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"sailsSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"meteorSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"railsSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"djangoSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"laravelSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"springSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"aspnetCoreSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"flaskSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"symfonySignals\"");
    expect(serverFrameworkReadinessText).toContain("\"ginSignals\"");
    expect(serverFrameworkReadinessText).toContain("\"echoSignals\"");
    expect(serverFrameworkReadinessText).toContain("Meteor.startup Meteor.methods Meteor.publish Meteor.subscribe DDP.connect Mongo.Collection WebApp.connectHandlers check Match Tracker.autorun Template.events Template.helpers Blaze.render Tinytest meteor test");
    expect(serverFrameworkReadinessText).toContain("Rails.application.routes.draw resources namespace scope root ActionController::Base ApplicationController before_action params.require permit render json redirect_to ActiveRecord::Base ApplicationRecord has_many belongs_to validates ActiveJob::Base ActionMailer::Base ActionCable ActiveSupport::TestCase ActionDispatch::IntegrationTest Django urlpatterns path re_path include reverse resolve INSTALLED_APPS MIDDLEWARE ROOT_URLCONF settings.configure DJANGO_SETTINGS_MODULE models.Model ForeignKey ManyToManyField QuerySet Manager forms.Form forms.ModelForm MiddlewareMixin process_request process_response admin.site.register ModelAdmin migrations.Migration BaseCommand django-admin manage.py TestCase Client RequestFactory");
    expect(serverFrameworkReadinessText).toContain("Laravel Route:: routes/web.php routes/api.php Controller Middleware FormRequest Validator::make Eloquent Model fillable guarded casts hasMany belongsTo Schema::create migration factory seeder Blade view artisan command schedule queue job Mailable Notification Broadcast Event Listener PHPUnit Pest");
    expect(serverFrameworkReadinessText).toContain("SpringBootApplication SpringApplication.run RestController RequestMapping GetMapping PostMapping PathVariable RequestParam RequestBody ResponseEntity Configuration AutoConfiguration ConfigurationProperties Bean ConditionalOn Service Repository Component Entity JpaRepository Transactional Actuator HealthIndicator MeterRegistry WebMvc WebFlux RouterFunction MockMvc WebTestClient TestRestTemplate Testcontainers");
    expect(serverFrameworkReadinessText).toContain("WebApplication.CreateBuilder WebApplication.CreateSlimBuilder builder.Services MapGet MapPost MapPut MapDelete MapPatch MapGroup MapControllers AddControllers ControllerBase ApiController Route HttpGet HttpPost FromBody FromRoute FromQuery IActionResult ActionResult IResult Results TypedResults ProblemDetails AddProblemDetails AddRouting UseRouting UseAuthentication UseAuthorization AddAuthentication AddAuthorization AddOpenApi MapOpenApi Swagger UI SignalR MapHub AddHealthChecks MapHealthChecks TestServer WebApplicationFactory UseTestServer CreateClient xUnit Flask Blueprint route add_url_rule MethodView request jsonify render_template redirect abort url_for session g current_app app_context request_context before_request after_request teardown_request errorhandler config from_prefixed_env test_client test_request_context test_cli_runner pytest Symfony FrameworkBundle Route RouteCollection AbstractController RequestStack JsonResponse RedirectResponse HttpKernel KernelInterface MicroKernelTrait ContainerBuilder services.yaml autowire autoconfigure CompilerPassInterface EventSubscriberInterface AsCommand CommandTester KernelBrowser WebTestCase KernelTestCase gin.Default gin.New gin.Engine gin.RouterGroup GET POST PUT PATCH DELETE Any Group Use gin.Context Param Query DefaultQuery PostForm DefaultPostForm GetHeader GetRawData ShouldBind BindJSON ShouldBindJSON ShouldBindQuery ShouldBindUri binding.Validator JSON String HTML Redirect File Abort AbortWithStatus NoRoute NoMethod Logger Recovery SetTrustedProxies Run RunTLS RunUnix httptest CreateTestContext SetMode TestMode");
    expect(serverFrameworkReadinessText).toContain("Fastify");
    expect(serverFrameworkReadinessText).toContain("NestJS");
    expect(serverFrameworkReadinessText).toContain("Hono");
    expect(serverFrameworkReadinessText).toContain("Hapi");
    expect(serverFrameworkReadinessText).toContain("Elysia");
    expect(serverFrameworkReadinessText).toContain("AdonisJS");
    expect(serverFrameworkReadinessText).toContain("Sails");
    expect(serverFrameworkReadinessText).toContain("Meteor");
    expect(serverFrameworkReadinessText).toContain("Rails");
    expect(serverFrameworkReadinessText).toContain("Django");
    expect(serverFrameworkReadinessText).toContain("Laravel");
    expect(serverFrameworkReadinessText).toContain("Spring");
    expect(serverFrameworkReadinessText).toContain("ASP.NET Core");
    expect(serverFrameworkReadinessText).toContain("Flask");
    expect(serverFrameworkReadinessText).toContain("Symfony");
    expect(serverFrameworkReadinessText).toContain("Gin");
    expect(serverFrameworkReadinessText).toContain("Echo");
    const serverFrameworkReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "server-framework-readiness.html"), "utf8");
    expect(serverFrameworkReadinessHtml).toContain("Server Framework Readiness");
    expect(serverFrameworkReadinessHtml).toContain("server-framework-readiness-card");
    expect(serverFrameworkReadinessHtml).toContain("data-source-pattern=\"Fastify Express Koa NestJS Hono Hapi Elysia AdonisJS Sails Meteor Rails Django Laravel Spring Boot ASP.NET Core Flask Symfony Gin Echo Fiber Chi Gorilla Mux\"");
    expect(serverFrameworkReadinessHtml).toContain("Server Setups");
    expect(serverFrameworkReadinessHtml).toContain("Lifecycle Signals");
    expect(serverFrameworkReadinessHtml).toContain("Fastify Signals");
    expect(serverFrameworkReadinessHtml).toContain("Express Signals");
    expect(serverFrameworkReadinessHtml).toContain("Koa Signals");
    expect(serverFrameworkReadinessHtml).toContain("NestJS Signals");
    expect(serverFrameworkReadinessHtml).toContain("Hono Signals");
    expect(serverFrameworkReadinessHtml).toContain("Hapi Signals");
    expect(serverFrameworkReadinessHtml).toContain("Elysia Signals");
    expect(serverFrameworkReadinessHtml).toContain("AdonisJS Signals");
    expect(serverFrameworkReadinessHtml).toContain("Sails Signals");
    expect(serverFrameworkReadinessHtml).toContain("Meteor Signals");
    expect(serverFrameworkReadinessHtml).toContain("Rails Signals");
    expect(serverFrameworkReadinessHtml).toContain("Django Signals");
    expect(serverFrameworkReadinessHtml).toContain("Laravel Signals");
    expect(serverFrameworkReadinessHtml).toContain("Spring Signals");
    expect(serverFrameworkReadinessHtml).toContain("ASP.NET Core Signals");
    expect(serverFrameworkReadinessHtml).toContain("Flask Signals");
    expect(serverFrameworkReadinessHtml).toContain("Symfony Signals");
    expect(serverFrameworkReadinessHtml).toContain("Gin Signals");
    expect(serverFrameworkReadinessHtml).toContain("Echo Signals");
    const serverFrameworkReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "server-framework-readiness.md"), "utf8");
    expect(serverFrameworkReadinessMarkdown).toContain("# Server Framework Readiness");
    expect(serverFrameworkReadinessMarkdown).toContain("Source pattern: Fastify Express Koa NestJS Hono Hapi Elysia AdonisJS Sails Meteor Rails Django Laravel Spring Boot ASP.NET Core Flask Symfony Gin");
    expect(serverFrameworkReadinessMarkdown).toContain("## Route Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Runtime Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Fastify Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Express Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Koa Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## NestJS Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Hono Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Hapi Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Elysia Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## AdonisJS Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Sails Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Meteor Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Rails Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Django Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Laravel Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Spring Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## ASP.NET Core Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Flask Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Symfony Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Gin Signals");
    expect(serverFrameworkReadinessMarkdown).toContain("## Echo Signals");
    const rpcReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "rpc-readiness-report.json"), "utf8");
    expect(rpcReadinessText).toContain("tRPC initTRPC router procedure query mutation subscription input output middleware context createTRPCClient links adapters TRPCError createCaller");
    expect(rpcReadinessText).toContain("\"rpcSetups\"");
    expect(rpcReadinessText).toContain("\"routerSignals\"");
    expect(rpcReadinessText).toContain("\"procedureSignals\"");
    expect(rpcReadinessText).toContain("\"validationSignals\"");
    expect(rpcReadinessText).toContain("\"contextSignals\"");
    expect(rpcReadinessText).toContain("\"clientSignals\"");
    expect(rpcReadinessText).toContain("\"adapterSignals\"");
    expect(rpcReadinessText).toContain("tRPC");
    const rpcReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "rpc-readiness.html"), "utf8");
    expect(rpcReadinessHtml).toContain("RPC Readiness");
    expect(rpcReadinessHtml).toContain("rpc-readiness-card");
    expect(rpcReadinessHtml).toContain("data-source-pattern=\"tRPC\"");
    expect(rpcReadinessHtml).toContain("RPC Setups");
    expect(rpcReadinessHtml).toContain("Client Signals");
    const rpcReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "rpc-readiness.md"), "utf8");
    expect(rpcReadinessMarkdown).toContain("# RPC Readiness");
    expect(rpcReadinessMarkdown).toContain("Source pattern: tRPC");
    expect(rpcReadinessMarkdown).toContain("## Procedure Signals");
    expect(rpcReadinessMarkdown).toContain("## Adapter Signals");
    const workspaceGraphReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "workspace-graph-readiness-report.json"), "utf8");
    expect(workspaceGraphReadinessText).toContain("Nx project graph nx.json project.json targets targetDefaults namedInputs plugins createNodes affected tags implicitDependencies enforce-module-boundaries depConstraints");
    expect(workspaceGraphReadinessText).toContain("\"workspaceFiles\"");
    expect(workspaceGraphReadinessText).toContain("\"projectSignals\"");
    expect(workspaceGraphReadinessText).toContain("\"graphSignals\"");
    expect(workspaceGraphReadinessText).toContain("\"boundarySignals\"");
    expect(workspaceGraphReadinessText).toContain("\"affectedSignals\"");
    expect(workspaceGraphReadinessText).toContain("\"targetSignals\"");
    expect(workspaceGraphReadinessText).toContain("\"pluginSignals\"");
    expect(workspaceGraphReadinessText).toContain("Nx");
    const workspaceGraphReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "workspace-graph-readiness.html"), "utf8");
    expect(workspaceGraphReadinessHtml).toContain("Workspace Graph Readiness");
    expect(workspaceGraphReadinessHtml).toContain("workspace-graph-readiness-card");
    expect(workspaceGraphReadinessHtml).toContain("data-source-pattern=\"Nx\"");
    expect(workspaceGraphReadinessHtml).toContain("Workspace Files");
    expect(workspaceGraphReadinessHtml).toContain("Boundary Signals");
    const workspaceGraphReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "workspace-graph-readiness.md"), "utf8");
    expect(workspaceGraphReadinessMarkdown).toContain("# Workspace Graph Readiness");
    expect(workspaceGraphReadinessMarkdown).toContain("Source pattern: Nx");
    expect(workspaceGraphReadinessMarkdown).toContain("## Graph Signals");
    expect(workspaceGraphReadinessMarkdown).toContain("## Affected Signals");
    const scaffoldingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "scaffolding-readiness-report.json"), "utf8");
    expect(scaffoldingReadinessText).toContain("Plop setGenerator prompts actions add addMany modify append templateFile helpers partials load skipIfExists force abortOnFail");
    expect(scaffoldingReadinessText).toContain("\"generatorFiles\"");
    expect(scaffoldingReadinessText).toContain("\"promptSignals\"");
    expect(scaffoldingReadinessText).toContain("\"actionSignals\"");
    expect(scaffoldingReadinessText).toContain("\"templateSignals\"");
    expect(scaffoldingReadinessText).toContain("\"safetySignals\"");
    expect(scaffoldingReadinessText).toContain("Plop");
    const scaffoldingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "scaffolding-readiness.html"), "utf8");
    expect(scaffoldingReadinessHtml).toContain("Scaffolding Readiness");
    expect(scaffoldingReadinessHtml).toContain("scaffolding-readiness-card");
    expect(scaffoldingReadinessHtml).toContain("data-source-pattern=\"Plop\"");
    expect(scaffoldingReadinessHtml).toContain("Generator Files");
    expect(scaffoldingReadinessHtml).toContain("Template Signals");
    const scaffoldingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "scaffolding-readiness.md"), "utf8");
    expect(scaffoldingReadinessMarkdown).toContain("# Scaffolding Readiness");
    expect(scaffoldingReadinessMarkdown).toContain("Source pattern: Plop");
    expect(scaffoldingReadinessMarkdown).toContain("## Action Signals");
    expect(scaffoldingReadinessMarkdown).toContain("## Safety Signals");
    const schedulerReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "scheduler-readiness-report.json"), "utf8");
    expect(schedulerReadinessText).toContain("node-cron schedule createTask cron expression timezone noOverlap maxExecutions start stop destroy execute validate ScheduledTask");
    expect(schedulerReadinessText).toContain("\"schedulerSetups\"");
    expect(schedulerReadinessText).toContain("\"scheduleSignals\"");
    expect(schedulerReadinessText).toContain("\"taskSignals\"");
    expect(schedulerReadinessText).toContain("\"lifecycleSignals\"");
    expect(schedulerReadinessText).toContain("\"reliabilitySignals\"");
    expect(schedulerReadinessText).toContain("node-cron");
    const schedulerReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "scheduler-readiness.html"), "utf8");
    expect(schedulerReadinessHtml).toContain("Scheduler Readiness");
    expect(schedulerReadinessHtml).toContain("scheduler-readiness-card");
    expect(schedulerReadinessHtml).toContain("data-source-pattern=\"node-cron\"");
    expect(schedulerReadinessHtml).toContain("Scheduler Setups");
    expect(schedulerReadinessHtml).toContain("Reliability Signals");
    const schedulerReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "scheduler-readiness.md"), "utf8");
    expect(schedulerReadinessMarkdown).toContain("# Scheduler Readiness");
    expect(schedulerReadinessMarkdown).toContain("Source pattern: node-cron");
    expect(schedulerReadinessMarkdown).toContain("## Schedule Signals");
    expect(schedulerReadinessMarkdown).toContain("## Reliability Signals");
    const buildToolReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "build-tool-readiness-report.json"), "utf8");
    expect(buildToolReadinessText).toContain("Vite defineConfig plugins createServer preview build optimizeDeps ssr loadEnv import.meta.env transformIndexHtml configureServer");
    expect(buildToolReadinessText).toContain("\"buildToolSetups\"");
    expect(buildToolReadinessText).toContain("\"configSignals\"");
    expect(buildToolReadinessText).toContain("\"pluginSignals\"");
    expect(buildToolReadinessText).toContain("\"devServerSignals\"");
    expect(buildToolReadinessText).toContain("\"dependencyOptimizationSignals\"");
    expect(buildToolReadinessText).toContain("Vite");
    const buildToolReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "build-tool-readiness.html"), "utf8");
    expect(buildToolReadinessHtml).toContain("Build Tool Readiness");
    expect(buildToolReadinessHtml).toContain("build-tool-readiness-card");
    expect(buildToolReadinessHtml).toContain("data-source-pattern=\"Vite\"");
    expect(buildToolReadinessHtml).toContain("Build Tool Setups");
    expect(buildToolReadinessHtml).toContain("Dependency Optimization Signals");
    const buildToolReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "build-tool-readiness.md"), "utf8");
    expect(buildToolReadinessMarkdown).toContain("# Build Tool Readiness");
    expect(buildToolReadinessMarkdown).toContain("Source pattern: Vite");
    expect(buildToolReadinessMarkdown).toContain("## Plugin Signals");
    expect(buildToolReadinessMarkdown).toContain("## Dependency Optimization Signals");
    const stylingReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "styling-readiness-report.json"), "utf8");
    expect(stylingReadinessText).toContain("Tailwind CSS @import tailwindcss @theme @utility @variant @source @config @plugin @apply content safelist darkMode prefix important");
    expect(stylingReadinessText).toContain("\"stylingSetups\"");
    expect(stylingReadinessText).toContain("\"directiveSignals\"");
    expect(stylingReadinessText).toContain("\"classSignals\"");
    expect(stylingReadinessText).toContain("\"integrationSignals\"");
    expect(stylingReadinessText).toContain("Tailwind CSS");
    const stylingReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "styling-readiness.html"), "utf8");
    expect(stylingReadinessHtml).toContain("Styling Readiness");
    expect(stylingReadinessHtml).toContain("styling-readiness-card");
    expect(stylingReadinessHtml).toContain("data-source-pattern=\"Tailwind CSS\"");
    expect(stylingReadinessHtml).toContain("Directive Signals");
    expect(stylingReadinessHtml).toContain("Integration Signals");
    const stylingReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "styling-readiness.md"), "utf8");
    expect(stylingReadinessMarkdown).toContain("# Styling Readiness");
    expect(stylingReadinessMarkdown).toContain("Source pattern: Tailwind CSS");
    expect(stylingReadinessMarkdown).toContain("## Directive Signals");
    expect(stylingReadinessMarkdown).toContain("## Integration Signals");
    const visualRegressionReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "visual-regression-readiness-report.json"), "utf8");
    expect(visualRegressionReadinessText).toContain("reg-suit regconfig actualDir expectedDir diffDir thresholdRate thresholdPixel matchingThreshold ximgdiff sync-expected compare publish report plugin storage notification");
    expect(visualRegressionReadinessText).toContain("\"visualRegressionSetups\"");
    expect(visualRegressionReadinessText).toContain("\"snapshotSignals\"");
    expect(visualRegressionReadinessText).toContain("\"thresholdSignals\"");
    expect(visualRegressionReadinessText).toContain("\"pluginSignals\"");
    expect(visualRegressionReadinessText).toContain("reg-suit");
    const visualRegressionReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "visual-regression-readiness.html"), "utf8");
    expect(visualRegressionReadinessHtml).toContain("Visual Regression Readiness");
    expect(visualRegressionReadinessHtml).toContain("visual-regression-readiness-card");
    expect(visualRegressionReadinessHtml).toContain("data-source-pattern=\"reg-suit\"");
    expect(visualRegressionReadinessHtml).toContain("Threshold Signals");
    expect(visualRegressionReadinessHtml).toContain("Plugin Signals");
    const visualRegressionReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "visual-regression-readiness.md"), "utf8");
    expect(visualRegressionReadinessMarkdown).toContain("# Visual Regression Readiness");
    expect(visualRegressionReadinessMarkdown).toContain("Source pattern: reg-suit");
    expect(visualRegressionReadinessMarkdown).toContain("## Threshold Signals");
    expect(visualRegressionReadinessMarkdown).toContain("## Plugin Signals");
    const infrastructureReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "infrastructure-readiness-report.json"), "utf8");
    expect(infrastructureReadinessText).toContain("OpenTofu terraform block provider resource data module variable output backend state lockfile init plan apply import workspace validate fmt test");
    expect(infrastructureReadinessText).toContain("\"infrastructureSetups\"");
    expect(infrastructureReadinessText).toContain("\"stateSignals\"");
    expect(infrastructureReadinessText).toContain("\"workflowSignals\"");
    expect(infrastructureReadinessText).toContain("\"policySignals\"");
    expect(infrastructureReadinessText).toContain("OpenTofu");
    const infrastructureReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "infrastructure-readiness.html"), "utf8");
    expect(infrastructureReadinessHtml).toContain("Infrastructure Readiness");
    expect(infrastructureReadinessHtml).toContain("infrastructure-readiness-card");
    expect(infrastructureReadinessHtml).toContain("data-source-pattern=\"OpenTofu\"");
    expect(infrastructureReadinessHtml).toContain("State Signals");
    expect(infrastructureReadinessHtml).toContain("Workflow Signals");
    const infrastructureReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "infrastructure-readiness.md"), "utf8");
    expect(infrastructureReadinessMarkdown).toContain("# Infrastructure Readiness");
    expect(infrastructureReadinessMarkdown).toContain("Source pattern: OpenTofu");
    expect(infrastructureReadinessMarkdown).toContain("## State Signals");
    expect(infrastructureReadinessMarkdown).toContain("## Workflow Signals");
    const iacDriftReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "iac-drift-readiness-report.json"), "utf8");
    expect(iacDriftReadinessText).toContain("IaC drift readiness driftctl scan from tfstate terraform plan -detailed-exitcode refresh-only state pull show json OpenTofu tofu Pulumi refresh preview stack export import Terragrunt run-all plan ignore unmanaged missing changed drift summary");
    expect(iacDriftReadinessText).toContain("\"driftSetups\"");
    expect(iacDriftReadinessText).toContain("\"refreshSignals\"");
    expect(iacDriftReadinessText).toContain("\"driftSignals\"");
    expect(iacDriftReadinessText).toContain("Run driftctl, Terraform/OpenTofu, Pulumi, Terragrunt, cloud provider, state, plan, refresh, import, apply, and destroy commands only in a trusted sandbox");
    expect(iacDriftReadinessText).not.toContain("실제 drift 판정은 원본 저장소에서 validate/generate/typecheck를 별도 실행하세요.");
    const iacDriftReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "iac-drift-readiness.html"), "utf8");
    expect(iacDriftReadinessHtml).toContain("IaC Drift Readiness");
    expect(iacDriftReadinessHtml).toContain("iac-drift-readiness-card");
    expect(iacDriftReadinessHtml).toContain("data-source-pattern=\"IaCDrift\"");
    expect(iacDriftReadinessHtml).toContain("Drift Signals");
    expect(iacDriftReadinessHtml).toContain("does not contact cloud APIs");
    expect(iacDriftReadinessHtml).not.toContain("실제 drift 판정은 원본 저장소에서 validate/generate/typecheck를 별도 실행하세요.");
    const iacDriftReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "iac-drift-readiness.md"), "utf8");
    expect(iacDriftReadinessMarkdown).toContain("# IaC Drift Readiness");
    expect(iacDriftReadinessMarkdown).toContain("Source pattern: IaC drift readiness");
    expect(iacDriftReadinessMarkdown).toContain("## Drift Signals");
    expect(iacDriftReadinessMarkdown).not.toContain("실제 drift 판정은 원본 저장소에서 validate/generate/typecheck를 별도 실행하세요.");
    const deploymentReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "deployment-readiness-report.json"), "utf8");
    expect(deploymentReadinessText).toContain("Helm Chart.yaml values.yaml templates helm lint template install upgrade rollback dependency package repo test");
    expect(deploymentReadinessText).toContain("\"deploymentSetups\"");
    expect(deploymentReadinessText).toContain("\"chartSignals\"");
    expect(deploymentReadinessText).toContain("\"templateSignals\"");
    expect(deploymentReadinessText).toContain("\"releaseSignals\"");
    expect(deploymentReadinessText).toContain("\"safetySignals\"");
    expect(deploymentReadinessText).toContain("Helm");
    const deploymentReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "deployment-readiness.html"), "utf8");
    expect(deploymentReadinessHtml).toContain("Deployment Readiness");
    expect(deploymentReadinessHtml).toContain("deployment-readiness-card");
    expect(deploymentReadinessHtml).toContain("data-source-pattern=\"Helm\"");
    expect(deploymentReadinessHtml).toContain("Release Signals");
    expect(deploymentReadinessHtml).toContain("Safety Signals");
    const deploymentReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "deployment-readiness.md"), "utf8");
    expect(deploymentReadinessMarkdown).toContain("# Deployment Readiness");
    expect(deploymentReadinessMarkdown).toContain("Source pattern: Helm");
    expect(deploymentReadinessMarkdown).toContain("## Release Signals");
    expect(deploymentReadinessMarkdown).toContain("## Safety Signals");
    const serverlessReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "serverless-readiness-report.json"), "utf8");
    expect(serverlessReadinessText).toContain("Serverless Framework serverless.yml service provider runtime stage region functions handler events httpApi schedule sqs sns resources package plugins deploy invoke offline logs");
    expect(serverlessReadinessText).toContain("\"serverlessSetups\"");
    expect(serverlessReadinessText).toContain("\"eventSignals\"");
    expect(serverlessReadinessText).toContain("\"deploymentSignals\"");
    const serverlessReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "serverless-readiness.html"), "utf8");
    expect(serverlessReadinessHtml).toContain("Serverless Readiness");
    expect(serverlessReadinessHtml).toContain("serverless-readiness-card");
    expect(serverlessReadinessHtml).toContain("data-source-pattern=\"Serverless Framework\"");
    const serverlessReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "serverless-readiness.md"), "utf8");
    expect(serverlessReadinessMarkdown).toContain("# Serverless Readiness");
    expect(serverlessReadinessMarkdown).toContain("Source pattern: Serverless Framework");
    expect(serverlessReadinessMarkdown).toContain("## Event Signals");
    const mobileReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "mobile-readiness-report.json"), "utf8");
    expect(mobileReadinessText).toContain("Expo app.json app.config eas.json expo start expo run:ios expo run:android eas build eas update expo-updates runtimeVersion scheme plugins assets permissions");
    expect(mobileReadinessText).toContain("\"mobileSetups\"");
    expect(mobileReadinessText).toContain("\"platformSignals\"");
    expect(mobileReadinessText).toContain("\"updateSignals\"");
    const mobileReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "mobile-readiness.html"), "utf8");
    expect(mobileReadinessHtml).toContain("Mobile Readiness");
    expect(mobileReadinessHtml).toContain("mobile-readiness-card");
    expect(mobileReadinessHtml).toContain("data-source-pattern=\"Expo\"");
    const mobileReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "mobile-readiness.md"), "utf8");
    expect(mobileReadinessMarkdown).toContain("# Mobile Readiness");
    expect(mobileReadinessMarkdown).toContain("Source pattern: Expo");
    expect(mobileReadinessMarkdown).toContain("## Build Signals");
    const desktopReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "desktop-readiness-report.json"), "utf8");
    expect(desktopReadinessText).toContain("Tauri tauri.conf.json capabilities permissions bundle updater createUpdaterArtifacts signing notarization Electron electron-builder electron-forge autoUpdater Wails wails.json wails build desktop app packaging");
    expect(desktopReadinessText).toContain("\"desktopSetups\"");
    expect(desktopReadinessText).toContain("\"permissionSignals\"");
    expect(desktopReadinessText).toContain("\"releaseSignals\"");
    const desktopReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "desktop-readiness.html"), "utf8");
    expect(desktopReadinessHtml).toContain("Desktop Readiness");
    expect(desktopReadinessHtml).toContain("desktop-readiness-card");
    expect(desktopReadinessHtml).toContain("data-source-pattern=\"Tauri\"");
    const desktopReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "desktop-readiness.md"), "utf8");
    expect(desktopReadinessMarkdown).toContain("# Desktop Readiness");
    expect(desktopReadinessMarkdown).toContain("Source pattern: Tauri");
    expect(desktopReadinessMarkdown).toContain("## Permission Signals");
    const edgeReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "edge-readiness-report.json"), "utf8");
    expect(edgeReadinessText).toContain("Cloudflare Workers wrangler.toml compatibility_date main fetch handler bindings kv_namespaces r2_buckets d1_databases durable_objects queues services vars routes workers_dev wrangler dev deploy tail secret Miniflare vitest-pool-workers");
    expect(edgeReadinessText).toContain("\"edgeSetups\"");
    expect(edgeReadinessText).toContain("\"bindingSignals\"");
    expect(edgeReadinessText).toContain("\"deploymentSignals\"");
    const edgeReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "edge-readiness.html"), "utf8");
    expect(edgeReadinessHtml).toContain("Edge Readiness");
    expect(edgeReadinessHtml).toContain("edge-readiness-card");
    expect(edgeReadinessHtml).toContain("data-source-pattern=\"Cloudflare Workers\"");
    const edgeReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "edge-readiness.md"), "utf8");
    expect(edgeReadinessMarkdown).toContain("# Edge Readiness");
    expect(edgeReadinessMarkdown).toContain("Source pattern: Cloudflare Workers");
    expect(edgeReadinessMarkdown).toContain("## Binding Signals");
    const composeReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "compose-readiness-report.json"), "utf8");
    expect(composeReadinessText).toContain("Docker Compose compose.yaml docker-compose.yml services build image ports volumes networks depends_on healthcheck profiles env_file secrets configs docker compose config up build run logs ps watch wait");
    expect(composeReadinessText).toContain("\"composeSetups\"");
    expect(composeReadinessText).toContain("\"workflowSignals\"");
    expect(composeReadinessText).toContain("\"resourceSignals\"");
    const composeReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "compose-readiness.html"), "utf8");
    expect(composeReadinessHtml).toContain("Compose Readiness");
    expect(composeReadinessHtml).toContain("compose-readiness-card");
    expect(composeReadinessHtml).toContain("data-source-pattern=\"Docker Compose\"");
    const composeReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "compose-readiness.md"), "utf8");
    expect(composeReadinessMarkdown).toContain("# Compose Readiness");
    expect(composeReadinessMarkdown).toContain("Source pattern: Docker Compose");
    expect(composeReadinessMarkdown).toContain("## Workflow Signals");
    const devContainerReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "devcontainer-readiness-report.json"), "utf8");
    expect(devContainerReadinessText).toContain("Dev Containers devcontainer.json .devcontainer devcontainer build up exec read-configuration run-user-commands features templates postCreateCommand forwardPorts customizations remoteUser containerEnv mounts workspaceFolder");
    expect(devContainerReadinessText).toContain("\"devContainerSetups\"");
    expect(devContainerReadinessText).toContain("\"workflowSignals\"");
    expect(devContainerReadinessText).toContain("\"lifecycleSignals\"");
    const devContainerReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "devcontainer-readiness.html"), "utf8");
    expect(devContainerReadinessHtml).toContain("Dev Container Readiness");
    expect(devContainerReadinessHtml).toContain("devcontainer-readiness-card");
    expect(devContainerReadinessHtml).toContain("data-source-pattern=\"Dev Containers\"");
    const devContainerReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "devcontainer-readiness.md"), "utf8");
    expect(devContainerReadinessMarkdown).toContain("# Dev Container Readiness");
    expect(devContainerReadinessMarkdown).toContain("Source pattern: Dev Containers");
    expect(devContainerReadinessMarkdown).toContain("## Lifecycle Signals");
    const kubernetesReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "kubernetes-readiness-report.json"), "utf8");
    expect(kubernetesReadinessText).toContain("Kubernetes apiVersion kind metadata labels annotations namespace Deployment StatefulSet DaemonSet Service Ingress ConfigMap Secret ServiceAccount Role RoleBinding ClusterRole ClusterRoleBinding NetworkPolicy PersistentVolume PersistentVolumeClaim readinessProbe livenessProbe resources requests limits HorizontalPodAutoscaler PodDisruptionBudget kustomization resources patches kubectl apply diff wait rollout logs describe port-forward delete");
    expect(kubernetesReadinessText).toContain("\"kubernetesSetups\"");
    expect(kubernetesReadinessText).toContain("\"kustomizeSignals\"");
    expect(kubernetesReadinessText).toContain("\"workflowSignals\"");
    const kubernetesReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "kubernetes-readiness.html"), "utf8");
    expect(kubernetesReadinessHtml).toContain("Kubernetes Readiness");
    expect(kubernetesReadinessHtml).toContain("kubernetes-readiness-card");
    expect(kubernetesReadinessHtml).toContain("data-source-pattern=\"Kubernetes\"");
    const kubernetesReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "kubernetes-readiness.md"), "utf8");
    expect(kubernetesReadinessMarkdown).toContain("# Kubernetes Readiness");
    expect(kubernetesReadinessMarkdown).toContain("Source pattern: Kubernetes");
    expect(kubernetesReadinessMarkdown).toContain("## Kustomize Signals");
    const gitopsReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "gitops-readiness-report.json"), "utf8");
    expect(gitopsReadinessText).toContain("GitOps Argo CD Application ApplicationSet AppProject");
    expect(gitopsReadinessText).toContain("\"gitopsSetups\"");
    expect(gitopsReadinessText).toContain("\"fluxSourceSignals\"");
    expect(gitopsReadinessText).toContain("\"workflowSignals\"");
    const gitopsReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "gitops-readiness.html"), "utf8");
    expect(gitopsReadinessHtml).toContain("GitOps Readiness");
    expect(gitopsReadinessHtml).toContain("gitops-readiness-card");
    expect(gitopsReadinessHtml).toContain("data-source-pattern=\"GitOps\"");
    const gitopsReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "gitops-readiness.md"), "utf8");
    expect(gitopsReadinessMarkdown).toContain("# GitOps Readiness");
    expect(gitopsReadinessMarkdown).toContain("Source pattern: GitOps");
    expect(gitopsReadinessMarkdown).toContain("## Flux Source Signals");
    const backupReadinessText = await fs.readFile(path.join(result.session.outputPaths.analysis, "backup-readiness-report.json"), "utf8");
    expect(backupReadinessText).toContain("Backup readiness Velero Backup Schedule Restore");
    expect(backupReadinessText).toContain("\"backupSetups\"");
    expect(backupReadinessText).toContain("\"restoreDrillSignals\"");
    expect(backupReadinessText).toContain("\"resticSignals\"");
    const backupReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.html, "backup-readiness.html"), "utf8");
    expect(backupReadinessHtml).toContain("Backup Readiness");
    expect(backupReadinessHtml).toContain("backup-readiness-card");
    expect(backupReadinessHtml).toContain("data-source-pattern=\"Backup\"");
    const backupReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "backup-readiness.md"), "utf8");
    expect(backupReadinessMarkdown).toContain("# Backup Readiness");
    expect(backupReadinessMarkdown).toContain("Source pattern: Backup readiness");
    expect(backupReadinessMarkdown).toContain("## Restore Drill Signals");
    const contextPackText = await fs.readFile(path.join(result.session.outputPaths.analysis, "context-pack-report.json"), "utf8");
    expect(contextPackText).toContain("Repomix token counting git-aware ignore AI-friendly context pack");
    expect(contextPackText).toContain("output styles compression token budget split output MCP skill generation");
    expect(contextPackText).toContain("\"budgetProfiles\"");
    expect(contextPackText).toContain("\"directoryTokenTree\"");
    expect(contextPackText).toContain("\"splitPlans\"");
    expect(contextPackText).toContain("\"contextPackSignals\"");
    expect(contextPackText).toContain("google-ai-studio-1mb");
    const contextPackHtml = await fs.readFile(path.join(result.session.outputPaths.html, "context-pack.html"), "utf8");
    expect(contextPackHtml).toContain("Context Pack");
    expect(contextPackHtml).toContain("context-pack-card");
    expect(contextPackHtml).toContain("context-pack-source-link");
    expect(contextPackHtml).toContain("data-source-pattern=\"Repomix\"");
    expect(contextPackHtml).toContain("Split Output Plan");
    expect(contextPackHtml).toContain("Context Pack Signals");
    const contextPackMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "context-pack.md"), "utf8");
    expect(contextPackMarkdown).toContain("# Context Pack");
    expect(contextPackMarkdown).toContain("Source pattern: Repomix");
    expect(contextPackMarkdown).toContain("## Split Output Plan");
    expect(contextPackMarkdown).toContain("## Context Pack Signals");
    const mcpHandoffText = await fs.readFile(path.join(result.session.outputPaths.analysis, "mcp-handoff-report.json"), "utf8");
    expect(mcpHandoffText).toContain("Codebase MCP getCodebase getRemoteCodebase saveCodebase tool handoff");
    expect(mcpHandoffText).toContain("\"getCodebase\"");
    expect(mcpHandoffText).toContain("\"getRemoteCodebase\"");
    expect(mcpHandoffText).toContain("\"saveCodebase\"");
    const mcpHandoffHtml = await fs.readFile(path.join(result.session.outputPaths.html, "mcp-handoff.html"), "utf8");
    expect(mcpHandoffHtml).toContain("MCP Handoff");
    expect(mcpHandoffHtml).toContain("mcp-handoff-card");
    expect(mcpHandoffHtml).toContain("data-source-pattern=\"codebase-mcp\"");
    const mcpHandoffMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "mcp-handoff.md"), "utf8");
    expect(mcpHandoffMarkdown).toContain("# MCP Handoff");
    expect(mcpHandoffMarkdown).toContain("Source pattern: Codebase MCP");
    const agentMemoryText = await fs.readFile(path.join(result.session.outputPaths.analysis, "agent-memory-report.json"), "utf8");
    expect(agentMemoryText).toContain("Claude Code Obsidian Graphify persistent memory token-saving context navigation");
    expect(agentMemoryText).toContain("\"tokenSavings\"");
    expect(agentMemoryText).toContain("\"memoryNotes\"");
    expect(agentMemoryText).toContain("project-context");
    const agentMemoryHtml = await fs.readFile(path.join(result.session.outputPaths.html, "agent-memory.html"), "utf8");
    expect(agentMemoryHtml).toContain("Agent Memory");
    expect(agentMemoryHtml).toContain("agent-memory-card");
    expect(agentMemoryHtml).toContain("data-source-pattern=\"Obsidian Graphify\"");
    expect(agentMemoryHtml).toContain("estimated reduction");
    const agentMemoryMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "agent-memory.md"), "utf8");
    expect(agentMemoryMarkdown).toContain("# Agent Memory");
    expect(agentMemoryMarkdown).toContain("Source pattern: Claude Code Obsidian Graphify");
    expect(agentMemoryMarkdown).toContain("## Context Navigation Rules");
    const graphQueryText = await fs.readFile(path.join(result.session.outputPaths.analysis, "graph-query-report.json"), "utf8");
    expect(graphQueryText).toContain("Graphify query path explain graph traversal command guide");
    expect(graphQueryText).toContain("\"queryModes\"");
    expect(graphQueryText).toContain("\"nodeExplanations\"");
    expect(graphQueryText).toContain("\"pathPrompts\"");
    const graphQueryHtml = await fs.readFile(path.join(result.session.outputPaths.html, "graph-query.html"), "utf8");
    expect(graphQueryHtml).toContain("Graph Query");
    expect(graphQueryHtml).toContain("graph-query-card");
    expect(graphQueryHtml).toContain("data-source-pattern=\"Graphify\"");
    expect(graphQueryHtml).toContain("graphify explain");
    const graphQueryMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "graph-query.md"), "utf8");
    expect(graphQueryMarkdown).toContain("# Graph Query");
    expect(graphQueryMarkdown).toContain("Source pattern: Graphify");
    expect(graphQueryMarkdown).toContain("## Path Prompts");
    const tutorialAbstractionText = await fs.readFile(path.join(result.session.outputPaths.analysis, "tutorial-abstraction-report.json"), "utf8");
    expect(tutorialAbstractionText).toContain("PocketFlow codebase tutorial identify abstractions analyze relationships order chapters");
    expect(tutorialAbstractionText).toContain("\"abstractions\"");
    expect(tutorialAbstractionText).toContain("\"relationships\"");
    expect(tutorialAbstractionText).toContain("\"chapterOrder\"");
    const tutorialAbstractionHtml = await fs.readFile(path.join(result.session.outputPaths.html, "tutorial-abstractions.html"), "utf8");
    expect(tutorialAbstractionHtml).toContain("Tutorial Abstractions");
    expect(tutorialAbstractionHtml).toContain("tutorial-abstraction-card");
    expect(tutorialAbstractionHtml).toContain("data-source-pattern=\"PocketFlow\"");
    expect(tutorialAbstractionHtml).toContain("Chapter Order");
    const tutorialAbstractionMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "tutorial-abstractions.md"), "utf8");
    expect(tutorialAbstractionMarkdown).toContain("# Tutorial Abstractions");
    expect(tutorialAbstractionMarkdown).toContain("Source pattern: PocketFlow");
    expect(tutorialAbstractionMarkdown).toContain("## Chapter Order");
    const decisionRecordText = await fs.readFile(path.join(result.session.outputPaths.analysis, "decision-record-report.json"), "utf8");
    expect(decisionRecordText).toContain("Log4brains ADR docs-as-code status context decision consequences timeline package-specific records");
    expect(decisionRecordText).toContain("\"records\"");
    expect(decisionRecordText).toContain("\"statusCounts\"");
    expect(decisionRecordText).toContain("\"timeline\"");
    expect(decisionRecordText).toContain("\"packageScopes\"");
    const decisionRecordsHtml = await fs.readFile(path.join(result.session.outputPaths.html, "decision-records.html"), "utf8");
    expect(decisionRecordsHtml).toContain("Decision Records");
    expect(decisionRecordsHtml).toContain("decision-record-card");
    expect(decisionRecordsHtml).toContain("data-source-pattern=\"Log4brains\"");
    expect(decisionRecordsHtml).toContain("Positive Consequences");
    const decisionRecordsMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "decision-records.md"), "utf8");
    expect(decisionRecordsMarkdown).toContain("# Decision Records");
    expect(decisionRecordsMarkdown).toContain("Source pattern: Log4brains");
    expect(decisionRecordsMarkdown).toContain("## Timeline");
    const dependencyHealthText = await fs.readFile(path.join(result.session.outputPaths.analysis, "dependency-health-report.json"), "utf8");
    expect(dependencyHealthText).toContain("dependency-cruiser no-circular no-orphans forbidden rules dependency graph validation");
    expect(dependencyHealthText).toContain("\"localDependencyEdges\"");
    expect(dependencyHealthText).toContain("\"cycles\"");
    expect(dependencyHealthText).toContain("\"orphanModules\"");
    expect(dependencyHealthText).toContain("\"ruleViolations\"");
    const dependencyHealthHtml = await fs.readFile(path.join(result.session.outputPaths.html, "dependency-health.html"), "utf8");
    expect(dependencyHealthHtml).toContain("Dependency Health");
    expect(dependencyHealthHtml).toContain("dependency-health-card");
    expect(dependencyHealthHtml).toContain("data-source-pattern=\"dependency-cruiser\"");
    expect(dependencyHealthHtml).toContain("no-circular");
    expect(dependencyHealthHtml).toContain("no-orphans");
    const dependencyHealthMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "dependency-health.md"), "utf8");
    expect(dependencyHealthMarkdown).toContain("# Dependency Health");
    expect(dependencyHealthMarkdown).toContain("Source pattern: dependency-cruiser");
    expect(dependencyHealthMarkdown).toContain("## Rule Violations");
    const exportManifestText = await fs.readFile(path.join(result.session.outputPaths.html, "manifest.json"), "utf8");
    expect(exportManifestText).toContain("\"entrypoints\"");
    expect(exportManifestText).toContain("html/vibe-coding-start.html");
    expect(exportManifestText).toContain("html/learning-path.html");
    expect(exportManifestText).toContain("html/quiz-print.html");
    expect(exportManifestText).toContain("소스 역할을 AI에게 줄 목적, 책임, 검증 기준");
    expect(exportManifestText).toContain("AI 구현 지시 기준표");
    expect(exportManifestText).toContain("틀린 문제를 AI에게 다시 설명할 목적");
    expect(exportManifestText).toContain("html/suggested-reads.html");
    expect(exportManifestText).toContain("html/runtime-environment.html");
    expect(exportManifestText).toContain("html/interface-map.html");
    expect(exportManifestText).toContain("html/symbol-map.html");
    expect(exportManifestText).toContain("html/api-reference.html");
    expect(exportManifestText).toContain("html/search-index.html");
    expect(exportManifestText).toContain("html/learning-journal.html");
    expect(exportManifestText).toContain("html/daily-summary.html");
    expect(exportManifestText).toContain("html/vibe-coding-prompt-pack.html");
    expect(exportManifestText).toContain("html/project-activity.html");
    expect(exportManifestText).toContain("html/code-metrics-readiness.html");
    expect(exportManifestText).toContain("html/code-ownership-readiness.html");
    expect(exportManifestText).toContain("html/large-asset-readiness.html");
    expect(exportManifestText).toContain("html/license-rights.html");
    expect(exportManifestText).toContain("html/sbom.html");
    expect(exportManifestText).toContain("html/security-readiness.html");
    expect(exportManifestText).toContain("html/sast-readiness.html");
    expect(exportManifestText).toContain("html/dast-readiness.html");
    expect(exportManifestText).toContain("html/threat-model-readiness.html");
    expect(exportManifestText).toContain("html/scorecard.html");
    expect(exportManifestText).toContain("html/provenance.html");
    expect(exportManifestText).toContain("html/advisories.html");
    expect(exportManifestText).toContain("html/vex.html");
    expect(exportManifestText).toContain("html/policy-gates.html");
    expect(exportManifestText).toContain("html/api-contracts.html");
    expect(exportManifestText).toContain("html/observability.html");
    expect(exportManifestText).toContain("html/performance.html");
    expect(exportManifestText).toContain("html/profiling-readiness.html");
    expect(exportManifestText).toContain("html/tracing-readiness.html");
    expect(exportManifestText).toContain("html/debug-readiness.html");
    expect(exportManifestText).toContain("html/crash-reporting-readiness.html");
    expect(exportManifestText).toContain("html/incident-response-readiness.html");
    expect(exportManifestText).toContain("html/slo-readiness.html");
    expect(exportManifestText).toContain("html/cost-readiness.html");
    expect(exportManifestText).toContain("html/progressive-delivery-readiness.html");
    expect(exportManifestText).toContain("html/load-testing-readiness.html");
    expect(exportManifestText).toContain("html/benchmark-readiness.html");
    expect(exportManifestText).toContain("html/e2e.html");
    expect(exportManifestText).toContain("html/flaky-test-readiness.html");
    expect(exportManifestText).toContain("html/test-impact-readiness.html");
    expect(exportManifestText).toContain("html/test-reporting-readiness.html");
    expect(exportManifestText).toContain("html/snapshot-readiness.html");
    expect(exportManifestText).toContain("html/property-based-testing-readiness.html");
    expect(exportManifestText).toContain("html/fuzz-readiness.html");
    expect(exportManifestText).toContain("html/test-data-readiness.html");
    expect(exportManifestText).toContain("html/integration-test-environment-readiness.html");
    expect(exportManifestText).toContain("html/chaos-engineering-readiness.html");
    expect(exportManifestText).toContain("html/accessibility.html");
    expect(exportManifestText).toContain("html/storybook.html");
    expect(exportManifestText).toContain("html/design-tokens.html");
    expect(exportManifestText).toContain("html/i18n.html");
    expect(exportManifestText).toContain("html/release-readiness.html");
    expect(exportManifestText).toContain("html/secret-readiness.html");
    expect(exportManifestText).toContain("html/secret-management-readiness.html");
    expect(exportManifestText).toContain("html/container-readiness.html");
    expect(exportManifestText).toContain("html/container-scan-readiness.html");
    expect(exportManifestText).toContain("html/code-quality.html");
    expect(exportManifestText).toContain("html/documentation.html");
    expect(exportManifestText).toContain("html/database-readiness.html");
    expect(exportManifestText).toContain("html/database-migration-readiness.html");
    expect(exportManifestText).toContain("html/database-orm-readiness.html");
    expect(exportManifestText).toContain("html/data-transformation-readiness.html");
    expect(exportManifestText).toContain("html/data-quality-readiness.html");
    expect(exportManifestText).toContain("html/data-lineage-readiness.html");
    expect(exportManifestText).toContain("html/data-catalog-readiness.html");
    expect(exportManifestText).toContain("html/data-annotation-readiness.html");
    expect(exportManifestText).toContain("html/lakehouse-table-readiness.html");
    expect(exportManifestText).toContain("html/feature-store-readiness.html");
    expect(exportManifestText).toContain("html/model-registry-readiness.html");
    expect(exportManifestText).toContain("html/experiment-tracking-readiness.html");
    expect(exportManifestText).toContain("html/model-monitoring-readiness.html");
    expect(exportManifestText).toContain("html/model-serving-readiness.html");
    expect(exportManifestText).toContain("html/model-training-readiness.html");
    expect(exportManifestText).toContain("html/ci-cd.html");
    expect(exportManifestText).toContain("html/unit-tests.html");
    expect(exportManifestText).toContain("html/coverage-readiness.html");
    expect(exportManifestText).toContain("html/mutation-testing-readiness.html");
    expect(exportManifestText).toContain("html/typecheck-readiness.html");
    expect(exportManifestText).toContain("html/package-manager.html");
    expect(exportManifestText).toContain("html/git-hooks.html");
    expect(exportManifestText).toContain("html/task-runner.html");
    expect(exportManifestText).toContain("html/dependency-updates.html");
    expect(exportManifestText).toContain("html/dependency-review-readiness.html");
    expect(exportManifestText).toContain("html/lint-readiness.html");
    expect(exportManifestText).toContain("html/format-readiness.html");
    expect(exportManifestText).toContain("html/commit-conventions.html");
    expect(exportManifestText).toContain("html/changelog-readiness.html");
    expect(exportManifestText).toContain("html/bundle-analysis.html");
    expect(exportManifestText).toContain("html/mocking-readiness.html");
    expect(exportManifestText).toContain("html/data-fetching-readiness.html");
    expect(exportManifestText).toContain("html/routing-readiness.html");
    expect(exportManifestText).toContain("html/state-management-readiness.html");
    expect(exportManifestText).toContain("html/form-readiness.html");
    expect(exportManifestText).toContain("html/auth-readiness.html");
    expect(exportManifestText).toContain("html/authorization-readiness.html");
    expect(exportManifestText).toContain("html/payment-readiness.html");
    expect(exportManifestText).toContain("html/email-readiness.html");
    expect(exportManifestText).toContain("html/queue-readiness.html");
    expect(exportManifestText).toContain("html/event-stream-readiness.html");
    expect(exportManifestText).toContain("html/data-connector-readiness.html");
    expect(exportManifestText).toContain("html/semantic-layer-readiness.html");
    expect(exportManifestText).toContain("html/schema-registry-readiness.html");
    expect(exportManifestText).toContain("html/stream-processing-readiness.html");
    expect(exportManifestText).toContain("html/pipeline-orchestration-readiness.html");
    expect(exportManifestText).toContain("html/service-mesh-readiness.html");
    expect(exportManifestText).toContain("html/ingress-controller-readiness.html");
    expect(exportManifestText).toContain("html/dns-readiness.html");
    expect(exportManifestText).toContain("html/certificate-readiness.html");
    expect(exportManifestText).toContain("html/helm-readiness.html");
    expect(exportManifestText).toContain("html/admission-policy-readiness.html");
    expect(exportManifestText).toContain("html/api-gateway-readiness.html");
    expect(exportManifestText).toContain("html/cache-readiness.html");
    expect(exportManifestText).toContain("html/logging-readiness.html");
    expect(exportManifestText).toContain("html/feature-flag-readiness.html");
    expect(exportManifestText).toContain("html/rate-limit-readiness.html");
    expect(exportManifestText).toContain("html/error-tracking-readiness.html");
    expect(exportManifestText).toContain("html/desktop-readiness.html");
    expect(exportManifestText).toContain("html/iac-drift-readiness.html");
    expect(exportManifestText).toContain("html/edge-readiness.html");
    expect(exportManifestText).toContain("html/compose-readiness.html");
    expect(exportManifestText).toContain("html/devcontainer-readiness.html");
    expect(exportManifestText).toContain("html/kubernetes-readiness.html");
    expect(exportManifestText).toContain("html/gitops-readiness.html");
    expect(exportManifestText).toContain("html/backup-readiness.html");
    expect(exportManifestText).toContain("html/llm-eval-readiness.html");
    expect(exportManifestText).toContain("html/llm-observability-readiness.html");
    expect(exportManifestText).toContain("html/vector-db-readiness.html");
    expect(exportManifestText).toContain("html/search-service-readiness.html");
    expect(exportManifestText).toContain("html/object-storage-readiness.html");
    expect(exportManifestText).toContain("html/realtime-collaboration-readiness.html");
    expect(exportManifestText).toContain("html/workflow-orchestration-readiness.html");
    expect(exportManifestText).toContain("html/openapi-client-readiness.html");
    expect(exportManifestText).toContain("html/webhook-readiness.html");
    expect(exportManifestText).toContain("html/notification-readiness.html");
    expect(exportManifestText).toContain("html/consent-readiness.html");
    expect(exportManifestText).toContain("html/privacy-readiness.html");
    expect(exportManifestText).toContain("html/context-pack.html");
    expect(exportManifestText).toContain("html/mcp-handoff.html");
    expect(exportManifestText).toContain("html/agent-memory.html");
    expect(exportManifestText).toContain("html/graph-query.html");
    expect(exportManifestText).toContain("html/tutorial-abstractions.html");
    expect(exportManifestText).toContain("html/decision-records.html");
    expect(exportManifestText).toContain("html/dependency-health.html");
    expect(exportManifestText).toContain("\"integrity\"");
    expect(exportManifestText).toContain("\"bytes\"");
    expect(exportManifestText).toContain("\"sha256\"");
    expect(exportManifestText).toContain("\"readmePath\"");
    expect(exportManifestText).toContain("assets/component-graph.mmd");
    expect(exportManifestText).toContain("assets/learning-path.tour.json");
    expect(exportManifestText).toContain("assets/search-index.json");
    expect(exportManifestText).toContain("assets/learning-journal-template.md");
    const exportReadmeText = await fs.readFile(path.join(result.session.outputPaths.html, "EXPORT-README.md"), "utf8");
    expect(exportReadmeText).toContain("RepoTutor HTML Export");
    expect(exportReadmeText).toContain("Entry Points");
    expect(exportReadmeText).toContain("Integrity metadata uses sha256");
    expect(exportReadmeText).toContain("assets/component-graph.mmd");
    expect(exportReadmeText).toContain("assets/learning-path.tour.json");
    expect(exportReadmeText).toContain("assets/search-index.json");
    expect(exportReadmeText).toContain("assets/learning-journal-template.md");
    expect(exportReadmeText).toContain("생성된 세션 `source/` 스냅샷을 장기 앱 지식으로 내장하지 않고 보존 증거, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 남긴 뒤 정리 검토 후보로 둘지 판단합니다.");
    expect(exportReadmeText).not.toContain("생성된 세션 `source/` 스냅샷을 장기 앱 지식으로 내장하지 않고 보존 증거, 세션 검증, 검증 기록, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 남긴 뒤 정리 검토 후보로 둘지 판단합니다.");
    expect(exportReadmeText).not.toContain("생성된 세션 `source/` 스냅샷을 장기 앱 지식으로 내장하지 않고 무엇을 남긴 뒤 정리 검토 후보로 둘지 판단합니다.");
    expect(exportReadmeText).not.toContain("생성된 세션 source/ 스냅샷을 장기 앱 지식으로 내장하지 않고 무엇을 남긴 뒤 정리 검토 후보로 둘지 판단합니다.");
    const filesHtml = await fs.readFile(path.join(result.session.outputPaths.html, "files.html"), "utf8");
    expect(filesHtml).toContain("file-nav-toolbar");
    expect(filesHtml).toContain("data-file-ext-filter");
    expect(filesHtml).toContain("data-file-dir-filter");
    expect(filesHtml).toContain("data-file-dir");
    expect(filesHtml).toContain("data-source-evidence-filter");
    expect(filesHtml).toContain("data-source-evidence");
    expect(filesHtml).toContain("근거 있음");
    expect(filesHtml).toContain("소스 근거");
    expect(filesHtml).toContain("source-evidence");
    expect(filesHtml).toContain("source-link");
    expect(filesHtml).toContain("원본 열기");
    expect(filesHtml).toContain("Vibe-Coding Source Role Map");
    expect(filesHtml).toContain("Source Role Summary");
    expect(filesHtml).toContain("AI Prompt Brief");
    expect(filesHtml).toContain("파일 구현 요청 후보");
    expect(filesHtml).toContain("AI에게 보낼 문장으로 다듬으세요");
    expect(filesHtml).not.toContain("비슷한 파일을 만들게 할 때는");
    expect(filesHtml).toContain("파일 구현 요청 후보");
    expect(filesHtml).toContain("파일 검증 요청 후보");
    expect(filesHtml).not.toContain("구현시키는 프롬프트를");
    expect(filesHtml).toContain("Verification Questions");
    expect(filesHtml).toContain("data-file-role=");
    expect(filesHtml).toContain("../source/src/main.ts");
    expect(filesHtml).toContain("import { createGreeting }");
    const filesMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "files.md"), "utf8");
    expect(filesMarkdown).toContain("### Source Role Summary");
    expect(filesMarkdown).toContain("### AI Prompt Brief");
    expect(filesMarkdown).toContain("파일 구현 요청 후보");
    expect(filesMarkdown).not.toContain("비슷한 파일을 만들게 할 때는");
    expect(filesMarkdown).toContain("파일 이해 요청 후보");
    expect(filesMarkdown).toContain("파일 검증 요청 후보");
    expect(filesMarkdown).not.toContain("구현시키는 프롬프트를");
    expect(filesMarkdown).toContain("### Vibe-Coding Prompts");
    expect(filesMarkdown).toContain("### Verification Questions");
    expect(filesMarkdown).toContain("### 소스 근거");
    expect(filesMarkdown).toContain("[원본](../source/src/main.ts)");
    const evidenceMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "evidence.md"), "utf8");
    expect(evidenceMarkdown).toContain("# 소스 근거 인덱스");
    expect(evidenceMarkdown).toContain("[src/main.ts](files.md#src-main-ts)");
    const evidenceHtml = await fs.readFile(path.join(result.session.outputPaths.html, "evidence.html"), "utf8");
    expect(evidenceHtml).toContain("소스 근거 인덱스");
    expect(evidenceHtml).toContain("evidence-index-cards");
    expect(evidenceHtml).toContain("evidence-kind-toolbar");
    expect(evidenceHtml).toContain("data-evidence-kind-filter");
    expect(evidenceHtml).toContain("data-evidence-kind=\"import\"");
    expect(evidenceHtml).toContain("files.html#src-main.ts");
    expect(evidenceHtml).toContain("../source/src/main.ts");
    const learningPathHtml = await fs.readFile(path.join(result.session.outputPaths.html, "learning-path.html"), "utf8");
    expect(learningPathHtml).toContain("학습 경로");
    expect(learningPathHtml).toContain("learning-path-step");
    expect(learningPathHtml).toContain("data-learning-primary");
    expect(learningPathHtml).toContain("기본 투어");
    expect(learningPathHtml).toContain("id=\"learning-step-1\"");
    expect(learningPathHtml).toContain("learning-step-nav");
    expect(learningPathHtml).toContain("href=\"#learning-step-2\"");
    expect(learningPathHtml).toContain("다음 단계");
    expect(learningPathHtml).toContain("이전 단계");
    expect(learningPathHtml).toContain("data-learning-step");
    expect(learningPathHtml).toContain("data-learning-step-complete");
    expect(learningPathHtml).toContain("data-reset-learning-progress");
    expect(learningPathHtml).toContain("data-learning-progress-summary");
    expect(learningPathHtml).toContain("근거 검토 표시 0 /");
    expect(learningPathHtml).toContain("진도 초기화");
    expect(learningPathHtml).toContain("근거 확인 후 검토 표시");
    expect(learningPathHtml).toContain("data-source-pattern=\"CodeTour\"");
    expect(learningPathHtml).toContain("component-graph.html");
    expect(learningPathHtml).toContain("runtime-environment.html");
    expect(learningPathHtml).toContain("interface-map.html");
    expect(learningPathHtml).toContain("symbol-map.html");
    expect(learningPathHtml).toContain("api-reference.html");
    expect(learningPathHtml).toContain("search-index.html");
    expect(learningPathHtml).toContain("learning-journal.html");
    expect(learningPathHtml).toContain("daily-summary.html");
    expect(learningPathHtml).toContain("vibe-coding-prompt-pack.html");
    expect(learningPathHtml).toContain("AI 지시용 기술 맥락 파악");
    expect(learningPathHtml).toContain("문법을 외우기보다 런타임, 의존성, 빌드/검증 맥락");
    expect(learningPathHtml).toContain("소스 역할을 AI 지시 문장으로 바꾸는 능력");
    expect(learningPathHtml).toContain("AI에게 줄 근거 기반 구현 지시서");
    expect(learningPathHtml).not.toContain("AI에게 줄 copy/paste 프롬프트");
    const improvementBacklogMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "improvement-backlog.md"), "utf8");
    expect(improvementBacklogMarkdown).toContain("high 항목부터 작은 vertical slice 요청 후보로 다듬으세요");
    expect(improvementBacklogMarkdown).toContain("요청 후보에는 목적, 사용자 흐름, 아키텍처 역할, 산출물, 검증 기준을 같이 넣으세요");
    expect(improvementBacklogMarkdown).toContain("오래된 생성 세션 `source/` 스냅샷을 정리하거나 축약할지 검토하기 전에");
    expect(improvementBacklogMarkdown).toContain("생성된 세션 `source/` 스냅샷은 장기 앱 지식이 아니지만, 보존 증거와 검증 기록 없이 요약만 남기면 재검증이 어려워집니다.");
    expect(improvementBacklogMarkdown).not.toContain("오래된 생성 세션 source/ 스냅샷을 정리하거나 축약할지 검토하기 전에");
    expect(improvementBacklogMarkdown).not.toContain("원본 전체를 오래 내장할 필요는 없지만");
    expect(improvementBacklogMarkdown).not.toContain("high 항목부터 AI에게 작은 vertical slice로 요청하세요");
    expect(improvementBacklogMarkdown).not.toContain("요청에는 목적, 사용자 흐름, 아키텍처 역할, 산출물, 검증 기준을 같이 넣으세요");
    expect(improvementBacklogMarkdown).not.toContain("오래된 source snapshot을 삭제하거나 축약하기 전에");
    const improvementBacklogHtml = await fs.readFile(path.join(result.session.outputPaths.html, "improvement-backlog.html"), "utf8");
    expect(improvementBacklogHtml).toContain("오래된 생성 세션 <code>source/</code> 스냅샷을 정리하거나 축약할지 검토하기 전에");
    expect(improvementBacklogHtml).toContain("생성된 세션 source/ 스냅샷은 장기 앱 지식이 아니지만, 보존 증거와 검증 기록 없이 요약만 남기면 재검증이 어려워집니다.");
    expect(improvementBacklogHtml).not.toContain("오래된 생성 세션 source/ 스냅샷을 정리하거나 축약할지 검토하기 전에");
    expect(improvementBacklogHtml).not.toContain("원본 전체를 오래 내장할 필요는 없지만");
    expect(improvementBacklogHtml).not.toContain("오래된 source snapshot을 삭제하거나 축약하기 전에");
    const teachingWorkspaceHtml = await fs.readFile(path.join(result.session.outputPaths.html, "teaching-workspace.html"), "utf8");
    expect(teachingWorkspaceHtml).toContain("Stateful Teaching Workspace");
    expect(teachingWorkspaceHtml).toContain("<dt>reference</dt><dd>15</dd>");
    expect(teachingWorkspaceHtml).toContain("용어를 정의 암기표가 아니라 AI 지시 문장과 AI 출력 리뷰 질문으로 다시 보는 카드입니다");
    expect(teachingWorkspaceHtml).toContain("첫 vertical slice, 수락 기준, 검증 계획을 AI 구현 지시로 바꾸는 prompt guide입니다");
    expect(teachingWorkspaceHtml).toContain("AI 산출물을 목적, 아키텍처, 근거, 검증 기준으로 PASS_REVIEW/REVISE/BLOCK 검토 상태로 확인합니다. PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보입니다.");
    expect(teachingWorkspaceHtml).not.toContain("AI 산출물을 목적, 아키텍처, 근거, 검증 기준으로 판정합니다.");
    expect(teachingWorkspaceHtml).not.toContain("재구현 순서와 copy/paste prompt를 빠르게 확인합니다");
    expect(teachingWorkspaceHtml).not.toContain("자주 다시 보는 용어 cheat sheet");
    expect(teachingWorkspaceHtml).toContain("앱이 흡수한 학습 기능과 더 조사하지 않아도 되는 항목을 AI 확인 프롬프트와 함께 분리합니다");
    expect(teachingWorkspaceHtml).toContain("보존 증거, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 기준으로 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(teachingWorkspaceHtml).not.toContain("보존 증거, 세션 검증, 검증 기록, 현재 학습 목표를 기준으로 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(teachingWorkspaceHtml).not.toContain("보존 증거와 현재 학습 목표를 기준으로 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(teachingWorkspaceHtml).not.toContain("보존 증거와 현재 학습 목표를 기준으로 생성된 세션 source/ 스냅샷 정리 검토 여부를 판단합니다");
    expect(teachingWorkspaceHtml).not.toContain("분석이 끝난 원본 source snapshot을 계속 둘지 정리할지 판단합니다");
    const languageHtml = await fs.readFile(path.join(result.session.outputPaths.html, "language.html"), "utf8");
    expect(languageHtml).toContain("언어는 목표가 아니라 AI 지시 맥락입니다");
    expect(languageHtml).toContain("기술 맥락");
    expect(languageHtml).toContain("문법을 외우는 대신");
    expect(languageHtml).toContain("런타임, 프레임워크, 빌드 도구, 파일 역할, 검증 기준");
    expect(languageHtml).toContain("data-source-pattern=\"vibe-tech-context\"");
    expect(languageHtml).toContain("문법 암기 아님");
    expect(languageHtml).toContain("AI 지시 힌트");
    const languageMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "language.md"), "utf8");
    expect(languageMarkdown).toContain("이 페이지의 목표는 언어 문법을 배우는 것이 아닙니다");
    expect(languageMarkdown).toContain("AI에게 알려줄 기술 맥락");
    expect(languageMarkdown).toContain("내가 직접 외울 문법이 아니라 AI 산출물을 검토할 기준");
    expect(languageMarkdown).toContain("AI 지시 힌트");
    const vibeStartHtml = await fs.readFile(path.join(result.session.outputPaths.html, "vibe-coding-start.html"), "utf8");
    expect(vibeStartHtml).toContain("바이브코딩 우선 시작");
    expect(vibeStartHtml).toContain("처음 15분");
    expect(vibeStartHtml).toContain("전통적인 문법 암기");
    expect(vibeStartHtml).toContain("AI에게 줄 설명과 검증 기준");
    expect(vibeStartHtml).toContain("왜 소스를 보는가");
    expect(vibeStartHtml).toContain("AI는 일반 개발지식을 이미 가지고 있습니다");
    expect(vibeStartHtml).toContain("지식을 내장하기 위해서가 아니라");
    expect(vibeStartHtml).toContain("학습자는 문법을 암기하지 않고");
    expect(vibeStartHtml).toContain("AI 설명 요청 후보");
    expect(vibeStartHtml).not.toContain("맥락으로 설명하라고 지시하세요");
    expect(vibeStartHtml).toContain("vibe-coding-learning-boundary");
    expect(vibeStartHtml).toContain("배우지 않는 것");
    expect(vibeStartHtml).toContain("언어 문법 암기");
    expect(vibeStartHtml).toContain("라인별 코딩");
    expect(vibeStartHtml).toContain("반드시 배우는 것");
    expect(vibeStartHtml).toContain("AI에게 맡기는 것");
    expect(vibeStartHtml).toContain("실제 코딩은 AI가 담당");
    expect(vibeStartHtml).toContain("architecture-principle-playbook.html");
    expect(vibeStartHtml).toContain("source-to-build-interview.html");
    expect(vibeStartHtml).toContain("similar-app-transfer-map.html");
    expect(vibeStartHtml).toContain("learner-goal-alignment.html");
    expect(vibeStartHtml).toContain("ai-implementation-loop.html");
    expect(vibeStartHtml).toContain("learner-role-contract.html");
    expect(vibeStartHtml).toContain("ai-output-review-rubric.html");
    expect(vibeStartHtml).toContain("PASS_REVIEW, REVISE, BLOCK 검토 상태");
    expect(vibeStartHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보입니다");
    expect(vibeStartHtml).toContain("vibe-coding-mastery-checklist.html");
    expect(vibeStartHtml).toContain("READY_REVIEW / REVIEW / BLOCKED");
    expect(vibeStartHtml).toContain("READY_REVIEW도 학습자가 근거, 검증 기준, 검증 기록을 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(vibeStartHtml).toContain("vibe-coding-implementation-brief.html");
    expect(vibeStartHtml).toContain("검증 계획을 내 목표에 맞춘 구현 지시 후보로 압축합니다");
    expect(vibeStartHtml).toContain("AI에게 넘길 첫 구현 브리프를 내 목표에 맞게 다듬습니다");
    expect(vibeStartHtml).not.toContain("검증 계획을 내 목표에 맞춘 구현 지시서로 압축합니다");
    expect(vibeStartHtml).not.toContain("검증 계획을 한 장짜리 지시서로 압축합니다");
    expect(vibeStartHtml).not.toContain("복사용 구현 프롬프트를 한 장짜리 지시서로 압축합니다");
    expect(vibeStartHtml).not.toContain("AI에게 넘길 첫 구현 브리프를 복사합니다");
    expect(vibeStartHtml).toContain("ai-prompt-ab-lab.html");
    expect(vibeStartHtml).toContain("READY_REVIEW / REVISE / BLOCK");
    expect(vibeStartHtml).toContain("READY_REVIEW도 문제 설명, source evidence, acceptance criteria, verification assertion, 검증 기록 보고 형식을 학습자가 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다.");
    expect(vibeStartHtml).toContain("source-retention-guide.html");
    expect(vibeStartHtml).toContain("생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartHtml).toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰이 준비된 뒤에만 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartHtml).toContain("source-retention-guide.html에서 보존 증거, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤에만 생성된 세션 <code>source/</code> 스냅샷");
    expect(vibeStartHtml).not.toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰이 준비된 뒤에만 생성된 세션 <code>source/</code>");
    expect(vibeStartHtml).not.toContain("source-retention-guide.html에서 보존 증거, 세션 검증, 검증 기록, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤에만 생성된 세션 <code>source/</code> 스냅샷");
    expect(vibeStartHtml).not.toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 현재 학습 목표가 남았는지 확인한 뒤 생성된 세션 <code>source/</code>");
    expect(vibeStartHtml).not.toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 현재 학습 목표가 남았는지 확인한 뒤 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartHtml).not.toContain("보존 증거, daily summary, prompt pack, 검증 기록, 현재 학습 목표가 남았는지 확인한 뒤 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartHtml).not.toContain("source-retention-guide.html에서 보존 증거와 현재 학습 목표를 확인한 뒤 생성된 세션 <code>source/</code> 스냅샷");
    expect(vibeStartHtml).not.toContain("source-retention-guide.html에서 보존 증거, 세션 검증, 검증 기록, 현재 학습 목표를 확인한 뒤 생성된 세션 source/ 스냅샷");
    expect(vibeStartHtml).not.toContain("evidence, daily summary, prompt pack, verification이 남았는지 확인한 뒤 생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartHtml).not.toContain("evidence, daily summary, prompt pack, verification이 남았는지 확인한 뒤 생성된 세션 source/ 스냅샷");
    expect(vibeStartHtml).not.toContain("source-retention-guide.html을 보고 원본 source snapshot을 계속 둘지 판단합니다");
    expect(vibeStartHtml).not.toContain("evidence, daily summary, prompt pack, verification이 남았는지 확인한 뒤 정리합니다");
    expect(vibeStartHtml).toContain("소스 정리 전 체크포인트");
    expect(vibeStartHtml).toContain("정리 전에는 흡수한 기능 기록, 세션 검증과 검증 기록, 보존/정리 판단 가이드의 READY_REVIEW, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 함께 봅니다");
    expect(vibeStartHtml).not.toContain("정리 전에는 흡수한 기능 기록, 세션 검증과 검증 기록, 보존/정리 판단 가이드의 READY_REVIEW, 현재 학습 목표 확인, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 함께 봅니다");
    expect(vibeStartHtml).not.toContain("정리 전에 흡수한 기능 기록, 세션 검증과 검증 기록, 보존/정리 판단 가이드의 READY_REVIEW, 현재 학습 목표 확인을 함께 봅니다");
    expect(vibeStartHtml).toContain("보존/정리 판단 가이드의 READY_REVIEW");
    expect(vibeStartHtml).toContain("READY_REVIEW는 정리 검토 후보이지 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(vibeStartHtml).not.toContain("보존/삭제 판단 가이드가 READY_REVIEW");
    expect(vibeStartHtml).toContain("source-absorption-ledger.html");
    expect(vibeStartHtml).toContain("세션 검증 열기");
    expect(vibeStartHtml).toContain("보존 판단 열기");
    expect(vibeStartHtml).toContain("vibe-coding-prompt-pack.html");
    expect(vibeStartHtml).toContain("7. 검토 후 다듬을 첫 구현 프롬프트");
    expect(vibeStartHtml).toContain("vibe-coding-prompt-pack.html의 orient/architect/plan 단계를 검토 후 AI 요청 후보로 다듬습니다");
    expect(vibeStartHtml).not.toContain("7. 첫 구현 프롬프트");
    expect(vibeStartHtml).not.toContain("vibe-coding-prompt-pack.html의 orient/architect/plan 단계를 검토 후 AI에게 요청합니다");
    expect(vibeStartHtml).not.toContain("vibe-coding-prompt-pack.html의 orient/architect/plan 단계만 AI에게 먼저 요청합니다");
    expect(vibeStartHtml).toContain("session-verification.html");
    const vibeStartMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "vibe-coding-start.md"), "utf8");
    expect(vibeStartMarkdown).toContain("# 바이브코딩 우선 시작");
    expect(vibeStartMarkdown).toContain("왜 소스를 보는가");
    expect(vibeStartMarkdown).toContain("AI는 일반 개발지식을 이미 가지고 있습니다");
    expect(vibeStartMarkdown).toContain("지식을 내장하기 위해서가 아니라");
    expect(vibeStartMarkdown).toContain("학습자는 문법을 암기하지 않고");
    expect(vibeStartMarkdown).toContain("## 바이브코딩 학습 경계");
    expect(vibeStartMarkdown).toContain("배우지 않는 것: 언어 문법 암기");
    expect(vibeStartMarkdown).toContain("반드시 배우는 것: 목적, 아키텍처 이유");
    expect(vibeStartMarkdown).toContain("AI에게 맡기는 것: 실제 코딩은 AI가 담당");
    expect(vibeStartMarkdown).toContain("아키텍처 원리");
    expect(vibeStartMarkdown).toContain("소스-빌드 인터뷰");
    expect(vibeStartMarkdown).toContain("비슷한 앱 전이 지도");
    expect(vibeStartMarkdown).toContain("학습자 목표 정렬");
    expect(vibeStartMarkdown).toContain("AI 구현 대화 루프");
    expect(vibeStartMarkdown).toContain("학습자 역할 계약");
    expect(vibeStartMarkdown).toContain("AI 산출물 검토");
    expect(vibeStartMarkdown).toContain("PASS_REVIEW, REVISE, BLOCK 검토 상태");
    expect(vibeStartMarkdown).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보입니다");
    expect(vibeStartMarkdown).toContain("비슷한 앱 제작 준비도");
    expect(vibeStartMarkdown).toContain("READY_REVIEW / REVIEW / BLOCKED");
    expect(vibeStartMarkdown).toContain("READY_REVIEW도 학습자가 근거, 검증 기준, 검증 기록을 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(vibeStartMarkdown).toContain("## 7. 검토 후 다듬을 첫 구현 프롬프트");
    expect(vibeStartMarkdown).not.toContain("## 7. 첫 구현 프롬프트");
    expect(vibeStartMarkdown).toContain("구현 브리프");
    expect(vibeStartMarkdown).toContain("READY_REVIEW / REVISE / BLOCK");
    expect(vibeStartMarkdown).toContain("READY_REVIEW도 문제 설명, source evidence, acceptance criteria, verification assertion, 검증 기록 보고 형식을 학습자가 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다.");
    expect(vibeStartMarkdown).toContain("프롬프트 A/B 랩");
    expect(vibeStartMarkdown).toContain("원본 소스는 앱 지식으로 내장하지 않습니다");
    expect(vibeStartMarkdown).toContain("생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, `DELETE-SOURCE-SNAPSHOT` 확인 토큰이 준비된 뒤에만 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).not.toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 현재 학습 목표, 학습자 명시 확인, `DELETE-SOURCE-SNAPSHOT` 확인 토큰이 준비된 뒤에만 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).not.toContain("보존 증거, daily summary, prompt pack, 세션 검증, 검증 기록, 현재 학습 목표가 남았는지 확인한 뒤 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).not.toContain("보존 증거, daily summary, prompt pack, 검증 기록, 현재 학습 목표가 남았는지 확인한 뒤 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).not.toContain("생성된 세션 source/ 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).not.toContain("evidence, daily summary, prompt pack, verification이 남았는지 확인한 뒤 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다");
    expect(vibeStartMarkdown).not.toContain("evidence, daily summary, prompt pack, verification이 남았는지 확인한 뒤 정리합니다");
    expect(vibeStartMarkdown).toContain("소스 정리 전 체크포인트");
    expect(vibeStartMarkdown).toContain("정리 전에는 흡수한 기능 기록, 세션 검증과 검증 기록, 보존/정리 판단 가이드의 READY_REVIEW, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, `DELETE-SOURCE-SNAPSHOT` 확인 토큰을 함께 봅니다");
    expect(vibeStartMarkdown).not.toContain("정리 전에는 흡수한 기능 기록, 세션 검증과 검증 기록, 보존/정리 판단 가이드의 READY_REVIEW, 현재 학습 목표 확인, 학습자 명시 확인, `DELETE-SOURCE-SNAPSHOT` 확인 토큰을 함께 봅니다");
    expect(vibeStartMarkdown).not.toContain("정리 전에 네 가지를 확인합니다: 흡수한 기능이 기록됐는지, 세션 검증과 검증 기록이 남았는지, 보존/정리 판단 가이드가 READY_REVIEW인지, 현재 학습 목표에서 source 링크가 더 이상 필요 없는지입니다");
    expect(vibeStartMarkdown).toContain("소스 흡수 기록");
    expect(vibeStartMarkdown).toContain("세션 검증");
    expect(vibeStartMarkdown).toContain("보존/정리 판단 가이드의 READY_REVIEW");
    expect(vibeStartMarkdown).not.toContain("보존/삭제 판단 가이드가 READY_REVIEW");
    expect(learningPathHtml).toContain("project-activity.html");
    expect(learningPathHtml).toContain("code-metrics-readiness.html");
    expect(learningPathHtml).toContain("code-ownership-readiness.html");
    expect(learningPathHtml).toContain("large-asset-readiness.html");
    expect(learningPathHtml).toContain("license-rights.html");
    expect(learningPathHtml).toContain("sbom.html");
    expect(learningPathHtml).toContain("security-readiness.html");
    expect(learningPathHtml).toContain("sast-readiness.html");
    expect(learningPathHtml).toContain("dast-readiness.html");
    expect(learningPathHtml).toContain("threat-model-readiness.html");
    expect(learningPathHtml).toContain("scorecard.html");
    expect(learningPathHtml).toContain("provenance.html");
    expect(learningPathHtml).toContain("advisories.html");
    expect(learningPathHtml).toContain("vex.html");
    expect(learningPathHtml).toContain("policy-gates.html");
    expect(learningPathHtml).toContain("api-contracts.html");
    expect(learningPathHtml).toContain("observability.html");
    expect(learningPathHtml).toContain("performance.html");
    expect(learningPathHtml).toContain("profiling-readiness.html");
    expect(learningPathHtml).toContain("tracing-readiness.html");
    expect(learningPathHtml).toContain("debug-readiness.html");
    expect(learningPathHtml).toContain("crash-reporting-readiness.html");
    expect(learningPathHtml).toContain("incident-response-readiness.html");
    expect(learningPathHtml).toContain("slo-readiness.html");
    expect(learningPathHtml).toContain("cost-readiness.html");
    expect(learningPathHtml).toContain("progressive-delivery-readiness.html");
    expect(learningPathHtml).toContain("load-testing-readiness.html");
    expect(learningPathHtml).toContain("benchmark-readiness.html");
    expect(learningPathHtml).toContain("e2e.html");
    expect(learningPathHtml).toContain("flaky-test-readiness.html");
    expect(learningPathHtml).toContain("test-impact-readiness.html");
    expect(learningPathHtml).toContain("test-reporting-readiness.html");
    expect(learningPathHtml).toContain("snapshot-readiness.html");
    expect(learningPathHtml).toContain("property-based-testing-readiness.html");
    expect(learningPathHtml).toContain("fuzz-readiness.html");
    expect(learningPathHtml).toContain("test-data-readiness.html");
    expect(learningPathHtml).toContain("integration-test-environment-readiness.html");
    expect(learningPathHtml).toContain("chaos-engineering-readiness.html");
    expect(learningPathHtml).toContain("accessibility.html");
    expect(learningPathHtml).toContain("storybook.html");
    expect(learningPathHtml).toContain("design-tokens.html");
    expect(learningPathHtml).toContain("i18n.html");
    expect(learningPathHtml).toContain("release-readiness.html");
    expect(learningPathHtml).toContain("secret-readiness.html");
    expect(learningPathHtml).toContain("secret-management-readiness.html");
    expect(learningPathHtml).toContain("container-readiness.html");
    expect(learningPathHtml).toContain("container-scan-readiness.html");
    expect(learningPathHtml).toContain("code-quality.html");
    expect(learningPathHtml).toContain("documentation.html");
    expect(learningPathHtml).toContain("database-readiness.html");
    expect(learningPathHtml).toContain("database-migration-readiness.html");
    expect(learningPathHtml).toContain("database-orm-readiness.html");
    expect(learningPathHtml).toContain("data-transformation-readiness.html");
    expect(learningPathHtml).toContain("data-quality-readiness.html");
    expect(learningPathHtml).toContain("data-lineage-readiness.html");
    expect(learningPathHtml).toContain("data-catalog-readiness.html");
    expect(learningPathHtml).toContain("data-annotation-readiness.html");
    expect(learningPathHtml).toContain("lakehouse-table-readiness.html");
    expect(learningPathHtml).toContain("feature-store-readiness.html");
    expect(learningPathHtml).toContain("model-registry-readiness.html");
    expect(learningPathHtml).toContain("experiment-tracking-readiness.html");
    expect(learningPathHtml).toContain("model-monitoring-readiness.html");
    expect(learningPathHtml).toContain("model-serving-readiness.html");
    expect(learningPathHtml).toContain("model-training-readiness.html");
    expect(learningPathHtml).toContain("ci-cd.html");
    expect(learningPathHtml).toContain("unit-tests.html");
    expect(learningPathHtml).toContain("coverage-readiness.html");
    expect(learningPathHtml).toContain("mutation-testing-readiness.html");
    expect(learningPathHtml).toContain("typecheck-readiness.html");
    expect(learningPathHtml).toContain("package-manager.html");
    expect(learningPathHtml).toContain("git-hooks.html");
    expect(learningPathHtml).toContain("task-runner.html");
    expect(learningPathHtml).toContain("dependency-updates.html");
    expect(learningPathHtml).toContain("dependency-review-readiness.html");
    expect(learningPathHtml).toContain("lint-readiness.html");
    expect(learningPathHtml).toContain("format-readiness.html");
    expect(learningPathHtml).toContain("commit-conventions.html");
    expect(learningPathHtml).toContain("changelog-readiness.html");
    expect(learningPathHtml).toContain("bundle-analysis.html");
    expect(learningPathHtml).toContain("mocking-readiness.html");
    expect(learningPathHtml).toContain("data-fetching-readiness.html");
    expect(learningPathHtml).toContain("routing-readiness.html");
    expect(learningPathHtml).toContain("state-management-readiness.html");
    expect(learningPathHtml).toContain("form-readiness.html");
    expect(learningPathHtml).toContain("auth-readiness.html");
    expect(learningPathHtml).toContain("authorization-readiness.html");
    expect(learningPathHtml).toContain("payment-readiness.html");
    expect(learningPathHtml).toContain("email-readiness.html");
    expect(learningPathHtml).toContain("queue-readiness.html");
    expect(learningPathHtml).toContain("event-stream-readiness.html");
    expect(learningPathHtml).toContain("data-connector-readiness.html");
    expect(learningPathHtml).toContain("semantic-layer-readiness.html");
    expect(learningPathHtml).toContain("schema-registry-readiness.html");
    expect(learningPathHtml).toContain("stream-processing-readiness.html");
    expect(learningPathHtml).toContain("pipeline-orchestration-readiness.html");
    expect(learningPathHtml).toContain("service-mesh-readiness.html");
    expect(learningPathHtml).toContain("ingress-controller-readiness.html");
    expect(learningPathHtml).toContain("dns-readiness.html");
    expect(learningPathHtml).toContain("certificate-readiness.html");
    expect(learningPathHtml).toContain("helm-readiness.html");
    expect(learningPathHtml).toContain("admission-policy-readiness.html");
    expect(learningPathHtml).toContain("api-gateway-readiness.html");
    expect(learningPathHtml).toContain("cache-readiness.html");
    expect(learningPathHtml).toContain("logging-readiness.html");
    expect(learningPathHtml).toContain("feature-flag-readiness.html");
    expect(learningPathHtml).toContain("rate-limit-readiness.html");
    expect(learningPathHtml).toContain("error-tracking-readiness.html");
    expect(learningPathHtml).toContain("desktop-readiness.html");
    expect(learningPathHtml).toContain("iac-drift-readiness.html");
    expect(learningPathHtml).toContain("llm-eval-readiness.html");
    expect(learningPathHtml).toContain("llm-observability-readiness.html");
    expect(learningPathHtml).toContain("vector-db-readiness.html");
    expect(learningPathHtml).toContain("search-service-readiness.html");
    expect(learningPathHtml).toContain("object-storage-readiness.html");
    expect(learningPathHtml).toContain("realtime-collaboration-readiness.html");
    expect(learningPathHtml).toContain("workflow-orchestration-readiness.html");
    expect(learningPathHtml).toContain("openapi-client-readiness.html");
    expect(learningPathHtml).toContain("webhook-readiness.html");
    expect(learningPathHtml).toContain("notification-readiness.html");
    expect(learningPathHtml).toContain("consent-readiness.html");
    expect(learningPathHtml).toContain("privacy-readiness.html");
    expect(learningPathHtml).toContain("backup-readiness.html");
    expect(learningPathHtml).toContain("context-pack.html");
    expect(learningPathHtml).toContain("mcp-handoff.html");
    expect(learningPathHtml).toContain("agent-memory.html");
    expect(learningPathHtml).toContain("graph-query.html");
    expect(learningPathHtml).toContain("tutorial-abstractions.html");
    expect(learningPathHtml).toContain("decision-records.html");
    expect(learningPathHtml).toContain("dependency-health.html");
    const sessionVerificationHtml = await fs.readFile(path.join(result.session.outputPaths.html, "session-verification.html"), "utf8");
    expect(sessionVerificationHtml).toContain("이 페이지는 생성된 세션의 필수 산출물, HTML export 무결성, 소스 근거 링크를 검증합니다.");
    expect(sessionVerificationHtml).not.toContain("이 세션은 생성 완료 후 필수 산출물, HTML export 무결성, 소스 근거 링크를 검증합니다.");
    expect(sessionVerificationHtml).toContain("세션 검증");
    expect(sessionVerificationHtml).toContain("../analysis/session-verification-report.json");
    expect(sessionVerificationHtml).toContain("../markdown/session-verification.md");
    expect(sessionVerificationHtml).toContain("repo-tutor verify-session");
    const quizPrintHtml = await fs.readFile(path.join(result.session.outputPaths.html, "quiz-print.html"), "utf8");
    expect(quizPrintHtml).toContain("퀴즈 정답지");
    expect(quizPrintHtml).toContain("print-answer-key");
    expect(quizPrintHtml).toContain("코딩 문법 암기표가 아니라");
    expect(quizPrintHtml).toContain("AI 구현 지시 문장");
    expect(quizPrintHtml).toContain("<strong>정답:</strong>");
    expect(quizPrintHtml).toContain("<strong>해설:</strong>");
    expect(quizPrintHtml).toContain("생성된 세션 <code>source/</code> 스냅샷은 임시 프로젝트 근거");
    expect(quizPrintHtml).not.toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
    const wrongNotesHtml = await fs.readFile(path.join(result.session.outputPaths.html, "wrong-notes.html"), "utf8");
    expect(wrongNotesHtml).toContain("AI 지시 맥락 복습");
    expect(wrongNotesHtml).toContain("틀린 코드를 외우는 곳이 아니라");
    expect(wrongNotesHtml).toContain("AI에게 다시 설명할 항목");
    const quizHtml = await fs.readFile(path.join(result.session.outputPaths.html, "quiz.html"), "utf8");
    expect(quizHtml).toContain("quiz-reset-toolbar");
    expect(quizHtml).toContain("data-reset-quiz");
    expect(quizHtml).toContain("복습 초기화");
    expect(quizHtml).toContain("quiz-section-toolbar");
    expect(quizHtml).toContain("data-quiz-section-filter");
    expect(quizHtml).toContain("data-quiz-difficulty-filter");
    expect(quizHtml).toContain("data-quiz-section");
    expect(quizHtml).toContain("data-quiz-difficulty");
    expect(quizHtml).toContain("AI에게 설명할 때 가장 알맞은 지시 맥락");
    expect(quizHtml).toContain("언어 문법을 먼저 외우라고");
    expect(quizHtml).toContain("소스에서 확인한 역할을 AI에게 줄 목적, 책임, 검증 기준");
    expect(quizHtml).toContain("생성된 세션 <code>source/</code> 스냅샷은 임시 프로젝트 근거");
    expect(quizHtml).not.toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
    const appJs = await fs.readFile(path.join(result.session.outputPaths.html, "assets", "app.js"), "utf8");
    expect(appJs).toContain("[data-evidence-kind-filter]");
    expect(appJs).toContain("[data-quiz-section-filter]");
    expect(appJs).toContain("repotutor:learning-path");
    expect(appJs).toContain("learningProgress");
    expect(appJs).toContain("근거 검토 표시 ");
    expect(appJs).toContain("[data-reset-learning-progress]");
    expect(appJs).toContain("learningProgress.clear()");
    expect(appJs).toContain("[data-learning-progress-summary]");
    expect(appJs).toContain("updateLearningProgressSummary");
    expect(appJs).toContain("localStorage");
    expect(appJs).toContain("[data-reset-quiz]");
    expect(appJs).toContain("picked.clear()");
    expect(appJs).toContain("[data-download-mermaid]");
    const fileLessonsText = await fs.readFile(path.join(result.session.outputPaths.analysis, "file-lessons.json"), "utf8");
    expect(fileLessonsText).toContain("\"sourceRoleSummary\"");
    expect(fileLessonsText).toContain("\"architectureResponsibility\"");
    expect(fileLessonsText).toContain("\"aiPromptBrief\"");
    expect(fileLessonsText).toContain("파일 구현 요청 후보");
    expect(fileLessonsText).toContain("AI에게 보낼 문장으로 다듬으세요");
    expect(fileLessonsText).not.toContain("비슷한 파일을 만들게 할 때는");
    expect(fileLessonsText).toContain("\"vibeCodingPrompts\"");
    expect(fileLessonsText).toContain("파일 이해 요청 후보");
    expect(fileLessonsText).toContain("파일 검증 요청 후보");
    expect(fileLessonsText).not.toContain("구현시키는 프롬프트를");
    expect(fileLessonsText).toContain("\"verificationQuestions\"");
    expect(fileLessonsText).toContain("나는 전통 개발자가 아니라 바이브코딩 개발자야");
    expect(fileLessonsText).toContain("\"sourceEvidence\"");
    expect(fileLessonsText).toContain("\"snippet\"");
    const evidenceIndexText = await fs.readFile(path.join(result.session.outputPaths.analysis, "evidence-index-report.json"), "utf8");
    expect(evidenceIndexText).toContain("\"totalEvidenceItems\"");
    expect(evidenceIndexText).toContain("\"evidenceByKind\"");
    expect(evidenceIndexText).toContain("\"lessonHref\"");
    expect(evidenceIndexText).toContain("\"sourceHref\"");
    expect(evidenceIndexText).toContain("html/files.html#src-main.ts");
    expect(evidenceIndexText).toContain("source/src/main.ts");
    const sessionVerificationText = await fs.readFile(path.join(result.session.outputPaths.analysis, "session-verification-report.json"), "utf8");
    expect(sessionVerificationText).toContain("\"ok\": true");
    expect(sessionVerificationText).toContain("\"checkedRequiredArtifacts\"");
    expect(sessionVerificationText).toContain("\"evidenceIndex\"");
    const sessionVerificationMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "session-verification.md"), "utf8");
    expect(sessionVerificationMarkdown).toContain("# 세션 검증");
    expect(sessionVerificationMarkdown).toContain("상태: PASS");
    expect(sessionVerificationMarkdown).toContain("evidenceIndex: PASS");
    const evidenceVerification = await verifyEvidenceIndexReport(result.session.outputPaths.root);
    expect(evidenceVerification.ok).toBe(true);
    expect(evidenceVerification.checkedItems).toBeGreaterThan(0);
    expect(evidenceVerification.checkedSourceFiles).toBeGreaterThan(0);
    expect(evidenceVerification.checkedSourceLinks).toBeGreaterThan(0);
    expect(evidenceVerification.checkedLessonLinks).toBeGreaterThan(0);
    expect(evidenceVerification.failures).toHaveLength(0);
    const zip = await writeHtmlZipBundle(result.session.outputPaths.root);
    expect(zip.fileCount).toBeGreaterThan(5);
    expect(zip.bytes).toBeGreaterThan(100);
    const zipSignature = await fs.readFile(zip.zipPath).then((buffer) => buffer.subarray(0, 4).toString("hex"));
    expect(zipSignature).toBe("504b0304");
    const zipText = await fs.readFile(zip.zipPath, "latin1");
    expect(zipText).toContain("assets/component-graph.mmd");
    expect(zipText).toContain("assets/learning-path.tour.json");
    expect(zipText).toContain("assets/search-index.json");
    expect(zipText).toContain("assets/learning-journal-template.md");
    const exportVerification = await verifyHtmlExportManifest(result.session.outputPaths.root);
    expect(exportVerification.ok).toBe(true);
    expect(exportVerification.checkedFiles).toBeGreaterThan(5);
    expect(exportVerification.failures).toHaveLength(0);
    const sessionVerification = await verifyStudySessionArtifacts(result.session.outputPaths.root);
    expect(sessionVerification.ok).toBe(true);
    expect(sessionVerification.checks.session).toBe(true);
    expect(sessionVerification.checks.requiredArtifacts).toBe(true);
    expect(sessionVerification.checks.htmlExport).toBe(true);
    expect(sessionVerification.checks.evidenceIndex).toBe(true);
    expect(sessionVerification.checkedRequiredArtifacts).toBeGreaterThan(5);
    expect(sessionVerification.failures).toHaveLength(0);
    const sourcePrune = await writeSourcePrunePlan(result.session.outputPaths.root);
    expect(sourcePrune.plan.applyReady).toBe(true);
    expect(sourcePrune.plan.sourcePresent).toBe(true);
    expect(sourcePrune.plan.sourceFiles).toBeGreaterThan(0);
    expect(sourcePrune.plan.sourceBytes).toBeGreaterThan(0);
    expect(sourcePrune.plan.blockers).toHaveLength(0);
    expect(sourcePrune.plan.checks.preservedArtifactsOk).toBe(true);
    expect(sourcePrune.plan.checks.preservedEvidenceBundleOk).toBe(true);
    expect(sourcePrune.plan.recommendedAction).toContain("session verification, verification records, the preserved evidence bundle");
    expect(sourcePrune.plan.recommendedAction).toContain("generated session `source/` snapshot is a cleanup review candidate");
    expect(sourcePrune.plan.recommendedAction).toContain("learner explicit confirmation that source links no longer need to open for this learning goal");
    expect(sourcePrune.plan.recommendedAction).toContain("explicit `DELETE-SOURCE-SNAPSHOT` confirmation token");
    expect(sourcePrune.plan.recommendedAction).toContain("READY_REVIEW alone is not final ACCEPT, deployment, or cleanup permission");
    expect(sourcePrune.plan.recommendedAction).not.toContain("preserved evidence bundle and confirms source links");
    expect(sourcePrune.plan.recommendedAction).not.toContain("and learner confirmation that source links no longer need to open for this learning goal.");
    expect(sourcePrune.plan.recommendedAction).not.toContain("source snapshot is ready for explicit user-approved cleanup");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/learner-role-contract.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/architecture-principle-playbook.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/architecture-principle-playbook.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/source-to-build-interview.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/source-to-build-interview.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/similar-app-transfer-map.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/similar-app-transfer-map.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/learner-goal-alignment.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/learner-goal-alignment.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-implementation-loop.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-implementation-loop.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/learner-role-contract.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-output-review-rubric.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-output-review-rubric.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/vibe-coding-mastery-checklist.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/vibe-coding-mastery-checklist.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/vibe-coding-implementation-brief.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/vibe-coding-implementation-brief.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-prompt-readiness-checklist.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-prompt-readiness-checklist.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-prompt-ab-lab.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/ai-prompt-ab-lab.md");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/source-absorption-ledger.html");
    expect(sourcePrune.plan.preservedArtifacts.map((artifact) => artifact.path)).toContain("reference/source-retention-guide.html");
    expect(sourcePrune.plan.preservedEvidenceBundle.map((artifact) => artifact.path)).toContain("reference/source-absorption-ledger.html");
    expect(sourcePrune.plan.preservedEvidenceBundle.map((artifact) => artifact.path)).toContain("analysis/daily-summary-report.json");
    expect(sourcePrune.plan.preservedEvidenceBundle.map((artifact) => artifact.path)).toContain("html/vibe-coding-prompt-pack.html");
    expect(sourcePrune.plan.preservedEvidenceBundle.map((artifact) => artifact.path)).toContain("reference/vibe-coding-implementation-brief.html");
    expect(sourcePrune.plan.preservedEvidenceBundle.map((artifact) => artifact.path)).toContain("html/session-verification.html");
    expect(sourcePrune.plan.preservedEvidenceBundle.map((artifact) => artifact.path)).toContain("reference/source-retention-guide.html");
    expect(sourcePrune.plan.sourceKnowledgePolicy).toContain("temporary project evidence");
    expect(sourcePrune.plan.learnerCleanupDecision.sourcePurpose).toContain("project-specific evidence");
    expect(sourcePrune.plan.learnerCleanupDecision.applyWhen).toContain("Session verification, verification records, and the preserved evidence bundle all pass.");
    expect(sourcePrune.plan.learnerCleanupDecision.applyWhen).toContain("The learner explicitly confirms that source links no longer need to open for this learning goal.");
    expect(sourcePrune.plan.learnerCleanupDecision.holdWhen).toContain("Verification commands, verification records, or human review criteria are missing.");
    expect(sourcePrune.plan.learnerCleanupDecision.applyWhen).not.toContain("Session verification and the preserved evidence bundle both pass.");
    expect(sourcePrune.plan.learnerCleanupDecision.holdWhen).not.toContain("Verification commands or human review criteria are missing.");
    expect(sourcePrune.plan.learnerCleanupDecision.holdWhen).toContain("The learner has not explicitly confirmed that source links are no longer needed for the current learning goal.");
    expect(sourcePrune.plan.learnerCleanupDecision.retainedEvidence).toContain("html/vibe-coding-prompt-pack.html");
    await expect(fs.access(sourcePrune.reportPath)).resolves.toBeUndefined();
    await expect(fs.access(sourcePrune.markdownPath)).resolves.toBeUndefined();
    const sourcePruneReport = await fs.readFile(sourcePrune.reportPath, "utf8");
    expect(sourcePruneReport).toContain('"learnerCleanupDecision"');
    expect(sourcePruneReport).toContain('"sourceKnowledgePolicy"');
    expect(sourcePruneReport).toContain("learner explicit confirmation that source links no longer need to open for this learning goal");
    expect(sourcePruneReport).toContain("explicit `DELETE-SOURCE-SNAPSHOT` confirmation token");
    expect(sourcePruneReport).not.toContain("and learner confirmation that source links no longer need to open for this learning goal.");
    const sourcePruneMarkdown = await fs.readFile(sourcePrune.markdownPath, "utf8");
    expect(sourcePruneMarkdown).toContain("# RepoTutor Source Prune Plan");
    expect(sourcePruneMarkdown).toContain("Status: READY_REVIEW");
    expect(sourcePruneMarkdown).toContain("Status: READY_REVIEW (review candidate, token-required before cleanup)");
    expect(sourcePruneMarkdown).toContain("READY_REVIEW is a learner cleanup review candidate");
    expect(sourcePruneMarkdown).toContain("not final ACCEPT, deployment, or deletion permission");
    expect(sourcePruneMarkdown).toContain("## Preserved Evidence Bundle");
    expect(sourcePruneMarkdown).toContain("이 묶음은 생성된 세션 `source/` 스냅샷 정리 전 반드시 남아야 합니다");
    expect(sourcePruneMarkdown.replace(/\s+/g, " ")).toContain("사용자 원본 소스를 장기 앱 지식으로 보관하지 않아도");
    expect(sourcePruneMarkdown).toContain("AI에게 다시 지시할 수 있게 만드는 최소 증거입니다");
    expect(sourcePruneMarkdown).toContain("## 학습자 정리 판단");
    expect(sourcePruneMarkdown).toContain("생성된 세션 `source/` 스냅샷은 AI에게 개발 지식을 새로 가르치는 내장 데이터가 아닙니다");
    expect(sourcePruneMarkdown).not.toContain("원본 source/는 AI에게 개발 지식을 새로 가르치는 내장 데이터가 아닙니다");
    expect(sourcePruneMarkdown).toContain("AI에게 보낼 프롬프트가 source evidence와 acceptance criteria를 포함하나요?");
    expect(sourcePruneMarkdown).toContain("세션 검증, 검증 기록, 보존 증거 묶음이 모두 PASS인가요?");
    expect(sourcePruneMarkdown).not.toContain("세션 검증과 보존 증거 묶음이 PASS인가요?");
    expect(sourcePruneMarkdown).toContain("source 링크가 더 이상 열리지 않아도 학습자가 비슷한 앱을 지시하고 리뷰할 수 있나요?");
    expect(sourcePruneMarkdown).toContain("정리 전 명시 확인");
    expect(sourcePruneMarkdown).toContain("source 링크가 더 이상 열리지 않아도 된다고 직접 확인했나요?");
    expect(sourcePruneMarkdown).toContain("학습자의 명시 확인이 없으면 READY_REVIEW 상태여도 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(sourcePruneMarkdown).toContain("learner explicit confirmation that source links no longer need to open for this learning goal");
    expect(sourcePruneMarkdown).toContain("explicit `DELETE-SOURCE-SNAPSHOT` confirmation token");
    expect(sourcePruneMarkdown).toContain("READY_REVIEW alone is not final ACCEPT, deployment, or cleanup permission");
    expect(sourcePruneMarkdown).not.toContain("and learner confirmation that source links no longer need to open for this learning goal.");
    await expect(fs.access(result.session.outputPaths.source)).resolves.toBeUndefined();
    await fs.appendFile(path.join(result.session.outputPaths.html, "index.html"), "\n<!-- tampered -->\n");
    const tamperedVerification = await verifyHtmlExportManifest(result.session.outputPaths.root);
    expect(tamperedVerification.ok).toBe(false);
    expect(tamperedVerification.failures[0]?.path).toBe("html/index.html");
    await fs.rm(path.join(result.session.outputPaths.source, "src", "main.ts"));
    const missingSourceVerification = await verifyEvidenceIndexReport(result.session.outputPaths.root);
    expect(missingSourceVerification.ok).toBe(false);
    expect(missingSourceVerification.failures.some((failure) => failure.reason === "missing-source-path" && failure.path === "source/src/main.ts")).toBe(true);
    const failedSessionVerification = await verifyStudySessionArtifacts(result.session.outputPaths.root);
    expect(failedSessionVerification.ok).toBe(false);
    expect(failedSessionVerification.checks.htmlExport).toBe(false);
    expect(failedSessionVerification.checks.evidenceIndex).toBe(false);
    expect(failedSessionVerification.failures.some((failure) => failure.check === "html-export" && failure.path === "html/index.html")).toBe(true);
    expect(failedSessionVerification.failures.some((failure) => failure.check === "evidence-index" && failure.path === "source/src/main.ts")).toBe(true);
    const quizText = await fs.readFile(path.join(result.session.outputPaths.analysis, "quiz.json"), "utf8");
    expect(quizText).toContain("\"choices\"");
    expect(quizText).toContain("\"aiPromptUse\"");
    expect(quizText).toContain("\"aiReviewQuestion\"");
    expect(quizText).toContain("\"memorizationWarning\"");
    expect(quizText).toContain("AI에게 설명할 때 가장 알맞은 지시 맥락");
    expect(quizText).toContain("목적, 책임, 연결된 검증 기준을 AI 지시 맥락");
    expect(quizText).toContain("AI 지시 문장:");
    expect(quizText).toContain("AI 출력 리뷰 질문:");
    expect(quizText).toContain("외우지 말 것:");
    expect(quizText).toContain("AI 결과를 검토할 질문");
    expect(quizText).not.toContain("AI 결과를 판정할 질문");
    expect(quizText).toContain("언어 문법을 먼저 외우라고");
    expect(quizText).toContain("원본 소스를 앱에 영구 내장 지식");
    expect(quizText).toContain("사용자 원본 소스는 앱 지식이나 정리 대상이 아닙니다");
    expect(quizText).toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
    expect(quizText).toContain("흡수한 학습 산출물, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 남긴 뒤");
    expect(quizText).not.toContain("흡수한 학습 산출물, 세션 검증, 검증 기록, 현재 학습 목표 확인, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 남긴 뒤");
    expect(quizText).not.toContain("흡수한 학습 산출물, 세션 검증, 검증 기록, 현재 학습 목표 확인을 남긴 뒤");
    expect(quizText).not.toContain("생성된 세션 <code>source/</code> 스냅샷은 임시 프로젝트 근거");
    expect(quizText).not.toContain("흡수한 학습 산출물과 검증 기록을 남긴 뒤");
    expect(quizText).toContain("정리 검토 여부를 판단해야 합니다");
    expect(quizText).not.toContain("원본 source snapshot은 임시 프로젝트 근거");
    expect(quizText).toContain("검증 기준 없이 AI에게 전체 구현");
    expect(quizText).toContain("문법 암기가 아니라");
    const wrongNotesMarkdown = await fs.readFile(path.join(result.session.outputPaths.markdown, "wrong-notes.md"), "utf8");
    expect(wrongNotesMarkdown).toContain("틀린 코드를 외우는 기록이 아니라");
    expect(wrongNotesMarkdown).toContain("AI에게 어떤 목적, 책임, 검증 기준");
  }, 10000);

  it("applies source prune only after the preserved artifact gate passes", async () => {
    const studiesRoot = await fs.mkdtemp(path.join(os.tmpdir(), "repotutor-source-prune-apply-"));
    const result = await runStudy({ source: fixtureRoot, mode: "quick", level: "beginner", studiesRoot, enableCodex: false });
    const sourcePruneSource = await fs.readFile(sourcePruneSourcePath, "utf8");
    expect(sourcePruneSource).toContain("Refusing to prune a path that is not inside the generated session `source/` snapshot.");
    expect(sourcePruneSource).not.toContain("Refusing to prune a path that is not the session source snapshot.");
    await expect(fs.access(result.session.outputPaths.source)).resolves.toBeUndefined();
    await expect(applySourcePrunePlan(result.session.outputPaths.root)).rejects.toThrow("source prune apply requires confirm DELETE-SOURCE-SNAPSHOT.");
    const applied = await applySourcePrunePlan(result.session.outputPaths.root, { confirm: "DELETE-SOURCE-SNAPSHOT" });
    expect(applied.plan.applyReady).toBe(true);
    expect(applied.apply.applied).toBe(true);
    expect(applied.apply.removedFiles).toBeGreaterThan(0);
    expect(applied.apply.removedBytes).toBeGreaterThan(0);
    expect(applied.apply.sourceKnowledgePolicy).toContain("temporary project evidence");
    expect(applied.apply.learnerCleanupDecision.sourcePurpose).toContain("project-specific evidence");
    expect(applied.apply.learnerCleanupDecision.applyWhen).toContain("Session verification, verification records, and the preserved evidence bundle all pass.");
    expect(applied.apply.learnerCleanupDecision.applyWhen).toContain("The learner explicitly confirms that source links no longer need to open for this learning goal.");
    expect(applied.apply.learnerCleanupDecision.holdWhen).toContain("Verification commands, verification records, or human review criteria are missing.");
    expect(applied.apply.learnerCleanupDecision.applyWhen).not.toContain("Session verification and the preserved evidence bundle both pass.");
    expect(applied.apply.learnerCleanupDecision.holdWhen).not.toContain("Verification commands or human review criteria are missing.");
    expect(applied.apply.learnerCleanupDecision.holdWhen).toContain("The learner has not explicitly confirmed that source links are no longer needed for the current learning goal.");
    expect(applied.apply.learnerCleanupDecision.retainedEvidence).toContain("html/vibe-coding-prompt-pack.html");
    expect(applied.apply.retainedLearningArtifacts.map((artifact) => artifact.path)).toContain("reference/source-absorption-ledger.html");
    expect(applied.apply.retainedLearningArtifacts.map((artifact) => artifact.path)).toContain("html/vibe-coding-prompt-pack.html");
    await expect(fs.access(result.session.outputPaths.source)).rejects.toThrow();
    await expect(fs.access(applied.apply.applyReportPath)).resolves.toBeUndefined();
    await expect(fs.access(applied.apply.tombstonePath)).resolves.toBeUndefined();
    const applyReportText = await fs.readFile(applied.apply.applyReportPath, "utf8");
    expect(applyReportText).toContain('"sourceKnowledgePolicy"');
    expect(applyReportText).toContain('"learnerCleanupDecision"');
    expect(applyReportText).toContain('"holdWhen"');
    expect(applyReportText).toContain('"retainedLearningArtifacts"');
    const tombstone = await fs.readFile(applied.apply.tombstonePath, "utf8");
    const normalizedTombstone = tombstone.replace(/\s+/g, " ");
    expect(tombstone).toContain("# Generated Session `source/` Snapshot Pruned");
    expect(tombstone).not.toContain("# Source Snapshot Pruned");
    expect(normalizedTombstone).toContain("The generated session `source/` snapshot for this study session was cleaned up");
    expect(tombstone).not.toContain("The original source snapshot for this study session was removed");
    expect(tombstone.replace(/\s+/g, " ")).toContain("Source links in older evidence pages may no longer open");
    expect(tombstone).toContain("The core apply guard requires the explicit `DELETE-SOURCE-SNAPSHOT` token.");
    expect(tombstone).toContain("confirmed the preserved evidence bundle");
    expect(tombstone).toContain("explicitly confirmed that source links no longer need to open for the current");
    expect(tombstone).not.toContain("and\nconfirmed that source links no longer need to open for the current learning goal.");
    expect(tombstone).toContain("## 정리 판단 조건");
    expect(tombstone).toContain("### 정리 검토 조건");
    expect(tombstone).not.toContain("### 정리 가능 조건");
    expect(tombstone).toContain("### 정리 보류 조건");
    expect(tombstone).toContain("생성된 세션 `source/` 스냅샷은 특정 프로젝트 맥락을 뽑기 위한 임시 증거였고");
    expect(tombstone).toContain("## 남긴 학습 자산");
    expect(tombstone).toContain("생성된 세션 `source/` 스냅샷은 AI에게 개발 지식을 새로 가르치는 내장 데이터가 아니라");
    expect(tombstone).not.toContain("원본 source/는 AI에게 개발 지식을 새로 가르치는 내장 데이터가 아니라");
    expect(tombstone).toContain("정리 후 학습은 보존된 리포트와 프롬프트를");
    expect(tombstone).not.toContain("삭제 후 학습");
    expect(tombstone).toContain("vibe-coding-prompt-pack: 비슷한 앱을 AI에게 지시할 프롬프트 확인");
    expect(tombstone).toContain("source-retention-guide: 생성된 세션 `source/` 스냅샷 재확인 필요 여부 판단");
    expect(tombstone).toContain("## 다시 소스가 필요한 경우");
    expect(tombstone).toContain("같은 원본 저장소나 폴더에서 새 세션");
    expect(normalizedTombstone).toContain("`source/` 스냅샷을 다시 생성해 분석합니다");
    expect(normalizedTombstone).toContain("사용자 원본 소스 전체를 장기 앱 지식으로 보관하지 않습니다");
    const prunedEvidenceVerification = await verifyEvidenceIndexReport(result.session.outputPaths.root);
    expect(prunedEvidenceVerification.ok).toBe(true);
    expect(prunedEvidenceVerification.sourcePruned).toBe(true);
    expect(prunedEvidenceVerification.skippedPrunedSourceLinks).toBeGreaterThan(0);
    const prunedSessionVerification = await verifyStudySessionArtifacts(result.session.outputPaths.root);
    expect(prunedSessionVerification.ok).toBe(true);
    expect(prunedSessionVerification.checks.evidenceIndex).toBe(true);
    const alreadyPrunedPlan = await writeSourcePrunePlan(result.session.outputPaths.root);
    expect(alreadyPrunedPlan.plan.sourcePresent).toBe(false);
    expect(alreadyPrunedPlan.plan.sourcePruned).toBe(true);
    expect(alreadyPrunedPlan.plan.applyReady).toBe(false);
    expect(alreadyPrunedPlan.plan.blockers).toHaveLength(0);
    expect(alreadyPrunedPlan.plan.recommendedAction).toContain("Already cleaned up");
    expect(alreadyPrunedPlan.plan.recommendedAction).toContain("generated session `source/` snapshot");
    expect(alreadyPrunedPlan.plan.recommendedAction).not.toContain("source snapshot has been explicitly removed");
  }, 10000);

  it("accepts inline learner brief text for desktop goal alignment", async () => {
    const studiesRoot = await fs.mkdtemp(path.join(os.tmpdir(), "repotutor-inline-learner-brief-"));
    const learnerBriefText = [
      "# Desktop Similar App Prompt",
      "User: vibe-coding learner.",
      "Problem: build a RepoTutor-like source learning app.",
      "Architecture: keep scanner, HTML reports, CLI, desktop UI, and verification separate.",
      "First slice: input a GitHub source and produce architecture terms plus a copyable prompt.",
      "Acceptance criteria: learner goal alignment says whether my prompt is ready.",
      "Verify: run typecheck and keep unverified runtime claims as BLOCK."
    ].join("\n");
    const result = await runStudy({ source: fixtureRoot, mode: "quick", level: "beginner", studiesRoot, enableCodex: false, learnerBriefText });
    const copiedBrief = await fs.readFile(path.join(result.session.outputPaths.root, "inputs", "learner-brief.md"), "utf8");
    expect(copiedBrief).toContain("Desktop Similar App Prompt");
    const learnerGoalAlignmentHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "learner-goal-alignment.html"), "utf8");
    expect(learnerGoalAlignmentHtml).toContain("learner-brief: provided");
    expect(learnerGoalAlignmentHtml).toContain("Desktop Similar App Prompt");
    expect(learnerGoalAlignmentHtml).toContain("ACCEPT_REVIEW / CLARIFY / REWRITE / BLOCK");
    expect(learnerGoalAlignmentHtml).toContain("ACCEPT_REVIEW는 구현 시작, 최종 ACCEPT, 배포, 삭제 허가가 아니라");
  }, 10000);

  it("creates teaching workspace state and appends learning records only after quiz evidence", async () => {
    const studiesRoot = await fs.mkdtemp(path.join(os.tmpdir(), "repotutor-teach-workspace-"));
    const learnerBriefPath = path.join(studiesRoot, "learner-brief.md");
    await fs.writeFile(learnerBriefPath, [
      "# Similar App PRD",
      "User: solo vibe-coding learner.",
      "Problem: turn a source repo into an AI-guided learning app.",
      "Architecture: split scanner, report generator, CLI, and verification responsibilities.",
      "First slice: import one source folder and produce one source-grounded prompt.",
      "Source file evidence: src/main.ts and package.json should be referenced.",
      "Acceptance criteria: generated report shows purpose, source evidence, and verification boundaries.",
      "Verify: run typecheck and mark unrun checks as BLOCK.",
      "AI role: implement repetitive code while I review architecture and product decisions."
    ].join("\n"));
    const result = await runStudy({ source: fixtureRoot, mode: "quick", level: "beginner", studiesRoot, enableCodex: false, learnerBriefPath });
    await expect(fs.access(path.join(result.session.outputPaths.root, "inputs", "learner-brief.md"))).resolves.toBeUndefined();
    const copiedBrief = await fs.readFile(path.join(result.session.outputPaths.root, "inputs", "learner-brief.md"), "utf8");
    expect(copiedBrief).toContain("Similar App PRD");
    const recordsDir = path.join(result.session.outputPaths.root, "learning-records");
    const initialRecords = await fs.readdir(recordsDir);
    expect(initialRecords).toEqual(["README.md"]);
    const sourceRetentionTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "source-retention-guide");
    expect(sourceRetentionTarget).toMatchObject({
      fileName: "../reference/source-retention-guide.html",
      title: "소스 보존 판단",
      description: "원본 소스를 앱 지식으로 내장하지 않고 보존 증거, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 남긴 뒤 생성된 세션 source/ 스냅샷 정리 검토 후보로 둘지 판단합니다.",
      terminalCommand: "repo-tutor open <session> --target source-retention-guide"
    });
    expect(sourceRetentionTarget?.description).not.toContain("무엇을 남긴 뒤 생성된 세션 source/ 스냅샷");
    expect(sourceRetentionTarget?.description).not.toContain("source snapshot을 정리할지");
    const vibeStartTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "vibe-coding-start");
    expect(vibeStartTarget).toMatchObject({
      fileName: "vibe-coding-start.html",
      title: "바이브코딩 시작",
      terminalCommand: "repo-tutor open <session> --target vibe-coding-start"
    });
    const sourceAbsorptionTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "source-absorption-ledger");
    expect(sourceAbsorptionTarget).toMatchObject({
      fileName: "../reference/source-absorption-ledger.html",
      title: "소스 흡수 기록",
      terminalCommand: "repo-tutor open <session> --target source-absorption-ledger"
    });
    const architecturePrincipleTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "architecture-principle-playbook");
    expect(architecturePrincipleTarget).toMatchObject({
      fileName: "../reference/architecture-principle-playbook.html",
      title: "아키텍처 원리",
      terminalCommand: "repo-tutor open <session> --target architecture-principle-playbook"
    });
    const sourceToBuildInterviewTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "source-to-build-interview");
    expect(sourceToBuildInterviewTarget).toMatchObject({
      fileName: "../reference/source-to-build-interview.html",
      title: "소스-빌드 인터뷰",
      terminalCommand: "repo-tutor open <session> --target source-to-build-interview"
    });
    const similarAppTransferTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "similar-app-transfer-map");
    expect(similarAppTransferTarget).toMatchObject({
      fileName: "../reference/similar-app-transfer-map.html",
      title: "비슷한 앱 전이 지도",
      terminalCommand: "repo-tutor open <session> --target similar-app-transfer-map"
    });
    const learnerGoalAlignmentTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "learner-goal-alignment");
    expect(learnerGoalAlignmentTarget).toMatchObject({
      fileName: "../reference/learner-goal-alignment.html",
      title: "학습자 목표 정렬",
      terminalCommand: "repo-tutor open <session> --target learner-goal-alignment"
    });
    const aiImplementationLoopTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "ai-implementation-loop");
    expect(aiImplementationLoopTarget).toMatchObject({
      fileName: "../reference/ai-implementation-loop.html",
      title: "AI 구현 대화 루프",
      terminalCommand: "repo-tutor open <session> --target ai-implementation-loop"
    });
    const learnerRoleTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "learner-role-contract");
    expect(learnerRoleTarget).toMatchObject({
      fileName: "../reference/learner-role-contract.html",
      title: "학습자 역할 계약",
      terminalCommand: "repo-tutor open <session> --target learner-role-contract"
    });
    const aiOutputReviewTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "ai-output-review-rubric");
    expect(aiOutputReviewTarget).toMatchObject({
      fileName: "../reference/ai-output-review-rubric.html",
      title: "AI 산출물 검토",
      description: "AI가 만든 결과를 목적, 아키텍처, 근거, 검증 기준으로 PASS_REVIEW/REVISE/BLOCK 검토 후보로 확인합니다. PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보입니다.",
      terminalCommand: "repo-tutor open <session> --target ai-output-review-rubric"
    });
    expect(aiOutputReviewTarget?.description).not.toContain("PASS/REVISE/BLOCK 판정합니다");
    const masteryChecklistTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "vibe-coding-mastery-checklist");
    expect(masteryChecklistTarget).toMatchObject({
      fileName: "../reference/vibe-coding-mastery-checklist.html",
      title: "바이브코딩 숙련도",
      terminalCommand: "repo-tutor open <session> --target vibe-coding-mastery-checklist"
    });
    const implementationBriefTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "vibe-coding-implementation-brief");
    expect(implementationBriefTarget).toMatchObject({
      fileName: "../reference/vibe-coding-implementation-brief.html",
      title: "구현 브리프",
      terminalCommand: "repo-tutor open <session> --target vibe-coding-implementation-brief"
    });
    const promptReadinessTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "ai-prompt-readiness-checklist");
    expect(promptReadinessTarget).toMatchObject({
      fileName: "../reference/ai-prompt-readiness-checklist.html",
      title: "프롬프트 준비도",
      terminalCommand: "repo-tutor open <session> --target ai-prompt-readiness-checklist"
    });
    const promptAbLabTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "ai-prompt-ab-lab");
    expect(promptAbLabTarget).toMatchObject({
      fileName: "../reference/ai-prompt-ab-lab.html",
      title: "프롬프트 A/B 랩",
      terminalCommand: "repo-tutor open <session> --target ai-prompt-ab-lab"
    });
    const quizTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "quiz");
    expect(quizTarget).toMatchObject({
      fileName: "quiz.html",
      title: "퀴즈",
      description: "소스 역할을 AI에게 줄 목적, 책임, 검증 기준으로 바꿀 수 있는지 확인합니다.",
      terminalCommand: "repo-tutor open <session> --target quiz"
    });
    const wrongNotesTarget = CORE_LEARNING_REPORT_TARGETS.find((target) => target.target === "wrong-notes");
    expect(wrongNotesTarget).toMatchObject({
      fileName: "wrong-notes.html",
      title: "오답노트",
      description: "틀린 개념을 AI에게 다시 설명할 목적, 책임, 검증 기준으로 바꿉니다.",
      terminalCommand: "repo-tutor open <session> --target wrong-notes"
    });
    const learnerRoleHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "learner-role-contract.html"), "utf8");
    expect(learnerRoleHtml).toContain("바이브코딩 학습자 역할 계약");
    expect(learnerRoleHtml).toContain("AI에게 맡겨도 되는 것");
    expect(learnerRoleHtml).toContain("이 세션에서 목표가 아닌 것");
    expect(learnerRoleHtml).toContain("소스는 AI를 훈련시키는 데이터가 아니라");
    expect(learnerRoleHtml).toContain("프로젝트 맥락을 뽑는 증거입니다");
    expect(learnerRoleHtml).toContain("구체적인 코드 문법과 반복 구현은 AI가 맡아도 돼");
    expect(learnerRoleHtml).toContain("수락/검증 기준을 먼저 분리해줘");
    expect(learnerRoleHtml).not.toContain("완료 기준을 먼저 분리해줘");
    expect(learnerRoleHtml).toContain("검토 후 다듬을 역할 프롬프트");
    expect(learnerRoleHtml).not.toContain("복사용 역할 프롬프트");
    const learnerRoleMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "learner-role-contract.md"), "utf8");
    expect(learnerRoleMarkdown).toContain("# 바이브코딩 학습자 역할 계약");
    expect(learnerRoleMarkdown).toContain("손코딩 문법 암기 수업이 아닙니다");
    expect(learnerRoleMarkdown).not.toContain("전문 개발자가 되기 위한 문법 수업이 아닙니다");
    expect(learnerRoleMarkdown).toContain("소스는 AI를 훈련시키는 데이터가 아니라");
    expect(learnerRoleMarkdown).toContain("수락/검증 기준을 먼저 분리해줘");
    expect(learnerRoleMarkdown).not.toContain("완료 기준을 먼저 분리해줘");
    expect(learnerRoleMarkdown).toContain("## 검토 후 다듬을 역할 프롬프트");
    const aiOutputReviewHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-output-review-rubric.html"), "utf8");
    expect(aiOutputReviewHtml).toContain("AI 산출물 검토 루브릭");
    expect(aiOutputReviewHtml).toContain("PASS_REVIEW");
    expect(aiOutputReviewHtml).toContain("REVISE");
    expect(aiOutputReviewHtml).toContain("BLOCK");
    expect(aiOutputReviewHtml).toContain("통과 후보·수정·차단 상태");
    expect(aiOutputReviewHtml).toContain("리뷰 검토 상태");
    expect(aiOutputReviewHtml).toContain("최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewHtml).toContain("목적과 구조가 맞고 검증 기준이 분리된 검토 후보입니다. 근거와 검증 기록 확인 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewHtml).toContain("최종 ACCEPT 여부 검토는 학습자에게 남깁니다");
    expect(aiOutputReviewHtml).toContain("PASS_REVIEW도 근거와 검증 기록 확인 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewHtml).toContain("PASS_REVIEW / REVISE / BLOCK");
    expect(aiOutputReviewHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라");
    expect(aiOutputReviewHtml).toContain("바이브코딩 리뷰어처럼 목적, 근거, 검증 기준으로 검토해줘");
    expect(aiOutputReviewHtml).toContain("맥락이나 수락/검증 기준을 보강해야 합니다");
    expect(aiOutputReviewHtml).toContain("검증 기록 없이 수락을 요구하거나 실패 시 보고 형식이 없습니다");
    expect(aiOutputReviewHtml).not.toContain("맥락이나 완료 기준을 보강해야 합니다");
    expect(aiOutputReviewHtml).not.toContain("테스트나 실행 없이 완료를 단정하거나 실패 시 보고 형식이 없습니다");
    expect(aiOutputReviewHtml).not.toContain("전문 개발자 리뷰어처럼 검토해줘");
    expect(aiOutputReviewHtml).not.toContain("목적과 구조가 맞고 검증 기준이 분리된 검토 후보입니다. 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewHtml).not.toContain("최종 통과 여부 검토");
    expect(aiOutputReviewHtml).not.toContain("<strong>PASS:</strong>");
    expect(aiOutputReviewHtml).not.toContain("리뷰 판정");
    expect(aiOutputReviewHtml).not.toContain("PASS / REVISE / BLOCK으로 나눠줘");
    expect(aiOutputReviewHtml).toContain("검토 후 다듬을 리뷰 프롬프트");
    expect(aiOutputReviewHtml).not.toContain("복사용 리뷰 프롬프트");
    const aiOutputReviewMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-output-review-rubric.md"), "utf8");
    expect(aiOutputReviewMarkdown).toContain("# AI 산출물 검토 루브릭");
    expect(aiOutputReviewMarkdown).toContain("목적, 아키텍처, 근거, 검증 기준");
    expect(aiOutputReviewMarkdown).toContain("통과 후보·수정·차단 상태");
    expect(aiOutputReviewMarkdown).toContain("## 리뷰 검토 상태");
    expect(aiOutputReviewMarkdown).toContain("- PASS_REVIEW:");
    expect(aiOutputReviewMarkdown).toContain("최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewMarkdown).toContain("목적과 구조가 맞고 검증 기준이 분리된 검토 후보입니다. 근거와 검증 기록 확인 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewMarkdown).toContain("최종 ACCEPT 여부 검토는 학습자에게 남깁니다");
    expect(aiOutputReviewMarkdown).toContain("PASS_REVIEW도 근거와 검증 기록 확인 후보일 뿐 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK");
    expect(aiOutputReviewMarkdown).toContain("맥락이나 수락/검증 기준을 보강해야 합니다");
    expect(aiOutputReviewMarkdown).toContain("검증 기록 없이 수락을 요구하거나 실패 시 보고 형식이 없습니다");
    expect(aiOutputReviewMarkdown).not.toContain("맥락이나 완료 기준을 보강해야 합니다");
    expect(aiOutputReviewMarkdown).not.toContain("테스트나 실행 없이 완료를 단정하거나 실패 시 보고 형식이 없습니다");
    expect(aiOutputReviewMarkdown).not.toContain("목적과 구조가 맞고 검증 기준이 분리된 검토 후보입니다. 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(aiOutputReviewMarkdown).not.toContain("최종 통과 여부 검토");
    expect(aiOutputReviewMarkdown).not.toContain("- PASS:");
    expect(aiOutputReviewMarkdown).not.toContain("## 리뷰 판정");
    expect(aiOutputReviewMarkdown).not.toContain("PASS / REVISE / BLOCK으로 나눠줘");
    expect(aiOutputReviewMarkdown).toContain("## 검토 후 다듬을 리뷰 프롬프트");
    const masteryChecklistHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "vibe-coding-mastery-checklist.html"), "utf8");
    expect(masteryChecklistHtml).toContain("바이브코딩 숙련도 체크리스트");
    expect(masteryChecklistHtml).toContain("비슷한 앱 제작 준비도");
    expect(masteryChecklistHtml).toContain("READY_REVIEW");
    expect(masteryChecklistHtml).toContain("REVIEW");
    expect(masteryChecklistHtml).toContain("BLOCKED");
    expect(masteryChecklistHtml).toContain("AI는 이미 일반 개발 지식이 있습니다");
    expect(masteryChecklistHtml).toContain("학습자가 근거, 검증 기준, 검증 기록을 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(masteryChecklistHtml).not.toContain("학습자가 근거와 검증 기준을 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(masteryChecklistHtml).toContain("READY_REVIEW / REVIEW / BLOCKED");
    expect(masteryChecklistHtml).toContain("준비도 검토표");
    expect(masteryChecklistHtml).toContain("READY_REVIEW도 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 학습자가 근거, 검증 기준, 검증 기록을 확인할 후보 상태로만 표시해줘");
    expect(masteryChecklistHtml).not.toContain("READY_REVIEW도 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 학습자가 근거와 검증 기준을 확인할 후보 상태로만 표시해줘");
    expect(masteryChecklistHtml).toContain("요청 후보를 검토 후 다듬을 수 있습니다");
    expect(masteryChecklistHtml).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태 후보를 만들 수 있습니다");
    expect(masteryChecklistHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보로만 표시해줘");
    expect(masteryChecklistHtml).toContain("<pre>생성된 세션 source/ 스냅샷 정리를 검토해도 되는지, 보존 증거 묶음, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰");
    expect(masteryChecklistHtml).not.toContain("<pre>생성된 세션 source/ 스냅샷 정리를 검토해도 되는지, 보존 증거 묶음, 세션 검증, 검증 기록, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰");
    expect(masteryChecklistHtml).not.toContain("<pre>생성된 세션 source/ 스냅샷 정리를 검토해도 되는지, 남은 산출물과 끊기는 evidence link");
    expect(masteryChecklistHtml).not.toContain("<pre>생성된 세션 <code>source/</code> 스냅샷 정리를 검토해도 되는지");
    expect(masteryChecklistHtml).toContain("검토 질문에 답하고 근거를 보강해야 진행 후보가 됩니다");
    expect(masteryChecklistHtml).toContain("검토 후 다듬을 자기평가 프롬프트");
    expect(masteryChecklistHtml).toContain("학습자가 검토 후 답해야 할 다음 질문 3개");
    expect(masteryChecklistHtml).not.toContain("PASS/REVISE/BLOCK 판정 후보를 만들 수 있습니다");
    expect(masteryChecklistHtml).not.toContain("방금 AI 산출물을 목적, 아키텍처, 소스 근거, 검증, 사람 판단 기준으로 PASS/REVISE/BLOCK 판정해줘.");
    expect(masteryChecklistHtml).not.toContain("목적, 입력 근거, 산출물, 완료 기준을 포함한 요청 후보");
    expect(masteryChecklistHtml).not.toContain("목적, 입력 근거, 산출물, 완료 기준을 함께 요청할 수 있습니다");
    expect(masteryChecklistHtml).not.toContain("AI 결과를 PASS/REVISE/BLOCK으로 판정할 수 있습니다");
    expect(masteryChecklistHtml).not.toContain("원본 source snapshot을 삭제해도 되는지");
    expect(masteryChecklistHtml).not.toContain("한 번 더 요약하거나 질문하면 진행할 수 있습니다");
    expect(masteryChecklistHtml).not.toContain("부족한 항목은 바로 다음 질문 3개");
    expect(masteryChecklistHtml).not.toContain("전송/최종 ACCEPT/배포/삭제 허가가 아닙니다");
    expect(masteryChecklistHtml).not.toContain("READY_REVIEW도 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 학습자 검토 상태로만 표시해줘");
    expect(masteryChecklistHtml).not.toContain("<h2>판정표</h2>");
    expect(masteryChecklistHtml).not.toContain("<strong>READY:</strong>");
    expect(masteryChecklistHtml).not.toContain("READY / REVIEW / BLOCKED로 평가해줘");
    expect(masteryChecklistHtml).not.toContain("복사용 자기평가 프롬프트");
    const masteryChecklistMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "vibe-coding-mastery-checklist.md"), "utf8");
    expect(masteryChecklistMarkdown).toContain("# 바이브코딩 숙련도 체크리스트");
    expect(masteryChecklistMarkdown).toContain("손코딩 문법을 외웠는지 보지 않습니다");
    expect(masteryChecklistMarkdown).not.toContain("전문 개발자처럼 문법을 손으로 외웠는지");
    expect(masteryChecklistMarkdown).toContain("READY_REVIEW는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 학습자가 근거, 검증 기준, 검증 기록을 확인할 후보 상태입니다");
    expect(masteryChecklistMarkdown).not.toContain("READY_REVIEW는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 학습자가 근거와 검증 기준을 확인할 후보 상태입니다");
    expect(masteryChecklistMarkdown).toContain("READY_REVIEW / REVIEW / BLOCKED");
    expect(masteryChecklistMarkdown).toContain("- READY_REVIEW:");
    expect(masteryChecklistMarkdown).toContain("요청 후보를 검토 후 다듬을 수 있습니다");
    expect(masteryChecklistMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태 후보를 만들 수 있습니다");
    expect(masteryChecklistMarkdown).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보로만 표시해줘");
    expect(masteryChecklistMarkdown).toContain("- Prompt: 생성된 세션 source/ 스냅샷 정리를 검토해도 되는지, 보존 증거 묶음, 세션 검증, 검증 기록, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰");
    expect(masteryChecklistMarkdown).not.toContain("- Prompt: 생성된 세션 source/ 스냅샷 정리를 검토해도 되는지, 보존 증거 묶음, 세션 검증, 검증 기록, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰");
    expect(masteryChecklistMarkdown).not.toContain("- Prompt: 생성된 세션 source/ 스냅샷 정리를 검토해도 되는지, 남은 산출물과 끊기는 evidence link");
    expect(masteryChecklistMarkdown).not.toContain("- Prompt: 생성된 세션 `source/` 스냅샷 정리를 검토해도 되는지");
    expect(masteryChecklistMarkdown).toContain("## 검토 후 다듬을 자기평가 프롬프트");
    expect(masteryChecklistMarkdown).toContain("학습자가 검토 후 답해야 할 다음 질문 3개");
    expect(masteryChecklistMarkdown).not.toContain("PASS/REVISE/BLOCK 판정 후보를 만들 수 있습니다");
    expect(masteryChecklistMarkdown).not.toContain("방금 AI 산출물을 목적, 아키텍처, 소스 근거, 검증, 사람 판단 기준으로 PASS/REVISE/BLOCK 판정해줘.");
    expect(masteryChecklistMarkdown).not.toContain("목적, 입력 근거, 산출물, 완료 기준을 포함한 요청 후보");
    expect(masteryChecklistMarkdown).not.toContain("목적, 입력 근거, 산출물, 완료 기준을 함께 요청할 수 있습니다");
    expect(masteryChecklistMarkdown).not.toContain("AI 결과를 PASS/REVISE/BLOCK으로 판정할 수 있습니다");
    expect(masteryChecklistMarkdown).not.toContain("원본 source snapshot을 삭제해도 되는지");
    expect(masteryChecklistMarkdown).not.toContain("부족한 항목은 바로 다음 질문 3개");
    expect(masteryChecklistMarkdown).not.toContain("READY_REVIEW는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 학습자가 근거를 검토할 상태입니다");
    expect(masteryChecklistMarkdown).not.toContain("- READY:");
    expect(masteryChecklistMarkdown).not.toContain("READY / REVIEW / BLOCKED로 평가해줘");
    const implementationBriefHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "vibe-coding-implementation-brief.html"), "utf8");
    expect(implementationBriefHtml).toContain("바이브코딩 구현 브리프");
    expect(implementationBriefHtml).toContain("첫 vertical slice");
    expect(implementationBriefHtml).toContain("수락 기준");
    expect(implementationBriefHtml).toContain("검증 계획");
    expect(implementationBriefHtml).toContain("학습자 판단 체크포인트");
    expect(implementationBriefHtml).toContain("누구의 어떤 문제를 해결해야 하는지 제품 방향을 학습자가 검토");
    expect(implementationBriefHtml).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태");
    expect(implementationBriefHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 구현 완료, 배포, 삭제 허가가 아니라");
    expect(implementationBriefHtml).not.toContain("누구의 어떤 문제를 해결해야 하는지 최종 판단");
    expect(implementationBriefHtml).not.toContain("PASS / REVISE / BLOCK");
    expect(implementationBriefHtml).toContain("생성된 세션 <code>source/</code> 스냅샷 정리 검토 여부를 판단");
    const plainImplementationBriefSourceItem = [
      "<li>source-absorption-ledger, session-verification, source-retention-guide를 본 뒤 생성된 세션 ",
      "source/",
      " 스냅샷 정리 검토 여부를 판단합니다.</li>"
    ].join("");
    expect(implementationBriefHtml).not.toContain(plainImplementationBriefSourceItem);
    expect(implementationBriefHtml).toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤 생성된 세션");
    expect(implementationBriefHtml).not.toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤 생성된 세션");
    expect(implementationBriefHtml).not.toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide를 본 뒤 생성된 세션");
    expect(implementationBriefHtml).not.toContain("원본 source/ 보존 여부를 결정");
    expect(implementationBriefHtml).toContain("검토 후 다듬을 구현 프롬프트");
    expect(implementationBriefHtml).not.toContain("복사용 구현 프롬프트");
    const implementationBriefMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "vibe-coding-implementation-brief.md"), "utf8");
    expect(implementationBriefMarkdown).toContain("# 바이브코딩 구현 브리프");
    expect(implementationBriefMarkdown).toContain("소스를 AI 지식으로 내장하지 않고");
    expect(implementationBriefMarkdown).toContain("## 학습자 판단 체크포인트");
    expect(implementationBriefMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태");
    expect(implementationBriefMarkdown).toContain("- source-absorption-ledger, session-verification, verification records, source-retention-guide, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다.");
    expect(implementationBriefMarkdown).not.toContain("- source-absorption-ledger, session-verification, verification records, source-retention-guide, 현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 확인한 뒤 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다.");
    expect(implementationBriefMarkdown).not.toContain("- source-absorption-ledger, session-verification, verification records, source-retention-guide를 본 뒤 생성된 세션 `source/` 스냅샷 정리 검토 여부를 판단합니다.");
    expect(implementationBriefMarkdown).not.toContain("- source-absorption-ledger, session-verification, source-retention-guide를 본 뒤 생성된 세션 source/ 스냅샷 정리 검토 여부를 판단합니다.");
    expect(implementationBriefMarkdown).not.toContain("원본 source/ 보존 여부를 결정");
    expect(implementationBriefMarkdown).toContain("검토 근거로 볼지 정합니다");
    expect(implementationBriefMarkdown).not.toContain("PASS / REVISE / BLOCK");
    expect(implementationBriefMarkdown).toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide, 학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰");
    const promptReadinessHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-prompt-readiness-checklist.html"), "utf8");
    expect(promptReadinessHtml).toContain("AI 프롬프트 준비도 체크리스트");
    expect(promptReadinessHtml).toContain("GitHub Copilot식 well-scoped task");
    expect(promptReadinessHtml).toContain("promptfoo식 assertion");
    expect(promptReadinessHtml).toContain("READY_REVIEW");
    expect(promptReadinessHtml).toContain("REVISE");
    expect(promptReadinessHtml).toContain("BLOCK");
    expect(promptReadinessHtml).toContain("READY_REVIEW는 문제 설명, source evidence, acceptance criteria, verification assertion, 검증 기록 보고 형식을 학습자가 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(promptReadinessHtml).not.toContain("READY_REVIEW는 문제 설명, source evidence, acceptance criteria, verification assertion을 학습자가 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(promptReadinessHtml).toContain("READY_REVIEW / REVISE / BLOCK");
    expect(promptReadinessHtml).toContain("READY_REVIEW / REVISE / BLOCK 검토 상태로 나눠줘");
    expect(promptReadinessHtml).toContain("READY_REVIEW여도 바로 보내지 말고");
    expect(promptReadinessHtml).toContain("프롬프트 전송 중단 조건");
    expect(promptReadinessHtml).toContain("검토 상태표");
    expect(promptReadinessHtml).toContain("사용자 문제와 제외 범위가 없으면 보내지 않습니다");
    expect(promptReadinessHtml).toContain("일반 개발 지식 답변으로 흐르므로 보강합니다");
    expect(promptReadinessHtml).toContain("첫 vertical slice와 하지 않을 일을 분리하지 못하면");
    expect(promptReadinessHtml).toContain("acceptance criteria, verification assertion, 검증 기록 보고 형식이 없으면 READY_REVIEW 검토 상태를 유보합니다");
    expect(promptReadinessHtml).not.toContain("acceptance criteria와 verification assertion이 없으면 READY_REVIEW 검토 상태를 유보합니다");
    expect(promptReadinessHtml).toContain("수락 기준이 관찰 가능한 bullet");
    expect(promptReadinessHtml).toContain("전송 전 직접 확인해야 합니다");
    expect(promptReadinessHtml).toContain("verification assertion, 검증 기록 보고 형식을 전송 전 직접 확인해야 합니다");
    expect(promptReadinessHtml).not.toContain("verification assertion을 전송 전 직접 확인해야 합니다");
    expect(promptReadinessHtml).toContain("변경 파일, 실행 명령, 실패, 가정, 다음 질문 형식");
    expect(promptReadinessHtml).toContain("검토 후 다듬을 프롬프트 평가 요청");
    expect(promptReadinessHtml).toContain("검토 후 다듬을 점검 요청 후보");
    expect(promptReadinessHtml).toContain("프롬프트 점검 요청 후보");
    expect(promptReadinessHtml).toContain("학습자가 검토 후 다듬어 보낼 개선 프롬프트");
    expect(promptReadinessHtml).not.toContain("바로 보낼 수 있는 개선 프롬프트");
    expect(promptReadinessHtml).not.toContain("<strong>READY:</strong>");
    expect(promptReadinessHtml).not.toContain("READY_REVIEW / REVISE / BLOCK으로 판정해줘");
    expect(promptReadinessHtml).not.toContain("READY_REVIEW 판정을 유보합니다");
    expect(promptReadinessHtml).not.toContain("READY / REVISE / BLOCK으로 판정");
    expect(promptReadinessHtml).not.toContain("완료 판정을 유보합니다");
    expect(promptReadinessHtml).not.toContain("verification assertion을 최종 확인해야 합니다");
    expect(promptReadinessHtml).not.toContain("완료 조건이 관찰 가능한 bullet");
    expect(promptReadinessHtml).not.toContain("복사용 프롬프트 평가 요청");
    expect(promptReadinessHtml).not.toContain("<h2>판정표</h2>");
    expect(promptReadinessHtml).not.toContain("READY_REVIEW는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 보내기 전 학습자 검토 상태입니다");
    const promptReadinessMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-prompt-readiness-checklist.md"), "utf8");
    expect(promptReadinessMarkdown).toContain("# AI 프롬프트 준비도 체크리스트");
    expect(promptReadinessMarkdown).toContain("source evidence");
    expect(promptReadinessMarkdown).toContain("## 프롬프트 전송 중단 조건");
    expect(promptReadinessMarkdown).toContain("## 검토 후 다듬을 프롬프트 평가 요청");
    expect(promptReadinessMarkdown).toContain("READY_REVIEW는 문제 설명, source evidence, acceptance criteria, verification assertion, 검증 기록 보고 형식을 학습자가 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(promptReadinessMarkdown).not.toContain("READY_REVIEW는 문제 설명, source evidence, acceptance criteria, verification assertion을 학습자가 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(promptReadinessMarkdown).toContain("READY_REVIEW 검토 상태를 유보합니다");
    expect(promptReadinessMarkdown).toContain("수락 기준이 관찰 가능한 bullet");
    expect(promptReadinessMarkdown).toContain("전송 전 직접 확인해야 합니다");
    expect(promptReadinessMarkdown).toContain("verification assertion, 검증 기록 보고 형식을 전송 전 직접 확인해야 합니다");
    expect(promptReadinessMarkdown).not.toContain("verification assertion을 전송 전 직접 확인해야 합니다");
    expect(promptReadinessMarkdown).not.toContain("완료 판정을 유보합니다");
    expect(promptReadinessMarkdown).not.toContain("READY_REVIEW 판정을 유보합니다");
    expect(promptReadinessMarkdown).not.toContain("verification assertion을 최종 확인해야 합니다");
    expect(promptReadinessMarkdown).not.toContain("완료 조건이 관찰 가능한 bullet");
    expect(promptReadinessMarkdown).toContain("READY_REVIEW / REVISE / BLOCK");
    expect(promptReadinessMarkdown).toContain("READY_REVIEW / REVISE / BLOCK 검토 상태로 나눠줘");
    expect(promptReadinessMarkdown).toContain("- READY_REVIEW:");
    expect(promptReadinessMarkdown).toContain("검토 후 다듬을 점검 요청 후보");
    expect(promptReadinessMarkdown).toContain("프롬프트 점검 요청 후보");
    expect(promptReadinessMarkdown).toContain("학습자가 검토 후 다듬어 보낼 개선 프롬프트");
    expect(promptReadinessMarkdown).not.toContain("바로 보낼 수 있는 개선 프롬프트");
    expect(promptReadinessMarkdown).not.toContain("- READY:");
    expect(promptReadinessMarkdown).not.toContain("READY_REVIEW / REVISE / BLOCK으로 판정해줘");
    expect(promptReadinessMarkdown).not.toContain("READY / REVISE / BLOCK으로 판정");
    expect(promptReadinessMarkdown).not.toContain("- Prompt check:");
    expect(promptReadinessMarkdown).not.toContain("READY_REVIEW는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 보내기 전 학습자 검토 상태입니다");
    expect(promptReadinessMarkdown).toContain("관련 source evidence가 한 줄도 없으면");
    const promptAbLabHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-prompt-ab-lab.html"), "utf8");
    expect(promptAbLabHtml).toContain("AI 프롬프트 A/B 랩");
    expect(promptAbLabHtml).toContain("프롬프트 A");
    expect(promptAbLabHtml).toContain("검토 후 다듬을 프롬프트 B 후보");
    expect(promptAbLabHtml).toContain("프롬프트 B 후보");
    expect(promptAbLabHtml).toContain("source-grounded 구현 요청 후보");
    expect(promptAbLabHtml).toContain("시작 작업 후보");
    expect(promptAbLabHtml).toContain("A/B 검토 상태표");
    expect(promptAbLabHtml).toContain("보낼 프롬프트 검토 상태");
    expect(promptAbLabHtml).toContain("프롬프트 A는 BLOCK입니다");
    expect(promptAbLabHtml).toContain("프롬프트 B 후보는 READY_REVIEW 검토 후보입니다");
    expect(promptAbLabHtml).toContain("READY_REVIEW도 내 제품 목적, source evidence, acceptance criteria, verification assertion, 학습자 판단 체크포인트, 검증 기록 보고 형식을 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(promptAbLabHtml).toContain("관찰 가능한 assertion");
    expect(promptAbLabHtml).toContain("수락/검증 기준이 흐립니다");
    expect(promptAbLabHtml).not.toContain("완료 여부가 흐립니다");
    expect(promptAbLabHtml).toContain("검증 기록 보고 형식을 확인할 후보일 뿐");
    expect(promptAbLabHtml).not.toContain("프롬프트 B 후보는 READY 검토 후보입니다");
    expect(promptAbLabHtml).not.toContain("PASS/FAIL assertion");
    expect(promptAbLabHtml).not.toContain("프롬프트 B는 READY 후보입니다");
    expect(promptAbLabHtml).not.toContain("<strong>프롬프트 B:</strong>");
    expect(promptAbLabHtml).not.toContain("READY_REVIEW도 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니며");
    expect(promptAbLabHtml).not.toContain("source evidence, acceptance criteria, verification assertion, 학습자 판단 체크포인트가 맞는지 확인한 뒤 전송 후보로 둡니다");
    expect(promptAbLabHtml).toContain("REVISE로 바꾸고 다시 검토합니다");
    expect(promptAbLabHtml).not.toContain("확인한 뒤 보냅니다");
    expect(promptAbLabHtml).not.toContain("REVISE로 바꾼 뒤 보냅니다");
    expect(promptAbLabHtml).toContain("전송 중단 조건부터 보강합니다");
    expect(promptAbLabHtml).toContain("검토 후 다듬을 A/B 평가 프롬프트");
    expect(promptAbLabHtml).toContain("어느 쪽이 안전한 검토 후보인지 나눠줘");
    expect(promptAbLabHtml).toContain("학습자가 검토 후 다듬어 보낼 개선 프롬프트");
    expect(promptAbLabHtml).toContain("확인하지 않은 성공 주장이 있으면 BLOCK 검토 상태로 둡니다.");
    expect(promptAbLabHtml).toContain("AI가 성공이라고 말하면 검증 기록 없이 수락하는 구조입니다");
    expect(promptAbLabHtml).not.toContain("AI가 성공이라고 말하면 끝나는 구조입니다");
    expect(promptAbLabHtml).not.toContain("바로 보낼 수 있는 개선 프롬프트");
    expect(promptAbLabHtml).not.toContain("복사용 A/B 평가 프롬프트");
    expect(promptAbLabHtml).not.toContain("A/B 판정표");
    expect(promptAbLabHtml).not.toContain("보낼 프롬프트 판정");
    expect(promptAbLabHtml).not.toContain("어느 쪽이 안전한지 판정해줘");
    expect(promptAbLabHtml).not.toContain("확인하지 않은 성공 주장이 있으면 BLOCK으로 판정합니다.");
    const promptAbLabMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-prompt-ab-lab.md"), "utf8");
    expect(promptAbLabMarkdown).toContain("# AI 프롬프트 A/B 랩");
    expect(promptAbLabMarkdown).toContain("AI는 이미 일반 개발 지식이 충분합니다");
    expect(promptAbLabMarkdown).toContain("## 검토 후 다듬을 프롬프트 B 후보");
    expect(promptAbLabMarkdown).toContain("프롬프트 B 후보");
    expect(promptAbLabMarkdown).toContain("프롬프트 B 후보는 READY_REVIEW 검토 후보입니다");
    expect(promptAbLabMarkdown).toContain("READY_REVIEW도 내 제품 목적, source evidence, acceptance criteria, verification assertion, 학습자 판단 체크포인트, 검증 기록 보고 형식을 확인할 후보일 뿐 전송, 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(promptAbLabMarkdown).toContain("관찰 가능한 assertion");
    expect(promptAbLabMarkdown).toContain("수락/검증 기준이 흐립니다");
    expect(promptAbLabMarkdown).not.toContain("완료 여부가 흐립니다");
    expect(promptAbLabMarkdown).toContain("source-grounded 구현 요청 후보");
    expect(promptAbLabMarkdown).toContain("시작 작업 후보");
    expect(promptAbLabMarkdown).not.toContain("프롬프트 B 후보는 READY 검토 후보입니다");
    expect(promptAbLabMarkdown).not.toContain("PASS/FAIL assertion");
    expect(promptAbLabMarkdown).not.toContain("## 프롬프트 B\n");
    expect(promptAbLabMarkdown).not.toContain("- 프롬프트 B:");
    expect(promptAbLabMarkdown).not.toContain("READY_REVIEW도 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니며");
    expect(promptAbLabMarkdown).toContain("## 보낼 프롬프트 검토 상태");
    expect(promptAbLabMarkdown).toContain("## 검토 후 다듬을 A/B 평가 프롬프트");
    expect(promptAbLabMarkdown).toContain("어느 쪽이 안전한 검토 후보인지 나눠줘");
    expect(promptAbLabMarkdown).toContain("학습자가 검토 후 다듬어 보낼 개선 프롬프트");
    expect(promptAbLabMarkdown).toContain("확인하지 않은 성공 주장이 있으면 BLOCK 검토 상태로 둡니다.");
    expect(promptAbLabMarkdown).toContain("AI가 성공이라고 말하면 검증 기록 없이 수락하는 구조입니다");
    expect(promptAbLabMarkdown).not.toContain("AI가 성공이라고 말하면 끝나는 구조입니다");
    expect(promptAbLabMarkdown).not.toContain("바로 보낼 수 있는 개선 프롬프트");
    expect(promptAbLabMarkdown).not.toContain("## 보낼 프롬프트 판정");
    expect(promptAbLabMarkdown).not.toContain("어느 쪽이 안전한지 판정해줘");
    expect(promptAbLabMarkdown).not.toContain("확인하지 않은 성공 주장이 있으면 BLOCK으로 판정합니다.");
    expect(promptAbLabMarkdown).toContain("A/B 모두 부족하면 구현 요청을 멈추고");
    const architecturePrincipleHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "architecture-principle-playbook.html"), "utf8");
    expect(architecturePrincipleHtml).toContain("아키텍처 원리 플레이북");
    expect(architecturePrincipleHtml).toContain("문법 암기 대신 구조 판단");
    expect(architecturePrincipleHtml).toContain("왜 필요한가");
    expect(architecturePrincipleHtml).toContain("검토 후 다듬을 요청 후보");
    expect(architecturePrincipleHtml).toContain("아키텍처 요청 후보");
    expect(architecturePrincipleHtml).not.toContain("AI에게 이렇게 지시");
    expect(architecturePrincipleHtml).toContain("잘못 만들면 생기는 문제");
    expect(architecturePrincipleHtml).toContain("검증 질문");
    expect(architecturePrincipleHtml).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태 후보를 만들 수 없는 항목");
    expect(architecturePrincipleHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보입니다");
    expect(architecturePrincipleHtml).not.toContain("PASS/REVISE/BLOCK 판정 후보를 만들 수 없는 항목");
    expect(architecturePrincipleHtml).not.toContain("PASS/REVISE/BLOCK으로 판정할 수 없는 항목");
    expect(architecturePrincipleHtml).toContain("검토 후 다듬을 아키텍처 설명 프롬프트");
    expect(architecturePrincipleHtml).not.toContain("복사용 아키텍처 설명 프롬프트");
    const architecturePrincipleMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "architecture-principle-playbook.md"), "utf8");
    expect(architecturePrincipleMarkdown).toContain("# 아키텍처 원리 플레이북");
    expect(architecturePrincipleMarkdown).toContain("검토 후 다듬을 요청 후보");
    expect(architecturePrincipleMarkdown).toContain("아키텍처 요청 후보");
    expect(architecturePrincipleMarkdown).not.toContain("AI에게 이렇게 지시");
    expect(architecturePrincipleMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태 후보를 만들 수 없는 항목");
    expect(architecturePrincipleMarkdown).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라 근거와 검증 기록 확인 후보입니다");
    expect(architecturePrincipleMarkdown).not.toContain("PASS/REVISE/BLOCK 판정 후보를 만들 수 없는 항목");
    expect(architecturePrincipleMarkdown).not.toContain("PASS/REVISE/BLOCK으로 판정할 수 없는 항목");
    expect(architecturePrincipleMarkdown).toContain("## 검토 후 다듬을 아키텍처 설명 프롬프트");
    const sourceToBuildInterviewHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "source-to-build-interview.html"), "utf8");
    expect(sourceToBuildInterviewHtml).toContain("소스에서 비슷한 앱 만들기 인터뷰");
    expect(sourceToBuildInterviewHtml).toContain("내가 먼저 답할 질문");
    expect(sourceToBuildInterviewHtml).toContain("소스 기반 예시 답");
    expect(sourceToBuildInterviewHtml).toContain("AI에게 확인시킬 질문");
    expect(sourceToBuildInterviewHtml).toContain("AI에게 확인시킬 질문을 검토해 본인 말로 바꿉니다");
    expect(sourceToBuildInterviewHtml).not.toContain("AI에게 확인시킬 질문을 복사합니다");
    expect(sourceToBuildInterviewHtml).toContain("검토 기준");
    expect(sourceToBuildInterviewHtml).not.toContain("<h2>판정 기준</h2>");
    expect(sourceToBuildInterviewHtml).toContain("검토 후 다듬을 인터뷰 프롬프트");
    expect(sourceToBuildInterviewHtml).not.toContain("복사용 인터뷰 프롬프트");
    const sourceToBuildInterviewMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "source-to-build-interview.md"), "utf8");
    expect(sourceToBuildInterviewMarkdown).toContain("# 소스에서 비슷한 앱 만들기 인터뷰");
    expect(sourceToBuildInterviewMarkdown).toContain("문법 질문은 줄이고");
    expect(sourceToBuildInterviewMarkdown).toContain("## 검토 후 다듬을 인터뷰 프롬프트");
    const similarAppTransferHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "similar-app-transfer-map.html"), "utf8");
    expect(similarAppTransferHtml).toContain("비슷한 앱 전이 지도");
    expect(similarAppTransferHtml).toContain("그대로 가져갈 원리");
    expect(similarAppTransferHtml).toContain("새 앱에 맞게 바꿀 결정");
    expect(similarAppTransferHtml).toContain("검토 후 다듬을 전이 요청 후보");
    expect(similarAppTransferHtml).toContain("전이 요청 후보");
    expect(similarAppTransferHtml).toContain("전이 검토");
    expect(similarAppTransferHtml).toContain("VERIFY는 수락/검증 기준으로 씁니다");
    expect(similarAppTransferHtml).toContain("학습자가 수락/검증할 관찰 기준이 있는지 확인합니다");
    expect(similarAppTransferHtml).not.toContain("VERIFY는 완료 기준으로 씁니다");
    expect(similarAppTransferHtml).not.toContain("AI가 끝났다고 주장할 기준이 있는지 확인합니다");
    expect(similarAppTransferHtml).not.toContain("<h2>전이 판정</h2>");
    expect(similarAppTransferHtml).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태 표");
    expect(similarAppTransferHtml).toContain("PASS_REVIEW도 구현 시작, 최종 ACCEPT, 배포, 삭제 허가가 아니라 source evidence와 검증 기록 확인 후보로 표시해줘");
    expect(similarAppTransferHtml).not.toContain("AI에게 물을 질문");
    expect(similarAppTransferHtml).toContain("검토 후 다듬을 전이 프롬프트");
    expect(similarAppTransferHtml).not.toContain("AI 구현 프롬프트 문장으로 바꿔줘");
    expect(similarAppTransferHtml).not.toContain("복사용 전이 프롬프트");
    expect(similarAppTransferHtml).not.toContain("PASS/REVISE/BLOCK 표");
    expect(similarAppTransferHtml).toContain("No Copy Rule");
    const similarAppTransferMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "similar-app-transfer-map.md"), "utf8");
    expect(similarAppTransferMarkdown).toContain("# 비슷한 앱 전이 지도");
    expect(similarAppTransferMarkdown).toContain("검토 후 다듬을 전이 요청 후보");
    expect(similarAppTransferMarkdown).toContain("전이 요청 후보");
    expect(similarAppTransferMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태 표");
    expect(similarAppTransferMarkdown).toContain("학습자가 수락/검증할 관찰 기준이 있는지 확인합니다");
    expect(similarAppTransferMarkdown).not.toContain("AI가 끝났다고 주장할 기준이 있는지 확인합니다");
    expect(similarAppTransferMarkdown).toContain("PASS_REVIEW도 구현 시작, 최종 ACCEPT, 배포, 삭제 허가가 아니라 source evidence와 검증 기록 확인 후보로 표시해줘");
    expect(similarAppTransferMarkdown).not.toContain("AI에게 물을 질문");
    expect(similarAppTransferMarkdown).toContain("## 검토 후 다듬을 전이 프롬프트");
    expect(similarAppTransferMarkdown).not.toContain("AI 구현 프롬프트 문장으로 바꿔줘");
    expect(similarAppTransferMarkdown).not.toContain("PASS/REVISE/BLOCK 표");
    expect(similarAppTransferMarkdown).toContain("KEEP / ADAPT / ASK AI / VERIFY");
    const learnerGoalAlignmentHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "learner-goal-alignment.html"), "utf8");
    expect(learnerGoalAlignmentHtml).toContain("학습자 목표 정렬");
    expect(learnerGoalAlignmentHtml).toContain("learner-brief: provided");
    expect(learnerGoalAlignmentHtml).toContain("내 목표/PRD/이슈/프롬프트");
    expect(learnerGoalAlignmentHtml).toContain("source-grounded gap");
    expect(learnerGoalAlignmentHtml).toContain("ACCEPT_REVIEW / CLARIFY / REWRITE / BLOCK");
    expect(learnerGoalAlignmentHtml).toContain("검토 기준");
    expect(learnerGoalAlignmentHtml).toContain("수락/검증 기준이 있습니다");
    expect(learnerGoalAlignmentHtml).not.toContain("수락/검증 기준이 관찰 가능하지 않습니다");
    expect(learnerGoalAlignmentHtml).not.toContain("완료/수락 기준이 있습니다");
    expect(learnerGoalAlignmentHtml).not.toContain("완료 기준이 관찰 가능하지 않습니다");
    expect(learnerGoalAlignmentHtml).toContain("ACCEPT_REVIEW는 구현 시작, 최종 ACCEPT, 배포, 삭제 허가가 아니라");
    expect(learnerGoalAlignmentHtml).toContain("ACCEPT_REVIEW도 구현 시작, 최종 ACCEPT, 배포, 삭제 허가가 아니라");
    expect(learnerGoalAlignmentHtml).toContain("source evidence, 검증 기준, 검증 기록을 학습자가 다시 확인할 목표 정렬 후보입니다");
    expect(learnerGoalAlignmentHtml).toContain("학습자가 source evidence, 검증 기준, 검증 기록을 확인할 목표 정렬 후보로만 표시해줘");
    expect(learnerGoalAlignmentHtml).not.toContain("source evidence와 검증 기준을 학습자가 다시 확인할 목표 정렬 후보");
    expect(learnerGoalAlignmentHtml).not.toContain("학습자가 source evidence와 검증 기준을 확인할 목표 정렬 후보로만 표시해줘");
    expect(learnerGoalAlignmentHtml).not.toContain("ACCEPT / CLARIFY / REWRITE / BLOCK");
    expect(learnerGoalAlignmentHtml).not.toContain("<h2>판정 기준</h2>");
    expect(learnerGoalAlignmentHtml).toContain("검토 후 다듬을 목표 정렬 프롬프트");
    expect(learnerGoalAlignmentHtml).not.toContain("복사용 목표 정렬 프롬프트");
    expect(learnerGoalAlignmentHtml).toContain("Similar App PRD");
    const learnerGoalAlignmentMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "learner-goal-alignment.md"), "utf8");
    expect(learnerGoalAlignmentMarkdown).toContain("# 학습자 목표 정렬");
    expect(learnerGoalAlignmentMarkdown).toContain("learner-brief: provided");
    expect(learnerGoalAlignmentMarkdown).toContain("## 검토 기준");
    expect(learnerGoalAlignmentMarkdown).toContain("수락/검증 기준이 있습니다");
    expect(learnerGoalAlignmentMarkdown).not.toContain("수락/검증 기준이 관찰 가능하지 않습니다");
    expect(learnerGoalAlignmentMarkdown).not.toContain("완료/수락 기준이 있습니다");
    expect(learnerGoalAlignmentMarkdown).not.toContain("완료 기준이 관찰 가능하지 않습니다");
    expect(learnerGoalAlignmentMarkdown).toContain("## 검토 후 다듬을 목표 정렬 프롬프트");
    expect(learnerGoalAlignmentMarkdown).toContain("ACCEPT_REVIEW / CLARIFY / REWRITE / BLOCK");
    expect(learnerGoalAlignmentMarkdown).toContain("ACCEPT_REVIEW는 구현 시작, 최종 ACCEPT, 배포, 삭제 허가가 아니라");
    expect(learnerGoalAlignmentMarkdown).toContain("source evidence, 검증 기준, 검증 기록을 학습자가 다시 확인할 목표 정렬 후보입니다");
    expect(learnerGoalAlignmentMarkdown).toContain("학습자가 source evidence, 검증 기준, 검증 기록을 확인할 목표 정렬 후보로만 표시해줘");
    expect(learnerGoalAlignmentMarkdown).not.toContain("source evidence와 검증 기준을 학습자가 다시 확인할 목표 정렬 후보");
    expect(learnerGoalAlignmentMarkdown).not.toContain("학습자가 source evidence와 검증 기준을 확인할 목표 정렬 후보로만 표시해줘");
    expect(learnerGoalAlignmentMarkdown).not.toContain("ACCEPT / CLARIFY / REWRITE / BLOCK");
    expect(learnerGoalAlignmentMarkdown).not.toContain("## 판정 기준");
    const aiImplementationLoopHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-implementation-loop.html"), "utf8");
    expect(aiImplementationLoopHtml).toContain("AI 구현 대화 루프");
    expect(aiImplementationLoopHtml).toContain("PLAN / OBSERVE / CHECK / REVISE / VERIFY / NEXT");
    expect(aiImplementationLoopHtml).toContain("검토 후 다듬을 다음 질문 후보");
    expect(aiImplementationLoopHtml).toContain("구현 루프 요청 후보");
    expect(aiImplementationLoopHtml).toContain("이 후보는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 source evidence와 검증 기록을 학습자가 확인할 구현 루프 요청 후보입니다");
    expect(aiImplementationLoopHtml).toContain("AI 답변에서 확인할 것");
    expect(aiImplementationLoopHtml).toContain("멈춤 조건");
    expect(aiImplementationLoopHtml).toContain("PASS_REVIEW / REVISE / BLOCK");
    expect(aiImplementationLoopHtml).toContain("PASS_REVIEW도 최종 ACCEPT, 배포, 삭제 허가가 아니라");
    expect(aiImplementationLoopHtml).toContain("검토 상태를 정한 뒤 다음 AI 질문 후보");
    expect(aiImplementationLoopHtml).toContain("검토 후 다듬을 구현 루프 프롬프트");
    expect(aiImplementationLoopHtml).not.toContain("복사용 구현 루프 프롬프트");
    expect(aiImplementationLoopHtml).not.toContain("<strong>다음 질문:</strong>");
    expect(aiImplementationLoopHtml).not.toContain("PASS / REVISE / BLOCK");
    expect(aiImplementationLoopHtml).not.toContain("완료 판정 뒤 다음 AI 질문 후보");
    const aiImplementationLoopMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "ai-implementation-loop.md"), "utf8");
    expect(aiImplementationLoopMarkdown).toContain("# AI 구현 대화 루프");
    expect(aiImplementationLoopMarkdown).toContain("## 검토 후 다듬을 구현 루프 프롬프트");
    expect(aiImplementationLoopMarkdown).toContain("PLAN / OBSERVE / CHECK / REVISE / VERIFY / NEXT");
    expect(aiImplementationLoopMarkdown).toContain("검토 후 다듬을 다음 질문 후보");
    expect(aiImplementationLoopMarkdown).toContain("구현 루프 요청 후보");
    expect(aiImplementationLoopMarkdown).toContain("이 후보는 전송, 최종 ACCEPT, 배포, 삭제 허가가 아니라 source evidence와 검증 기록을 학습자가 확인할 구현 루프 요청 후보입니다");
    expect(aiImplementationLoopMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK");
    expect(aiImplementationLoopMarkdown).toContain("검토 상태를 정한 뒤 다음 AI 질문 후보");
    expect(aiImplementationLoopMarkdown).not.toContain("- 다음 질문:");
    expect(aiImplementationLoopMarkdown).not.toContain("PASS / REVISE / BLOCK");
    expect(aiImplementationLoopMarkdown).not.toContain("완료 판정 뒤 다음 AI 질문 후보");
    const retentionGuideText = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "source-retention-guide.html"), "utf8");
    expect(retentionGuideText).toContain("소스 보존/정리 판단 가이드");
    expect(retentionGuideText).not.toContain("소스 보존/삭제 판단 가이드");
    expect(retentionGuideText).toContain("현재 생성 세션 <code>source/</code> 스냅샷");
    expect(retentionGuideText).not.toContain("현재 생성 세션 source/ 스냅샷");
    expect(retentionGuideText).not.toContain("현재 source snapshot");
    expect(retentionGuideText).toContain("정리 전 기준");
    expect(retentionGuideText).not.toContain("삭제 전 기준");
    expect(retentionGuideText).toContain("원본 소스를 앱 지식으로 내장");
    expect(retentionGuideText).toContain("생성된 세션 <code>source/</code> 스냅샷은 근거입니다");
    expect(retentionGuideText).not.toContain("생성된 세션 source/ 스냅샷은 근거입니다");
    expect(retentionGuideText).toContain("사용자 원본 소스는 자동 정리 대상이 아니며");
    expect(retentionGuideText).not.toContain("사용자 제공 세션 source는 근거입니다");
    expect(retentionGuideText).toContain("AI에게 줄 확인 프롬프트");
    expect(retentionGuideText).toContain("현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 되는지");
    expect(retentionGuideText).toContain("보존 증거 묶음이 남았는지를 학습자가 명시 확인");
    expect(retentionGuideText).toContain("source absorption ledger, session verification, verification records");
    expect(retentionGuideText).not.toContain("source absorption ledger, session verification이 보존 증거 묶음");
    expect(retentionGuideText).toContain("session verification이 통과했고 verification records가 남았는가?");
    expect(retentionGuideText).not.toContain("session verification이 통과했는가?");
    expect(retentionGuideText).toContain("source 링크가 더 이상 열리지 않아도 목적, 아키텍처 이유, AI 프롬프트, 검증 기준을 설명할 수 있다고 명시 확인");
    expect(retentionGuideText).toContain("READY_REVIEW는 정리 검토 후보이지 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(retentionGuideText).toContain("<code>DELETE-SOURCE-SNAPSHOT</code> 확인 토큰은 READY_REVIEW가 만든 최종 ACCEPT, 배포, 삭제 권한이 아니라");
    expect(retentionGuideText).toContain("학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다고 마지막으로 명시 확인했다는 뜻입니다");
    expect(retentionGuideText).toContain("READY_REVIEW는 정리 검토 후보이지 최종 ACCEPT, 배포, 삭제 허가가 아니며 DELETE-SOURCE-SNAPSHOT 확인 토큰은 source 링크가 더 이상 열리지 않아도 된다는 학습자의 마지막 명시 확인이라는 경계도 확인해줘");
    expect(retentionGuideText).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰은 학습자의 마지막 현재 학습 목표 확인과 명시 확인이라는 경계도 확인해줘");
    expect(retentionGuideText).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰은 학습자의 마지막 현재 목표 확인이라는 경계도 확인해줘");
    const absorptionHtml = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "source-absorption-ledger.html"), "utf8");
    expect(absorptionHtml).toContain("Source Absorption Ledger");
    expect(absorptionHtml).toContain("앱이 흡수한 기능");
    expect(absorptionHtml).toContain("현재 생성 세션 <code>source/</code> 스냅샷");
    expect(absorptionHtml).not.toContain("현재 생성 세션 source/ 스냅샷");
    expect(absorptionHtml).not.toContain("현재 source snapshot");
    expect(absorptionHtml).toContain("현재 목표 조사 요약");
    expect(absorptionHtml).toContain("생성된 세션 <code>source/</code> 스냅샷");
    expect(absorptionHtml).not.toContain("생성된 세션 source/ 스냅샷");
    expect(absorptionHtml).not.toContain("삭제를 검토합니다");
    expect(absorptionHtml).not.toContain("조사 종료 요약");
    expect(absorptionHtml).toContain("흡수한 학습 산출물");
    expect(absorptionHtml).toContain("정리 판단");
    expect(absorptionHtml).toContain("정리 판단: 정리");
    expect(absorptionHtml).toMatch(/정리 판단: 정리 (검토 후보|보류)/);
    expect(absorptionHtml).not.toContain("정리 판단: 정리 검토 가능");
    expect(absorptionHtml).toContain("정리 전 보존 증거 묶음");
    expect(absorptionHtml).toContain("장기 지식으로 보관하지 않고도");
    expect(absorptionHtml).not.toContain("원본 source/를 장기 지식으로 보관하지 않고도");
    expect(absorptionHtml).toContain("analysis/daily-summary-report.json");
    expect(absorptionHtml).toContain("html/vibe-coding-prompt-pack.html");
    expect(absorptionHtml).toContain("reference/vibe-coding-implementation-brief.html");
    expect(absorptionHtml).toContain("html/session-verification.html");
    expect(absorptionHtml).toContain("reference/source-retention-guide.html");
    expect(absorptionHtml).toContain("README.study.md");
    expect(absorptionHtml).not.toContain("source snapshot은");
    expect(absorptionHtml).not.toContain("source snapshot을");
    expect(absorptionHtml).not.toContain("현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다고 확인한 뒤에만 삭제");
    expect(absorptionHtml).toContain("학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다고 명시 확인한 뒤");
    expect(absorptionHtml).toContain("DELETE-SOURCE-SNAPSHOT</code> 확인 토큰으로 source 링크가 더 이상 열리지 않아도 된다는 마지막 학습자 명시 확인을 남긴 경우에만");
    expect(absorptionHtml).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰으로 마지막 현재 학습 목표 확인과 학습자 명시 확인을 남긴 경우에만");
    expect(absorptionHtml).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰으로 마지막 현재 목표 확인");
    expect(absorptionHtml).toContain("source evidence 링크 검증이 더 필요 없는 세션에서만 수동 정리 검토");
    expect(absorptionHtml).toContain("session verification, verification records, source retention guide");
    expect(absorptionHtml).not.toContain("session verification, source retention guide");
    expect(absorptionHtml).toContain("READY_REVIEW는 정리 검토 후보이지 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(absorptionHtml).toContain("실제 적용은 보존 증거 묶음, 세션 검증, 검증 기록 확인 뒤");
    expect(absorptionHtml).not.toContain("실제 적용은 보존 증거 묶음 확인 뒤");
    expect(absorptionHtml).toContain("<code>DELETE-SOURCE-SNAPSHOT</code> 확인 토큰을 입력한 경우에만 검토합니다");
    expect(absorptionHtml).toContain("현재 학습 목표 기준:");
    expect(absorptionHtml).toContain("현재 목표 조사 판단");
    expect(absorptionHtml).toContain("정적 분석으로 확인한 것과 실행해야 할 것은 session verification, verification records, daily summary에 남았습니다.");
    expect(absorptionHtml).toContain("용량 정리 판단은 retention guide, verification records, 이 ledger를 기준으로 하고");
    expect(absorptionHtml).not.toContain("정적 분석으로 확인한 것과 실행해야 할 것은 session verification과 daily summary에 남았습니다.");
    expect(absorptionHtml).not.toContain("용량 정리 판단은 retention guide와 이 ledger를 기준으로 하고");
    expect(absorptionHtml).toContain("현재 학습 목표와 정적 근거 기준에서 맞는지");
    expect(absorptionHtml).toContain("현재 목표 추가 조사 필요 여부");
    expect(absorptionHtml).toContain("추가 조사 필요 여부");
    expect(absorptionHtml).toContain("학습자가 검토 후 다듬어 보낼 orient/architect/plan/review 프롬프트");
    expect(absorptionHtml).not.toContain("AI에게 바로 줄 orient/architect/plan/review 프롬프트");
    expect(absorptionHtml).not.toContain("<strong>추가 조사 판단:</strong>");
    expect(absorptionHtml).toContain("원본 소스 전체를 앱 지식으로 내장하지 않습니다");
    expect(absorptionHtml).toContain("AI에게 줄 확인 프롬프트");
    expect(absorptionHtml).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태");
    expect(absorptionHtml).toContain("PASS_REVIEW도 현재 항목 검토 후보일 뿐 전체 조사 종료, 최종 ACCEPT, 배포, source 삭제 허가가 아니라");
    expect(absorptionHtml).not.toContain("PASS_REVIEW도 현재 항목 검토 후보일 뿐 전체 조사 종료, source 삭제, 최종 승인 허가가 아니라");
    expect(absorptionHtml).not.toContain("PASS_REVIEW도 조사 종료, source 삭제, 최종 승인 허가가 아니라");
    expect(absorptionHtml).not.toContain("PASS / REVISE / BLOCK");
    expect(absorptionHtml).toContain("부족하면 추가 조사 질문 3개");
    const absorptionMarkdown = await fs.readFile(path.join(result.session.outputPaths.root, "reference", "source-absorption-ledger.md"), "utf8");
    expect(absorptionMarkdown).toContain("# Source Absorption Ledger");
    expect(absorptionMarkdown).toContain("## Generated Session Source Snapshot");
    expect(absorptionMarkdown).not.toContain("## Source Snapshot");
    expect(absorptionMarkdown).toContain("## 현재 목표 조사 요약");
    expect(absorptionMarkdown).not.toContain("## 조사 종료 요약");
    expect(absorptionMarkdown).toContain("## 정리 전 보존 증거 묶음");
    expect(absorptionMarkdown).toContain("학습 목적, 아키텍처 이유, AI 프롬프트, 검증 경계, 정리 판단");
    expect(absorptionMarkdown).toContain("정리 판단");
    expect(absorptionMarkdown).toContain("정리 판단: 정리");
    expect(absorptionMarkdown).toMatch(/정리 판단: 정리 (검토 후보|보류)/);
    expect(absorptionMarkdown).not.toContain("정리 판단: 정리 검토 가능");
    expect(absorptionMarkdown).toContain("판단 근거");
    expect(absorptionMarkdown).toContain("session verification, verification records, source retention guide");
    expect(absorptionMarkdown).not.toContain("session-verification, source-retention-guide");
    expect(absorptionMarkdown).toContain("README.study.md");
    expect(absorptionMarkdown).toContain("생성된 세션 `source/` 스냅샷");
    expect(absorptionMarkdown).toContain("READY_REVIEW는 정리 검토 후보이지 최종 ACCEPT, 배포, 삭제 허가가 아닙니다");
    expect(absorptionMarkdown).toContain("실제 적용은 보존 증거 묶음, 세션 검증, 검증 기록 확인 뒤");
    expect(absorptionMarkdown).not.toContain("실제 적용은 보존 증거 묶음 확인 뒤");
    expect(absorptionMarkdown).toContain("`DELETE-SOURCE-SNAPSHOT` 확인 토큰을 입력한 경우에만 검토합니다");
    expect(absorptionMarkdown).not.toContain("생성된 세션 source/ 스냅샷");
    expect(absorptionMarkdown).not.toContain("source snapshot은");
    expect(absorptionMarkdown).not.toContain("source snapshot을");
    expect(absorptionMarkdown).toContain("학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다고 명시 확인한 뒤");
    expect(absorptionMarkdown).toContain("`DELETE-SOURCE-SNAPSHOT` 확인 토큰으로 source 링크가 더 이상 열리지 않아도 된다는 마지막 학습자 명시 확인을 남긴 경우에만");
    expect(absorptionMarkdown).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰으로 마지막 현재 학습 목표 확인과 학습자 명시 확인을 남긴 경우에만");
    expect(absorptionMarkdown).not.toContain("DELETE-SOURCE-SNAPSHOT 확인 토큰으로 마지막 현재 목표 확인");
    expect(absorptionMarkdown).not.toContain("원본 source/를 장기 지식으로 보관하지 않고도");
    expect(absorptionMarkdown).toContain("현재 학습 목표 기준:");
    expect(absorptionMarkdown).toContain("현재 목표 조사 판단");
    expect(absorptionMarkdown).toContain("정적 분석으로 확인한 것과 실행해야 할 것은 session verification, verification records, daily summary에 남았습니다.");
    expect(absorptionMarkdown).toContain("용량 정리 판단은 retention guide, verification records, 이 ledger를 기준으로 하고");
    expect(absorptionMarkdown).not.toContain("정적 분석으로 확인한 것과 실행해야 할 것은 session verification과 daily summary에 남았습니다.");
    expect(absorptionMarkdown).not.toContain("용량 정리 판단은 retention guide와 이 ledger를 기준으로 하고");
    expect(absorptionMarkdown).toContain("현재 학습 목표와 정적 근거 기준에서 맞는지");
    expect(absorptionMarkdown).toContain("## 현재 목표 추가 조사 필요 여부");
    expect(absorptionMarkdown).toContain("학습자가 검토 후 다듬어 보낼 orient/architect/plan/review 프롬프트");
    expect(absorptionMarkdown).not.toContain("AI에게 바로 줄 orient/architect/plan/review 프롬프트");
    expect(absorptionMarkdown).not.toContain("- 추가 조사 판단:");
    expect(absorptionMarkdown).toContain("원본 소스 전체를 앱 지식으로 내장하지 않습니다");
    expect(absorptionMarkdown).toContain("AI에게 줄 확인 프롬프트");
    expect(absorptionMarkdown).toContain("PASS_REVIEW / REVISE / BLOCK 검토 상태");
    expect(absorptionMarkdown).toContain("PASS_REVIEW도 현재 항목 검토 후보일 뿐 전체 조사 종료, 최종 ACCEPT, 배포, source 삭제 허가가 아니라");
    expect(absorptionMarkdown).not.toContain("PASS_REVIEW도 현재 항목 검토 후보일 뿐 전체 조사 종료, source 삭제, 최종 승인 허가가 아니라");
    expect(absorptionMarkdown).not.toContain("PASS_REVIEW도 조사 종료, source 삭제, 최종 승인 허가가 아니라");
    expect(absorptionMarkdown).not.toContain("PASS / REVISE / BLOCK");
    const resourcesText = await fs.readFile(path.join(result.session.outputPaths.root, "RESOURCES.md"), "utf8");
    expect(resourcesText).toContain("reference/architecture-principle-playbook.html");
    expect(resourcesText).toContain("reference/source-to-build-interview.html");
    expect(resourcesText).toContain("reference/similar-app-transfer-map.html");
    expect(resourcesText).toContain("reference/learner-goal-alignment.html");
    expect(resourcesText).toContain("reference/ai-implementation-loop.html");
    expect(resourcesText).toContain("reference/learner-role-contract.html");
    expect(resourcesText).toContain("reference/ai-output-review-rubric.html");
    expect(resourcesText).toContain("reference/vibe-coding-mastery-checklist.html");
    expect(resourcesText).toContain("reference/vibe-coding-implementation-brief.html");
    expect(resourcesText).toContain("reference/ai-prompt-readiness-checklist.html");
    expect(resourcesText).toContain("reference/ai-prompt-ab-lab.html");
    expect(resourcesText).toContain("reference/source-retention-guide.html");
    expect(resourcesText).toContain("reference/source-absorption-ledger.html");
    const notesText = await fs.readFile(path.join(result.session.outputPaths.root, "NOTES.md"), "utf8");
    expect(notesText).toContain("## Learner Role");
    expect(notesText).toContain("학습자는 전문 개발자가 아니라 바이브코딩 개발자입니다");
    expect(notesText).toContain("제품 목적, 아키텍처 이유, 책임 경계, 핵심 용어, AI 프롬프트, 수락 기준, 검증 질문");
    expect(notesText).toContain("AI는 실제 코드 작성과 반복 구현을 담당");
    expect(notesText).toContain("전통적인 문법 강의나 라인별 코딩 수업");
    expect(notesText).toContain("source evidence를 AI 구현 지시와 리뷰 기준");
    expect(notesText).toContain("architecture-principle-playbook.html");
    expect(notesText).toContain("source-to-build-interview.html");
    expect(notesText).toContain("similar-app-transfer-map.html");
    expect(notesText).toContain("learner-goal-alignment.html");
    expect(notesText).toContain("ai-implementation-loop.html");
    expect(notesText).toContain("learner-role-contract.html");
    expect(notesText).toContain("ai-output-review-rubric.html");
    expect(notesText).toContain("vibe-coding-mastery-checklist.html");
    expect(notesText).toContain("vibe-coding-implementation-brief.html");
    expect(notesText).toContain("ai-prompt-readiness-checklist.html");
    expect(notesText).toContain("ai-prompt-ab-lab.html");
    expect(notesText).toContain("source-retention-guide.html");
    expect(notesText).toContain("source-absorption-ledger.html");

    const quizText = await fs.readFile(path.join(result.session.outputPaths.analysis, "quiz.json"), "utf8");
    const quiz = JSON.parse(quizText) as { questions: Array<{ id: string }> };
    const answers = Object.fromEntries(quiz.questions.map((question) => [question.id, "A" as const]));
    const attempt = await scoreQuizAttempt(result.session.outputPaths.root, answers);

    expect(attempt.score).toBe(100);
    await expect(findQuizLearningRecord(result.session.outputPaths.root, attempt.attemptId)).resolves.toMatch(/0001-quiz-attempt-passed\.md$/);
    const records = await fs.readdir(recordsDir);
    expect(records).toContain("0001-quiz-attempt-passed.md");
    const recordText = await fs.readFile(path.join(recordsDir, "0001-quiz-attempt-passed.md"), "utf8");
    expect(recordText).toContain("Quiz Attempt Learning Record");
    expect(recordText).toContain("Status: mastered");
    expect(recordText).toContain("## Vibe-coding evidence");
    expect(recordText).toContain("문법 암기표가 아니라 AI에게 줄 구현 맥락 준비도");
    expect(recordText).not.toContain("문법 암기 완료표가 아니라 AI에게 줄 구현 맥락 준비도");
    expect(recordText).toContain("제품 목적, 책임 경계, 핵심 용어, AI 구현 프롬프트, 수락 기준, 검증 질문");
    expect(recordText).toContain("AI repair prompt");
    expect(recordText).toContain("이 기록만으로 생성된 세션 `source/` 스냅샷 정리를 결정하지 않고");
    expect(recordText).not.toContain("이 기록만으로 생성된 세션 source/ 스냅샷 정리를 결정하지 않고");
    expect(recordText).toContain("source-absorption-ledger, session-verification, verification records, source-retention-guide");
    expect(recordText).toContain("학습자가 현재 학습 목표에서 source 링크가 더 이상 열리지 않아도 된다는 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 함께 확인합니다");
    expect(recordText).not.toContain("현재 학습 목표, 학습자 명시 확인, DELETE-SOURCE-SNAPSHOT 확인 토큰을 함께 확인합니다");
    expect(recordText).not.toContain("source-retention-guide, 현재 학습 목표를 함께 확인합니다");
    expect(recordText).not.toContain("이 기록만으로 source/ 정리를 결정하지 않고");

    const secondAttempt = await scoreQuizAttempt(result.session.outputPaths.root, answers);
    expect(secondAttempt.score).toBe(100);
    await expect(findQuizLearningRecord(result.session.outputPaths.root, secondAttempt.attemptId)).resolves.toMatch(/0002-quiz-attempt-passed\.md$/);
    const recordsAfterSecondAttempt = await fs.readdir(recordsDir);
    expect(recordsAfterSecondAttempt).toContain("0001-quiz-attempt-passed.md");
    expect(recordsAfterSecondAttempt).toContain("0002-quiz-attempt-passed.md");
    const sourceRetentionWrongAnswers = Object.fromEntries(quiz.questions.map((question) => [question.id, "C" as const]));
    const htmlInputAfterPassedAttempts = await loadStudyHtmlInput(result.session.outputPaths.root);
    const reviewAttempt = await scoreQuizAttempt(result.session.outputPaths.root, sourceRetentionWrongAnswers, htmlInputAfterPassedAttempts);
    expect(reviewAttempt.wrongCount).toBe(quiz.questions.length);
    await expect(findQuizLearningRecord(result.session.outputPaths.root, reviewAttempt.attemptId)).resolves.toMatch(/0003-quiz-attempt-review\.md$/);
    const reviewRecordText = await fs.readFile(path.join(recordsDir, "0003-quiz-attempt-review.md"), "utf8");
    expect(reviewRecordText).toContain("Status: needs-review");
    expect(reviewRecordText).toContain("이 기록은 이해 확정 선언이 아니라 다음 수업 난이도를 낮추는 근거입니다");
    expect(reviewRecordText).toContain("문법 암기표가 아니라 AI에게 줄 구현 맥락 준비도");
    expect(reviewRecordText).toContain("선택지 복습");
    expect(reviewRecordText).toContain("생성된 세션 `source/` 스냅샷은 임시 프로젝트 근거");
    expect(reviewRecordText).not.toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
    expect(reviewRecordText).not.toContain("이 기록은 이해 완료가 아니라 다음 수업 난이도를 낮추는 근거입니다");
    expect(reviewRecordText).not.toContain("문법 암기 완료표가 아니라 AI에게 줄 구현 맥락 준비도");
    const wrongNotesAfterReviewText = await fs.readFile(path.join(result.session.outputPaths.analysis, "wrong-notes.json"), "utf8");
    expect(wrongNotesAfterReviewText).toContain("\"selectedChoiceRationale\"");
    expect(wrongNotesAfterReviewText).toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
    expect(wrongNotesAfterReviewText).not.toContain("생성된 세션 <code>source/</code> 스냅샷은 임시 프로젝트 근거");
    const wrongNotesMarkdownAfterReview = await fs.readFile(path.join(result.session.outputPaths.markdown, "wrong-notes.md"), "utf8");
    expect(wrongNotesMarkdownAfterReview).toContain("- 선택지 복습:");
    expect(wrongNotesMarkdownAfterReview).toContain("생성된 세션 `source/` 스냅샷은 임시 프로젝트 근거");
    expect(wrongNotesMarkdownAfterReview).not.toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
    const wrongNotesHtmlAfterReview = await fs.readFile(path.join(result.session.outputPaths.html, "wrong-notes.html"), "utf8");
    expect(wrongNotesHtmlAfterReview).toContain("<h4>선택지 복습</h4>");
    expect(wrongNotesHtmlAfterReview).toContain("생성된 세션 <code>source/</code> 스냅샷은 임시 프로젝트 근거");
    expect(wrongNotesHtmlAfterReview).not.toContain("생성된 세션 source/ 스냅샷은 임시 프로젝트 근거");
  }, 10000);
});
